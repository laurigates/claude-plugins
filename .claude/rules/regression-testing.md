---
created: 2026-02-26
modified: 2026-09-23
reviewed: 2026-09-23
paths:
  - "**/skills/**"
  - "scripts/**"
---

# Regression Testing for Script Checks

When you fix a skill quality issue, a context command bug, or a skill body corruption problem, you **MUST** add a regression check to prevent the same issue from recurring.

## Rule

> **Every bug fix to a SKILL.md file must be accompanied by a new regression check in the appropriate script.**

This ensures the CI catches the same class of problem in any future skill, not just the one you just fixed.

## Semantic vs. syntactic gates

A regression check has two layers, and both matter:

| Gate | Asks | Catches | Misses |
|------|------|---------|--------|
| **Syntactic** | Does the file parse? Is the YAML schema valid? Does the frontmatter have the required fields? | Truncated files, broken YAML, missing fields | A description that parses but no longer triggers auto-invocation; an error enum that compiles but drops a user-facing code; a response shape that validates but omits a contract field |
| **Semantic** | Does the artefact still carry the **intent** it was designed for — the trigger phrase, the contract field, the invariant the consumer depends on? | Bulk-edit drift where an agent "tightens" prose and silently breaks downstream matchers | Pure parse failures (covered by the syntactic gate) |

Syntactic-only gates are easy to write and feel like coverage, but bulk-edit agents reliably produce syntactically-valid output that violates semantic invariants. The auto-invocation matcher does not care that the YAML parses; it cares that the literal trigger phrase is present.

**Canonical example (issue #1278):** `scripts/audit-skill-descriptions.py --strict-all` is a semantic gate — it checks that auto-invokable descriptions contain the literal `Use when` substring (the matcher's only trigger). In a 68-skill bulk edit, four of six refactor subagents replaced `Use when...` with `Use to...` / `Use for...`. Every output was YAML-valid; agent self-reports said "done"; pre-edit reviews approved the diffs. The semantic gate fired:

> `68 auto-invokable skills need description fixes / Audited 706 skills across 41 plugins`

Without it, all 68 unmatchable descriptions would have shipped silently.

### Required pattern

When you author a regression script for a class of bulk-edit risk (descriptions, error enums, exit codes, response shapes, format versions), the script **MUST** encode the *semantic invariant*, not just the *syntactic shell*. A frontmatter parser is not enough; a `Use when` substring check is.

### Script-design checklist

Your regression script should answer:

| Question | Why |
|----------|-----|
| (a) Does the artefact still parse? | Syntactic floor — necessary, not sufficient |
| (b) Does it still carry the trigger / contract / invariant it was designed for? | The semantic question that bulk-edit agents reliably break |
| (c) Is the failure message actionable enough that an agent can self-repair? | A pre-commit failure that emits `--json` lets a follow-up agent enumerate paths, repair, and re-run without human relay |

The audit script in this repo is the canonical example — it pairs a human report with `--json` output for scriptable repair. PR #1314 bakes that same audit into `agents-plugin/agents/refactor.md` as a mandatory post-pass, so the refactor agent self-verifies before reporting completion.

## Where to Add the Check

| Problem Type | Add Check To |
|-------------|-------------|
| Context command antipattern (API calls, shell operators) | `scripts/lint-context-commands.sh` |
| Skill body structure corruption (spurious headings, leaked frontmatter) | `scripts/plugin-compliance-check.sh` → `check_skill_body()` |
| Frontmatter field missing or malformed | `scripts/plugin-compliance-check.sh` → `check_skill_frontmatter()` |
| Description fails auto-invocation matching (missing/empty, or no "Use when..." trigger) | `scripts/plugin-compliance-check.sh` → `check_skill_descriptions()` (delegates to `scripts/audit-skill-descriptions.py`) |
| Skill size exceeding limit | `scripts/plugin-compliance-check.sh` → `check_skill_size()` |
| Blueprint upgrade target drift (migrations added without updating `blueprint-upgrade`) | `scripts/check-blueprint-upgrade-target.sh` |
| Hyphenated taskwarrior tag names in plugin docs (parser silently swallows them) | `scripts/lint-taskwarrior-tags.sh` |
| Plugin agent body missing `## Tool Selection` section (agents re-discover hook-blocked idioms) | `scripts/check-agent-tool-selection.sh` |
| A looping skill loses its independent stop condition or compact-state-packet fields (checkpoint plan drops `Verifier result`/`Changed since last run`/`Exit condition`/the independent-verifier step, or a sibling skill loses its `loop-integrity.md` cross-reference) | `scripts/check-loop-integrity.sh --strict` (+ `scripts/tests/test-check-loop-integrity.sh`); wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml` (self-test + run). See `.claude/rules/loop-integrity.md` |
| Plugin agent `model:` drifts off `opus` (a weaker delegate's output re-enters the main loop and degrades downstream work; `effort`, not `model`, is the cost lever) | `scripts/check-agent-model.sh` |
| Agent-dispatch skills lose the loud-failure contract (dispatched agents may surrender with one-word summaries) | `scripts/check-agent-failure-contract.sh` |
| A skill body names a `subagent_type` that resolves to no agent (stale rename, invented name), so every dispatch it instructs fails at "Agent type not found" | `scripts/check-subagent-types.sh` (+ `scripts/tests/test-check-subagent-types.sh`); extend `BUILTIN_SUBAGENT_TYPES` for a new harness-provided type, or `SUBAGENT_TYPE_EXCEPTIONS` for a genuinely illustrative non-dispatch |
| Skill description exceeds listing-budget length band (>200 WARN, >300 ERROR) | `scripts/plugin-compliance-check.sh` → `check_skill_descriptions()` (length axis); `audit-skill-descriptions.py --strict-length` in pre-commit + CI |
| Skill body or `allowed-tools` references an MCP tool name that the server doesn't actually expose | `scripts/lint-mcp-tool-references.sh` — extend the `denylist=()` array with the unavailable tool name and the suggested fix |
| Skill body documents an install/import for a package name that doesn't exist on its registry (npm/PyPI/crates/RubyGems/Go) — a dependency-confusion hazard | `scripts/lint-package-references.sh` — extend the `denylist=()` array with the `(ecosystem, wrong-name, fix)` triple |
| A `## Context` backtick command aborts the skill in a fresh project (writes stderr / exits non-zero on a missing file, dir, remote, or git state) — the whole class behind the regex rules above | `scripts/check-context-command-execution.sh` (semantic backstop: actually EXECUTES every published skill's Context command in a bare one-commit sandbox; `--strict` fails on any non-zero exit / non-empty stderr). No new rule needed per variant — it catches the class. Regression test: `scripts/tests/test-check-context-command-execution.sh` |
| Skill markdown ships a version pin (`uses:`/`FROM`/`image:`/`rev:`) in a shape Renovate's customManagers can't see, so it silently rots | `scripts/check-version-pin-coverage.sh` — extend the managed-form predicates **and** the matching `renovate.json` customManager together (see `.claude/rules/version-pinning.md`) |
| `.claude/settings.json` `enabledPlugins` drifts from the plugins published in `.claude-plugin/marketplace.json` (a plugin is added to the marketplace but never enabled, or a removed plugin is left enabled) | `scripts/check-enabled-plugins-drift.sh --strict` (+ `scripts/tests/test-check-enabled-plugins-drift.sh`); wired into `.pre-commit-config.yaml` and the `Plugin: Enablement drift` workflow |
| A test/hook shell script builds a throwaway repo with `VAR=$(mktemp -d)` then `git -C "$VAR"` / `git init --bare "$VAR"` / `git clone … "$VAR"` without guarding the dir — if `mktemp` fails, `$VAR` is empty and `git -C ""` falls back to the CWD, corrupting the real repo in a shared checkout (issue #1692) | `scripts/check-git-sandbox-guards.sh --strict` (+ `scripts/tests/test-check-git-sandbox-guards.sh`); file-level gate requiring every `mktemp -d` in a repo-targeting-git script to carry a guard. Python is exempt (`mkdtemp`/`tmp_path` raise, never return empty). Wired into `.pre-commit-config.yaml` |
| A `.github/workflows/*.yml` Claude invocation drifts off `--model opus`, or sets opus without an explicit `--effort` (opus defaults to `high`, forfeiting the cost savings; haiku/sonnet are weaker per the cost-economics in `.claude/rules/workflow-model-effort.md`) | `scripts/check-workflow-model.sh` (+ `scripts/tests/test-check-workflow-model.sh`); classifies invoking vs reusable-only vs no-invocation workflows and asserts opus + valid explicit effort. Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml` |
| `session-distill` loses its deterministic substrate — the `distill-survey.sh` collector invocation, or the `--process` / `.claude/skills/` process-routing tokens | `scripts/plugin-compliance-check.sh` → `check_skill_body()` (session-distill block asserts all three tokens survive); the collector's own coverage is `session-plugin/scripts/tests/test-distill-survey.sh` |
| Blueprint level-3 workflow templates (`blueprint-plugin/templates/*.workflow.yml`, ADR-0020 / #2005) lose their model/effort, script-injection-safety, or loop-integrity invariants — they live OUTSIDE `.github/workflows/` so `check-workflow-model.sh` + `actionlint` never scan them | `scripts/check-blueprint-level3-templates.sh` (+ `scripts/tests/test-check-blueprint-level3-templates.sh`); asserts `--model opus` + explicit `--effort` per invocation, the untrusted issue body referenced exactly once (env binding), the independent `verify:` job + state-packet fields, and runs `actionlint`. The scaffold SKILL body is guarded by `plugin-compliance-check.sh` `check_skill_body()`; the gate/parser scripts by `blueprint-plugin/scripts/tests/test-blueprint-wo-{guard,packet}.sh`. Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml` |
| The blueprint `feature-tracker.json` diverges from itself (the `statistics` block is a **cache** of the features collection) or from doc frontmatter | `blueprint-plugin/scripts/blueprint-tracker-check.sh` (+ `blueprint-plugin/scripts/tests/test-blueprint-tracker-check.sh`); recomputes `statistics`, validates the feature-status enum against `schemas/feature-tracker.schema.json`, cross-checks `tasks.*[]` membership, finds FR ids cited in `docs/**` but never minted, compares doc frontmatter status, and flags dead buckets / duplicate timestamp fields. Repo conventions come from the manifest `validation` block via `scripts/get-validation-config.sh`. Wired into `blueprint-feature-tracker-sync` + `-status` (pinned by `plugin-compliance-check.sh`) |
| The ADR collision guard misses a claimant because the number is in **frontmatter** rather than the filename, or lives in a **second** ADR directory | `blueprint-plugin/skills/blueprint-adr-validate/scripts/check-adr-numbers.sh` (widened) + `generate-adr-index.sh` (+ their `scripts/tests/`); ADR directory set comes from the manifest `validation.adr_dirs` via `get-validation-config.sh` — **not** a second config mechanism |
| A plugin agent body instructs ancestry-based branch **deletion** (`git branch --merged` piped into `git branch -d`/`-D`) — ancestry misses every squash-merged branch, so a fully-landed branch reads as unmerged and the delete runs on a known-wrong signal (the defect already fixed for `deadbranch`, #1869) | `scripts/check-agent-tool-selection.sh` — per-line shape denylist: a `git branch … --merged` command at line start, or `--merged` composed with a `branch -d`/`-D` on one line. Prose that merely *names* the command is not flagged (that is how the hazard gets taught). `BRANCH_CLEANUP_ALLOWLIST` is seeded with `experiments/claude-probe/`, which uses the broken idiom deliberately as a trap fixture; test seam `CHECK_AGENT_TOOL_SELECTION_ALLOWLIST` |
| A skill grants a bare `Bash` in `allowed-tools` but runs no shell at all — pure permission surface advertising a capability it never exercises. (The *inverse*, a bare `Bash` in a skill that does run shell, is the ratified standard and is deliberately NOT linted — see `.claude/rules/agentic-permissions.md`.) | `scripts/check-unused-bash-grant.sh` (+ `scripts/tests/test-check-unused-bash-grant.sh`). "Runs shell" = a shell-language **or unlabeled** fence with a known command (incl. `./`, `.venv/`, `<placeholder>/`, `$VAR/` prefixes), a known command in inline backticks, a sibling `scripts/` dir, a `` !`…` `` Context command, or any of those in a bundled sidecar. Markdown structure comes from `scripts/lib/extract-md-elements.py`, never a hand-rolled fence toggle (#2009). **Blocking (`--strict`) as of #2255** at both call sites; gate held at the flip (`SKILLS_SCANNED=408`, `BASH_GRANTEES=186`, `ISSUE_COUNT=0`, allowlist EMPTY), so the ratchet locks in a clean state rather than a suppressed one |
| A workflow RUNS a repo script that its own `pull_request` `paths:` filter does not match, so a PR changing only that script never runs it in CI — the guard has no CI signal for its own regressions | `scripts/check-workflow-script-triggers.sh --strict` (+ `scripts/tests/test-check-workflow-script-triggers.sh`); a workflow with no `paths:` filter is never flagged. Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml` |
| A guard reports `STATUS=OK` / exit 0 having scanned **zero** files, because a `*/.claude/worktrees/*` prune against an absolute base matched the scan root itself (the root IS an agent worktree) | Run discovery from inside the root against **relative** paths (`cd "$proj_dir"` + `find .`), per `scripts/check-subagent-types.sh`. Then make zero-scan distinguishable: `STATUS=ERROR` + `TYPE=nothing_scanned` when the corpus should be non-empty, `SCANNED_EMPTY=true` when it is legitimately empty. Class test: `scripts/tests/test-guard-worktree-root-discovery.sh` |
| A skill body ships Mustache/Jinja/Handlebars template conditionals (`{{ if X }}` / `{{ endif }}` / `{{#if}}` / `{{/if}}`) that **nothing renders** — Claude Code has no template engine in the skill-invocation path, so every branch reaches the agent verbatim with the condition variable never bound | `scripts/check-unrendered-templates.sh` (+ `scripts/tests/test-check-unrendered-templates.sh`). Exempt structurally: Go/Helm chart templates (`{{-` / `-}}` trim markers, bare `{{ end }}`, a condition rooted at `.Values` / `$.`) and the `{{ if ... }}` ellipsis prose form. Exempt by **declaration**: a file containing the phrase `markers after substitution` is a generator template the skill instructs the agent to render and strip. Wired into `.pre-commit-config.yaml` |
| A skill grants a bare `Bash` in `allowed-tools` but runs no shell at all — pure permission surface advertising a capability it never exercises. (The *inverse*, a bare `Bash` in a skill that does run shell, is the ratified standard and is deliberately NOT linted — see `.claude/rules/agentic-permissions.md`.) | `scripts/check-unused-bash-grant.sh` (+ `scripts/tests/test-check-unused-bash-grant.sh`). "Runs shell" = a shell-language **or unlabeled** fence with a known command (incl. `./`, `.venv/`, `<placeholder>/`, `$VAR/` prefixes), a known command in inline backticks, a sibling `scripts/` dir, a `` !`…` `` Context command, or any of those in a bundled sidecar. Markdown structure comes from `scripts/lib/extract-md-elements.py`, never a hand-rolled fence toggle (#2009). Advisory on landing; `--strict` flips it to blocking once the allowlist stays empty |
| The `TaskCompleted` **agent-hook prompt** loses a verification behaviour — committed work stops counting as evidence, the base ref stops being the DEFAULT branch (`@{u}` resolves to the branch's own remote counterpart, so `BASE..HEAD` is empty on any pushed branch), the working-tree fallback disappears, scope reverts to raw `git status`, or the no-evidence block is dropped | `scripts/check-taskcompleted-verification.sh --strict` (+ `scripts/tests/test-check-taskcompleted-verification.sh`). The artefact is a prompt **string inside `plugin.json`**, not a SKILL.md, so `plugin-compliance-check.sh` `check_skill_body()` cannot reach it. Each rule is a CONCEPT with several accepted spellings, so a reword passes and a revert fails. `base_ref_hardcoded` forbids only an **unconditional** base (a bare `<branch>..HEAD` range, or `BASE=<branch>`) — a documented `git rev-parse --verify --quiet` probe of `origin/main`/`origin/master` is the correct fallback and must NOT fire (that over-match blocked the #2301 repair itself) |
| `git-stash-reminder.sh` / `git-stash-session-init.sh` lose a namespace, age-bound, or write-once invariant — a repo entered mid-session or reached through a linked worktree stops reporting genuine session stashes, or a SessionStart re-fire un-reports an already-flagged one — **or** the hook's block stops being *satisfiable*: a stash whose tree equals the working tree is reported anyway, an already-surfaced stash re-blocks forever, an `auto-checkpoint` entry is prescribed `git stash pop`, a `git stash push -u` stash's untracked payload is silently declared redundant, or `CLAUDE_HOOKS_DISABLE_GIT_STASH_REMINDER=1` stops disabling the hook (#2686) | `hooks-plugin/hooks/test-git-stash-reminder.sh` (auto-discovered via `*/hooks/test-*.sh`). Hermetic: sandboxed with `CLAUDE_STASH_BASELINE_DIR`, full `GIT_DIR` family unset per #1745, stash dates pinned to git's raw `@<epoch> <tz>` form so GNU/BSD `date` cannot diverge. Every invariant is **paired** — a false-positive case beside a true-positive one — so the suite fails both an over-reporting hook and a permanently-silent one |
| A skill's `${CLAUDE_SKILL_DIR}` / `${CLAUDE_SESSION_ID}` reaches pi's `bash` tool unset (pi substitutes nothing, so `bash "${CLAUDE_SKILL_DIR}/../../scripts/x.sh"` runs as `bash "/../../scripts/x.sh"`), or `claude-${CLAUDE_SESSION_ID:0:8}` collides between pi sessions started in the same minute because the raw UUIDv7 timestamp prefix was exported | `adapters/tests/pi-binding.test.ts` (`claude-env helpers` + the factory `tool_call` dispatch tests); run by `just adapters-test`. Resolution tiers, ambiguity and unresolved blocks, quoting, and the session-id prefix are unit-tested on `adapters/pi/claude-env.ts`; the dispatch tests fail if the `tool_call` registration is removed (mutation-checked on landing) |
| A `.github/workflows` Claude prompt tells Claude to run a Bash command that its own step's `--allowedTools` cannot reach — either no `Bash(...)` grant matches at all, or the call is granted-in-principle but cannot PREFIX match because it begins with a `for`/`while` loop, chains with `;`/`&&`/`\|\|`, or opens with a `VAR=$(...)` assignment. Each denial still burns a turn, so the two classes compound into a `--max-turns` overrun (#2493: 4 denials then RED at 42/40 turns on `workflow-model-audit`; 6 denials at 59/60 on `golden-set-evaluation`) | `scripts/check-workflow-tool-grants.sh` (+ `scripts/tests/test-check-workflow-tool-grants.sh`, whose fixtures replay the ten observed denial shapes); wired into `.pre-commit-config.yaml`, self-test auto-discovered by `test-skill-scripts.yml`. Heredoc bodies and non-shell fences are skipped — case I is the load-bearing counter-case, since the real `gh issue create --body "$(cat <<'EOF' … EOF )"` call site ships in three workflows |
| A `scripts/check-*.sh` guard emits a status off `OK`/`WARN`/`ERROR` (`FAIL`, `PASS`), omits `ISSUE_COUNT=`, or reports `WARN`/`ERROR` with no `REASON=` naming the cause, so a rollup reading the summary lines sees "unknown" or a bare count | `scripts/check-structured-output-contract.sh --strict` (+ `scripts/tests/test-check-structured-output-contract.sh`); a script not yet migrated to `REASON=` goes in `scripts/structured-output-reason-pending.txt` and leaves it in the commit that migrates it; the script's twin pipes its failing fixture through `--validate` |

## Known Regressions (Documented Bugs)

The ledger of fixed bugs and the checks that now guard them lives in
[`docs/regression-ledger.md`](../../docs/regression-ledger.md). **Append one row
at the end of its table** for every fix you guard; the file is `merge=union`, so
parallel appends merge without conflicts.

It is kept out of this rule on purpose. This rule is path-scoped to
`**/skills/**` and `scripts/**`, so it loads whole into every agent that touches
a skill or a script — and the ledger, at 513 KB, pushed eval subagents past
their context window (issue #2667). `scripts/check-context-engineering.py
--strict` now fails any rule over its per-rule size ceiling, whatever its
scoping, so this file cannot silently regrow a ledger.

## How to Add a Regression Check

### For `lint-context-commands.sh`

Add a `check_pattern` call with a descriptive rule name, a grep regex anchored to context command lines (`^- .*!\``), and a fix description. Include a regression comment referencing the PR:

```bash
# <Description of what this catches>
# Regression: <skill-name> had <description of bug> (PR #NNN)
check_pattern WARN \
  "rule-name" \
  '^- .*!`[^`]*pattern[^`]*`' \
  "fix description here"
```

Use `ERROR` for patterns that always break, `WARN` for patterns that break in some environments.

Update the comment block at the top of the file to include the new regression number.

### For `plugin-compliance-check.sh`

Add detection logic inside the appropriate `check_*` function (or create a new one). Include a regression comment:

```bash
# Detect <pattern description>
# Regression: <skill-name> had <description of bug> (PR #NNN)
```

If adding a new check function:
1. Define the function following the existing naming pattern (`check_skill_*`)
2. Add a `results_*=()` array in the status tracking section
3. Wire it into the main loop: `new_status=0; check_skill_new "$plugin" || new_status=$?`
4. Add `results_new+=("$(to_symbol $new_status)")` and `results_new+=("❌")` for missing dirs
5. Add the new column to the output table header and row
6. Add `$new_status` to the overall status loop

## Checklist for Bug Fixes

When fixing a skill-related bug:

- [ ] The original bug is fixed in the SKILL.md file
- [ ] A regression check is added to the appropriate script
- [ ] The check includes a comment referencing the PR number
- [ ] A row is appended at the end of the table in `docs/regression-ledger.md`
- [ ] The fix commit follows conventional commit format: `fix(plugin): description`
| `session-survey.sh` reported `PROJECT_CONFIDENCE=high` beside an `OPEN_TASKS=` count that could belong to a **near-empty** slug while the real backlog sat under a **prefix sibling**. taskwarrior's CLI `project:<p>` filter is a **prefix** match, so `task project:comfyui list` returns all 60 `comfyui-nodes` tasks — a wrong slug verified that way looks confirmed. Observed live: `comfyui` (1 task) and `comfyui-nodes` (60) coexisting, the wrong slug enshrined in `CLAUDE.md` on exactly that evidence, and four follow-ups filed into the near-empty sibling. The collector's own scoping is a **hierarchy** match (`scope_of`: exact slug + `.`-separated children, #2271), so it never miscounted — which is precisely why the failure was invisible: `PROJECT_CONFIDENCE` answers *"is the detected slug trustworthy?"*, not *"do these tasks actually carry it?"*, and a prefix hit collapses the two questions into one. | Emit the split rather than pick a side. `PROJECT_EXACT_TASKS=` (always, an integer, like `OPEN_TASKS`) is the slug ALONE; `OPEN_TASKS` stays the hierarchy scope; `PROJECT_PREFIX_SIBLINGS=` / `PROJECT_PREFIX_SIBLING_TASKS=` (omitted when the store has none) name what a CLI prefix filter would ALSO have swept in, capped at 8 slugs and passed through the same row/column sanitiser every projected field gets. All three are computed against the slug `OPEN_TASKS` was counted under (`PROJECT_RESOLVED` when a rung resolved one). `PROJECT_CONFIDENCE` drops to `low` on **strict dominance** (`sibling_tasks > OPEN_TASKS`) — not on "a sibling exists", which would make every `dotfiles` beside a one-task `dotfiles-archive` read as unresolved forever. The downgrade deliberately applies to an **asserted** slug too (`--project` / a `.claude/session.json` declaration), unlike the adoption rungs: assertion fixes the slug's IDENTITY, it cannot assert what the store holds — and the reported case was a slug asserted on exactly this evidence. Nothing is adopted or rewritten; only the flag drops and the siblings are named. Reuses the single all-projects snapshot — no second `task` call, no `project:` filter. | `session-plugin/scripts/tests/test-session-survey.sh` TEST **AK** (47 assertions) EXECUTES the collector against the issue's own store (1 × `comfyui`, 60 × `comfyui-nodes`). AK1 is decisive: from the `comfyui` repo, `OPEN_TASKS=1` (never 60), `PROJECT_EXACT_TASKS=1`, `PROJECT_PREFIX_SIBLINGS=comfyui-nodes`, `PROJECT_PREFIX_SIBLING_TASKS=60`, `PROJECT_CONFIDENCE=low`. **Guard integrity is weighted equally**, since "always report low" would satisfy AK1 while destroying the signal: AK2 runs the SAME store from the owning slug and requires `high` + `OPEN_TASKS=60` + **no** sibling keys; AK3 requires a *minority* sibling to be named without costing confidence; AK4 pins that a `.` child counts inside `OPEN_TASKS`, is excluded from `PROJECT_EXACT_TASKS`, and is never reported as a prefix sibling; AK9 keeps an empty store a confident zero. AK5 pins the asserted-slug downgrade (and that `PROJECT=` is never rewritten), AK6 the summary/hook path, AK7 that still exactly one `export` runs with no `project:` filter, AK8/AK9 that a missing `jq` or `task` yields `TASK_SCOPE=unknown`/`none` + `low` + a parse-stable `PROJECT_EXACT_TASKS=0` and claims no siblings. Verified by **mutation**: against the pre-fix collector 20 assertions fail; an unconditional downgrade fails 13 (4 of them new); a blanket `startswith` sibling match fails AK4's 2. | issue #2323 |
| `cold-read-gate`'s reader prompt asked only for QUESTIONS / HESITATIONS / a verdict, so it measured **local** legibility and passed an artifact whose **ask** was incoherent. Observed 2026-08 on a registry-maintainer appeal that argued a scanner finding was unfixable by publishers and then asked those publishers to approve individual versions — four rounds of author revision had not surfaced it, because each half was individually true, and it took an **ad-hoc** prompt clause (not in the skill) for two independent readers to catch it. Two sibling gaps in the same class: a hesitation of the form "impact is environment-specific / I can't verify this" was triaged as prose to soften, when it is frequently a **testable hypothesis** (capping `RAYON_NUM_THREADS` at 1/2/4/8/24 answered exactly that objection and INVERTED it — the win saturates near 4 threads, so it is not a big-iron-only result: tracel-ai/burn#5332); and the persona table offered one persona per audience with no guidance for an artifact adapted to **two** channels, where an issue-triage reader and a chat-skimmer reader returned non-overlapping findings. | Promoted all three into the skill: the template's `Produce` list leads with `1. THE ASK — state in ONE sentence what this text wants you to DO` (with "if you cannot state it, that is the most important finding"); Step 3's two-bucket triage table gains a third column, **Answerable with evidence — measure it**, plus the rule that a bounded run beats a hedge; and the persona section gains "when one artifact serves two channels, give each reader its real audience". | `plugin-compliance-check.sh` `check_skill_body()` — the existing `cold-read-gate` block (which already pins `run_in_background: false` / `task-completion result` for #2063) gains a second loop asserting `THE ASK`, `Answerable with evidence`, and `give each reader its real audience`. Each is the marker of one addition, so a bulk edit "tightening" the prose cannot silently revert the gate to local-legibility-only critique, drop the measurement bucket, or lose the two-channel guidance. Verified by mutation in both directions: renaming any one token produces the ❌ naming it, and the restored file leaves the repo's only ❌ as the pre-existing `parallel-agent-dispatch` size error. | issue #2347 |
| `session-survey.sh` emitted a bare `GH_READY=false` with no cause attached, so a consumer could not tell a transient GitHub 5xx from an expired token, a repo with no GitHub remote, or a budget kill — and `session-end`'s own guidance leans on exactly that distinction. Only the budget kill was distinguishable, via the separate `GH_TIMEOUT=true`. The four causes want four **different** responses (re-run / `gh auth login` / nothing at all / raise the budget), so a single `false` forces the consumer to guess, and the natural guess — re-run — is futile for the two most common non-transient cases. Compounding it, `gh_job` discarded each call's stderr to `/dev/null`, so the one artefact that could have classified the failure was thrown away at the source. The default per-call budget made the failure likelier than it needed to be: `SESSION_SURVEY_GH_TIMEOUT` defaulted to **4s**, but `gh pr list --author @me` resolves the `@me` handle *before* it can run the query, so the budget covers TWO round-trips — tight on a slow link or a degraded API, and every overrun silently disabled the `GITHUB_DRIFT` dedup guard. | Capture each job's stderr to a per-job `.err` file and classify `(rc, stderr)` into one `GH_FAIL_REASON=` emitted alongside every `GH_READY=false` (all three sites: `PRS`, `GITHUB_DRIFT`, and the hook summary). Classification order is load-bearing: gh's remote-less message **also** says `` `gh auth login` ``, so the no-remote test must precede the auth test or every remote-less repo reads as an auth failure and the user is sent to a login that cannot help. `137`/`143` (the watchdog's SIGTERM/SIGKILL) → `timeout`; a job set that was never launched → `no-cli` (gh absent — distinct from `auth`, since neither a re-run nor a login helps). `GH_FAIL_DETAIL=` carries the first stderr line for `api-error` and `unknown` only — the two classifications the reason alone cannot act on — sanitised (control bytes stripped, capped at 200 chars) so foreign text cannot break the `KEY=VALUE` row. Matching uses a here-string, never a pipe, so a `grep -q` closing the pipe early cannot trip `pipefail` (the #1744 SIGPIPE race). Budget default raised 4s → 8s (and the invalid-value fallback with it); the calls are backgrounded and parallel, so the higher ceiling costs wall-clock time only when something is actually slow, and the SessionStart hook's own 15s timeout still bounds the run. | `session-plugin/scripts/tests/test-session-survey.sh` TEST **AM** (39 assertions) EXECUTES the collector against a stub reproducing the real `gh` stderr for each cause. **AM1 is the guard-integrity half and runs first**: a healthy `gh` must emit **no** `GH_FAIL_REASON` and no `GH_FAIL_DETAIL` at all — without it, a key emitted unconditionally would satisfy every other assertion while meaning nothing. **AM3 is the discriminator**: the fixture's stderr contains `gh auth login`, and the reason must still be `no-remote` and must never be `auth` — the natural (wrong) implementation orders those tests the other way. AM2/AM4–AM7 pin `auth` / `api-error` (+ the quoted first line, with the *second* stderr line asserted absent) / `unknown` (nothing fabricated from empty stderr) / `no-cli` / `timeout` (with `GH_TIMEOUT=true` still firing and never misread as `api-error`). AM8 pins that the reason reaches `GITHUB_DRIFT` and the hook summary, not just `PRS`. AM9 feeds 400 chars of stderr laced with `\001\002\037` and asserts the snippet is stripped, capped, lands as exactly one row, and that **every** emitted line is still a section marker or a `KEY=VALUE` row. AM10 pins the 8s default and that an explicit budget still overrides it. AM11 pins the consumer side — `session-end` must name `GH_FAIL_REASON` and map all six values, since a reason code nothing acts on is the gap the issue reported. Mutation-verified three ways: against the pre-fix collector **14 assertions fail** (AM1 correctly passing on both); reordering auth-before-no-remote fails exactly AM3's 2; and restoring `2>/dev/null` on the job fails 6. | issue #2425 |
| `git-plugin:git-merge-hazards` prescribed push-by-SHA as `<sha>:<branch>` in all three of its push instructions. That short form is only accepted when `<branch>` **already exists on the remote**: the left side is a bare commit object, so git has no ref namespace to infer the destination from and refuses — `error: The destination you provided is not a full refname (i.e., starting with "refs/")` (git wraps the line right after `(i.e.,`, so triage greps the `not a full refname` fragment). The stacked-chain scenario the guidance was written from always has the child branch already pushed, which is exactly why the gap survived; it lands hardest in the **auto-close recovery** step, which explicitly opens a fresh PR from a branch that may not exist remotely yet. The error also misdirects: it names a ref the author never typed, and the skill's own zsh word-modifier warning sits two lines above, so the natural reading is that the quoting advice failed rather than that the refspec is incomplete. A **second instance of the same defect** lived in `scripts/check-stranded-work.sh`, whose `### Recovery` recipe emitted the short form on one line and the full refname two lines below — so the block contradicted itself, and that recipe is pasted verbatim by whoever refiles a stranded branch. | Use the full refname unconditionally — `"${sha}:refs/heads/<branch>"` is correct whether or not the branch exists, so there is no branch-state to reason about — and state *why* (a bare commit object gives git nothing to infer the namespace from). It compounds with the existing brace rule rather than replacing it: `"$sha:refs/heads/x"` is the `:r` word-modifier row already in the table, so unbraced zsh sends `<sha>efs/heads/x`. The quoted error text carries git's real comma and names the grep fragment, since a quoted string that does not match the real output defeats grep-for-the-error triage. The `check-stranded-work.sh` recipe is fixed in the same commit rather than filed as a follow-up — leaving it means an agent pastes the broken form from the other file. | `git-plugin/skills/git-merge-hazards/scripts/tests/test-push-refspec.sh` (15 assertions, auto-discovered via `*/skills/*/scripts/tests/test-*.sh`, listed in `scripts/required-to-run-tests.txt`). A grep for `refs/heads/` would be a syntactic-only gate that passes just as happily against a wrong claim about git (#1417 → #1819), so the test **EXECUTES** git in a sandbox: the short form must FAIL for a branch absent from the remote (and must not create it), the full refname must SUCCEED in the identical situation, and — **guard integrity** — the short form must still SUCCEED once the branch exists, without which a git that rejected every SHA refspec would satisfy the first assertion and the test would pin the wrong reason. Three further invariants close gaps a first draft left open. (a) The **VERBATIM prescribed command** is run: assertions 1–3 use a plain `git push`, but the skill prescribes `--force-with-lease`, whose valueless form compares the destination against its remote-tracking ref — which a branch absent from the remote does not have — so "can the prescribed command create it?" is a separate fact the earlier assertions cannot answer. (b) The refspec is **extracted from the recovery bullet and pushed**, bridging the file back to git so the SKILL.md assertions cannot degrade into the grep gate; only the refspec is lifted, never eval'd as a command. (c) The recovery pin is anchored to the **recovery bullet's own text** (an `awk` slice of the `- **Recovery**` block), because the brace bullet prescribes a byte-identical command — a whole-file grep for it is not a separate pin at all, and reverting only the recovery step left the first draft's "dedicated" assertion satisfied by the other occurrence. Every setup step is guarded so a failure reads as a setup error rather than an assertion mismatch (this file may never skip), and `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_SYSTEM` plus `-c commit.gpgsign=false -c core.hooksPath=/dev/null` neutralise the operator's own git config. The zsh table rows carry `$sha:` but no `git push`, so they are correctly out of scope — that table documents mangling, it does not prescribe a command. The `check-stranded-work.sh` half is pinned by `scripts/tests/test-check-stranded-work.sh` §11, which renders the real `--issue-body` recipe and **counts** push-by-SHA lines against those naming `refs/heads/` (rather than grepping for a literal), so the two lines in that block cannot drift apart again — with a non-vacuity assertion that the recipe still prescribes a push at all. Mutation-verified: reverting **only** the recovery step fails 3 (the anchored pin, the aggregate, and the executed extracted refspec); reverting **only** the brace bullet fails 1 (the aggregate, recovery correctly green); the verbatim pre-fix SKILL.md fails 3 naming all three lines (99/116/139); and reverting the `check-stranded-work.sh` line alone turns its own suite red. | issue #2478 |
| `agent-patterns-plugin:multi-model-delegation` documented PAL's MCP tools with a hardcoded **`mcp__pal__*`** prefix — `allowed-tools`, the Protocol steps, and the Agentic Optimizations table. The callable name is `mcp__<server-name>__<tool>`, where `<server-name>` is the key the server is **registered under**, not the product name — and the key can live in any scope (a project `.mcp.json`, or a `claude mcp add -s user` entry in `~/.claude.json`, which has no `.mcp.json` at all; this repo's own `.mcp.json` registers no PAL server). This repo registers PAL as `pal-mcp-server`, so every documented lookup returned `No matching deferred tools found` while `claude mcp list` reported the server **Connected** and a direct stdio probe returned all 18 tools under their bare names. `testing-plugin:test-analyze` carried the same defect, in its SKILL body, its bundled `workflows/test-analyze.workflow.js`, and the **compiled** `git-repo-agent` prompt derived from it. | Documented the prefix as **derived**, not fixed: a new `## The Tool Prefix Is Derived, Not Fixed` section gives the `mcp__<server-name>__<tool>` shape, names `claude mcp list` / `claude mcp get` as the authoritative reading **in every registration scope**, and states that the `mcp__pal-mcp-server__*` names below assume that registration. A second subsection fixes a misreading the first draft would have taught: **`No matching deferred tools found` has two causes.** Issue #2437's own "Additional context 1" records that even under the CORRECT prefix ToolSearch found nothing and PAL's tools never appeared in any deferred-tool reminder — so the message is not proof of a wrong prefix, and the reporter's actual workaround (drive the server over stdio JSON-RPC, **keeping stdin open until the response arrives** — `subprocess.run(input=…)` closes it and the server shuts down mid-call, returning an empty result that looks like a hung model) is recorded beside it. `test-analyze` and its bundled workflow were swept in the same commit, the compiled prompt regenerated (`just compile-prompts`, restricted to the one bundle this change feeds so 21 pre-existing stale bundles are not swept in), and the two remaining `docs/plans/dynamic-workflow-migration.md` references fixed by hand. | `scripts/lint-mcp-tool-references.sh` — the denylist becomes **(tool, scope, fix) triples**, and the `mcp__pal__` entry is **PATH-SCOPED** (`*/multi-model-delegation/*\|*/test-analyze/*\|*/prompts/generated/*`). An unscoped ban would be a design error, not a stricter guard: `mcp__pal__` is the CORRECT callable prefix for any repo that registers the server under the key `pal`, so banning it repo-wide hardcodes exactly the assumption the fix tells readers not to make. The #1429 entry keeps scope `*` — no server exposes `resolve_review_thread` under any registration. `case` alternation is **syntactic**, so a `\|` arriving inside a variable is a literal character; `scope_match()` splits on `\|` with globbing disabled (an unquoted `*` in a `for` list pathname-expands to the cwd's filenames) and matches each branch on its own. The walk widened from skill files to **also** `*.workflow.js` and the compiled `prompts/generated/**/*.md` — the two families the fix had to correct BY HAND — and the header now states the real coverage and why `.claude/rules/`, `docs/**` and the generated changelogs are deliberately OUT of scope (they cite broken names on purpose; a walk including them would fire on this very file). New regression test `scripts/tests/test-lint-mcp-tool-references.sh` (18 assertions) is SEMANTIC — it EXECUTES a copy of the real linter against planted fixture trees, because a grep of the script for `mcp__pal__` would pass against a denylist whose **triple indexing** broke. **Narrowness is weighted equally with detection**: `mcp__pal-mcp-server__chat` must pass, a blockquote citing the broken form must pass, and — the scope's own guard — an `mcp__pal__chat` in a skill OUTSIDE the scope must pass. **Guard integrity**: the #1429 entry must still fire *with its fix text* from a path outside the pal scope (that is what catches a broken triple index), and the `Tool:` line's wording is pinned so a reword is a deliberate, tested act. Writing the test exposed a latent **silent no-scan** (the #2219/#2290 class): discovery ran in a `(cd "$repo_root" && find .)` subshell while the `grep` ran in the caller's cwd, so any invocation from another directory opened **no file** and still exited 0 — fixed by entering the scan root before discovery, and pinned by a case that runs the fixture linter from `/`. The pre-commit trigger was widened with the walk (`*.workflow.js`, the compiled prompts, and the script itself) so the guard has a signal on every surface it now reads, and a `test-lint-mcp-tool-references` self-test hook joins it; the repo-root `scripts/tests/` glob added by #2333 already runs the suite in CI. Mutation-verified five ways: the verbatim pre-fix linter fails 9 of 18; removing ONLY the new triple fails 7; narrowing the walk back to skill files fails exactly the 2 coverage cases; **unscoping** the pal entry fails exactly the 1 narrowness case; reverting the cwd fix fails 9. End-to-end on the real tree, from the repo root and from `/` alike: restoring all four pre-fix artifacts makes the linter report **14** findings and exit 1 (7 in `multi-model-delegation/SKILL.md`, 3 in `test-analyze/SKILL.md`, 2 in the workflow `.js`, 2 in the compiled prompt) — the SKILL.md figure is **7 ERROR blocks over 7 lines carrying 10 occurrences**, since `grep -nF` reports per line and `allowed-tools` carries four names on line 5. | issue #2437 |
| `git-plugin:git-pr-watch` is catalog-present, and its reaction table told the agent to "Address it via `/git:pr-feedback`" — but `git-pr-feedback` carries `disable-model-invocation: true`, so the model cannot reach it. Unlike the #1843 precedent (same flag, same orchestrator conflict), the delegation here is **prose**, not a `Skill` tool call, so there is nothing to refuse: no `cannot be used with Skill tool` error, no log line anywhere. The agent reads an instruction it cannot execute and improvises or drops the review thread — the CI branch (`/git:fix-pr`, unflagged since #2434) worked while the review branch silently did not. **The same shape was live in four more places**, each found by widening the guard's own scope: `git-pr-sync-check/SKILL.md:71` and `git-triage/SKILL.md:206`/`:208` (the latter delegating to the gated `/git:issue`), `git-pr-watch/SKILL.md:32`, and — outside any skill — `git-plugin/hooks/git-drift-probe.sh:73`, which emitted `/git:pr-feedback` as a **SessionStart suggested action** the agent reads and cannot act on. | Kept the flag and fixed the **mismatch** (the issue's option (b), and the maintainer's stated default): `git-pr-feedback` posts replies and resolves threads on *other people's* PRs by default and `--all` fans out worktree subagents that push, so deliberate invocation is the right posture. Every site now says to summarise and **recommend the user run** the gated skill, matching the house phrasing already used by `blueprint-story-audit`/`-reconcile`. Two surfaces mattered more than the skill body: `.claude/rules/pr-branch-sync.md` is in CLAUDE.md's rules table, so its `changes_requested` verdict row (:49) and its "delegates reactions to `/git:pr-feedback`" sentence (:62-64) were loaded into **every** session — fixing the skill while leaving those defective fixes the quieter copy; and the drift probe's suggested action was re-pointed at `/git:pr-sync-check` (reachable) with the recommendation carried in the message. The skill's own frontmatter `description` — the line the model routes on — was reworded to match the new behaviour. `git-plugin/README.md` landed in the same change per `docs-currency.md`. | `scripts/check-delegation-reachability.sh` (+ `scripts/tests/test-check-delegation-reachability.sh`, 71 assertions) — SEMANTIC: it EXECUTES the checker against planted fixtures, and gated status is read from the **sibling's own frontmatter**, never a hardcoded list, so re-flagging a skill re-decides every reference to it. Two defects in the guard's first shape were the reason four instances survived it, and both are now pinned. (a) **Scope**: `DELEGATION_SCOPE_DEFAULT` was a single file, so CI reported `FILES_SCANNED=1 ISSUE_COUNT=0 STATUS=OK` over a tree holding three live instances. It now covers every git-plugin skill that references a gated sibling, and the audit set is NAMED in the output (`SCOPE=` / `AUDITED=` / `SCOPE_IS_REPO_WIDE=false`) so `STATUS=OK` can never be read as "the marketplace is clean" — the marketplace-wide sweep is tracked as issue #2483. (b) **Regex**: `USER_REFERRAL_RE` matched a bare `suggest|recommend|manual|user-invocable` anywhere on the line, so "Address it via `/git:pr-feedback` (user-invocable)" — still an imperative — passed; every alternative now binds a referral verb to *the user*. TEST I pins five weakly-hedged forms as ERROR **and** four genuine referral forms as clean, so the tightening cannot degrade into flagging every mention. (c) **Matching window**: the marker is now matched over the reference's LOGICAL UNIT — a table row alone, or the contiguous prose paragraph — because this repo wraps prose at ~80 columns, so a raw-line match false-positives the moment a correct paragraph is reflowed. TEST J pins that a hedge ending one line ABOVE its token stays clean, and weights the two ways widening could go wrong equally hard: a hedge across a **blank line** must NOT exempt, and a sibling **table row's** hedge must NOT exempt the next row. The negative controls carry as much weight as the positive one: TEST C requires the same line hedged toward the user to stay clean, TEST D flips ONLY the sibling's `disable-model-invocation` line and requires the verdict to follow the flag, TEST E requires a bare pointer under `Related Skills` / `When to Use This Skill` / `See Also` to pass **while the identical line under `## Execution` still fires**. Guard integrity: TEST A asserts `SCOPE == FILES_SCANNED`, a scan of ≥3 skills, and each fixed skill named in `AUDITED`, so a clean verdict cannot come from a collapsed or re-narrowed scan (#2219 — pinned again by TEST H's worktree-shaped scan root). Mutation-verified per half against the **verbatim pre-fix** files: the widened scope reports all 5 real sites (`git-pr-watch:32`/`:74`, `git-pr-sync-check:71`, `git-triage:206`/`:208`) and exits 1; restoring the loose regex alone kills 14 assertions, restoring the one-file scope alone kills 3, dropping the `SCOPE`/`AUDITED` keys alone kills 5, and reverting to raw-line matching alone kills exactly TEST J's 2 reflow assertions while both of its guard-integrity halves stay green on **both** versions. The pre-commit trigger was widened from a three-file allowlist to `git-plugin/skills/.*/SKILL\.md` so it reaches every file the whole-scope verdict depends on (#2164's allowlist-drift lesson). | issue #2442 |
| `hooks-plugin/hooks/bash-antipatterns.sh` misread an awk-program comparison (`awk 'NR>1{…}'`) as a file redirect and blocked `git commit -F <path>` while allowing its `gh --body-file` analogue. The first repair masked quoted spans with `sed "s/'[^']*'/''/g"`, which **desynchronises on an odd apostrophe** (`it's`) or the `'\''` escaping idiom and swallows the rest of the line — a genuine `awk … > file` then passed, and a `|` living inside quotes destroyed the `[^|]*` barrier the #2131 fix relies on, producing a new false positive. | Replaced with `mask_quoted_content`, a per-line `in_s`/`in_d` scanner that is a sibling of the existing `strip_trailing_comments` rather than a fourth naive pre-stripping pass (the shape this file's own header records getting wrong across #1701, #1721, #1722, #1848, #1900, #2052, #2058). **A second fail-open was found in that repair and fixed before merge**: `mask_quoted_content … | grep -Eq …` runs under `set -o pipefail`, and `grep -q` exits at its first match while the char-by-char awk masker is still writing — the masker takes SIGPIPE, the pipeline returns 141, and the `if` is false, so the safety block **silently stopped firing from ~6 KB of command text upward**. Both call sites now capture to a variable and match with a here-string. | `hooks-plugin/hooks/test-bash-antipatterns.sh`: the apostrophe and `'\''` shapes must still BLOCK, the quoted-pipe read-only awk must PASS, an unrelated `-F` inside `$( … )` must not grant the commit-file exemption, and the heredoc-body guard now carries a line that *would* exempt under the raw view so it is no longer vacuous. The SIGPIPE fail-open is pinned at three command sizes straddling the observed threshold (40 / 200 / 800 filler lines); mutation-verified — restoring the pipe form fails exactly the 200- and 800-line rows (229 pass / 2 fail) while the 40-line control stays green, and the repaired hook is 231 / 0. | issue #2463, #2462 |
| `code-quality-plugin:bulk-sweep-classify` started at "you have a pattern — classify its matches", so two failures sat OUTSIDE its frame and each produced a clean-looking result over an incomplete scope. **(1) The term set was derived from what a mechanism is NAMED, never from what it CLAIMS.** Removing a mechanism means removing its assertions as well as its identifiers, and the two share no tokens: in `ForumViriumHelsinki/podio-mcp` (#160/#163) a build-time credential injector was swept on the identifiers `BUILD_DEFAULTS`, `inject-build-defaults`, `postbuild` — correctly finding the script, npm hook, constants module and workflow passthrough — while `src/index.ts:134` (`PODIO_CLIENT_ID: … (baked into published package)`) survived in a file the sweep had already edited, because it contains none of the three identifiers. typedoc renders it into the API reference, so the claim outlived the thing that made it true. **(2) An exclusion in the verification scope carried an unverified justification.** The final sweep excluded `docs/api/` reasoning that CI regenerates and publishes it; both halves were false (GitHub Pages was never enabled — `/repos/.../pages` → 404 — and the deploy workflow had failed every run for ten days), so the excluded directory was the ONLY rendered copy and still carried the stale claim. The report said "no matches remain", which was true of what it searched and false of the repo. | Added a `Derive the term set from what the mechanism *claims*, not just what it is *named*` subsection under Step 1 carrying the derivation question ("what did this mechanism promise, and in whose words?"), the identifiers-vs-claims table, and the consequence that decides severity — an identifier left behind is dead code, but a claim left behind is documentation that is now false. **The Verification Trap** gained the exclusion rule: every exclusion in a verification scope is a claim about the world and needs the same treatment as a negative result (state why in one line, then verify that line), pairing with the existing rule that a zero-match verdict must be control-tested. | `scripts/plugin-compliance-check.sh` `check_skill_body()` — a block scoped to `code-quality-plugin/bulk-sweep-classify` pinning three tokens, one per addition: `what did this mechanism promise, and in whose words?` (the derivation subsection), `a claim left behind is documentation that is now false` (the identifiers-vs-claims distinction), `every exclusion in a verification scope is a claim about the world` (the exclusion rule). All three mutation-verified individually — each replaced in isolation produces a ❌ naming that token, and the restored file reports `Body ✅`. **The guard caught a real defect in its own fix**: `grep -qF` is line-scoped, and two of the three phrases had been split across an ~80-col wrap, so the first draft failed its own check — the same trap #2174 records for `WIP at checkpoints`. Fixed by reflowing each pinned phrase onto one line. | issue #2479 |
| `scripts/check-delegation-reachability.sh` could not be widened past its 5-skill git-plugin scope, because a full-corpus run (419 `*-plugin/skills/*/SKILL.md`) took **247.3 s** and reported **47 findings dominated by false positives**. Three structurally distinct classes. **(A) Self-reference**, 30 of the 47: a skill naming its OWN slash command is documenting its invocation, not delegating — `git-api-pr` is gated and writes `/git:api-pr` six times in its own Quick Reference table, so the guard reported it for describing itself (same shape in `hooks-permission-request-hook`, `hooks-session-start-hook`, `hooks-session-end-issue-hook`, `project-init`, `langchain-init`, `blog-post`, `git-issue`, `git-pr-feedback`, `git-upstream-pr`, `blueprint-prp-execute`). **(B) The preamble**: `section` is set only on `## `, so everything above the first H2 — the YAML frontmatter, the `# /ns:command` H1 title this repo puts there by convention, any lead paragraph — was judged with `section=""`, which `is_navigational_section` does not exempt; a skill's own H1 title is not an instruction to the agent. **(C) Teaching the invariant**, the most important: `blueprint-autopilot:82` states this guard's OWN rule correctly *and* forbids the invocation ("Work-order **creation stays human-only** (`/blueprint:work-order` keeps `disable-model-invocation: true` — never invoke it from autopilot)") and was flagged for it. A guard that is red on correct content gets disabled. | All three exempted structurally, the same instruction-vs-explanation discrimination `check-agent-tool-selection.sh` and `check-branch-containment-guidance.sh` already make: (A) a reference resolving to the scanned file itself is skipped before the gating test; (B) nothing above the first `## ` is an action section; (C) a `GATED_STATEMENT_RE` over the reference's LOGICAL UNIT (the same window `USER_REFERRAL_RE` uses, so an 80-column reflow cannot break it) — deliberately narrow, naming the GATE (`disable-model-invocation`, `human-only`) or a PROHIBITION (`never invoke`, `must not be run`, `the model cannot reach`), never a bare `manual` / `user-invocable` / `recommended`. Both exemptions are COUNTED and emitted even at 0 (`SELF_REFS_SKIPPED=`, `GATED_STATEMENT_EXEMPTIONS=`) — an exemption that is never reported is indistinguishable from a checker that stopped judging. Performance: sibling resolution memoized per plugin (one index build, not a re-glob + frontmatter re-read per reference) and frontmatter parsed in pure bash — but the dominant cost was neither: a `printf \| grep -oE` **per scanned line** (~125,000 lines) was ~94% of the 247 s, now a bash `case` pre-filter plus `[[ =~ ]]` extraction. Both `grep` matches moved to here-strings (the #1744 / #2462 SIGPIPE-under-`pipefail` race). Measured after: **FILES_SCANNED=419 ISSUE_COUNT=13 wall clock 15.4 s** (`SELF_REFS_SKIPPED=258`, `GATED_STATEMENT_EXEMPTIONS=3`). **Scope deliberately NOT widened**: performance is no longer the blocker, but all 13 residual findings are in `blueprint-plugin` and each is a judgement call (rewrite into the recommendation form, or un-gate the sibling) reserved for the repo owner — widening now would make the guard red on `main` over unadjudicated content. `SCOPE_IS_REPO_WIDE=false` stays accurate and the measured reason is recorded in the script header. | `scripts/tests/test-check-delegation-reachability.sh` TESTS **K/L/M/N** (71 → 105 assertions), SEMANTIC throughout — each EXECUTES the checker against a planted fixture. **Both polarities for every class**, because a guard that silently stopped detecting anything would pass the positive half alone: K1 a self-reference table is clean (with `SELF_REFS_SKIPPED=2`) while **K2** the identical row aimed at a DIFFERENT gated sibling still ERRORs with `SELF_REFS_SKIPPED=0`; L1 a preamble reference is clean while **L2** the identical sentence under `## Execution` still ERRORs; M1 the verbatim `blueprint-autopilot` unit is clean with `GATED_STATEMENT_EXEMPTIONS=1`, M2 pins each accepted spelling in a sentence carrying **one marker only** (so dropping one regex alternative turns exactly one row red), while **M3** a bare imperative still ERRORs with `GATED_STATEMENT_EXEMPTIONS=0` and **M4** pins that `user-invocable` / `manual` did not become gated statements. N pins that the memoized index still resolves an unprefixed sibling `name`. Guard integrity: every clean case asserts `FILES_SCANNED=1` and TEST A asserts `SCANNED_EMPTY=false` plus a ≥3-skill scan, so no "clean" verdict can come from a walk that opened no file (#2219 / #2290). Mutation-verified per class against the fixed script: reverting A alone fails 3 assertions, B alone 2, C alone 11, and the verbatim pre-fix script 18 — with the guard-integrity assertions green under every individual mutation. | issue #2483 |
| `scripts/check-delegation-reachability.sh` audited **5 git-plugin skills of 423** in the marketplace, so `STATUS=OK` said nothing about the other 418 — the widening #2487 deferred. Two things blocked it, and only one was real. **Performance was a red herring by the time it was measured**: the 2026-09-04 re-run took 26.6 s over 423 skills, and profiling put nearly all of it in two `grep` forks per LINE carrying a `/ns:token` (~4,600 of them), on lines that mostly resolve to a non-gated sibling or to the file itself. **The real blocker was 13 residual findings**, all in `blueprint-plugin`, all toward `/blueprint:work-order` or `/blueprint:prp-execute`, each a judgement call (reword into the recommendation form, or un-gate the sibling) reserved for the repo owner. Widening with them unresolved would have put the guard red on `main` over unadjudicated content, and a guard red on `main` gets bypassed. | Widened to the whole marketplace, discovered at run time rather than listed (a hand-maintained list drifts from the corpus it mirrors — #2164), with the residuals **declared rather than hidden**. One of the 13 is a false positive cleared structurally as **Class D**: `document-linking:402` is the literal H3 `### /blueprint:work-order`, flagged for titling the section documenting that command — an ATX heading is a label, the same reasoning class B already applies to the `# /ns:command` H1. The other 12 findings go in `DELEGATION_ALLOWLIST_DEFAULT` as **9** entries keyed `<file>\|<ref>` and owned by #2483 — the counts differ because a key matches every occurrence in its file, and three files carry two: every marketplace skill is audited and any NEW unreachable delegation anywhere is blocking from this commit, while `ALLOWLISTED=` is emitted **even at 0** and each suppression is itemised, so a suppressed finding is never indistinguishable from no finding (#2219/#2255). A key matching nothing in the audited scope is itself an ERROR (`stale_allowlist_entry`), so the list can only shrink and the ratchet ends when #2483 is settled — no code change needed to remove the last entry. The key is deliberately `<file>\|<ref>`, not `<file>:<line>:<ref>`: a line number drifts on every edit above it, and an allowlist that goes stale on unrelated edits is noise; the cost is that a NEW bad reference to the *same* gated sibling in an already-declared file is not caught, the same coarseness as `check-agent-tool-selection.sh`'s directory-prefix allowlist. Performance was fixed rather than budgeted around: deferring both marker matches until a reference is KNOWN to be a gated cross-skill delegation cuts ~4,600 line-level matches to ~30, and `resolve_sibling` returns through a global instead of a per-reference command substitution — **26.6 s → 2.7 s** over the same 423 files with identical counters, so pre-commit pays ~3 s on a skill edit. Both triggers widened with the scope (`[^/]*-plugin/skills/.*/SKILL\.md`) per #2164. | `scripts/tests/test-check-delegation-reachability.sh` TESTS **O/P** (105 → 127 assertions), SEMANTIC throughout. **Both polarities for each class**, because a guard that stopped judging would pass the positive half alone: O1 an H3 naming a gated sibling is clean (with `FILES_SCANNED=1`, so the pass is attributable to a real read) while **O2** the body *underneath* a heading still ERRORs — without O2, skipping the whole file would pass; P1 a declared residual is suppressed, counted and itemised while **P2** the identical reference ERRORs undeclared with `ALLOWLISTED=0`, so the counter is attributable rather than always-on; **P3** a DIFFERENT gated sibling in an already-declared file still ERRORs, bounding the coarseness; P4 a stale key is an ERROR naming the key, and **P5** an out-of-scope key is not judged stale, without which every narrow fixture run would drown in stale findings for the real repo's residuals. TEST A's coverage assertion was rewritten to compare `FILES_SCANNED` against an **independently computed** corpus count rather than the checker's own claim, with a non-vacuity floor (≥100 skills) — a one-file default once reported `STATUS=OK` over a tree holding three live instances (#2442 review). Mutation-verified in place (a copied `scripts/` dir makes TEST A vacuous, which A3 now fails loudly on): removing Class D kills 4, disabling the allowlist branch 7, dropping the stale check 4, re-narrowing the scope 2, and emitting `ALLOWLISTED=` only when non-zero 1. | issue #2483 |
| `agent-patterns-plugin:parallel-agent-dispatch` documented `isolation: "worktree"` exhaustively but said nothing about **`isolation: "remote"`**, and offered no way to notice that a remote dispatch had **run locally**. Three agents dispatched with `isolation: "remote"` all ran in local worktrees in the shared checkout; nothing in the dispatch tool result indicated the mode in effect. Two were killed by a 600s stall watchdog, and recovery followed the skill's "audit remote, then resume" protocol — `gh pr list` and `git ls-remote --heads origin`, both empty — so the work was reported to the user as unrecoverable. Twice. `git worktree list` showed the branch holding a **complete, unpushed** commit. The remote audit was accurate; the conclusion drawn from it was wrong, because the protocol assumes remote-dispatched work lands remotely. **An empty remote is evidence about the PUSH, not about the WORK**, and the two come apart exactly when an agent dies between committing and pushing — the likeliest single place to die, since the push is last. | Added an `isolation: "remote"` hazard section to `references/worktree-hazards.md` (the mode is a REQUEST that may resolve to a local worktree; the two tells are `git worktree list` and the completion notification's `worktreePath`), an "Audit local worktrees alongside the remote" section to `references/failure-recovery.md` (only remote-empty **and** no worktree **and** no unpushed commits **and** a clean tree supports "nothing landed"), wired into the existing usage-limit recovery protocol's `gh pr list` step, plus the draft-PR-early mitigation that actually worked (commit, push, open a **draft** PR before the bulk of the work, then push after each commit — the retry agent survived its own branch being merged and deleted mid-task on that basis; recovery was one cherry-pick). Bulk of the prose lives in `references/` because SKILL.md sits 442 chars under the 26,000-char ERROR ceiling. | `scripts/check-agent-failure-contract.sh` — five markers, each pinned against the file that OWNS it: SKILL.md `#2447` + `worktreePath`; `worktree-hazards.md` `may resolve to a LOCAL worktree` + `draft PR before the bulk of the work`; `failure-recovery.md` `Audit local worktrees alongside the remote`. Guard cases **N–R** in `scripts/tests/test-check-agent-failure-contract.sh` (13 → 18 assertions) each strip one marker from a fixture and require exit 1. Mutation-verified in both directions: stripping all five `require_marker` calls fails exactly N–R (13 passed / 5 failed), and stripping each marker from the real files makes the checker exit 1 naming that marker. | issue #2447 |
| `agent-patterns-plugin:tool-result-traps` covered "a rejected flag looks exactly like *no results*" but not its mirror, where **the tool did its job**: `gh pr diff <N> -- <path>` takes no pathspec, so it exits non-zero and writes `accepts at most 1 arg(s), received 2` to stderr — and the caller's own `2>/dev/null` discarded the diagnostic, leaving an empty stdout indistinguishable from "the PR does not touch that file". The exit code was lost too: a pipeline reports the status of its **last** command, so `grep`'s status survived rather than `gh`'s. Observed 2026-08 reviewing a PR that claimed `Closes #29`; the empty result was one step from being reported as "the change was never made", and the file was `+34/-5`. No tool-side improvement reaches this class, because the message was written and printed before being thrown away downstream. Worse, the skill's own prescribed remedy **confirms** the false negative: re-running the same shape against a file the PR definitely touches also returns empty, because the shape is broken for every input, so the control agrees with the wrong answer instead of exposing it. | Added a `## Your own `2>/dev/null` turns a loud rejection into a clean negative` section immediately after the rejected-flag section it mirrors, carrying the verbatim reproduction, the explicit control-test-defeat paragraph (what breaks it open is dropping the redirect, not changing the input), the generalising habit (suppress stderr only on a command whose failure mode you have already seen; prefer `2>&1` while learning a shape), and the two correct forms for the concrete case (`gh pr diff <N>` + filter, `gh pr view <N> --json files`). The intro law was qualified in the same commit — it previously asserted "nothing on stderr" for *every* trap, which this one falsifies. | `scripts/plugin-compliance-check.sh` `check_skill_body()` — a block scoped to `agent-patterns-plugin/tool-result-traps` pinning one literal token per load-bearing idea: `failure mode you have already seen` (the habit) and `the control agrees with the false negative` (the control-test defeat — the subtlest half, and the one a prose "tightening" pass would drop first). Both verified to sit on a single line, since `grep -qF` is line-scoped (the `WIP at checkpoints` trap from #2174). Mutation-verified in both directions: replacing either token individually produces a ❌ naming it, and the restored file reports no ❌. | issue #2504 |
| `configure-security.sh` warned `missing_dependabot` on any repo without `.github/dependabot.yml`, with **no Renovate probe anywhere in the script** — so a repo that has run Renovate for years reported `DEPENDABOT=false STATUS=WARN TYPE=missing_dependabot`, reading as a straightforward gap. Acting on it is actively harmful rather than merely noisy: Renovate and Dependabot are **alternatives, not complements** — both open dependency-update PRs and both rewrite lockfiles, so adding the second bot makes them race each other on the same upgrades. The failure mode is a `--fix` run, or an agent working from the report without checking, installing `.github/dependabot.yml` on a repo that deliberately does not want one. Reported against `ForumViriumHelsinki/silverbucket-helper`, whose root `renovate.json` the check simply never looked for; the FVH org standardises on Renovate centrally, so the recommendation was wrong in every repo it fired on. `SECURITY_LAYERS_PRESENT` was also understated, because it counted the layer off `dependabot` alone. | Detect the incumbent before recommending a replacement (the `tool-migration-cutover` instinct). The script now probes eight Renovate config paths plus a **top-level** `renovate` key in `package.json`, emits `RENOVATE=` and a `DEPENDENCY_AUTOMATION=` layer verdict (`RENOVATE \|\| DEPENDABOT`) beside the unchanged `DEPENDABOT=`, counts the layer off the verdict, and warns only when **neither** is configured — phrased as `no dependency automation (Renovate or Dependabot)` rather than naming one tool. `TYPE=` was renamed `missing_dependency_automation`: `grep -rn "missing_dependabot" .` returned exactly one hit repo-wide (the script's own `add_issue` call), so nothing consumed the literal and the token now names a different condition. Still a pure filesystem probe — no network call was added. The `package.json` key is read with `jq -e 'type == "object" and has("renovate")'`, falling back to an anchored `"renovate"\s*:` match without jq; a naive `grep renovate package.json` would also match a devDependency on the Renovate CLI. SKILL.md tells the agent to read `DEPENDENCY_AUTOMATION`, not `DEPENDABOT`, and Step 4 checks the incumbent before writing config — detection alone would have fixed the report while leaving the action wrong. | `configure-plugin/skills/configure-security/scripts/tests/test-configure-security.sh` — a new group EXECUTES the script against fixtures and pins **both polarities**, since a script that never warns would satisfy the positive half alone: a Renovate-only project reports `RENOVATE=true DEPENDABOT=false DEPENDENCY_AUTOMATION=true`, counts the layer in `SECURITY_LAYERS_PRESENT`, and raises no dependency warning; a Dependabot-only project still does the same; and — **guard integrity** — a project with neither still warns. The `package.json` discriminator is planted deliberately: a manifest whose only mention of Renovate is a **devDependency** must report `RENOVATE=false`, which is exactly what a naive substring match gets wrong. The retired `missing_dependabot` token survives only as a negative assertion that it must not reappear. Mutation-verified: with the pre-fix script and the new test, the run aborts at `FAIL: expected RENOVATE=false`; run directly against a Renovate-only fixture the pre-fix script reproduces the report verbatim (`SECURITY_LAYERS_PRESENT=0`, `TYPE=missing_dependabot`). | issue #2495 |
| `session-survey.sh`'s `GIT` section reported `IN_GIT` / `BRANCH` / `DIRTY` / `UNPUSHED` — every direction a checkout can diverge **except behind**. So a stale checkout was indistinguishable from a current one, and the digest's silence read as a positive signal: a three-commits-behind tree produced a confident, specific, entirely obsolete finding (a gitleaks config error already fixed upstream) with nothing anywhere hinting the basis was stale. The `session-spinup` nudge inherited the gap — it fires on "open threads detected", and a clean-but-stale checkout has none, so it stayed silent. | Emit `BEHIND=<n>` beside `UNPUSHED`, from `git rev-list --count 'HEAD..@{upstream}'`. `@{upstream}` rather than a hardcoded `origin/main`: it resolves per branch and simply fails (nothing on stdout, stderr suppressed) on a branch with no upstream or a detached HEAD, which is exactly the degradation `UNPUSHED` already takes. Piped into `awk '{n=$1} END{print n+0}'`, **not** the `\|\| echo 0` form the issue sketched: a fallback after a command that can both print and fail yields a TWO-LINE value and breaks the `KEY=VALUE` contract — the #2286 trap, in the very key whose sibling it broke. `BEHIND>0` is a **caveat, not an error**: `STATUS` stays `OK` exactly as it does for `PROJECT_CONFIDENCE=low`, and the key is emitted unconditionally so the section shape never depends on repo state. Consumers mirror `UNPUSHED`'s treatment — the summary block, the `session-spinup-nudge.sh` thread list, the spinup SURFACE filter, and the collector README's git table. `distill-survey.sh` parses no `GIT` block at all, so it is correctly untouched. | `session-plugin/scripts/tests/test-session-survey.sh` TEST **AO** (26 assertions) EXECUTES the collector against a real bare-origin + two-clone fixture. **AO1 is the guard-integrity half and runs first**: an in-sync branch *with an upstream* must report `BEHIND=0`, without which every other `BEHIND=0` assertion is satisfied by a collector that hardcodes zero. AO2 pins the real count (3) with a **fixture-validity** assertion that `git rev-list` itself agrees, so the test cannot pass against a fixture that never diverged. AO3 pins the caveat contract inside the `GIT` block (`STATUS=OK`, no WARN/ERROR) and that `BEHIND` sits **directly after** `UNPUSHED` by line number. AO4/AO5 pin the two degradations in all three axes at once — value `0`, exit 0, and **empty stderr** — each with its own fixture-validity check that the repo genuinely has no upstream / is genuinely detached, plus the #2286 bare-integer-line guard applied to the new key. AO6 pins parse stability outside a repo; AO7 pins the consumers (summary block, spinup SKILL, README). The nudge half is `session-plugin/hooks/test-session-spinup-nudge.sh` **W3**, whose own guard-integrity assertion requires an in-sync clone to stay SILENT before asserting the stale one fires. Mutation-verified both ways: against the pre-fix collector 12 AO assertions fail, and deleting only the hook's thread line fails exactly W3's 2. | issue #2500 |
| `configure-plugin:configure-ux-testing`'s collector probed **only** `bun.lockb` (`[ -f "${project_dir}/bun.lockb" ] && echo "BUN_LOCKFILE=true"`). Bun's default lockfile is now the text-based **`bun.lock`**, so a current all-bun project reported `BUN_LOCKFILE=false` — a confident wrong answer, indistinguishable from a project that uses npm. `bun.lockb` is still real (older repos, and any project setting `saveTextLockfile: false`), so the fix has to accept BOTH names rather than swap one for the other. A sweep of the six sibling probes named in the issue found the same single-name defect in three more executable detection paths: `project-plugin:project-discovery`'s `discover.sh` MANIFESTS loop (a bun repo reports `package.json` but not the bun signal), `code-quality-plugin:code-dep-audit`'s js-ecosystem OR-chain, and `code-quality-plugin`'s `code-quality-preflight-cue.sh` always-silent lockfile list — the last inverted (a `bun.lock` write cues a lint pre-flight on every `bun install`). Two sites were already correct and deliberately untouched: `hooks-plugin/hooks/test-verification.sh:128` and `git-repo-agent/…/repo_analyzer.py:102` both test for `bun.lock` **or** `bun.lockb`. | Accept both names at all four sites. `configure-ux-testing.sh` uses the issue's prescribed `if`/`else` (the `&&`/`\|\|` one-liner is fine here, but the block form is what the issue specifies and it keeps the comment attachable); the other three add `bun.lock` beside the existing `bun.lockb` in their list/chain. Every emitted key is unchanged — `BUN_LOCKFILE=`, `MANIFESTS:`, `ECOSYSTEM=` keep their names and positions, so the `structured-script-output.md` contract is untouched. The two Context `find` probes that mirror the changed scripts (`configure-ux-testing/SKILL.md`, `code-dep-audit/SKILL.md`) land in the same commit per `docs-currency.md`. Prose that merely LISTS lockfile names (`typescript-plugin:bun-*`, `tools-plugin:deps-install`, `configure-package-management`) is deliberately out of scope — it documents no probe changed here. | `configure-plugin/skills/configure-ux-testing/scripts/tests/test-configure-ux-testing.sh` Case 3 EXECUTES the collector against four planted scratch dirs — only `bun.lock`, only `bun.lockb`, both, neither — because a grep for the string `bun.lock` would also match `bun.lockb` and pass against the pre-fix probe verbatim. **Guard integrity**: the `neither` case must report `false`, without which a probe hardwired to `true` satisfies all three positive assertions; and each case asserts **exactly one** `^BUN_LOCKFILE=` row, so a fix that emits both branches cannot pass. `code-dep-audit`'s Case 8 pins that either lockfile alone yields `ECOSYSTEM=js` (its pre-existing Case 5 — no manifest → `ECOSYSTEM=none` — is the matching non-vacuity anchor), and `test-code-quality-preflight-cue.sh` (d) pins both names silent. `discover.sh` has no colocated test to extend. Mutation-verified: restoring the verbatim pre-fix one-liner turns the ux-testing suite red at `expected BUN_LOCKFILE=true for only bun.lock (current text format)` with the collector's own `BUN_LOCKFILE=false` quoted back, and restoring the fix returns all four rows to green. | issue #2496 |
| `hooks-plugin/hooks/bash-antipatterns-teach.sh` scanned raw `$COMMAND` with **zero** references to `COMMAND_SHELL_ONLY`, while the sibling safety hook `bash-antipatterns.sh` has 24 — so heredoc bodies and trailing `#` comments were counted as shell text. The `long-pipeline` segmenter strips quoted strings and splits on `;` / `&&` / `\|\|` / newlines; a heredoc body is neither quoted nor a statement separator, so every markdown table row survived as its own "pipeline". A row such as `\| `echo/printf > file` -> Write \| 18 \| 16 \|` carries 6 pipe characters and satisfies the discouraged-head test, because the cell text literally contains a `printf` token followed by a space — the detector matched a table cell **describing another detector**, firing on a `gh issue comment` command containing zero shell pipes. The load-bearing asymmetry: the identical table text arriving as a single-quoted argument was already handled correctly, so the hook had the "this is data, not shell" concept and simply did not extend it to heredocs. An audit of the four sibling nudges found the same raw-`$COMMAND` exposure by a **different** mechanism: `^` in grep anchors to the start of every **line**, so a heredoc-body line merely *beginning* with the watched word was read as the executed command — a `git commit -F -` whose message body contained `tail -20 build.log` emitted the Read hint, and `grep -rn foo src/`, `find . -name '*.ts'` and `ls *.md` did likewise. | Lift the projection rather than share it: the teach hook now computes its own `COMMAND_SHELL_ONLY` using the same heredoc-body-stripping awk and quote-state-aware `strip_trailing_comments()` that `bash-antipatterns.sh` has used since #2431/#2106, and all five detectors scan that view instead of raw `$COMMAND`. **`bash-antipatterns.sh` is deliberately untouched** — its detectors are pure-regex *by design* and fire inside heredoc bodies on purpose, because a false positive there is the correct trade for a safety block; refactoring it into a shared helper to serve a style nudge would put that behaviour at risk for no benefit (`.claude/rules/hook-block-vs-nudge.md`). The `cat` nudge was exposed only in principle — its own `! grep -Eq '<<\|cat\s*>'` exemption already suppressed it for any command containing a heredoc — and switching it to the projection is safe because the awk keeps the heredoc-**opening** line, so both `<<` and `cat >` still register. No doc change was owed: `.claude/rules/bash-tool-replacements.md` documents `long-pipeline` as counting pipes *per pipeline, not per invocation*, which stays exactly true — this fixes what counts as shell text, not the documented contract. | `hooks-plugin/hooks/test-bash-antipatterns-teach.sh` (44 → 50 assertions), each case piping JSON into the **shipped** script rather than retyping its logic. The issue's four-case matrix is pinned in both polarities, which is what stops a permissive patch passing silently: case **A** (heredoc body is a markdown table) must be silent, while control **C** (a genuine 5-pipe `cat`-headed pipeline) must still FIRE and **D** (`echo hello`) stay silent — a patch that simply disabled the detector would turn C red. Case **B** (the same table text as a single-quoted argument) pins the pre-existing correct behaviour so the fix cannot regress it. Two **over-strip** controls guard the projection itself: a `grep -rn` on the line *after* a heredoc terminator must still fire, and `cat README.md  # skim the intro` must still fire — a trailing comment annotates a command, it does not exempt it. Mutation-verified against `git show HEAD:…` (the shipped pre-fix hook): **44 passed / 6 failed** pre-fix — case A plus the four sibling-nudge heredoc cases and the trailing-comment case — with controls B, C and D all passing pre-fix, so the suite discriminates in exactly one direction; **50 / 0** post-fix. | issue #2518 |
| `hooks-plugin/hooks/bash-antipatterns-teach.sh` scanned the **raw** `$COMMAND` — 0 references to `COMMAND_SHELL_ONLY` against the sibling safety hook's 24 — so heredoc bodies and trailing `#` comments were read as shell. All six detectors are `^`-anchored and `^` matches the start of every LINE, so a heredoc-BODY line that merely looked like a watched command was nudged as if it were one. Found live when the nudge fired on a `gh issue comment` carrying **zero** shell pipes: the `\|` characters it counted were markdown table cells inside the heredoc body, and a cell containing the word `printf` satisfied the discouraged-head test — the `long-pipeline` detector matched a table cell *describing another detector*. The audit the issue asked for found the exposure in **5 of the 6** detectors (`head`, `tail`, `find`, `grep`/`rg`, `ls`); `cat` was silent only by accident, exempted by its own unrelated `<<` clause. The exposure also ran the **other way**: the read/search detectors disqualify themselves when the command contains a `\|`, so a `\|` anywhere in a heredoc body silently suppressed a genuine hint on the real command beside it. Not a safety-hook defect — the safety detectors are pure-regex by design and fire inside heredoc bodies deliberately, because a false positive there is the correct trade; this hook is a **non-blocking style nudge** with no such justification, so it was spending the agent's attention on markdown. | Feed the teach hook the same projection the safety hook already computes (heredoc bodies stripped, then trailing `#` comments) and scan it in all 13 detector reads. The two awk programs were lifted into `hooks-plugin/hooks/lib/command-views.sh` (`strip_heredoc_bodies` / `strip_trailing_comments` / `command_shell_only`), sourced **fail-open** — a missing library exits 0 rather than erroring, because a teach nudge is never worth a broken hook. The safety hook keeps its inline copy and is left **byte-identical**: its own header establishes that safety blocks must fire in EVERY context and must not depend on something that can be missing, and a sourced file is such a dependency; the two copies are pinned behaviourally by their respective suites rather than textually. Comment stripping runs AFTER heredoc stripping so it can only ever touch executable text, never a body line or its closing delimiter. | `hooks-plugin/hooks/test-bash-antipatterns-teach.sh` "heredoc bodies and trailing comments are DATA, not shell (#2518)" block (15 assertions) replays the issue's A/B/C/D matrix — **B is load-bearing**: the identical table text arriving as a quoted `--body` argument was ALREADY handled correctly, so the hook had the "this is data" concept and simply did not extend it to heredocs. One heredoc-body case per detector, with `cat` pinned rather than fixed. **Guard integrity is weighted equally**, since a projection that blanked the whole command would satisfy every `assert_silent` above: a real `grep` sharing the heredoc-opening line, one after the terminator, and one on line 2 of a multi-line command must all still fire — the last pins that per-line anchoring survives, which is the invariant #2431 established. The comment half is pinned in both polarities too: trailing-comment pipes must not inflate the pipe count, while a genuine 6-pipe scrape carrying a trailing comment must still fire. Mutation-verified with the library and tests held constant: 45 passed / **9 failed** against the pre-fix hook, 54 / 0 after — and two of those 9 are the *false negatives*, so the fix is not merely a suppression. | issue #2518 |
| `scripts/check-plugin-readme-currency.sh` gated its test seam on `-n "${PLUGIN_README_CURRENCY_STAGED:-}"`, so the seam could not express an **empty** staged set — the one state the no-op path exists to handle. `scripts/tests/test-check-plugin-readme-currency.sh` TEST H passes `''` to exercise that path; the `-n` test failed, and the check fell through to reading the developer's real `git diff --cached` instead of the fixture. The case therefore asserted local state rather than the fixture: green on a clean worktree, but two failed assertions (`H: empty staged set is OK`, `H: empty staged set counts nothing`) with anything staged — the normal condition when running `pre-commit run --all-files` before committing. The part that matters is the CI half: a CI checkout has nothing staged, so the fall-through read an empty diff and the case **passed for the wrong reason**. It could not fail in CI and could not reliably pass locally — a test pinning nothing in either environment. | Distinguish *set-but-empty* from *unset* with `${PLUGIN_README_CURRENCY_STAGED+set}` rather than `-n`. An explicitly-empty value now means "no staged files" and an unset variable keeps the existing `git diff --cached` read, so **no call site changes**: verified by a repo-wide grep (nothing outside the test sets the variable) and by `.pre-commit-config.yaml` invoking the script bare. The alternative the issue floated — a second variable disabling the git fall-through outright — was not taken, since it adds a seam where the existing one only needed the right emptiness test. | `scripts/tests/test-check-plugin-readme-currency.sh` TEST **I** (25 → 30 assertions). TEST H alone can never fail in CI — with a clean index the fall-through is indistinguishable from the fixture — so H cannot be the regression check for its own bug. TEST I supplies the missing half: a throwaway sandbox repo with a plugin skill **genuinely staged**, run with an explicitly-empty seam, so the fixture and the index disagree and only a correct seam can satisfy it. Mutation-verified against `git show HEAD:scripts/check-plugin-readme-currency.sh`: on a **clean index — the CI condition** — the new suite reports 28 passed / 2 failed, exit 1, which is precisely the property the bug removed; with a file staged, 26 / 4 (H's two plus I's two); the fixed script reports 30 / 0 in both index states. **Guard integrity**: an *unset* seam must still read the index (`PLUGINS_CHANGED=1`, `STATUS=WARN`), and a fixture-validity assertion confirms the sandbox index is genuinely non-empty — both pass against the pre-fix *and* fixed script, which is what a control should do. Sandbox follows #1692/#1745: guarded `mktemp -d`, full `GIT_DIR` family unset. | issue #2521 |
| `frontmatter()` in `health-plugin/scripts/config-drift.py` ended the frontmatter block at the first `---` **substring** (`body.split("---", 2)`), not at the closing fence **line**. Every key after an inner `---` was dropped — including `paths:`, so a path-scoped rule was billed to the always-loaded budget (`always_loaded_rules` 0 → 1, measured). **118 files in this corpus already use a bare `---` line as a thematic break in prose**, and the bug fires with no block scalar at all: `description: Use the --- separator form` yields `{'description': 'Use the'}` and drops `paths`. It was latent rather than live only because no rule happened to carry an inner `---` at the time; the same commit's docstring newly advertised `description: \|` as the supported multi-line form, which is what makes it reachable. | `frontmatter()` became a line scanner with four documented properties: the block ends at the first closing fence LINE; a key is recognised only at column 0 (so a list item or a `key: value` inside a block-scalar body cannot shadow a real key); a bare-empty value stays `""` so `paths:` keeps its KEY present (consumers test presence, never truthiness); and block bodies plus indented plain-scalar continuations join with single spaces. Accepted cost, pinned by its own assertion: frontmatter whose first non-blank line is a YAML comment now parses to `{}` — a markdown `# Heading` and a YAML comment are byte-identical, so admitting one reopens the prose-as-frontmatter hole. 0 of 757 fenced documents open with a comment. | `health-plugin/scripts/tests/test-config-drift.sh` TEST 11 plants a path-scoped rule whose description carries a `---` thematic break and asserts `always_loaded_tokens` does NOT rise; TEST 10 loads the shipped `frontmatter()` via `importlib` and feeds it real frontmatter, because the widened keys (`allowed-tools`, `maxTurns`) have no consumer and therefore no observable consequence in `--format=json`. Differential against PyYAML 6.0.3 over all 964 fenced files: 0 missing keys, 0 spurious keys, 0 truncated values. | PR #2531 |
| `health-plugin/hooks/config-drift-probe.sh` ran the analyzer with `2>/dev/null` and, on empty output, called `drift_emit` — whose empty `findings` array **is** the "checked, no drift" verdict per `hooks-plugin/hooks/lib/drift-protocol.sh`. A dead analyzer was therefore indistinguishable downstream from a clean corpus. `origin/main`'s analyzer imported nothing and could not die before producing output, so this failure class was introduced by the same PR that extracted `lib/probe.py` — a broken import fails silently, which made it the highest-risk defect in that change. The first repair gated detection on `ANALYZER_RC -ne 0` and was still wrong: an analyzer producing nothing (or non-JSON) while exiting **0** — a truncated file from an interrupted install — fell through to the same false all-clear, and the inline comment asserted the opposite of what the code did. | Key the branch on `[ -z "$OUT" ] \|\| ! jq empty` and let the exit code and stderr choose only severity and wording: `--format=json` prints unconditionally, so empty output is BY ITSELF proof the analyzer did not run. Exit 3 (bad root) is `warn`/`analyzer_bad_root`, not an analyzer failure. An unreadable corpus file is skipped and REPORTED (`corpus_unreadable`, `warn`, naming the path) rather than aborting the sweep — symlinked rules directories are normal under chezmoi/stow, and one dangling link previously cost the entire run; skipping it *silently* would have been worse, since a corpus two thirds of which failed to open would report a clean sweep. The stderr line is chosen by exception shape (`^[A-Za-z_.]*(Error\|Exception):`), not `tail -n 1`, because `atexit` handlers and mise/asdf shims write after the traceback. | `health-plugin/scripts/tests/test-probe-lib.sh` D1/D4/D5 copy the shipped hook into a fixture tree and drive it with a stub analyzer: exit 0 + empty stdout, and exit 0 + non-JSON stdout, must each emit `analyzer_failed` and never `"findings":[]`. **Guard integrity** carries equal weight — a healthy analyzer must forward its own findings and must NOT emit `analyzer_failed`, and a genuinely clean corpus must still write `[]`, without which every assertion passes against a hook that always cries wolf. Mutation-verified: re-gating on `ANALYZER_RC` alone kills 4 assertions. | PR #2536 |
| The acceptance gate for the `lib/probe.py` extraction (`health-plugin/scripts/tests/compare-ref-output.sh`) defaulted to `BASE_REF=origin/main` and would have **stopped comparing anything the moment its own PR merged** — baseline and subject become the same bytes, `cmp` agrees, and it reports `PASSED=7 STATUS=OK` having compared a file with itself. Its header anticipated the ref *ageing out of the clone*; identity arrives first, on merge, and silently. The sibling failure was live immediately: the gate extracted only `config-drift.py` from the ref and not the `lib/probe.py` it had just started importing, so the baseline died with `ModuleNotFoundError` at exit 1 — which is also the analyzer's normal "found warnings" code, so the exit-code guard passed, the BEFORE run's captured stderr was never read, and `cmp` reported a **content difference** with the whole report shown as newly added. A dead oracle attributed to the change under test. | Three states, named in the script's own header and each with its own exit code: an older ref across an intentional output change FAILs and that is correct (exit 1); a ref byte-identical to the subject is a HARNESS ERROR (exit 2, `PASSED=0`); only a genuinely comparable ref can pass. `cmp -s "$BEFORE" "$AFTER"` before any comparison; a non-empty BEFORE stderr, or an empty BEFORE stdout from a format that prints unconditionally, short-circuits to the harness-error branch rather than falling through to `cmp`. `lib/probe.py` is extracted from the ref when the path exists there. See `~/.claude/rules/validate-adversarial-constructions.md` § *A comparison harness pinned to a moving baseline stops discriminating*. | `test-probe-lib.sh` D2/D3 build dangling commits via `commit-tree` under a redirected `GIT_INDEX_FILE` (no ref, no HEAD, no working index touched — #1692/#1745) and drive the shipped gate against each state: identical ref → `HARNESS_ERROR`/exit 2/`PASSED=0`; a baseline that is `import sys; sys.exit(1)` → harness error, `FAILED=0`, never a content FAIL. **Controls**: a ref that differs in bytes but produces identical output must still report `ok`, and a genuine content difference must still be a `FAIL` and not a harness error — otherwise the fix degrades into "every ref is an error". Verified against all six states the gate can be in (older, identical, newer, missing ref, missing path at ref, empty-and-exiting-1). | PR #2536 |
| `config-drift.py` discovered agents with `root.glob("*-plugin/agents/*.md")` — anchored at **depth 2**, while rules, skills and CLAUDE.md all come from the recursive pruned `walk()`. Measured: `AGENTS=21` at the claude-plugins root but **0** at `~/repos/laurigates` and **0** at `~/repos`, against 21 really present below each. The SessionStart probe runs `--root "${DRIFT_CWD:-.}"` — the session cwd — and `~/repos` is a documented Claude Code working root with its own `CLAUDE.md` and `.claude/rules/`, so **the entire corpus widening was inert exactly where the probe runs**, reporting a bare `AGENTS=0` indistinguishable from "this tree has no agents". The #2219/#2290 `SCANNED_EMPTY` standard could not catch it: the other three kinds scanned fine, so this is a per-kind blind spot in a walk that is not empty. The glob's stated justification — excluding two Python source packages named `agents` (`git-repo-agent/src/…`, `vault-agent/src/…`) — is real, but it is bought by the `*-plugin/` PREFIX; the depth was never doing that work. `Path.match` is also fnmatch-based, so `*` matches a leading dot and the glob was ingesting `.claude-plugin/agents/*.md`. | Discover through the existing pruned `walk()` with `dirpath.match("*-plugin/agents")`, plus an explicit `.claude-plugin` exclusion. 21 at all three roots, file list byte-identical to `find`. Added `agent_discovery_misfire` (`error`) on the #2219/#2290 pattern: `*-plugin/agents` directories present with ZERO agent files is a discovery misfire, not a clean tree. Its denominator is agent **directories**, not plugin directories — the first draft used plugin dirs and would have raised a permanent error on nearly every tree, since 36 of 49 plugins define no agents. | `test-config-drift.sh` TEST 19 asserts an agent BELOW the root's immediate children IS discovered, with controls (decoupled from the depth assertion so they can pass while it fails) for both Python source-package shapes and for `.claude-plugin/agents`; plus a three-rung misfire ladder including `--gate` exit 2 and the opposite polarity (a plugin with skills but no `agents/` dir raises nothing). Non-false-positive control measured rather than argued: the analyzer run against **all 78 real git repos** under `~/repos/laurigates` and `~/repos/ForumViriumHelsinki` produced 0 misfire findings. | PR #2541 |
| Excluding the one scaffold-template `CLAUDE.md` from the widened corpus has two traps, and the obvious implementation hits both. Adding `templates` to `SKIP_PARTS` **deletes a real skill** — executed, it drops `obsidian-plugin/skills/templates/SKILL.md`, a genuine frontmattered skill, which also silently breaks `check_stub_integrity` for any stub delegating to it. The collector-local predicate that replaced it was then wrong in BOTH directions: it kept `packages/create-widget/template/` (npm's `create-*` convention **and** copier's own default `_subdirectory: template`) and an unrendered `{{cookiecutter.project_slug}}/` under `cruft.json`, while EXCLUDING any Flask/Django `webapp/app/templates/CLAUDE.md` and a template repo's own root document — the manifest check ran before the `cur == root` termination, so with `--root` on a template repo every CLAUDE.md in it vanished. The over-broad half is the worse one: it silently drops live configuration. **The counters could not have caught it** — before and after report identical `CLAUDE_MDS` and `CLAUDE_MD_TEMPLATES_EXCLUDED` over the same fixture tree while ingesting *opposite* documents. | A declare/suggest split. Sufficient alone: an unrendered path **component** (`{{`/`{%`), or a generator manifest at a **strict ancestor**. Corroboration only: a manifest beside the document, a `template`/`templates` component, an npm `create-*` parent. `cruft.json` added to the manifest set. The root's own CLAUDE.md is never excluded. Fixing `my-copier-template` only at `dirpath == root` was rejected — it makes the verdict depend on where `--root` was pointed, which is the same depth-anchoring disease as the agent glob one level over. An unrendered-marker scan over document CONTENT stays declined (`scripts/check-unrendered-templates.sh` owns that signal, and a CLAUDE.md may legitimately quote `{{ }}` in prose); a path COMPONENT carries no such ambiguity. | `test-config-drift.sh` TEST 20 is a six-fixture MATRIX, each as its own root so verdicts are exact, asserting both polarities — 4 of 6 are wrong against the pre-fix predicate, in both directions. **The assertion that kills the `SKIP_PARTS` implementation** is that a skill directory literally named `templates` is STILL discovered. Per-signal isolation was rebuilt after the first attempt was contaminated by a manifest at the fixture root: the unrendered-component signal is pinned by a fixture with NO manifest anywhere, with a rendered-name control, because the two corroborated fixtures let either single signal be deleted with the suite still green (mutation M4 survived on exactly that). | PR #2541 |
| The infra-compliance security scan flagged `.github/workflows/test-skill-scripts.yml` 🔴 for a leaked token it does not contain. The detector at `scripts/infra-compliance-check.sh` was `grep -qiE '(ghp_\|gho_\|github_pat_\|sk-\|Bearer [A-Za-z0-9])'`; the `sk-` alternative — an OpenAI key prefix — is only three characters and matches the literal string **`task-`**. All three hits were YAML comments (`taskwarrior-plugin/skills/task-add/`, `test-task-id-stability.sh`, `` `task-3.4.2.tar.gz` ``), and `grep -c 'secrets\.'` on that file returns **0** — it references no secret at all. A fourth uncounted instance sat in `.github/workflows/workflow-model-audit.yml` (`task-list tools`). A false 🔴 SECURITY row is the most expensive kind: it costs the dashboard 2 points and trains the reader to discount the section. | Length-anchor both loose alternatives: `sk-[A-Za-z0-9_-]{20,}` and `Bearer [A-Za-z0-9_.-]{20,}`. **No `\b`** — it is GNU-only and breaks BSD/macOS grep (`.claude/rules/shell-scripting.md`), and the anchor that actually does the work here is the length quantifier, not a word boundary. | `scripts/tests/test-infra-compliance-check.sh` — paired polarities, because a zero-findings-on-the-live-corpus assertion passes vacuously if the detector is deleted: a fixture whose only `sk-` is inside `task-` (and a `task-3.4.2.tar.gz`) must raise **no** security row, while positive controls carrying a synthetic `sk-`+40 chars and a `ghp_` token must still raise one. Driven through the new `--project-dir` flag so the classifier can be aimed at a `mktemp -d` fixture root. Mutation-verified: restoring the bare regex fails exactly the two negative assertions while both positive controls stay green. | PR #TBD (#2555) |
| The same dashboard's Filters check was **grep-anywhere** and therefore wrong in both directions at once. It grepped the whole file — comments included — for `paths:` without first establishing that the workflow even has a `pull_request` trigger. `.github/workflows/stranded-work-audit.yml` (cron + `workflow_dispatch` only, so it structurally *cannot* carry a path filter) was flagged ⚠️ because a prose comment on line 5 reads "rather than a `pull_request: closed` workflow". In the other direction `.github/workflows/plugin-pr-checks.yml` **falsely PASSED**: its comments contain the string `paths:` while arguing at length that it deliberately has none — the check was satisfied by the prose explaining its own absence. A detector satisfied by a comment about itself is not measuring the file. | Make the check trigger-aware: resolve `pull_request` structurally (`yq -r '((.on // .true) \| has("pull_request"))'` when `yq` is present, a comment-stripped anchored match otherwise), then match `(paths\|paths-ignore):` against the comment-stripped view only. Non-`pull_request` workflows report `N/A` rather than ⚠️. Crucially this needs an **explicit opt-out**, not just a tighter regex: `plugin-pr-checks.yml` and `validate-plugin-configs.yml` are REQUIRED status checks and a path-filtered required check never reports its context, wedging the merge permanently with no bypass (#2258) — so both carry an `# infra-compliance: paths-exempt` marker inside their `on: pull_request:` block, and the checker honours it. Without the marker the fix would trade one false positive for two false negatives-turned-positives. | `scripts/tests/test-infra-compliance-check.sh` — four fixture workflows pinning every branch: real `pull_request` + `paths:` → ✅; real `pull_request`, no `paths:`, no marker → ⚠️; the same with the `paths-exempt` marker → N/A; and a cron-only workflow whose *comments* mention both `pull_request:` and `paths:` → N/A. Each case runs on both the `yq` and the no-`yq` fallback branch. Mutation-verified: restoring the grep-anywhere form reproduces both polarities of the defect — the exemption is ignored, the cron-only workflow goes ⚠️, and `plugin-pr-checks.yml`'s shape reads ✅. | PR #TBD (#2555) |
| The weekly Research Radar surfaced a paper by proposing to stamp a Zero-Shot/Few-Shot/CoT/PoT technique catalog onto `prompt-engineering-plugin` — a surface that does not exist (the plugin holds one skill and no technique catalog; zero hits for any of those six terms across the plugin and `.claude/rules/`). The cause is in the generator, not the output: `.github/workflows/research-radar.yml` instructed the model *"Do not read plugin source files — you are mapping ideas to plugin **names**, not editing them"*, while `--allowedTools` already granted `Read,Grep,Glob`. Combined with a hardcoded area→plugin table row, the prompt made the mapping name-only, so the radar could not catch this class of error by construction. Note a path-existence probe would **not** have caught it — `prompt-engineering-plugin/skills/*/SKILL.md` does exist; only a **token** probe distinguishes "the plugin has files" from "the referenced content exists". | Replace the blanket clause with a bounded token-level grounding requirement: one `Grep` per surfaced paper for the concrete surface the suggestion asserts exists (technique name, rule heading, frontmatter key); on no hit, re-target or **drop the paper**; state the exact file(s) and pattern grepped in the issue body; cap at one probe per paper so the `--max-turns 20` budget still holds. `--allowedTools`, `--model opus` and `--effort` are untouched. | `scripts/check-research-radar-grounding.sh` (`--strict`, `=== RESEARCH RADAR GROUNDING ===` / `KEY=VALUE` / `STATUS=` per `structured-script-output.md`) asserts the three load-bearing tokens are PRESENT **and** that the old `Do not read plugin source files` clause is ABSENT — the prose lives in a YAML block scalar that stays valid YAML while losing the clause, so nothing else would notice. `scripts/tests/test-check-research-radar-grounding.sh` drives both directions over `mktemp -d` fixtures, with a guard-integrity assertion that the checker emits its block at all (an empty-output guard would make every `ISSUE_COUNT` comparison succeed against an empty string) and an empty-fixture case pinning `WORKFLOW_PRESENT=false`. Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml`. | PR #TBD (#2507) |
| `scripts/tests/test-agentic-audit-precompute.sh` passed locally and failed on the GitHub runner, on the one assertion that proves the stale list is populated (`FAIL: the 400-day-old skill appears in the stale list`). Its `section()` slicer passes boundary headings as awk regexes, and the stale-list boundary was written `'^## Skills with stale reviews \(>90 days\):$'`. **A backslash-escaped paren is undefined in POSIX awk**: the runner's mawk warns `escape sequence \`\(' treated as plain \`('` and then reads the parens as a GROUP, so the heading never matches, that section slices EMPTY, and the assertion fails. Newer mawk (1.3.4 20240123, on the dev box) tolerates the same input, so the local pass was a false negative — the two `awk: warning:` lines in the CI log were the only tell, and they are easy to read as noise beside a test failure. The deeper defect is that an empty slice is **silently satisfying**: every "must NOT contain" assertion over that section passes vacuously, so a mis-slice weakens the suite in a direction no failure reports. | Bracket expressions (`[(]>90 days[)]`) — unambiguous in POSIX ERE, no escape, no flavour-dependent behaviour — with the reasoning recorded at the line. Every other boundary in the file was already paren-free, which is why only this one section broke. | Two guards, since the fix and the class need different ones. The boundary itself is pinned by the existing assertions (an empty `stale_section` fails them). The CLASS is pinned by a new loop asserting all four sliced sections are **non-empty** before any assertion runs, failing with `FAIL: <name>_section sliced empty — a boundary heading did not match.` and exit 1 — so a future mis-slice reports the cause instead of a confusing downstream failure, and the vacuous-pass direction is closed. Verified by mutation in both directions: an old-mawk emulation shim (stripping `\(`/`\)` from `-v` values before handing off to the real awk) reproduces the CI failure exactly against the pre-fix file (`Passed: 21, Failed: 1`, same message) and reports 22/0 against the fixed one; replacing the boundary with a non-matching literal fires the new guard. | issue #2557 (CI) |
| Ten of the twelve `hooks-plugin/hooks/test-*.sh` regression suites had **no pre-commit trigger**, so editing a hook gave no local signal — the failure surfaced only after a push. Only `test-bash-antipatterns.sh` and `test-bash-antipatterns-git-env-isolation.sh` were wired. The reported premise was that these suites are *"never executed by CI or pre-commit"*; **the CI half is false** — `scripts/run-skill-script-tests.sh` discovers all 13 via its `*/hooks/test-*.sh` glob and `.github/workflows/test-skill-scripts.yml` fires on `*-plugin/hooks/**` (verified: `--only 'hooks-plugin/hooks/test-*.sh'` reports `TOTAL=13 PASSED=13`). The premise is an easy one to reach and worth recording: a repo-wide grep for a suite's **filename** finds only its own usage comment, because discovery is glob-based, so every glob-discovered test reads as entirely unwired. The real gap was local-only, the same shape as #2141 (a developer-friction defect invisible to every green PR) rather than the #2164/#2219/#2221 class where a guard genuinely had no CI signal. | Added one pre-commit entry per unwired suite, modelled on the two existing bash-antipatterns entries (`language: system`, `pass_filenames: false`, a `files:` regex naming the hook under test and the suite). Three pairings are not name-symmetric and were resolved by reading the suite rather than by pattern: `test-bash-antipatterns-teach.sh` also triggers on `lib/command-views.sh` (sourced by the hook under test, #2518); `test-git-stash-reminder.sh` triggers on `git-stash-session-init.sh` too (it drives both halves of the pair, #2306); and `test-test-verification.sh` covers a hook that is itself named `test-verification.sh` — neither is the other's typo, which a mechanical `s/test-//` would have gotten wrong. Slowest suite is 5.5s (`test-external-pr-merge-guard.sh`) and every regex is narrow, so the local cost lands only on a commit that touches that hook. | No new check — this is a **wiring** defect, not a detection one, and each suite IS the detection. Verified per entry rather than in aggregate: `pre-commit run --files hooks-plugin/hooks/<hook>.sh` fires exactly its own suite for all eleven hook files (including `git-stash-session-init.sh`, whose trigger belongs to a differently-named suite), and `pre-commit validate-config` passes. Composition with #2574 was checked rather than assumed. At the time of writing its fix was unmerged, so its branch's hook and suite were run together (**33 passed, 0 failed**) against a base where the suite was only 26 — the 33rd assertion is the control pinning `PATTERN_FUTURE`/`PATTERN_MARKER` against silent narrowing. That fix has since landed as #2579, and the merged tree runs **33 passed, 0 failed** through this entry, so the control is wired rather than merely anticipated. | issue #2574 (wiring half) |
| `scheduled-audits.yml`'s `docs-index` job failed on every run and the Layer-1 docs-drift audit never once executed. The label step is the job's FIRST step, so its failure skipped `Check for existing open issue`, `Run the audit script` and `Create the issue` — the run was red with none of the work attempted, and the workflow-level conclusion made the whole file look broken when its other three jobs (`agentic-audit`, `infra-compliance`, `blueprint-health`) were green. Cause: an unquoted `color: 5319e7`. The Actions runner resolves `with:` values under the **YAML 1.2 core schema**, where `<digits>e<digits>` is a float, so `gh label create` received `--color 53190000000` and answered `HTTP 422 / Label.color is invalid`. The three sibling colours in the same file survive only because each happens to contain a hex letter outside `[0-9e]` (`0e8a16`, `1d76db`, `d93f0b`), which makes the scalar unresolvable as a number — so the hazard is invisible on review and fires on roughly one colour in 250. `scripts/check-workflow-labels.sh` already owned this exact failure shape (a label step killing a job) but only asked whether a label was PROVISIONED, never whether the provisioning call would succeed. | Quoted the colour, and extended the guard to validate the colour a label create actually SENDS. **PyYAML cannot be used to do this**: it implements the YAML 1.1 resolver, whose float rule demands both a `.` and a signed exponent, so `yaml.safe_load` returns the *string* `"5319e7"` — a guard built on it reports OK on the very input that 422s in CI (measured; the first draft did exactly that and passed). The check therefore models the 1.2 core schema explicitly against the RAW source text. It also discriminates by context, which is what keeps it usable: a `with: color:` mapping value is number-resolved, while `--color 5319e7` inside a `run:` block scalar is opaque to the resolver and reaches `gh` verbatim — coercing both would report the working call site at `golden-set-evaluation.yml:109` as a defect. | `scripts/tests/test-check-workflow-labels.sh` cases **K/L/M** (33 → 49 assertions). K replays the verbatim broken shape and requires the file:line plus `SENT=53190000000` — the rendered number is the evidence, being what GitHub answered 422 to and the half a reader cannot reconstruct from the source. **L is the load-bearing counter-case**: the same token inside a `run:` block must NOT be flagged, with `COLOR_SITES=1` asserted so "not flagged" cannot pass against a scan that skipped the file. M pins the resolver's edges in both polarities — `000000` → `0` and `002200` → `2200` flagged (every leading-zero colour collapses), while `"5319e7"` quoted, `0e8a16` (unresolvable → string) and `123456` (resolves to an int that restringifies identically, so `gh` gets a valid colour) must all pass, so the fix cannot degrade into flagging every colour. **Guard integrity**: TEST A now asserts `COLOR_SITES` is non-zero on the real repo, without which every "colour is fine" assertion would hold for a checker that never looked at one. Mutation-verified twice — against the pre-fix guard **16** assertions fail; against a copy with ONLY the block-scalar discrimination removed exactly **5** fail, including the real-repo run reproducing the `golden-set-evaluation.yml` false positive. | issue #2493 (docs-index job) |
| `research-radar.yml` ran **15 times green** between 2026-06-15 and 2026-09-02 and filed **5** issues. Nine consecutive weekly runs — 2026-07-01 through 2026-08-19 — reported success and filed nothing. Comparing a gap run against one that filed shows the two differ in exactly one place: the gap run's SDK options carry **no `allowedTools` at all**, so the action's default policy denied every `gh issue create` (2026-08-05: 11 turns, $1.80, `permission_denials_count=6`, no issue; 2026-08-26: 6 turns, $0.95, `permission_denials_count=1`, filed #2507). The tool list had been written under `additional_permissions:`, which takes a GitHub permissions MAP, so it was never applied — the workflow's own comment now records that cause, but reads as though the inert list left tools *unrestricted*; the logs show the reverse. The SDK exits 0 on a denial, so every run was green. Cost: ~9 Opus runs, ~1,200 candidate papers triaged and discarded, nine weeks of research signal. **The permission bug is already fixed; what was missing is any way to notice.** A green run that files nothing is indistinguishable from a green run that had nothing to file — and the radar's prompt explicitly blesses "open no issue" as a common outcome, so the silence looked correct. | Added `scripts/check-audit-liveness.sh`, wired as a monthly `audit-liveness` job in `scheduled-audits.yml`. **Gating on `permission_denials_count` was rejected on measurement, not taste**: the HEALTHY 2026-08-26 run has one (the prompt forbids `WebFetch` and the allowlist excludes it, so that denial is the designed boundary working), and a `denials > 0` gate would be red on a good week and get switched off. The signal is the **streak**: one quiet week is legitimate, a run of them is not. A run counts as having filed when an issue carrying the workflow's label was created within two hours of the run starting — generous for a ~3-minute job, far tighter than the weekly cadence, so a run can never claim its neighbour's issue. The streak is counted from the newest run backwards and stops at the first run that filed, so the check asks whether output is happening **now**. | `scripts/tests/test-check-audit-liveness.sh` (24 assertions) drives the checker through a `--fixture` seam over the **real** run/issue history, so each assertion says what the check would actually have done at the time. **Cases B and C carry the weight and pull in opposite directions**: B is the mid-gap 2026-08-19 state (`SILENT_STREAK=8`) which must FAIL and whose remedy text must name both `allowedTools` and `permission_denials_count`, while C is a genuine two-silent-week stretch from the same history that must stay green — without C a checker hardwired to fail passes B, and without B one that never fires passes C. Replayed date by date, the default threshold first fires after the 2026-07-22 run, recovering 5 of the 9 lost weeks. **Guard integrity**: TEST A asserts `RUNS=14` / `ISSUES=5` / `SCANNED_EMPTY=false` on the current fixture, without which every "not reported" assertion would hold for a checker that parsed nothing. E pins that `--issue-body` emits **nothing** on a clean sweep (the `script-to-issue` skeleton files an issue from stdout, so a non-empty clean body would open a monthly issue saying nothing is wrong), and F pins the unknown-argument exit 2 (#2057) plus a non-integer threshold. Listed in `scripts/required-to-run-tests.txt`: it runs entirely off committed fixtures, so no environment makes a skip legitimate. | issue #2493 (research radar) |
| Two follow-on defects from the audit-liveness work, both found by pointing the new check at a second workflow. **(a)** `scripts/check-audit-liveness.sh` shipped watching one workflow. `changelog-review.yml` has the identical signature and is worse: **22 successful runs, 13 consecutive silent**, last tracking issue #1733 on 2026-06-21 (covering 2.1.138 → 2.1.185), and the 2026-08-31 run spent ~$4.77 over 22 turns with `permission_denials_count=15` while producing no issue, no PR and no commit. The script's own inclusion rule ("a workflow that also pushes commits or opens PRs has other evidence it ran") arguably excluded it — but changelog-review only opens its follow-up PR *after* its tracking issue exists, so when it goes quiet there is no artifact of any kind. **(b)** `.claude/rules/agent-development.md:139` instructed "**up to 5 levels deep** … a spawn beyond five levels is refused". Verified against the upstream changelog through 2.1.258: **2.1.217** turned nested spawning off by default and introduced `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH`, and **2.1.219** set the default to **3**. `SPAWN_DEPTH` had zero hits repo-wide. This is an instruction, not one of the file's dated `Note` callouts, so a wave designed for five levels is refused at four — surfacing as a failed spawn mid-wave rather than a configuration error. | (a) Both workflows are now in `WATCHED`, and the inclusion rule was restated around the property that matters: whether a silent run is INDISTINGUISHABLE from a working one, not whether the workflow could in principle emit something else. (b) The ceiling corrected to 3 with the full 2.1.172 → 2.1.181 → 2.1.217 → 2.1.219 history kept as a table, since the whole failure was someone reading a superseded changelog entry. **The two are one story**: 2.1.217/2.1.219 fall inside **2.1.186–2.1.231**, a band no changelog review ever covered — the automation went silent after 2.1.185 while `.claude-code-version-check.json` was advanced **by hand** to 2.1.257, so the ledger asserts a review that never happened. `agent-development.md` was touched by the Fable 5.1 sweep on 2026-09-02 (`modified:`/`reviewed:` both that date) and the stale ceiling survived, because that sweep's declared window started at 2.1.232. | `scripts/tests/test-check-audit-liveness.sh` cases **G/H** (24 → 33 assertions). G drives changelog-review's REAL run and issue history and requires `SILENT_STREAK=13`; its non-vacuity half asserts `ISSUES=8` and `RUNS=22`, without which a streak of 13 would only mean "no issues anywhere" and would prove nothing about the run↔issue correlation. **H is the case that matters for the production config**: `WATCHED` now has two entries, so the multi-workflow path is the one that actually runs and a single-entry suite would never exercise it — H asserts `WORKFLOWS_WATCHED=2`, exactly one finding, and that the healthy workflow's row is still `SILENT_STREAK=0 OVER=false`, i.e. that a silent neighbour does not drag it down. The nesting fix needs no new script check: the claim is verifiable only against the upstream changelog, and pinning a version number in a guard would just relocate the staleness. A repo-wide grep confirms no other file cites the 5-level ceiling. | issue #2493 (changelog review) |
| `scripts/infra-compliance-check.sh`'s checkout gate was **inverted and half-blind**, and #2575 fixed two other false-positive generators in the same file without touching it. `:137` warned whenever the version was **not** the hardcoded `v4`; every one of the repo's 28 `actions/checkout` refs is `@v6`, so all 28 warned permanently, the report unconditionally advised "Update workflow action versions" on actions already at the newest major, and an actual **downgrade** to `@v4` scored ✅. Separately `:134` used `grep -m1`, so only the first ref per file was read — five workflows carry more than one (`scheduled-audits.yml` has 5), making a stale pin in any later step invisible. Proven by fixture: a v4-only workflow rendered `\| a-v4.yml \| v4 \| … \| ✅ \|`, and a file pinning `v6` then `v3` rendered byte-identically to a compliant row. 20 of the 23 failing workflow-health checks were this one gate (66/89; overall score 92). `scripts/tests/test-infra-compliance-check.sh` contained **zero** occurrences of `checkout`. | A hardcoded "current" version inside an audit script is guaranteed to rot — the same disease as the version ledger and the subagent-depth ceiling in the sibling rows. Currency belongs to Renovate, which already manages this surface (`.claude/rules/version-pinning.md`); **drift** is what a repo audit can actually see. The gate now asks whether the repo's pins AGREE: every ref per file is collected (not just the first), a file pinning two distinct versions is drift on its face, and a single pin that disagrees with the repo's dominant major is a laggard. A Renovate bump that lands everywhere keeps every row green; a *partial* bump flags exactly the laggards. Measured after: 86/89, overall 99. Three `set -euo pipefail` traps were hit and fixed while writing it, each documented at the line: a `grep` matching nothing exits 1, a mid-pipeline `head` can SIGPIPE its upstream to 141 (#1744/#2462), and a false `[ … ] && var=…` as the last command of a `case` branch aborts the run. `find`/`xargs` are deliberately avoided — the suite's own restricted-PATH shim carries neither, so using them silently aborted 14 pre-existing filter assertions. | `scripts/tests/test-infra-compliance-check.sh` gains 11 assertions (33 → 44). **The control carries as much weight as the finding**: a *uniformly-v4* repo must raise no warning, which is what proves the gate measures agreement rather than a named version and therefore cannot rot — and it passes against BOTH the pre-fix and fixed scripts, exactly as an unchanged-behaviour control should. Against them: a lone `v4` among `v6` siblings must be flagged while its siblings are not (the inversion), a file pinning `v6`+`v3` must be flagged **and must report the `v3`** (the `grep -m1` blindness), and a repo with no checkout at all must stay `N/A`/✅/exit 0 rather than becoming a finding. Guard integrity: each clean case also asserts its row was rendered, so "no ⚠️" cannot pass against a run that emitted no table. Mutation-verified: **4** of the new assertions fail against the pre-fix script, naming the inversion and the swallowed later ref. | issue #2493 (infra-compliance) |
| Two halves of the same blind spot, both chosen by the maintainer after the audit. **(a)** `.claude-code-version-check.json` read `lastCheckedVersion: 2.1.257`, but the only sweep that produced it declared its window as **2.1.232–2.1.257** (`docs/audits/fable-5.1-adaptation-2026-09-02.md`). The excerpt is sliced from `lastCheckedVersion` FORWARD, so advancing the pointer past 46 unread versions meant **2.1.186–2.1.231 could never be offered to anything again** — and the automated review had already gone silent after 2.1.185 (22 runs, 13 consecutive filing nothing). Confirmed casualty: the subagent nesting ceiling still instructed 5 levels after 2.1.217 disabled nesting by default and 2.1.219 set it to 3. **(b)** `scripts/fetch-research-papers.sh:66` requested `max_results=80` with **no date qualifier and no `start=` pagination**, while the downstream filter at `:131` is discard-only — so `--since-days` could never widen the largest source. Measured live: `ARXIV_COUNT=75` **identical at 8, 30 and 90 days**; fetching the URL directly returned 80 entries spanning ~3 hours against `totalResults=500456`, i.e. the documented 8-day promise missed by ~60×. Raising the flag silently widened the HuggingFace and blog arms and not arXiv. | (a) Pointer rewound to **2.1.185** with `lastCheckedDate` moved to that entry's own date, plus a `rewindNote` explaining why it moved BACKWARDS (so a future reader does not "repair" it forward) and a `knownUnreviewedRanges` block recording **both** gaps — the recovered 2.1.186–2.1.231 and the older **2.1.77–2.1.137** (61 versions, where `reviewedChanges` jumps 2.1.76 → 2.1.138), which is deliberately NOT recovered because rewinding that far puts ~180 versions in one excerpt. All 13 `reviewedChanges` entries preserved; the `jq -r '.lastCheckedVersion'` consumer path re-verified. (b) The query is now date-bounded (`submittedDate:[FROM TO TO]`, GNU `date -d` with a BSD `-v` fallback per `shell-scripting.md`) and paginated in pages of 100 under a `--arxiv-max` ceiling (default 400), stopping on a short page — the only clean end signal, since arXiv keeps returning 200 past the end. `http://` → `https://`. **The ceiling is reported, not hidden**: `ARXIV_MAX=` and `ARXIV_TRUNCATED=` are emitted ALWAYS, both polarities, because a key that appears only when true is indistinguishable from one nobody emitted — the difference between "8 quiet days" and "8 days, of which you read the newest 400". A real 8-day window across five categories runs to thousands and every candidate is read by an Opus step, so the window is honoured *up to a stated bound* rather than without one. | `scripts/tests/test-fetch-arxiv-window.sh` (17 assertions). **The defect lives in the REQUEST, so the emitted counts cannot see it** — a test asserting only on `ARXIV_COUNT` passes against the broken script. It stubs `curl` on PATH (no network, no credentials) and asserts on the URLs actually requested. **Case B is decisive**: an 8-day and a 90-day run must produce *different* from-dates, with the wider one reaching further back — pre-fix the URL was byte-identical at every `--since-days`, which is precisely why the count never moved. Case C pins pagination in **both** directions: a full page must trigger a second request carrying `start=100`, while a short page must stop after ONE — without the short-page half, a script that always fetched to the cap would pass. Case D pins both truncation polarities plus a non-vacuity assertion that the stubbed entries were actually parsed (`ARXIV_COUNT` ≠ 0), without which every assertion above holds for a fetch that returned nothing. The parser reads `arxiv*.xml` by glob so the live per-page files AND the `RR_FIXTURE_DIR` seam's plain `arxiv.xml` both resolve — the pre-existing `test-fetch-research-papers.sh` stays green. Mutation-verified: **13 of 17 fail** against the pre-fix script; the short-page control passes on both, correctly, since the old script always made exactly one request. Listed in `scripts/required-to-run-tests.txt`. | issue #2493 (research radar) |
| `scripts/check-subagent-types.sh` had a WARN tier that **reached nobody**. Its only caller was `.pre-commit-config.yaml`, invoked without `--strict` and without `verbose: true`, and neither the guard nor its self-test appeared in **any** workflow — `git log -S 'check-subagent-types.sh --strict'` is empty, so `--strict` was never wired at all. It reported 6 unqualified bare `subagent_type` names and exited 0, so the finding was invisible on every surface: `evaluate-skill`/`evaluate-matrix` → `eval-grader`, `evaluate-improve` → `eval-analyzer`, and `test-run`/`test-full`/`test-quick` → `test-runner`. Latent rather than broken — all three agents exist and resolve **today** by plugin-name fallback, which the script's own header calls "works today only by luck of name resolution". A bare name resolves for user/project-level agents; for a plugin-provided agent the `plugin:agent` form is what the resolver is guaranteed to honour (`~/.claude/rules/agent-and-tool-selection.md`), which is exactly the discrimination #2170 fixed for `test-analyze` and did not sweep to these six. | Qualified all six to the `plugin:agent` form the script itself prescribes in its `FIX=` column (`evaluate-plugin:eval-grader`, `evaluate-plugin:eval-analyzer`, `testing-plugin:test-runner`), taking the corpus to `UNQUALIFIED=0` / `UNRESOLVABLE=0` / `STATUS=OK`, **then** flipped the gate to `--strict` in both `.pre-commit-config.yaml` and a new blocking step in `plugin-pr-checks.yml`. Order matters and is the #2255 ratchet pattern: flipping first would have been red on arrival and switched off; flipping at zero locks a clean state rather than a suppressed one. Verified in the right order — `--strict` exits **1** on the pre-fix corpus and **0** after. (The first measurement of that exit code was wrong: `bash … \| tail -20; echo $?` reports **tail's** status, not the script's — the same pipeline-status trap as the `set -o pipefail` rows above, hit while checking a guard *about* silent failures.) | No new check: the detection already existed and was correct — this is a **wiring** defect, the same class as #2164 and #2333, so the fix is the trigger, not the logic. The existing `scripts/tests/test-check-subagent-types.sh` (31 assertions, all green) already pins both tiers, including TEST B feeding the verbatim pre-#2170 stale table and TEST F asserting that prose mentions, a `jq` field read and an uppercase `SUBAGENT_TYPE=` shell var extract nothing. What changes is that its ERROR **and** WARN tiers now block: pre-commit runs `--strict`, and `plugin-pr-checks.yml` gains its own step, since this repo wraps the `plugin-compliance-check.sh` run in `\|\| true` and a guard's own step is CI's only real signal. | issue #2493 (subagent types) |
| Four tool names that no longer exist — `LS`, `BashOutput`, `KillShell`, `MultiEdit` — were granted in `allowed-tools:` / `tools:` frontmatter across **40 files**. Verified against the primary source: none appears at https://code.claude.com/docs/en/tools (2026-09-03, which lists 45 tools *including* their successors `Glob`, `Bash`, `Edit`; `TaskOutput` is still listed, deprecated, so it is **not** in this class). Two **always-loaded** authoring rules were minting them, which is why the count grew rather than decayed: `agent-development.md` carried `LS` in two copy-paste `tools:` templates, and `skill-development.md:198` listed `BashOutput` in its "Development" tool set — a markdown **table row**, which is why a frontmatter-only sweep would have left the generator intact. **Severity stated honestly**: nothing was established to break at load time; an unrecognised grant entry is most likely inert. The cost is a template that keeps minting dead names plus wasted round-trips when a run believes it holds a tool it does not. | Swept all 40, then fixed the two generators (`scaffold-fix-backport`: the instances are worthless without the source). The transform was drop-if-successor-present, substitute-otherwise — and **every one of the 40 turned out to be pure redundancy**: a set-wise diff of every changed file confirms `0` files gained a tool and `0` lost a live one, so no substitution was needed anywhere. `scripts/check-dead-tool-grants.sh` is a **denylist of removed names, never an allowlist of live ones**: an allowlist would have to enumerate every current tool and would rot the day one is added — precisely the disease as the hardcoded `v4` in the checkout gate two rows up — whereas a denylist only grows. Matches the shape of `lint-mcp-tool-references.sh` / `lint-package-references.sh`. `--strict` is **absent rather than inert**: every finding is an ERROR, and a flag accepted but ignored is the #2057 defect. | `scripts/tests/test-check-dead-tool-grants.sh` (27 assertions). **The negative cases carry as much weight as the positives**, because this guard scans a corpus that legitimately *discusses* the retired names — the rules documenting the removal, the test itself, and the guard's own header — so a checker that flagged every mention would be switched off within a day: prose mentions, and a `\| LS \| retired \|` table row with no backticked tool list, must NOT fire, while the backticked template row MUST. `LSP` and `Skill` must not trip the `LS` matcher (word boundaries via bracket expressions, not GNU-only `\b`). Each of the four is pinned by name **and by its `FIX=` successor**, since a finding without the successor is a puzzle rather than a fix; C replays two verbatim pre-sweep lines and requires `ISSUE_COUNT=2` when one line carries two dead names. Guard integrity: the clean real-repo case asserts `SCANNED_EMPTY=false` and a non-zero `GRANT_LINES`, and an empty corpus is `nothing_scanned`/exit 1 rather than clean (#2219/#2290). **Verified against the pre-sweep tree** (`git archive HEAD`): **54 findings** across all four names, exit 1. | issue #2493 (dead tool grants) |
| `evaluate-plugin/golden-set.json` lists **16** canaries and exactly **one** carries an `evals.json`. The sweep gate at `golden-set-evaluation.yml:183` is `eval_ready_count != '0'`, and the hard-fail at `:158` fires only on **zero enumerated** canaries — so a **6% sweep runs, passes, and files an issue**. The ratio existed only in `$GITHUB_STEP_SUMMARY`, which no issue mirrors and nothing persists, and the prompt handed Claude a bare `Eval-ready canaries: 1` **with no denominator**. "1" reads as fine; "1 of 16" does not. So a green monthly sweep was being read as "the golden set passed" when 15 of 16 skills were never exercised. | **Failing on the gap was rejected**, and that judgement is the substance of the fix: a guard that goes red until 15 eval files are written is red-on-arrival for as long as that takes, and a permanently-red monthly audit gets switched off — the same reasoning that rejected a `permission_denials_count > 0` gate two rows up. Three properties instead: (1) `evalCoverageFloor` is **declared** in `golden-set.json` and a drop below it is a hard error — a **ratchet**, so regression fails while a standing gap does not; (2) a partial sweep emits a `::warning::` naming **both** numbers, so the gap is visible on the run; (3) `eval_total_count` becomes a job output and reaches the **prompt**, which is the only path to the filed issue — the sole artifact that outlives the run. Only the floor is stored: `ready`/`total` stay derived from disk every run, because storing them would make the file a cache of its own canaries list — the divergence defect `blueprint-tracker-check.sh` exists to catch (#2128). | `scripts/tests/test-check-golden-set-coverage.sh` (20 assertions). **Case A is load-bearing in the unusual direction**: the real repo, at 1 of 16, must be **GREEN** — a guard that failed there would be switched off before it ever caught anything — while still *reporting* `CANARIES_TOTAL=16` / `CANARIES_EVAL_READY=1` / `COVERAGE_FLOOR=1`, without which it is indistinguishable from a check that measured nothing. B pins the ratchet in both directions: a fixture at floor 3 with 1 ready fails naming `1 of 16`, and the **same** fixture at a floor it meets passes, so the failure is attributable to the floor rather than a broken fixture. C pins that an undeclared floor is itself an error (undeclared intent is unauditable — `generated-fleet-drift.md`). D strips each of the three workflow invariants **one at a time** so a single missing property is attributable, and requires the intact fixture to pass, without which those three prove nothing. Mutation-verified against the pre-fix tree (`git archive HEAD`): **all 4 findings** fire, exit 1. Deferred deliberately: authoring the 15 missing `evals.json` files, and extracting the gather step's inline table build into a script — this change makes the gap **visible and non-regressing**, it does not close it. | issue #2493 (golden set) |
| `scripts/check-docs-index.sh` ran in CI (ungated, every PR) and monthly, but had **no `.pre-commit-config.yaml` entry**, so `doc_count_drift` — the per-plugin skill/agent counts stated in `README.md` / `docs/PLUGIN-MAP.md` / the d2 diagram — only surfaced after a push. Adding or removing a skill moves a count, so any such PR hits it: during #2520 `pre-commit run --all-files` was green locally while CI reported `README.md:140 states prose-plugin has 2 skills but 3 exist on disk`. The local suite and the CI step were checking different things — the local↔CI parity shape. | Added a `check-docs-index` pre-commit entry. **The issue's proposed wiring (`--strict`, blocking) would not have worked**, and the correction is the substance of the fix: `doc_count_drift` is **WARN**, and `check-docs-index.sh --strict` sets a non-zero exit only when some finding is **ERROR** (`exit_severity` is assigned in the `*SEVERITY=ERROR*` branch alone) — measured on a drifted fixture, `--strict` exits **0**. That exit-0-on-WARN behaviour is not a new finding: the #2333 row above already records it (`check-docs-index.sh --strict` exits 0 on a WARN, so CI was green while the self-test failed); what is new here is the consequence for a pre-commit entry. A hook that exits 0 prints nothing unless `verbose: true`, so a blocking-only entry would have detected the drift locally and still never shown it to the committer. The entry therefore carries **`verbose: true`** (what actually surfaces the WARN) **plus** `--strict`, so the six ERROR findings (`rule_table_dangling`, `plugin_map_drift`, `diagram_node_dangling`, `diagram_svg_node_missing`, `diagram_svg_stale`, `readme_row_dangling`) block **when the hook fires**. The trigger names the common count-moving paths — `^[^/]+-plugin/(skills\|agents\|\.claude-plugin)/` (the `nudge-plugin-readme-currency` shape), the three documents stating those counts, and the checker. It is not exhaustive even for counts: the repo-root `.claude-plugin/marketplace.json` feeds the "README.md headline states N plugins but marketplace lists M" comparison and is not matched by the plugin-scoped `.claude-plugin/` alternative, so a commit editing only that file gets no local signal. And it is deliberately NOT the guard's full verdict surface (a plugin's own `README.md`, `.claude/rules/*.md`, `CLAUDE.md`, the marketplace and release-please manifests). Because the entry is `pass_filenames: false`, that trigger bounds **whether** the hook runs, never **what** it reports: once it fires the checker walks the whole repo, so an ERROR rooted in one of those un-named inputs still blocks the commit. Reproduced — a dangling row planted in `feedback-plugin/README.md` makes `pre-commit run check-docs-index --files git-plugin/skills/git-commit/SKILL.md` fail with `SEVERITY=ERROR TYPE=readme_row_dangling`, exit 1, on a commit that staged neither that README nor that defect. The narrow trigger buys firing frequency (the walk costs ~5 s), not a narrower verdict; the ungated CI step remains the always-on read. **A second correction, to `docs/audits/README.md`'s pre-existing claim that the `--strict` CI step means "drift cannot merge"**: for the same WARN/ERROR reason it does not. The WARN tier is caught by `scripts/tests/test-check-docs-index.sh` TEST A (`STATUS=OK` on the real repo), which runs only in `Test: Skill scripts` — whose `paths:` exclude `*-plugin/skills/**` — and is absent from the always-on `compliance` job's `--only` list (verified by name, not by position — that list grows), so a skill-only PR can still merge with a stale count. Closing that CI gap was left out of scope. Cost measured before wiring: **4.6–6.0 s** per fire (423 skills, 44 plugins), not the ~1.5 s an earlier reading recorded. | No new check — a **wiring** defect, not a detection one, and the detection already exists twice over: `scripts/check-docs-index.sh` is the guard and `scripts/tests/test-check-docs-index.sh` is its self-test (run in CI since #2333 wired repo-root `scripts/tests/` into `run-skill-script-tests.sh`). Same precedent as the #2574 wiring row. Verified per path through **pre-commit's own filter** rather than by reading the regex: the hook is selected for a plugin `skills/` file, `agents/` file, `.claude-plugin/` file, `README.md`, `docs/PLUGIN-MAP.md`, both diagram artifacts and the checker; and **Skipped** for `.claude/rules/regression-testing.md` and for `git-plugin/README.md` (proving `^README\.md$` is anchored and does not catch plugin READMEs). End-to-end: planting a throwaway skill under `prose-plugin/skills/` reproduces the #2520 finding verbatim (`README.md:140 states prose-plugin has 3 skills but 4 exist on disk`) with the hook still reporting `Passed` — which is the WARN/exit-0 evidence above. | issue #2522 |
| `terraform-plugin/hooks/validate-terraform-apply.sh` matched `(^\|\s)terraform\s+apply(\s\|$)` against the **whole command string** and then fell through to an unconditional `block`. Two consequences, the second worse. (1) It fired on text that executes nothing — a `gh pr edit --body` quoting the phrase, a `python3` heredoc body, a read-only `gh issue list --search` — three times in one session in `ForumViriumHelsinki/infrastructure`, none of them an apply. (2) Its own prescribed remedy was refused identically: the message said to rerun "referencing the saved plan file", and `terraform apply tfplan` matched the same pattern, so **no input satisfied the gate** — it was an unconditional deny with instructions attached. The workaround was worse than the block: each time, the fix was to move the text out of the command string (a Python file written with the Write tool, or splitting the phrase into `TF=terraform`), which routes around the guard entirely for arbitrary content and teaches laundering commands through files. | Decide command position with **`ast-grep --lang bash`** (tree-sitter-bash), the classifier `hooks-plugin/hooks/bash-antipatterns.sh` adopted in #2008, and examine each `command` node on its own. **A hand-rolled awk projection was tried first and is the substance of this row.** It masked quoted spans to a `__Q__` placeholder, dropped heredoc bodies and trailing comments, and split at unquoted separators — 45 assertions green — and an adversarial re-review reproduced **six** bypasses, each of which the shell genuinely executes and each of which the pre-fix regex blocked: `__Q__` was counted as a saved plan file, so `terraform apply "-auto-approve"` and `terraform apply "-refresh=false"` both passed; `<`/`>` were ordinary separators, so `terraform apply 2>&1 \| tee log` projected to `terraform apply 2` and the fd digit read as a plan file; splitting at `;` left `do`/`then` as the first token and the walker broke on the unknown word, so `for d in a b; do terraform apply -auto-approve; done` passed; and the wrapper allowlist was a closed seven-name set, so `timeout`/`nice`/`stdbuf`/`xargs`/`uv run`/`eval` made the whole statement invisible, while a bare short flag after a wrapper swallowed the program itself (`env -i terraform apply`). **The lesson is the shape, not the six**: a hand-rolled shell projection buys one regex patch per bypass and leaves the seventh open, whereas a real parse settles quoting, redirection, loops, conditionals and wrappers as a class — a redirection's fd digit belongs to a `file_redirect` node, a loop body's command carries no `do` keyword, and `timeout 300 terraform apply` is one node whose word list contains `terraform`, so no wrapper allowlist and no flag-arity table exist to be incomplete. Quotes are now **removed rather than masked**, because the shell removes them: `terraform apply "-auto-approve"` really delivers `-auto-approve` to the process. The one thing tree-sitter cannot see into is a shell invoker's script argument, so `bash -c "…"` / `sh -c '…'` / `eval "…"` have their quoted arguments re-parsed as shell (which also closes `sudo -u tf bash -c "…"`, a hole on the pre-fix hook too). **It FAILS OPEN where ast-grep is absent**, exactly as `bash-antipatterns.sh` does — a PreToolUse safety block that hard-fails without its parser breaks every Bash call, which is worse than one that defers — and the README says so plainly rather than implying the gate is unconditional. Per `.claude/rules/hook-block-vs-nudge.md` the exit 2 is earned where the parser is present: the block does not exempt the dangerous variant, it refuses `-auto-approve` in every form including alongside a plan file. | `terraform-plugin/hooks/test-validate-terraform-apply.sh` (76 assertions, auto-discovered via `*/hooks/test-*.sh`, listed in `scripts/required-to-run-tests.txt` under the ast-grep group beside `test-bash-antipatterns.sh`). Every case pipes JSON into the SHIPPED hook. **The 45-assertion suite that shipped with the awk draft is why this row exists**: it was fully green with all six bypasses live, because it contained no case for a quoted flag operand, a redirection, a `do`/`then`-prefixed statement, or a wrapper outside the seven-name allowlist. Each of the six now has its own `assert_blocked` row with the reviewer's verbatim failing input, plus controls in the same section so the new mechanisms cannot be satisfied by blocking everything — `timeout 300 terraform apply tfplan`, `for d in a b; do terraform plan …; done` and a loop quoted inside a `gh pr comment --body` must stay ALLOWED. **The fail-open path is pinned in both directions**, which is the assertion that keeps the rest non-vacuous: with `CLAUDE_HOOKS_TERRAFORM_APPLY_NO_ASTGREP=1` an `-auto-approve` apply must exit 0, and without it the same command must exit 2 — absent the second, a run on a box lacking ast-grep would report every row green over a hook that no-ops. **The prescription check does not grep for a literal**: it extracts every `terraform …` line from each block message and feeds it back through the hook, requiring exit 0, with a non-vacuity assertion of >=3 extracted commands. One more defect came out of the external re-review and is pinned here too: `"${NEXT[@]}"` on an EMPTY array is a fatal unbound-variable error under `set -u` before bash 4.4, and `#!/usr/bin/env bash` finds bash **3.2** on a stock macOS, so the hook exited **1 on every command it meant to allow** — invisible to every blocked-case assertion. `BASH_COMPAT` does not restore that behaviour, so the pin runs three allow-cases through `/bin/bash` itself and prints its version; reintroducing the unguarded copy turns exactly those three rows red. Mutation-verified three ways: against the verbatim pre-fix hook (`git show origin/main:…`) **51 passed / 25 failed**; against the awk draft this change replaces, **57 passed / 19 failed** with every one of the six bypass rows red; and green at 76/0 on the shipped parser. | issue #2506 |
| `hooks-plugin/hooks/test-no-calendar-estimates.sh` (33 assertions, green at HEAD) was declared in **neither** wiring point when #2580 was filed. Half of that premise has since gone stale: #2584 added the `.pre-commit-config.yaml` entry at `:678-683`, so only `scripts/required-to-run-tests.txt` was still missing. **The omission is accidental, not deliberate** — checked before wiring, per the issue's own decision 2: `git log` on the hook and its test (#1379 → #1570 → #2561 → #2579) records no exclusion; a grep across `scripts/`, `.github/` and `hooks-plugin/README.md` finds the suite named nowhere but its own header; and the only README rows about this hook document its `CLAUDE_HOOKS_ENABLE_CALENDAR_ESTIMATES` opt-in, which gates the **hook**, never its test (the suite sets the variable itself). | Decision 1 (both wiring points, or one?) resolves to **both, for different reasons**. The manifest is not the sibling pattern for pre-commit: of the 13 hook suites only `test-bash-antipatterns.sh` is listed, and it is there because it SKIPs without `ast-grep`. But the manifest has since grown a second class — `test-check-audit-liveness.sh`, `test-check-golden-set-coverage.sh`, `test-fetch-arxiv-window.sh`, `test-check-dead-tool-grants.sh` — declared because no environment makes a skip legitimate, and this suite is squarely that class: it synthesizes its own JSONL transcripts, touches no network and no `gh`/`task` binary, and carries **no skip branch at all**. So the entry buys the manifest's other half, the denominator guard its header names: a listed path matching no discovered test is `required_test_missing`, which is what turns a rename, a move, or a discovery-glob collapse (#2219/#2333) from silence into exit 1. | No new check — a **wiring** defect, the same class as #2164 / #2333 / #2574, so the fix is the declaration and the manifest IS the guard. Verified in both directions on the real corpus rather than a fixture: with the entry present the runner reports `REQUIRED_DECLARED=15` / `REQUIRED_VIOLATIONS=0` and `PASS=` for the suite; with the test file moved aside it reports `SEVERITY=ERROR TYPE=required_test_missing` and exit 1. The pre-commit `files:` pattern was exercised on both halves too — `no-calendar-estimates.sh` and `test-no-calendar-estimates.sh` each fire the suite, while an unrelated hook file reports `Skipped`, so the trigger is neither dead nor a catch-all. | issue #2580 |
| `configure-security.sh` warned `missing_sast` on **every** repo with no CodeQL workflow, with no probe anywhere for whether CodeQL can run there. Where the org has no GitHub Advanced Security, CodeQL is not merely absent but **forbidden**: every `github/codeql-action/*` step fails with HTTP 403, so acting on the recommendation produces a workflow whose only fix is deleting it. Reported against `ForumViriumHelsinki/silverbucket-helper`, whose org rules list `upload-sarif` and any `security-events: write` step as forbidden for exactly that reason; `gh api repos/OWNER/REPO --jq .security_and_analysis` returned `code_security.status: disabled`. Nothing in the report separated that from a genuine gap — it cost a verification round to establish the recommendation was not actionable, and `STATUS=WARN` was partly driven by a layer the org is structurally unable to adopt. | Probe availability before choosing the severity, and report **why**. `CODEQL_AVAILABLE=yes\|no\|unknown` + `CODEQL_AVAILABILITY_REASON=<token>` are emitted unconditionally (a key present in only one branch is indistinguishable from one nobody emitted); `no` downgrades the finding to `SEVERITY=INFO TYPE=sast_unavailable` naming the cause, while `yes` **and** `unknown` keep the pre-probe WARN — so an offline, unauthenticated, or rate-limited run is never worse than before. Three cases the naive fix gets wrong are handled explicitly: a **public** repo is `yes` (code scanning is free there, so the gap is real), an **absent** `security_and_analysis` field is `unknown` rather than a guess, and `.private` is projected with `tostring` rather than `//` because jq's `//` treats `false` as null-ish and would erase the public-repo case entirely. The probe is the script's only network call and is narrow by construction: it runs **only** when `CODEQL=false`, is skipped offline on no remote / a non-GitHub remote / no `gh` / `CONFIGURE_SECURITY_NO_GHAS_PROBE`, and is bounded by a background watchdog rather than `timeout(1)` (GNU coreutils, absent from stock macOS). Failure classification matches with a here-string, never `printf \| grep -q`, per the #1744/#2462 SIGPIPE-under-`pipefail` race. | `configure-plugin/skills/configure-security/scripts/tests/test-configure-security.sh` Case 4 (10 sub-cases) EXECUTES the shipped collector against real fixture repos with a **stubbed, chmod-+x'd `gh`** whose argv log is asserted — a grep of the script for a reason token would pass against a probe that never runs. **The negative controls carry as much weight as the downgrade**, since a collector that simply stopped warning about SAST would satisfy the two payoff cases on its own: 4a (code security enabled), 4c (public repo), 4d (status field absent), 4e (unauthenticated `gh`), 4f (opt-out), 4h (no remote) and 4i (non-GitHub remote) all require the `missing_sast` WARN to SURVIVE. 4b pins the downgrade **and** that its message names the reason, the 403, and the settings route — the issue's "make the downgraded case say WHY", plus the caveat the external review surfaced: `code_security.status` reports whether code security is enabled HERE, not whether the org is licensed, so a repo in a licensed org with the toggle off returns the identical byte and the row must offer both remediations rather than assert a plan tier. 4g is a design pin: a repo that already has the workflow must invoke no `gh` call at all, structurally forbidding a later change that probes on every run; 4f/4h/4i assert the same emptiness for each offline skip. 4j is the payoff — every other layer present and SAST unavailable yields `STATUS=OK` / `ISSUE_COUNT=1` / exit 0, where the pre-fix collector reported `STATUS=WARN` over a layer the repo cannot adopt. Mutation-verified: the verbatim pre-fix collector turns the suite red at 4a, and reproduces the reported report (`STATUS=WARN`, `TYPE=missing_sast`) on the 4j fixture. Adding git to this suite brought it under `check-git-sandbox-guards.sh`'s file-level gate, so all eight pre-existing `mktemp -d` sites were guarded and the full `GIT_DIR` family is unset per #1745. | issue #2498 |
| `session-distill`'s `distill-survey.sh` read Claude Code transcripts only (`~/.claude/projects/<slug>/<id>.jsonl`), so once the four session-plugin skills dropped `compatibility: claude-code` and became visible in pi, the collector returned `TRANSCRIPT_AVAILABLE=false` for every pi session and distill fell back to re-reading the conversation. pi writes a different JSONL shape (`~/.pi/agent/sessions/--<cwd-slug>--/<ts>_<id>.jsonl`, line-1 `{"type":"session",…}` header, `toolCall`/`toolResult` message content), and two pi properties make a naive port wrong: sibling files in the session directory whose header carries `parentSession` are subagent / fork / `/new` sessions rather than separate sessions, so counting them inflates cross-session recurrence; and pi records **failed** edits as `toolCall`s too (29 of 322 real `edit` results were `isError: true`), so counting calls inflates `HOT_FILES`. | Additive only: the diff against `origin/main` removes zero lines. pi engages only when the Claude lookup finds nothing and `PI_SESSION_FILE` names a readable file with a pi header. Each pi file is translated by one jq program into the Claude-shaped lines the unchanged extraction already reads (`bash` toolCall → Bash `tool_use`; `edit`/`write` toolCall joined by `id` to a `toolResult` with `isError: false` → `toolUseResult`), so no Claude jq program changed. The window excludes `parentSession` siblings. A pi-shaped file under the Claude projects dir is rejected rather than parsed as Claude. New additive keys: `TRANSCRIPT_FORMAT=claude-code\|pi` in `SESSION_META` and `--summary`, and `RULE_HINTS_RECORDED=false` in pi mode, because pi has no denial record and a zero there means "not recorded". Shapes were taken from real pi 0.84.1 session files and `dist/core/session-manager.js`; the fixture content is synthetic. | `session-plugin/scripts/tests/test-distill-survey.sh` TESTS **K/L/M** (36 → 73 assertions, all 36 pre-existing unchanged). L runs a pi fixture describing the same logical session as the Claude fixture and asserts groups B, C and G return the same values, with F asserting no denial signal. The fixture carries a **failed** fourth edit (so a join that ignores `isError` reports `HOT_1_TOTAL=4`) and a `parentSession` sibling repeating a bracketed command (so an unfiltered window reports `SESSIONS_SCANNED=3` and `CANDIDATE_2_SESSIONS=2`). M pins the guards in both polarities: a pi file under the Claude projects dir SKIPs, yet the same lookup with `PI_SESSION_FILE` set falls back to pi; a non-pi or missing `PI_SESSION_FILE` SKIPs with exit 0; and the Claude transcript stays authoritative when both exist. Golden diff: default and `--summary` output of the `origin/main` script on the Claude fixture (pinned HOME, seams, `touch -t` mtimes) is byte-identical to the new script's except the one added `TRANSCRIPT_FORMAT=claude-code` line in each. Mutation-verified by snapshot-and-restore on three independent breaks (join ignores `isError`, `parentSession` filter removed, Claude-dir rejection removed), each turning the suite red on its own assertions. | issues #2661, #2662 (session-plugin pi exposure) |
| `agent-patterns-plugin:parallel-agent-dispatch` documented the **`Agent`**-spawned resume hazards exhaustively — `SendMessage`-resume loses worktree isolation (#1546), `resumeFromRunId` re-runs succeeded worktree agents (#1868) — but nothing anywhere said that a **`Workflow`**-spawned agent is not `SendMessage`-addressable **at all**. So the documented remedy "resume the original agent" reads as universally available while having no target on the `Workflow` substrate: after two completed runs (2026-09-04), `ListAgents` listed 14 peer *sessions* and zero `wf_*` agents. The second half was equally undocumented: a run completing with **zero errors** still leaves one worktree per *changed* agent pinning its branch, so the next `isolation: "worktree"` dispatch hits `fatal: '<branch>' is already checked out`. The only in-repo text on freeing those branches was `workflow-interrupted-run-recovery` § 5, scoped to a run **killed** mid-flight and prescribing `git worktree remove --force` — safe there only because § 3 salvages first, and exactly the unscoped sweep `agent-coworker-detection.md` forbids after a clean run. | Added a `## Workflow agents are unreachable and their worktrees pin branches (#2614)` section to `references/worktree-hazards.md` carrying both facts as a **dated 2026-09-04 observation** (not a standing invariant), the `Agent`-vs-`Workflow` addressability contrast table, and the non-force remedy with its four scoping conditions inline; indexed the section in `REFERENCE.md`; added a fourth `Workflow` caveat blockquote to `.claude/rules/agent-development.md` § Dynamic Workflows; and added a disambiguating note under `workflow-interrupted-run-recovery` § 5 so its `--force` line is not copied into the completed-run case. The issue named an **upstream** rule file (`repos/.claude/rules/agent-worktree-resume-for-pr-feedback.md`) that does not exist in this checkout — the guidance was re-homed into the in-repo surfaces that do. | `scripts/check-agent-failure-contract.sh` — two markers on the file that owns them: `worktree-hazards.md` `Workflow agents are unreachable` + `gated on the run's completion notification` (the safety half; losing it turns the remedy into the forbidden unscoped sweep). Guard cases **S/T** in `scripts/tests/test-check-agent-failure-contract.sh` (18 → 20 assertions) strip one marker each from a fixture and require exit 1. Mutation-verified both ways: dropping the two `require_marker` calls fails exactly S/T (18 passed / 2 failed), and stripping each marker from the real file makes the checker exit 1 naming that marker. | issue #2614 |
| `check-branch-sync-on-push.sh` read the **checked-out** branch (`symbolic-ref HEAD`), never the push refspec destination, and computed `behind` as `rev-list --count HEAD..origin/<branch>`. Two failures fell out (#2672): (a) after `git rebase --onto origin/main <squashed-base-tip> <branch>` the pre-rebase commits on the remote are unreachable from the new tip by construction, so a lease-pinned force-push was nudged with *"N commit(s) behind … someone … pushed since your last sync. Reconcile first (git pull --rebase)"* when nobody had pushed — and that advice would re-introduce the squashed base commits; (b) the push-by-SHA protocol (`git-merge-hazards` §3) writes `<sha>:refs/heads/<other>`, so from `main` the hook exited at the default-branch exclusion and said nothing, and from any other branch it nudged about the wrong branch. | The hook now resolves the refspec **destination** (and uses the refspec source as the behind-base), suppresses the behind-nudge only for an explicit `--force-with-lease=<branch>:<sha>` whose SHA prefix-matches the freshly-fetched `origin/<branch>` tip (a **bare** `--force-with-lease` must keep nudging — the hook's own `git fetch` advances the very tracking ref a bare lease is evaluated against), and rewords any other force-push to "N remote commit(s) … are not in the commit you are pushing (expected after a rebase)" with no `git pull --rebase` advice. The semantic invariant is that the plain-push path keeps the original "someone … pushed" wording verbatim while the force-push path must not carry it. | `git-plugin/hooks/test-check-branch-sync-on-push.sh` — 10 new assertions run the real hook against a scratch repo + bare origin with a coworker push: lease matching the fetched tip → silent; stale lease / lease naming another branch / bare `--force-with-lease` / `--force` → still `ask` with the rebase wording and **without** `git pull --rebase`; plain push → `ask` retaining both the "someone … pushed" sentence and the pull advice; `<sha>:refs/heads/feature` pushed from `main` and from `other` → names `feature`; `<sha>:refs/heads/main` → still excluded. 8 of the 10 fail against the pre-fix hook. **Plus 3 assertions for the repair**: the refspec destination is adopted only when it is an unambiguous, literal branch name, because the hook sees the command as WRITTEN — `git push -u origin $(git branch --show-current)` (this plugin's own documented idiom), `git push origin "$BRANCH"`, and the multi-refspec `git push origin main feature` each resolved to a token that fetches nothing, has no `origin/<token>` (so `BEHIND=0`) and matches no PR, silencing the guard completely. All three must fall back to HEAD and still name `feature`. They pass against the pre-fix hook and the repaired hook and fail only against the unrepaired first-token-wins parse, so the negative control against `HEAD` cannot prove them — a dedicated mutation control that reverts just the two guards does (26 passed / 3 failed). | this PR |
| `code-quality-preflight-cue.sh` emitted `Run /code-quality:code-lint as a pre-flight` as its `decision:block` reason. The agent pasted that straight into the `Skill` tool and got `Unknown skill: code-quality:code-lint` (#2682). The skill EXISTS at `code-quality-plugin/skills/code-lint/SKILL.md` — the cue named it in the `/<ns>:<name>` shorthand this repo invented for README tables, which nothing resolves at runtime: Claude Code namespaces skills `<plugin-name>:<skill-name>` and no plugin is called `code-quality`. Prose a human reads can carry the shorthand; a string a hook EMITS cannot, and the failure surfaces only at call time, one wasted tool call later. `scripts/check-skill-references.sh` never saw it — its coverage is SKILL.md / REFERENCE.md / `*.workflow.js` / `.claude/rules`, and `*/hooks/*.sh` is in no guard at all. | Both cue branches now name `/code-quality-plugin:code-lint` and `/evaluate-plugin:evaluate-skill`, as do the three explanatory comments that were teaching the short form, and the cue also names the ACTION as a fallback ("or this repo's own linter/formatter if that skill is unavailable") so it stays followable in a session where the plugin is not installed. `code-quality-plugin/README.md` gains a *Cue skill IDs are plugin-qualified* subsection stating the invariant. The same short form is still emitted by hooks in ~12 other plugins; that sweep is deliberately NOT bundled here (see the Check Added cell for how the gap is kept visible rather than hidden). | New `scripts/check-hook-cue-skill-refs.sh` — a RESOLUTION check, not a denylist: ground truth is rebuilt from `*/skills/*/SKILL.md` + `*/agents/*.md` on every run and an empty walk aborts (the #2219 empty-negative trap). It scans non-comment lines of `*-plugin/hooks/*.sh` minus `hooks/test-*.sh`; comments are exempt and counted, because header comments legitimately quote the bad form to teach the invariant. ENFORCED scope is `code-quality-plugin/hooks`; every other plugin's hooks are scanned ADVISORY-only with a per-file unresolvable count, so the remaining sweep is reported and self-updating instead of either going red on unfixed files or silently dropping out of scope. `scripts/tests/test-check-hook-cue-skill-refs.sh` EXECUTES the guard against planted fixture trees in both polarities (verbatim pre-fix line errors and names `code-quality-plugin:code-lint` as the copy-pasteable fix; fixed line clean) plus comment exemption, `test-*.sh` exclusion, advisory-never-fatal, a git-refspec false-positive control, empty-tree abort, and non-vacuous `FILES_SCANNED`/`REFS_CHECKED` assertions — 22 assertions. **The comment exemption is the subtle half and is pinned in both polarities.** Its first shipped glob, `[[:space:]]*\#* | \#*`, reads as "one whitespace char, then ANYTHING, then a `#`" — so it skipped any INDENTED line containing a `#` anywhere, which is precisely the line class the guard protects: hook cues here routinely cite issue numbers (`(issue #2682)`) and the real cue assignments sit indented inside a `case` block, so adding a citation to a cue would have silently disabled the enforced check on that line (measured: it was already hiding 3 live unresolvable IDs in `git-plugin` from the ADVISORY roll-up, which rose 40 → 43 once fixed). The guard now strips only the LEADING whitespace run and tests that. Case (i) plants an indented emitted line carrying `(issue #2682)` and requires `ISSUE_COUNT=1` with `REFS_CHECKED>=1` / `COMMENTS_SKIPPED=0` — the counters are what distinguish "checked and flagged" from "skipped"; case **(i2)** is the guard-integrity half, requiring an indented COMMENT citing an issue number to stay exempt while its sibling emitted line is still scanned, without which the narrowing could degrade into flagging every line and case (c) alone could not tell the difference (its comments carry no `#` citation). Mutation-verified: neutering the resolution check, the comment skip, or the empty-truth abort turns 5 / 3 / 1 distinct assertions red, and restoring the broken glob turns exactly (i)'s 3 red while (i2) stays green on both. `code-quality-plugin/hooks/test-code-quality-preflight-cue.sh` test (q) asserts the emitted `.reason` on both branches carries the qualified ID and not the short one — 4 of its 63 assertions fail against the pre-fix cue. | this PR |
| `hooks-plugin/hooks/workflow-scale-estimate.py` — the figure the scale guard shows before a Workflow run — miscounted the repo's own shipped templates four ways (#2670): a site inside one element of a literal array was multiplied by the array length (`parallel([() => agent(a), () => agent(b)])` read as 4); a trailing comma counted as an element; a template literal nested in a `${…}` interpolation closed the outer literal early, so a stray apostrophe blanked every later `agent()` call (evaluate-skill's preflight vanished); and an array declared `[]` then filled by `push()` read as 0 items. blueprint-story-audit was costed at 71 (true: 20) and verify-before-filing at 81 (49). Separately, nothing stated a template's agent count at authoring time — the cost variable behind a 496-agent, $528 session. | Estimator: a literal-array element runs once; a final empty segment (trailing comma) is not an element; an interpolation is walked structurally to find its end; a pushed-into array is unbounded. Deliberately NOT learned: a loop window `.slice(i, i + WAVE)` (it bounds concurrency, not the count) and evaluate-skill's `cellCap` abort (caller-overridable). Each template's `## Workflow harness (template)` framing now declares `**Agent budget:** N`, and `.claude/rules/workflow-vs-skill.md` gains § The cost model. | `hooks-plugin/hooks/test-workflow-scale-guard.sh` (15 → 25): exact `ESTIMATE=` assertions for the four shapes (all red pre-fix; the push case red on both the original and the partially-fixed estimator), a `.map`-over-literal control, and a loop-window pin. `scripts/check-workflow-js-model.sh` gains `missing_agent_budget` / `agent_budget_exceeded`: it re-runs the estimator, itemises `ESTIMATE=`/`BUDGET=` per file, and fails closed without it. `scripts/tests/test-check-workflow-js-model.sh` case N (11 red pre-fix; N3 guard integrity — the same harness passes once its budget is honest; N7 — every shipped template fits); disabling the comparison fails exactly N2's 4. `scripts/check-agent-failure-contract.sh` pins the cost-model heading, `CLAUDE_HOOKS_WORKFLOW_MAX_AGENTS` and the snippet slot (Z1–Z3, red against the pre-fix checker). | issue #2670 |
| The #2670 estimator fix walked template `${...}` interpolations as **code** to find a nested template's extent, and code holds what a quote-and-brace scanner cannot parse. A regex literal holding a quote (`${s.replace(/'/g, "")}`) opened a "string" that never closed, the rest of the file was blanked, the estimator returned `NO_AGENTS`, and `workflow-scale-guard.sh` went **silent** on a 32-agent pipeline that the #2668 estimator asked about. `check-workflow-js-model.sh` mapped `NO_AGENTS` to a budget of 0, so it inherited the same fail-open. An open-brace regex paired with a close-brace regex in a later template did the same without ever reaching end of file, so an "unterminated literal" check alone would not have caught every case. | The structural walk is now used only when it proves itself, and otherwise the #2668 flat scan (kept verbatim as `_flat_sanitize`) decides. There are four checks. (1) Every literal, interpolation and block comment closes before end of file. (2) No `'`/`"` string crosses a newline, which JS forbids. (3) The remaining code's brackets balance. (4) Every `agent(`, `parallel(`/`pipeline(`, `.map(`/`.flatMap(` and `.push(` token that the flat scan keeps as code is still code. The rollup reports which scan decided (`SANITIZER=structural\|flat`, `FALLBACK=<reason>`). `check-workflow-js-model.sh` now treats `NO_AGENTS` on a file its own call parser finds `agent()` calls in as `estimator_desync`, not as 0. | `hooks-plugin/hooks/test-workflow-scale-guard.sh` (25 → 59 assertions). One named fixture per check reports the #2668 figure plus the check's own `FALLBACK=` reason, so removing any single check fails its own case (verified by snapshot mutation; the pre-repair estimator fails 14). Checks 1, 3 and 4 each have a fixture where their removal also changes the estimate. The newline check does not: the end-of-file check catches everything it catches, so it is pinned by its reason alone. A **differential** against a frozen copy of the #2668 estimator (`hooks-plugin/hooks/fixtures/workflow-scale-estimate-2668.py`) runs over every bundled `*/workflows/*.js` plus the review's fixtures. The live estimate may fall below #2668's only at a listed correct count, and never to `NO_AGENTS`. It fails as vacuous unless it covered ≥1 template and ≥1 lowered count. `scripts/tests/test-check-workflow-js-model.sh` N10–N12 (106 → 113): a stubbed `NO_AGENTS` beside a real call is a desync, the same stub on a call-free harness is a genuine 0, and the regex-quote harness is over budget at 16 against the real estimator. | #2670 review |
| The #2670 round-2 repair let the structural walk decide whenever four proof checks passed, and none of them covers a token that sets a **bound**. A `{` regex in one template interpolation and a `}` regex in a later one keep the span between them inside one template: every literal closes, the brackets balance, no `agent(` or fan-out token is lost, and the `const items = [...]` declaration between them is blanked. The fan-out was costed at 8 instead of 12, and `workflow-scale-guard.sh` went **silent** on a script `origin/main` asked about. A fuzz run (30 seeds x 20,000 generated scripts) found 18 such regressions against `origin/main` on the round-2 estimator. | `analyze()` now costs the proven structural reading **and** the #2668 flat scan, and reports the higher estimate (ties go to the structural reading). The estimate can therefore never fall below the flat reading of the same script, whatever the walk blanks; `FALLBACK=` names the flat scan when it wins beside a proven walk. The cost is that a false positive the walk would have removed is kept wherever the flat scan sees it. No shipped template's figure changed (evaluate-skill stays 18; its flat reading is 17). The same fuzz run finds 0 regressions after the change. | `hooks-plugin/hooks/test-workflow-scale-guard.sh` (59 → 66 assertions): the brace-pair fixture and a pipeline variant as `assert_asks`, `assert_parse` cases for which scan decides, and both fixtures in the differential. A **property** check runs both readings over every bundled `*/workflows/*.js` plus every fixture and fails when the reported estimate is below the flat reading; it fails as vacuous unless a template was read and some input's structural reading was lower. Removing the flat reading fails 4 assertions and taking the minimum fails 9. `scripts/tests/test-check-workflow-js-model.sh` N13 (113 → 115): the brace-pair harness is over a budget of 10 at 12, which the round-2 estimator passed at 8. | #2670 review, round 3 |
| The #2670 round-3 estimator read an array that is `push()`ed to as unbounded and costed it at ASSUMED (8) regardless of its initializer. `const items = [1, …, 12]` followed by `items.push(x)` was costed at 8, below the #2668 figure of 12, and `workflow-scale-guard.sh` went **silent** on a script `origin/main` asked about. Two more ways to fall below #2668 surfaced while checking every rule that can: the literal-array rule also dropped a site in a `.filter()`-style callback to 1 (true count: once per element), and on a parse that cannot prove itself, the rule's correct 30 → 10 on a visible literal array turned #2668's accidental ask into silence where both scans had lost the rest of the file to a quote regex. The reviewer's verdict-flip fuzzer (8 seeds × 20,000) found 695 old-ask/new-silent flips. | A pushed array returns `Grown(initializer + 1)`, which is unbounded but costed at `max(floor, ASSUMED)`. It cannot fall below #2668, which read the initializer alone. The literal-array rule skips a site that sits in an unmodeled iterating callback. On an unproven parse, `analyze()` also costs the flat text with #2668's bound logic and keeps the higher figure, so only a proven parse can fall below #2668. Array length follows JS: a hole is an element and only a final empty segment is dropped. Ablating the literal-array and trailing-comma rules reproduces #2668 on all 8 templates. After the change the fuzzer finds 8 flips, all on a proven parse where both scans read a regex holding a backtick as a template; those are closed in round 5 (next row). | `hooks-plugin/hooks/test-workflow-scale-guard.sh` (66 → 79 assertions): the pushed 12-item array as `assert_estimate` 13 and `assert_asks`; the `.filter()` callback at 3; an unproven-parse fixture as `assert_asks` plus `assert_parse` 30; differential fixtures for the trailing comma (11 → 10, allow-listed), a hole, three pushed shapes and the callback. A new property requires every unproven input to be at least #2668's figure, and fails as vacuous unless the floor raised one. Each change goes red under snapshot mutation (push as None 5, no +1 2, no floor 4, no callback check 2, holes dropped 1); the round-3 estimator fails 12. | #2670 review, round 4 |
| The #2670 round-4 estimator still applied the literal-array and trailing-comma rules to both readings of a parse that proved itself. A regex literal holding a backtick (``s.match(/[{}`]/)``) opens a template in both scans and the next one closes it, so the pipeline between two such lines was blanked while every literal closed and the brackets balanced. The literal-array rule then took #2668's 30 to 10 on a script whose true count is 34, and `workflow-scale-guard.sh` went **silent** where `origin/main` asked: 8 flips in the reviewer's fuzzer (8 seeds × 20,000). The reviewer's prototype detector read a `/` as a regex opener only after `( , = : ! & \| ? [ { } ;` and missed the same shape after `=>`, `return`, `typeof`, `+`, an `if (…)` header or a block comment: 72 of 210 opener-position variants, and 5 flips on the round-5 generator extended with such snippets. | `analyze()` costs a third reading when the source holds a regex literal with a quote or backtick in its body (`_QUOTED_REGEX`): the flat text with #2668's bound logic (`proven=False`), so the #2668 figure is a floor on such a file as well. A `/` is read as a regex opener at the start of a line, after a punctuator that expects an operand (`( , = : ! & \| ? [ { } ; < > + - * % ~ ^`), and after a keyword that does (`return`, `typeof`, `instanceof`, `in`, `of`, `new`, `delete`, `void`, `throw`, `case`, `do`, `else`, `yield`, `await`), after the `)` that closes a one-line `if`/`while`/`for`/`with` header (up to two levels of nested parens), and after a closing `*/`. After any other `)`, a `]` or an identifier the `/` is division; matching every `/` would read path prose in 7 of the 8 templates as regex and raise two budgets (20 → 71, 49 → 81). Over-matching only adds a reading, which can raise an estimate but never lower one. `FALLBACK=` names the regex, and a structural reading that raises is reported as raising rather than compared against a flat one. All 8 template budgets are unchanged. | `hooks-plugin/hooks/test-workflow-scale-guard.sh` (79 → 90 assertions): the reviewer's repro as `assert_asks`, `assert_parse` 30 and a differential fixture (30 → 30); the same shape after `=>`, after `return`, after a two-deep `if (…)` header and after a block comment, each `assert_asks` plus a differential fixture. The #2668-floor property now covers every input holding a quoted regex (13 inputs; the floor raised 6). Snapshot mutations go red: third reading dropped 16, marked proven 16, prototype opener set 8, operators dropped 2, keywords dropped 2, control-header opener dropped 2, header nesting cut to one level 2, block-comment opener dropped 2. The eight review fuzzers (8 seeds × 20,000 each) find 0 flips; the same harness against the round-4 estimator reproduces 2, 0, 3, 0, 0, 1, 0, 2. The opener-position matrix flips 0 of 210, and the extended trailing-comma fuzzer's 622 flips (638 on the round-4 estimator) are all at the script's true count, within the limit. | #2670 review, round 5 |
| Six `scripts/check-*.sh` guards had drifted off the `STATUS=` contract with nothing to catch it: `check-audit-liveness.sh`, `check-workflow-labels.sh`, `check-actionlint-version-sync.sh` and `check-workflow-tool-grants.sh` printed `STATUS=FAIL`, `check-stranded-work.sh` printed `STATUS=PASS`, two of them emitted no `ISSUE_COUNT=`, and `check-workflow-labels.sh` printed `STATUS=OK` beside an unresolved label it had just reported. Every non-OK summary also carried a count and no cause, so a rollup reading `STATUS=` + `ISSUE_COUNT=` could say how many checks failed and never which, and the caller re-ran the script to find out. `git-triage.sh` printing `ISSUE_COUNT=0` under ten issue rows (#2714) breaks the same contract on the count. The contract guard's first sweep found the sixth drift (`check-workflow-tool-grants.sh`), which the issue's triage had missed. | `.claude/rules/structured-script-output.md` defines `REASON=` (one line, at most 200 characters, on WARN/ERROR only, `<TYPE>: <MSG>` of the first finding at the reported severity plus ` (+N more)`), the `ISSUE_COUNT=`-equals-rows rule, and the domain-key note (`ISSUES_FETCHED=`/`GH_ISSUE_COUNT=` for scripts whose objects are GitHub issues). The six guards now emit canonical status, `ISSUE_COUNT=` and `REASON=`; `check-workflow-labels.sh` reports an unresolved value as `WARN` outside `--strict`. Exit codes are unchanged. | New `scripts/check-structured-output-contract.sh --strict` (+ `scripts/tests/test-check-structured-output-contract.sh`, both in pre-commit): a static sweep of every `STATUS=` emitter under `scripts/check-*.sh` for canonical values (literal, or traced through a shell or Python variable), `ISSUE_COUNT=` and `REASON=`, with `scripts/structured-output-reason-pending.txt` as a shrink-only ratchet for the 30 scripts not yet migrated; and `--validate`, which checks one captured block (one canonical `STATUS=`, integer `ISSUE_COUNT=` equal to the `ISSUES:` rows, `REASON=` present and bounded iff not `OK`). Its test pairs every ERROR fixture with a clean control. The six twins pipe their failing and passing fixtures through `--validate`; every new twin assertion was red against the origin/main script. | issue #2691 |
| `git-plugin:git-triage` emitted `ISSUE_COUNT=0` four lines below ten populated `ISSUE_<n>_*` blocks, and its Step 1 told the agent to "Parse `STATUS=` and `ISSUES:`" — the collector's structured-script-output **diagnostic** keys, whose `ISSUES:` block is absent on every clean run. An agent following the documented parse got nothing; one reading `ISSUES_FETCHED` got the right answer by ignoring the documented field. Separately `--batch` mapped 1:1 to `gh issue list --limit`, with no count of the unbounded open set and no truncation marker, so a run fetching 10 of 68 open issues read as full coverage. The truncation was also biased, not random: `gh issue list` and `gh pr list` return the N most recently **created** open items (verified live 2026-09-23 against `ForumViriumHelsinki/infrastructure` — a PR created earlier but updated later sorts after its newer siblings), so the default batch excluded exactly the oldest items, which are the stale-threshold candidates. The issue's own wording ("most recently-updated") was corrected against that measurement. `--oldest-first` had no `case` arm and was swallowed by `*) shift`, so it could only reorder the fetched batch. | Kept `ISSUE_COUNT` in its convention meaning (a count of `ISSUES:` diagnostic rows, which `.claude/rules/structured-script-output.md` requires) and fixed the reader instead: Step 1 names `ISSUES_FETCHED` as the domain count, labels `STATUS=`/`ISSUE_COUNT=`/`ISSUES:` as not GitHub issues, and states the created-order sampling and that `--oldest-first` only reorders. The collector adds `<HALF>_TOTAL` from **one** `gh api graphql` `totalCount` call covering both halves (explicit `-f owner/name` with `--repo`, gh's `-F {owner}/{repo}` placeholders without it), `<HALF>_TRUNCATED` (`true` when the total exceeds the batch), and a trailer `TRUNCATED` roll-up (`true` > `unknown` > `false`). A count that could not be read is `unknown`, never `0`, so a failed or offline query cannot claim completeness; a truncated batch is a coverage caveat and never moves `STATUS`. The PR half carries the same keys because the same `--batch` caps `gh pr list`. | `git-plugin/skills/git-triage/scripts/tests/test-git-triage.sh` (26 → 39 PASS checkpoints) EXECUTES the collector through its fixture seams plus a new `GIT_TRIAGE_COUNTS_FIXTURE`, whose files reproduce the real GraphQL response captured live (including the real `repository: null` not-found shape). It pins `ISSUES_FETCHED` equal to the emitted `ISSUE_<n>_TITLE` count **by value** (the only prior counter check asserted `ISSUE_COUNT` merely existed); 2-of-68 → `TRUNCATED=true` paired with the 2-of-2 twin → `false`, so a collector hardwired to `true` fails; `unknown` for an absent, not-found, or non-JSON count; invalid issue JSON moving `ISSUE_COUNT` to 1 while `ISSUES_FETCHED` moves to 0 (opposite directions, so the keys are provably distinct) and reporting 0-of-5; the PR half; the roll-up precedence in both orders; and the **live** invocation, which every fixture seam bypasses, through an executable stub `gh` on `PATH` whose sentinel totals (4242/17) prove the stub answered, asserting one graphql call with the right owner/name form. Against the pre-fix collector 32 assertions fail. A six-mutation sweep over copies (truncation hardwired `true`, `unknown` dropped from the roll-up, unreadable count treated as `0`, `-F` for explicit owner/name, no placeholders without `--repo`, roll-up line deleted) turns the suite red each time, with the unmutated control green and each mutation landing on exactly one site. `scripts/plugin-compliance-check.sh` `check_skill_body()` pins the Step 1 contract on the **Step 1 slice** (`ISSUES_FETCHED`, `TRUNCATED=true`, `not GitHub issues`, `reorders only the fetched batch`), so a renamed heading empties the slice and fails every token, and errors if the literal `Parse \`STATUS=\` and \`ISSUES:\`` instruction returns; run against the pre-fix `SKILL.md` it reports all five findings and exits 1. | issue #2714 |
