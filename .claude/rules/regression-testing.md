---
created: 2026-02-26
modified: 2026-07-27
reviewed: 2026-07-27
paths:
  - "**/skills/**"
  - "scripts/**"
---

# Regression Testing for Script Checks

When you fix a skill quality issue, a context command bug, or a skill body corruption problem, you **MUST** add a regression check to prevent the same issue from recurring.

## Rule

> **Every bug fix to a SKILL.md file must be accompanied by a new regression check in the appropriate script.**

This ensures the CI catches the same class of problem in any future skill, not just the one you just fixed.

## Semantic vs. syntactic gates

A regression check has two layers, and both matter:

| Gate | Asks | Catches | Misses |
|------|------|---------|--------|
| **Syntactic** | Does the file parse? Is the YAML schema valid? Does the frontmatter have the required fields? | Truncated files, broken YAML, missing fields | A description that parses but no longer triggers auto-invocation; an error enum that compiles but drops a user-facing code; a response shape that validates but omits a contract field |
| **Semantic** | Does the artefact still carry the **intent** it was designed for — the trigger phrase, the contract field, the invariant the consumer depends on? | Bulk-edit drift where an agent "tightens" prose and silently breaks downstream matchers | Pure parse failures (covered by the syntactic gate) |

Syntactic-only gates are easy to write and feel like coverage, but bulk-edit agents reliably produce syntactically-valid output that violates semantic invariants. The auto-invocation matcher does not care that the YAML parses; it cares that the literal trigger phrase is present.

**Canonical example (issue #1278):** `scripts/audit-skill-descriptions.py --strict-all` is a semantic gate — it checks that auto-invokable descriptions contain the literal `Use when` substring (the matcher's only trigger). In a 68-skill bulk edit, four of six refactor subagents replaced `Use when...` with `Use to...` / `Use for...`. Every output was YAML-valid; agent self-reports said "done"; pre-edit reviews approved the diffs. The semantic gate fired:

> `68 auto-invokable skills need description fixes / Audited 706 skills across 41 plugins`

Without it, all 68 unmatchable descriptions would have shipped silently.

### Required pattern

When you author a regression script for a class of bulk-edit risk (descriptions, error enums, exit codes, response shapes, format versions), the script **MUST** encode the *semantic invariant*, not just the *syntactic shell*. A frontmatter parser is not enough; a `Use when` substring check is.

### Script-design checklist

Your regression script should answer:

| Question | Why |
|----------|-----|
| (a) Does the artefact still parse? | Syntactic floor — necessary, not sufficient |
| (b) Does it still carry the trigger / contract / invariant it was designed for? | The semantic question that bulk-edit agents reliably break |
| (c) Is the failure message actionable enough that an agent can self-repair? | A pre-commit failure that emits `--json` lets a follow-up agent enumerate paths, repair, and re-run without human relay |

The audit script in this repo is the canonical example — it pairs a human report with `--json` output for scriptable repair. PR #1314 bakes that same audit into `agents-plugin/agents/refactor.md` as a mandatory post-pass, so the refactor agent self-verifies before reporting completion.

## Where to Add the Check

| Problem Type | Add Check To |
|-------------|-------------|
| Context command antipattern (API calls, shell operators) | `scripts/lint-context-commands.sh` |
| Skill body structure corruption (spurious headings, leaked frontmatter) | `scripts/plugin-compliance-check.sh` → `check_skill_body()` |
| Frontmatter field missing or malformed | `scripts/plugin-compliance-check.sh` → `check_skill_frontmatter()` |
| Description fails auto-invocation matching (missing/empty, or no "Use when..." trigger) | `scripts/plugin-compliance-check.sh` → `check_skill_descriptions()` (delegates to `scripts/audit-skill-descriptions.py`) |
| Skill size exceeding limit | `scripts/plugin-compliance-check.sh` → `check_skill_size()` |
| Blueprint upgrade target drift (migrations added without updating `blueprint-upgrade`) | `scripts/check-blueprint-upgrade-target.sh` |
| Hyphenated taskwarrior tag names in plugin docs (parser silently swallows them) | `scripts/lint-taskwarrior-tags.sh` |
| Plugin agent body missing `## Tool Selection` section (agents re-discover hook-blocked idioms) | `scripts/check-agent-tool-selection.sh` |
| A looping skill loses its independent stop condition or compact-state-packet fields (checkpoint plan drops `Verifier result`/`Changed since last run`/`Exit condition`/the independent-verifier step, or a sibling skill loses its `loop-integrity.md` cross-reference) | `scripts/check-loop-integrity.sh --strict` (+ `scripts/tests/test-check-loop-integrity.sh`); wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml` (self-test + run). See `.claude/rules/loop-integrity.md` |
| Plugin agent `model:` drifts off `opus` (a weaker delegate's output re-enters the main loop and degrades downstream work; `effort`, not `model`, is the cost lever) | `scripts/check-agent-model.sh` |
| Agent-dispatch skills lose the loud-failure contract (dispatched agents may surrender with one-word summaries) | `scripts/check-agent-failure-contract.sh` |
| A skill body names a `subagent_type` that resolves to no agent (stale rename, invented name), so every dispatch it instructs fails at "Agent type not found" | `scripts/check-subagent-types.sh` (+ `scripts/tests/test-check-subagent-types.sh`); extend `BUILTIN_SUBAGENT_TYPES` for a new harness-provided type, or `SUBAGENT_TYPE_EXCEPTIONS` for a genuinely illustrative non-dispatch |
| Skill description exceeds listing-budget length band (>200 WARN, >300 ERROR) | `scripts/plugin-compliance-check.sh` → `check_skill_descriptions()` (length axis); `audit-skill-descriptions.py --strict-length` in pre-commit + CI |
| Skill body or `allowed-tools` references an MCP tool name that the server doesn't actually expose | `scripts/lint-mcp-tool-references.sh` — extend the `denylist=()` array with the unavailable tool name and the suggested fix |
| Skill body documents an install/import for a package name that doesn't exist on its registry (npm/PyPI/crates/RubyGems/Go) — a dependency-confusion hazard | `scripts/lint-package-references.sh` — extend the `denylist=()` array with the `(ecosystem, wrong-name, fix)` triple |
| A `## Context` backtick command aborts the skill in a fresh project (writes stderr / exits non-zero on a missing file, dir, remote, or git state) — the whole class behind the regex rules above | `scripts/check-context-command-execution.sh` (semantic backstop: actually EXECUTES every published skill's Context command in a bare one-commit sandbox; `--strict` fails on any non-zero exit / non-empty stderr). No new rule needed per variant — it catches the class. Regression test: `scripts/tests/test-check-context-command-execution.sh` |
| Skill markdown ships a version pin (`uses:`/`FROM`/`image:`/`rev:`) in a shape Renovate's customManagers can't see, so it silently rots | `scripts/check-version-pin-coverage.sh` — extend the managed-form predicates **and** the matching `renovate.json` customManager together (see `.claude/rules/version-pinning.md`) |
| `.claude/settings.json` `enabledPlugins` drifts from the plugins published in `.claude-plugin/marketplace.json` (a plugin is added to the marketplace but never enabled, or a removed plugin is left enabled) | `scripts/check-enabled-plugins-drift.sh --strict` (+ `scripts/tests/test-check-enabled-plugins-drift.sh`); wired into `.pre-commit-config.yaml` and the `Plugin: Enablement drift` workflow |
| A test/hook shell script builds a throwaway repo with `VAR=$(mktemp -d)` then `git -C "$VAR"` / `git init --bare "$VAR"` / `git clone … "$VAR"` without guarding the dir — if `mktemp` fails, `$VAR` is empty and `git -C ""` falls back to the CWD, corrupting the real repo in a shared checkout (issue #1692) | `scripts/check-git-sandbox-guards.sh --strict` (+ `scripts/tests/test-check-git-sandbox-guards.sh`); file-level gate requiring every `mktemp -d` in a repo-targeting-git script to carry a guard. Python is exempt (`mkdtemp`/`tmp_path` raise, never return empty). Wired into `.pre-commit-config.yaml` |
| A `.github/workflows/*.yml` Claude invocation drifts off `--model opus`, or sets opus without an explicit `--effort` (opus defaults to `high`, forfeiting the cost savings; haiku/sonnet are weaker per the cost-economics in `.claude/rules/workflow-model-effort.md`) | `scripts/check-workflow-model.sh` (+ `scripts/tests/test-check-workflow-model.sh`); classifies invoking vs reusable-only vs no-invocation workflows and asserts opus + valid explicit effort. Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml` |
| `session-distill` loses its deterministic substrate — the `distill-survey.sh` collector invocation, or the `--process` / `.claude/skills/` process-routing tokens | `scripts/plugin-compliance-check.sh` → `check_skill_body()` (session-distill block asserts all three tokens survive); the collector's own coverage is `session-plugin/scripts/tests/test-distill-survey.sh` |
| Blueprint level-3 workflow templates (`blueprint-plugin/templates/*.workflow.yml`, ADR-0020 / #2005) lose their model/effort, script-injection-safety, or loop-integrity invariants — they live OUTSIDE `.github/workflows/` so `check-workflow-model.sh` + `actionlint` never scan them | `scripts/check-blueprint-level3-templates.sh` (+ `scripts/tests/test-check-blueprint-level3-templates.sh`); asserts `--model opus` + explicit `--effort` per invocation, the untrusted issue body referenced exactly once (env binding), the independent `verify:` job + state-packet fields, and runs `actionlint`. The scaffold SKILL body is guarded by `plugin-compliance-check.sh` `check_skill_body()`; the gate/parser scripts by `blueprint-plugin/scripts/tests/test-blueprint-wo-{guard,packet}.sh`. Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml` |
| The blueprint `feature-tracker.json` diverges from itself (the `statistics` block is a **cache** of the features collection) or from doc frontmatter | `blueprint-plugin/scripts/blueprint-tracker-check.sh` (+ `blueprint-plugin/scripts/tests/test-blueprint-tracker-check.sh`); recomputes `statistics`, validates the feature-status enum against `schemas/feature-tracker.schema.json`, cross-checks `tasks.*[]` membership, finds FR ids cited in `docs/**` but never minted, compares doc frontmatter status, and flags dead buckets / duplicate timestamp fields. Repo conventions come from the manifest `validation` block via `scripts/get-validation-config.sh`. Wired into `blueprint-feature-tracker-sync` + `-status` (pinned by `plugin-compliance-check.sh`) |
| The ADR collision guard misses a claimant because the number is in **frontmatter** rather than the filename, or lives in a **second** ADR directory | `blueprint-plugin/skills/blueprint-adr-validate/scripts/check-adr-numbers.sh` (widened) + `generate-adr-index.sh` (+ their `scripts/tests/`); ADR directory set comes from the manifest `validation.adr_dirs` via `get-validation-config.sh` — **not** a second config mechanism |
| A plugin agent body instructs ancestry-based branch **deletion** (`git branch --merged` piped into `git branch -d`/`-D`) — ancestry misses every squash-merged branch, so a fully-landed branch reads as unmerged and the delete runs on a known-wrong signal (the defect already fixed for `deadbranch`, #1869) | `scripts/check-agent-tool-selection.sh` — per-line shape denylist: a `git branch … --merged` command at line start, or `--merged` composed with a `branch -d`/`-D` on one line. Prose that merely *names* the command is not flagged (that is how the hazard gets taught). `BRANCH_CLEANUP_ALLOWLIST` is seeded with `experiments/claude-probe/`, which uses the broken idiom deliberately as a trap fixture; test seam `CHECK_AGENT_TOOL_SELECTION_ALLOWLIST` |
| A skill grants a bare `Bash` in `allowed-tools` but runs no shell at all — pure permission surface advertising a capability it never exercises. (The *inverse*, a bare `Bash` in a skill that does run shell, is the ratified standard and is deliberately NOT linted — see `.claude/rules/agentic-permissions.md`.) | `scripts/check-unused-bash-grant.sh` (+ `scripts/tests/test-check-unused-bash-grant.sh`). "Runs shell" = a shell-language **or unlabeled** fence with a known command (incl. `./`, `.venv/`, `<placeholder>/`, `$VAR/` prefixes), a known command in inline backticks, a sibling `scripts/` dir, a `` !`…` `` Context command, or any of those in a bundled sidecar. Markdown structure comes from `scripts/lib/extract-md-elements.py`, never a hand-rolled fence toggle (#2009). **Blocking (`--strict`) as of #2255** at both call sites; gate held at the flip (`SKILLS_SCANNED=408`, `BASH_GRANTEES=186`, `ISSUE_COUNT=0`, allowlist EMPTY), so the ratchet locks in a clean state rather than a suppressed one |
| A workflow RUNS a repo script that its own `pull_request` `paths:` filter does not match, so a PR changing only that script never runs it in CI — the guard has no CI signal for its own regressions | `scripts/check-workflow-script-triggers.sh --strict` (+ `scripts/tests/test-check-workflow-script-triggers.sh`); a workflow with no `paths:` filter is never flagged. Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml` |
| A guard reports `STATUS=OK` / exit 0 having scanned **zero** files, because a `*/.claude/worktrees/*` prune against an absolute base matched the scan root itself (the root IS an agent worktree) | Run discovery from inside the root against **relative** paths (`cd "$proj_dir"` + `find .`), per `scripts/check-subagent-types.sh`. Then make zero-scan distinguishable: `STATUS=ERROR` + `TYPE=nothing_scanned` when the corpus should be non-empty, `SCANNED_EMPTY=true` when it is legitimately empty. Class test: `scripts/tests/test-guard-worktree-root-discovery.sh` |
| A skill body ships Mustache/Jinja/Handlebars template conditionals (`{{ if X }}` / `{{ endif }}` / `{{#if}}` / `{{/if}}`) that **nothing renders** — Claude Code has no template engine in the skill-invocation path, so every branch reaches the agent verbatim with the condition variable never bound | `scripts/check-unrendered-templates.sh` (+ `scripts/tests/test-check-unrendered-templates.sh`). Exempt structurally: Go/Helm chart templates (`{{-` / `-}}` trim markers, bare `{{ end }}`, a condition rooted at `.Values` / `$.`) and the `{{ if ... }}` ellipsis prose form. Exempt by **declaration**: a file containing the phrase `markers after substitution` is a generator template the skill instructs the agent to render and strip. Wired into `.pre-commit-config.yaml` |
| A skill grants a bare `Bash` in `allowed-tools` but runs no shell at all — pure permission surface advertising a capability it never exercises. (The *inverse*, a bare `Bash` in a skill that does run shell, is the ratified standard and is deliberately NOT linted — see `.claude/rules/agentic-permissions.md`.) | `scripts/check-unused-bash-grant.sh` (+ `scripts/tests/test-check-unused-bash-grant.sh`). "Runs shell" = a shell-language **or unlabeled** fence with a known command (incl. `./`, `.venv/`, `<placeholder>/`, `$VAR/` prefixes), a known command in inline backticks, a sibling `scripts/` dir, a `` !`…` `` Context command, or any of those in a bundled sidecar. Markdown structure comes from `scripts/lib/extract-md-elements.py`, never a hand-rolled fence toggle (#2009). Advisory on landing; `--strict` flips it to blocking once the allowlist stays empty |
| The `TaskCompleted` **agent-hook prompt** loses a verification behaviour — committed work stops counting as evidence, the base ref stops being the DEFAULT branch (`@{u}` resolves to the branch's own remote counterpart, so `BASE..HEAD` is empty on any pushed branch), the working-tree fallback disappears, scope reverts to raw `git status`, or the no-evidence block is dropped | `scripts/check-taskcompleted-verification.sh --strict` (+ `scripts/tests/test-check-taskcompleted-verification.sh`). The artefact is a prompt **string inside `plugin.json`**, not a SKILL.md, so `plugin-compliance-check.sh` `check_skill_body()` cannot reach it. Each rule is a CONCEPT with several accepted spellings, so a reword passes and a revert fails. `base_ref_hardcoded` forbids only an **unconditional** base (a bare `<branch>..HEAD` range, or `BASE=<branch>`) — a documented `git rev-parse --verify --quiet` probe of `origin/main`/`origin/master` is the correct fallback and must NOT fire (that over-match blocked the #2301 repair itself) |
| `git-stash-reminder.sh` / `git-stash-session-init.sh` lose a namespace, age-bound, or write-once invariant — a repo entered mid-session or reached through a linked worktree stops reporting genuine session stashes, or a SessionStart re-fire un-reports an already-flagged one | `hooks-plugin/hooks/test-git-stash-reminder.sh` (auto-discovered via `*/hooks/test-*.sh`). Hermetic: sandboxed with `CLAUDE_STASH_BASELINE_DIR`, full `GIT_DIR` family unset per #1745, stash dates pinned to git's raw `@<epoch> <tz>` form so GNU/BSD `date` cannot diverge. Every invariant is **paired** — a false-positive case beside a true-positive one — so the suite fails both an over-reporting hook and a permanently-silent one |

## Known Regressions (Documented Bugs)

| Issue | Root Cause | Check Added | Fixed In |
|-------|-----------|-------------|----------|
| `gh repo view` in context command fails with TLS/x509 error | Uses GitHub GraphQL API; fails in proxy/offline/cert-error environments | `lint-context-commands.sh` rule `gh-api-in-context` | PR #799 |
| `name: fieldname` + `---` in skill body renders as accidental H2 heading | YAML frontmatter key leaked into markdown body; `key\n---` is a setext heading | `plugin-compliance-check.sh` `check_skill_body()` | PR #799 |
| `model: haiku` with `AskUserQuestion` causes empty silent prompts | Haiku model doesn't reliably format AskUserQuestion tool calls; prompts return empty | `plugin-compliance-check.sh` `check_skill_frontmatter()` | PR #879 |
| `model: haiku` for any skill — quality risk vs Sonnet not justified | Cost savings vs Sonnet are modest while quality risk applies to non-interactive skills too. Sonnet is the floor; Opus reserved for deep reasoning. | `plugin-compliance-check.sh` `check_skill_frontmatter()` (broadened beyond AskUserQuestion) | PR #TBD (model-parameter restoration) |
| `test -f path && echo "EXISTS" \|\| echo "MISSING"` in context command fails | `test -f` requires Bash permission; `&&`/`\|\|` are blocked shell operators; `2>/dev/null` and pipes are blocked | `lint-context-commands.sh` rules `test-in-context`, `shell-operator-and`, `shell-operator-or`, `redirection-operator`, `pipe-operator` | issue #899 |
| `grep ... package.json pyproject.toml requirements.txt` fails when files missing | grep writes to stderr when hardcoded filenames don't exist; non-JS/Python projects lack these files | `lint-context-commands.sh` rule `grep-hardcoded-multi-file` | PR #TBD |
| `find ~/.claude/plugins ...` blocked by sandbox security | `find` on home-directory paths (`~/`, `$HOME`) is outside allowed working directories | `lint-context-commands.sh` rule `home-dir-in-context` | PR #TBD |
| `jq ... .claude/settings.json` fails when file missing | jq writes to stderr when target file doesn't exist; `2>/dev/null` is blocked in context commands | `lint-context-commands.sh` rule `jq-on-optional-settings` | PR #TBD |
| `find .claude ...` in a Context block leaks `bfs: error: .claude: No such file or directory` when the project has no `.claude/` dir | `find`/`bfs` errors to stderr when its start path is absent; the health-check/health-audit skills run precisely in projects that may lack `.claude/`. Fix: start from `.` (always present) and match the path — `find . -maxdepth 2 -path '*/.claude/settings.json'` | `lint-context-commands.sh` rule `find-dotclaude-start-path` (scoped to `health-plugin/`) | issue #1482 |
| `Bash(test *), Bash(jq *)` etc. causes ~20 approval prompts | Shell utility patterns in `allowed-tools` force inline bash that can't be allowlisted; each compound command needs individual approval | `plugin-compliance-check.sh` `check_bash_patterns()` | PR #TBD |
| `blueprint-{generate,derive}-rules` writes hardcoded `.claude/rules/` and clobbers hand-authored rules | SKILL.md hardcoded the output directory; no `structure.generated_rules_path` field | `plugin-compliance-check.sh` `check_skill_body()` (blueprint rules-path check) | issue #1043 |
| `/blueprint:upgrade` reports v3.2.0 as latest after v3.3 migration was added | `blueprint-upgrade/SKILL.md` hardcodes target version; PR #1026 added `migrations/v3.2-to-v3.3.md` without updating the upgrade skill | `check-blueprint-upgrade-target.sh` compares highest migration target to `blueprint-upgrade/SKILL.md` | PR #TBD |
| Skills with capability-list descriptions ("Check and configure X") never auto-invoke | Claude matches `description` against user intent; without "Use when..." and trigger phrases the matcher never fires. 133/325 skills were affected at baseline. | `plugin-compliance-check.sh` `check_skill_descriptions()` (warns on NO_TRIGGER for auto-invokable skills; errors on MISSING/EMPTY); `audit-skill-descriptions.py --strict` in pre-commit | PR #TBD |
| Skill descriptions drift past the listing-budget target band, eating context-window share that's capped at `skillListingBudgetFraction × context` (default 1%) per Claude Code 2.1.129+. The May 2026 tightening pass brought every description below 250 chars — without an automated gate the band silently re-inflates as new skills land. | The audit tool only checked trigger-phrase presence; description **length** was never linted. 12 skills sat in the 201-250 char WARN band post-tightening (1 plugin at 205, git-plugin × 6, typescript-plugin × 5). | `plugin-compliance-check.sh` `check_skill_descriptions()` extended with a length axis (WARN 201-300 → recommendation, ERROR >300 → block); `audit-skill-descriptions.py --strict-length` (default fails on >300; `--warn-on-200` flag fails on >200) wired into `.pre-commit-config.yaml` and `.github/workflows/lint-context-commands.yml` | PR #TBD |
| `/feedback-session` fails in a repo without a git remote with "Shell command failed ... no git remotes found" | `gh issue list` / `gh pr list` without `-R` require a configured remote; stderr from a context backtick aborts skill invocation. 5 user-invocable skills (`feedback-session`, `workflow-dev`, `git-commit-push-pr`, `git-issue-manage`, `git-issue`) had this pattern. | `lint-context-commands.sh` rule `gh-list-needs-remote` (skips matches that already pass `-R owner/repo`) | PR #TBD |
| `friction-learner` auto-prescribes "Avoid plan mode for conceptual Q&A" rule from any `plan:entered-plan-mode` cluster | `friction_cluster.py` hardcoded a rule body for the cluster regardless of evidence; ignored the much-larger `reject:exitplanmode` signal entirely. Issue #1110 showed the proposal didn't match the 34:3 rejection-vs-entry ratio. | `feedback-plugin/scripts/tests/test_friction_cluster.py` asserts plan-mode and `reject:exitplanmode` clusters produce `kind: classify-required` with empty `path` and no auto-prescribed rule body | issue #1110 |
| `command -v <tool>` in Context section triggers permission-approval prompt and aborts skill | `command` is a shell builtin not covered by `Bash(<tool> *)` permission patterns; harness rejects with "Shell command permission check failed". Affected `task-status`, `task-add`, `task-done`, `task-coordinate`, `configure-load-tests`, `configure-skaffold`. | `lint-context-commands.sh` rule `command-v-in-context` | issue #1205, PR #1210 |
| `git config --get <key>` in Context section aborts skill on repos without the key set | `git config --get` exits 1 when the requested key is unset (e.g. `remote.origin.url` on a remote-less repo); the non-zero exit aborts the skill before its body runs — ironic for the canonical taskwarrior-native scenario (offline / local-only queues). Affected `task-status`, `task-add`, `task-done`. Re-reported on still-buggy / cached checkouts as #1214 and #1220 (both verified fixed at 294bd08a). | `lint-context-commands.sh` rule `git-config-get-in-context` | issue #1206, PR #1210 |
| `git-repo-agent` `blueprint:onboard` writes blueprints with `format_version: 1.0.0` instead of the current `3.3.0` | `prompts/compiler.py` strips SKILL.md sections whose heading text is in `DROP_HEADINGS` (`when to use this skill`, `context`, `parameters`, `flags`, etc.). Five blueprint skills (`blueprint-init`, `blueprint-status`, `blueprint-feature-tracker-status`, `blueprint-sync`, `blueprint-generate-rules`) placed all their actionable steps directly under `## When to Use This Skill` with `**Steps**:` as bold-text faux-headings. The compiler dropped the entire body — `blueprint-init` compiled to 49 chars (just the intro line) — and the model invented an outdated manifest schema from training data. | `git-repo-agent/tests/test_blueprint_driver.py::test_all_phases_compile_to_substantive_content` (asserts ≥500 chars per compiled phase skill) and `test_blueprint_init_advertises_current_format_version` | PR #TBD |
| Skills autocomplete with the namespace prefix (`/git-plugin:git-issue`) instead of the short form (`/git-pr`), and the description column shows the body's first heading ("Context", "When to Use This Skill") | Unquoted `args:` / `argument-hint:` values whose content is YAML-significant break frontmatter parsing. Multiple `[...]` flow sequences raise "expected block end, but found '['"; values with embedded `:` raise "mapping values are not allowed here"; a single `[foo]` parses to a list, not a string. When parsing fails, Claude Code falls back to body content for the description and renders the namespaced form. 20 skills affected before fix. | `plugin-compliance-check.sh` `check_skill_frontmatter()` parses each frontmatter with PyYAML and rejects parse errors plus non-string `args` / `argument-hint` values | PR #TBD |
| `taskwarrior-plugin` documents hyphenated tags (`+blocked-on-merge`, `+pr-ready`, `+needs-review`, `+bulk-task`) that taskwarrior silently fails to parse | Taskwarrior treats `-` mid-token as exclude-filter syntax even inside a `+tag` argument: `+blocked-on-merge` parses as `+blocked` AND `-on-merge`, the tag never lands, and the literal string ends up appended to the description (urgency does not tick up). Quoting does not help — parser quirk. Affected `task-add`, `task-status`, `task-coordinate`, and the plugin README. | `scripts/lint-taskwarrior-tags.sh` scans `taskwarrior-plugin/**/*.md` for `+word-word` patterns outside blockquotes (the gotcha callouts intentionally cite the broken form) | issue #1237 |
| Agents spawned via the `Agent` tool re-emit hook-blocked bash idioms (`find`, `grep`, `cat`, `git add . && git commit`, `Edit` before `Read`) — 200+ weekly hook reminders. Agent threads do not reliably load `~/.claude/rules/*.md`, so user-level rules don't ride along into the agent's system prompt. | All 21 plugin agent `.md` bodies lacked an in-prompt "Tool Selection" block, so each spawned thread had to re-discover the bash hooks that the user had already documented. | `scripts/check-agent-tool-selection.sh` verifies every `*-plugin/agents/*.md` includes a `## Tool Selection` heading and references both `Glob` and `Grep` (catches stub headings) | issue #1109 |
| SKILL.md body size gate measured **lines** (`wc -l`), but lines are a poor token proxy — `chars/line` spans ~3.6× across this repo, so a 416-line dense-table body (`configure-claude-plugins`, ~5,970 tokens) sailed under the 500-line ERROR while costing more tokens than a 491-line prose body. The gate measured the wrong quantity; the 500-line ERROR caught **zero** files. The rule also misattributed a "200-line ideal" to Anthropic (their published figure is 500). | Switched the gate from `wc -l` to `wc -c` (characters ≈ bytes), the cheapest tight proxy for tokens (~4 chars/token), reported with a chars/4 token estimate — consistent with the description-budget convention. The 26,000-char ERROR ceiling ≈ Anthropic's published 500-line guidance at this repo's median line density (~34 chars/line). | `plugin-compliance-check.sh` `check_skill_size()` — WARN at 10,001–26,000 chars (~2,500–6,500 tok), ERROR at >26,000 chars (~6,500 tok); `scripts/tests/test-plugin-compliance-skill-size.sh` covers OK/WARN/ERROR boundaries (matches `.claude/rules/skill-quality.md`) | skill-line-count-validity |
| SKILL.md bodies grow past Anthropic's 200-line ideal and the local 500-line ceiling, accumulating reference material that should live in `REFERENCE.md` / `scripts/`. Description trimming (PR #1265) addressed the system-prompt surface; this addresses the on-invocation surface. | No size lint existed — bodies could grow to 500+ without surfacing during review. Four skills had crossed 500 (`blueprint-feature-tracker-sync` 557, `blueprint-upgrade` 528, `project-discovery` 526, `git-branch-pr-workflow` 502) when the lint was added. | `plugin-compliance-check.sh` `check_skill_size()` — WARN at 251–500, ERROR at >500 (superseded by the char-based gate above) | PR #TBD |
| `Agent(isolation: "worktree")` briefly leaks an untracked file into the parent checkout at the same relative path as a file the child wrote in its worktree; orphan vanishes after the child commits. A naive parent session would stash, restore, or commit the orphan onto the wrong branch. | `detect-coworkers.sh` had no signal that knew to look across worktrees, so a leaked file presented as ordinary user drift. The harness's worktree isolation is not perfectly sealed during a write→commit window. | `git-plugin/skills/git-coworker-check/scripts/tests/test_worktree_leak.sh` — fifth signal in `detect-coworkers.sh` cross-checks each untracked parent file against every linked worktree (`git worktree list --porcelain`) and raises `worktree_leak_suspected` when a match is found | issue #1319 |
| `configure-claude-plugins` SKILL documents two visually-similar but semantically-distinct marketplace suffix forms (`@claude-plugins` for `enabledPlugins`, `@laurigates-claude-plugins` for workflow `plugins:`); a reader copy-pasting between adjacent sections silently produced a non-functional config because the explanation lived ~50 lines away in "Important Notes". Wrong suffix in `enabledPlugins` → entry silently ignored; wrong suffix in workflow `plugins:` → action rejects the run. | The two suffix forms appeared without inline annotations — only a single far-away footnote distinguished them. The semantic invariant is that BOTH forms must carry an inline marker near each use, not just a bottom-of-file note. | `plugin-compliance-check.sh` `check_skill_body()` — for `configure-claude-plugins` specifically, asserts the SKILL.md body contains the literal strings `extraKnownMarketplaces key` AND `marketplace \`name\` in marketplace.json` (proves both suffix forms are annotated inline) | issue #1337 |
| `taskwarrior-plugin` skills (`task-status`, `task-add`, `task-claim`, `task-coordinate`, `task-done`, `task-release`) put `git rev-parse --show-toplevel` / `git remote` / `git branch --show-current` in Context blocks. These probes write to stderr ("fatal: not a git repository") when invoked outside a git repo. Stderr from a Context backtick aborts the skill before its body runs, and `2>/dev/null` / `\|\|` are blocked in Context commands — so there is no fallback form that survives the no-git case. PR #1210 (closing #1206) only fixed `git config --get`; the sibling probes regressed. Taskwarrior's canonical use case is no-git cwds (offline / local-only queues), so this turns the skills into hard aborts for the primary scenario. | The `lint-context-commands.sh` regression for #1206 (`git-config-get-in-context`) caught only `git config --get`; it didn't generalize to other stderr-emitting git probes. | `lint-context-commands.sh` rules `git-rev-parse-in-context`, `git-remote-in-context`, `git-branch-in-context` — scoped to `taskwarrior-plugin/` (other plugins are inherently git-scoped and Context probes are reasonable there) | issue #1351 |
| `hooks-plugin/hooks/bash-antipatterns.sh` block messages for `find`, `grep`/`rg`, `cat`, `head`/`tail` used advisory prose ("REMINDER: Use the X tool instead of Y") rather than a concrete substitution. W21 friction analysis (`~/.claude/rules/friction/2026-W21-frictions.md`) measured the `grep`/`rg` same-session repeat-block rate at 29% (up from 21% the prior week) even after the `bash-tool-replacements.md` rule landed — meaning the rule's existence isn't what teaches the agent, the block message itself is. The successful counter-example is W20's `gh-json-fields.md` rule, which named the exact substitution (`state == "MERGED"`) and drove that friction class from 10/10 sessions to 0/0 in one week. | The block messages told the agent which tool to use but not how to call it. An agent receiving "Use the Grep tool instead" still has to derive the call shape from memory; an agent receiving `Grep(pattern="pattern", path="src", -r=true, -n=true)` can substitute literally. | `hooks-plugin/hooks/test-bash-antipatterns.sh` "substitution-format block messages" block asserts each block message contains the literal substitution form (`Glob(pattern=...)`, `Grep(pattern=..., path=..., -r=true, -n=true)`, `Read(file_path=...)`, `Read(file_path=..., limit=50)`, `Read(file_path=..., offset=..., limit=50)`) and points at `.claude/rules/bash-tool-replacements.md` for the full table. Prevents future bulk edits from silently reverting to advisory prose. | issue #1377 |
| `git-plugin/hooks/check-pr-metadata-on-push.sh` re-blocked pushes after every `git rebase` even when PR metadata had been reconciled — because the retry-aware bypass added in #1041 compared `PR.updatedAt` against HEAD's **committer** date (`%cI`), which `git rebase` refreshes to "now" even when no content changes. The agent's only escape was a content-different `gh pr edit`, but `gh pr edit --body-file <file>` no-ops on GitHub when the new body matches the old, so the agent ended up trapped in a re-block loop (observed in PR #1388 drain: 5 wasted round-trips after a single rebase). | The bypass used committer time, which jumps on every rebase. Author time (`%aI`) is preserved across rebases — it reflects when the work was originally written, which is what "has the PR been updated since this commit was written" actually asks. | `git-plugin/hooks/test-check-pr-metadata-on-push.sh` "author-date bypass (rebase preserves author date)" block creates a commit with `GIT_AUTHOR_DATE` 2h in the past and default committer date = now (simulating a rebased commit), then asserts the bypass fires when `PR.updatedAt` is 1h ago (between author and committer times) and still blocks when `PR.updatedAt` is 3h ago (before author wrote). | issue #1400 |
| `hooks-plugin/hooks/branch-protection.sh` denied legitimate writes when the orchestrator's cwd was on `main` and the command targeted a feature-branch worktree via `git -C <path>`. The hook ran `git branch --show-current` in its own cwd (the orchestrator's), so a Wave dispatch driving 7 isolated worktrees from a main-branch checkout hit false-positive denies 3+ times in a single session for staging and pushing. The workaround required either an explicit-refspec push or SendMessage-ing into the worktree to commit from inside. | `git branch --show-current 2>/dev/null` doesn't honor `-C <path>` flags that appear in the command string — it reads from cwd unconditionally. The hook had no `-C` parser, so the protected-branch check ran against the wrong working tree. | `hooks-plugin/hooks/test-branch-protection.sh` "git -C <worktree> branch detection (#1389)" block creates a feature/probe worktree and a master worktree under TEST_REPO (which stays on main), then asserts `git -C $FEATURE_WT commit/add/push` are allowed while `git -C $MASTER_WT commit/push` is still denied — proving the parser routes to the worktree's branch, not the cwd's, without becoming a blanket bypass. | issue #1389 |
| `git-plugin/hooks/check-pr-metadata-on-push.sh` read commits and the retry-aware bypass timestamp from the running shell's `HEAD` instead of the branch being pushed. When the orchestrator's checkout was on `fix/B` and the command pushed `fix/A` (the default situation under `git-pr-feedback --all`, which drives multiple worktrees from a main-branch checkout), the block message showed wrong commits and the bypass compared `PR.updatedAt` against the wrong author time — trapping the agent in a re-block loop when the running shell's HEAD had more recent author time than the pushed branch. The only escape was a content-different `gh pr edit` (the same trap #1400 fixed for the same-branch case). | The hook hardcoded `HEAD` in `git merge-base HEAD origin/HEAD`, `git log .. HEAD`, and `git log -1 --format=%aI HEAD`. The branch being pushed was already correctly resolved into `PUSH_BRANCH` but never used. | `git-plugin/hooks/test-check-pr-metadata-on-push.sh` "cross-branch push reads pushed branch ref (#1419)" block creates an `older-target` branch authored 3h ago and a `newer-current` branch authored 1m ago, then from the `newer-current` checkout pushes `older-target` and asserts the bypass fires when `PR.updatedAt` is 1h ago (between the two author dates). Counter-tests confirm a genuinely-stale PR still blocks and that pushing a brand-new branch whose local ref doesn't yet exist falls back to HEAD without erroring. | issue #1419 |
| `git-plugin/skills/git-pr-feedback` referenced `mcp__github__resolve_review_thread` in five places (Step 6 resolve action, Step 1A.7.4 orchestrator finalisation, Agentic Optimizations table, REFERENCE.md resolution criteria, and `allowed-tools`). The standard github MCP server does not expose that tool — `ToolSearch` returns no match — so every attempted resolve was a wasted round trip before the agent fell back to `gh api graphql`. | No lint encoded the invariant "documented MCP tool names must be exposed by their server". The github MCP server ships `add_reply_to_pull_request_comment` and `pull_request_read` but not `resolve_review_thread`; GraphQL's `resolveReviewThread` mutation via `gh api graphql` is the only available path. | `scripts/lint-mcp-tool-references.sh` — denylist-based scan of `SKILL.md` / `skill.md` / `REFERENCE.md` for known-unavailable MCP tool names. Skips blockquote lines so gotcha callouts can still cite the broken form. Wired into `.pre-commit-config.yaml`. Seeded with `mcp__github__resolve_review_thread` plus its `gh api graphql` fix; extend the `denylist=()` array when more unavailable tools surface. | issue #1429 |
| Dispatched worktree-isolated agents return one-word summaries (`Terminal.`, `Done.`, `Stopped.`) on failure after 50–200 tool calls — no PR URL, no error explanation, no "what's blocked" list. A one-word summary is indistinguishable from success to the orchestrator, so the harness cleans up the worktree and the substantial in-progress work is silently lost. | `agent-patterns-plugin`'s `parallel-agent-dispatch` Return Contract mandated a success/partial/failed schema but never explicitly forbade the silent-surrender *negative* case, and `custom-agent-definitions` carried no failure-reporting guidance at all. | `scripts/check-agent-failure-contract.sh` asserts `parallel-agent-dispatch/SKILL.md` carries the literal "Loud-failure contract" heading, the banned `Terminal.` example, and the `#1422` reference, and that `custom-agent-definitions/SKILL.md` cross-references `#1422`. Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml`. | issue #1422 |
| Skill markdown ships ~150 hand-maintained version pins (`uses:`/`FROM`/`image:`/`rev:`) across `configure-plugin`; each is correct the day it lands and rots after. The W22 audit found several years-stale pins and one `trivy-action` SHA pin whose hand-transcribed commit hash did not match the version in its own trailing comment — the canonical hand-maintenance failure. | No tooling treated example pins as a managed dependency surface: Renovate had no `customManagers` for skill markdown, and nothing guarded that a new pin was even *visible* to Renovate. | `scripts/check-version-pin-coverage.sh` (+ `scripts/tests/test-check-version-pin-coverage.sh`) scans fenced code in `*/skills/**/*.md` and ERRORs on a version-shaped pin not in a Renovate-managed shape; `renovate.json` gains four `customManagers` + a `pinDigests` packageRule scoped to skill markdown. Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml`. | `.claude/rules/version-pinning.md` |
| `taskwarrior-plugin/skills/task-add` reported only the numeric ID after `task add` (`Created task 141.`). Taskwarrior numeric IDs are a display index over *pending* tasks and shift down by one whenever any other task completes (often in a parallel session). A session that created task 141, then 30 min later ran `task 141 annotate ...`, silently annotated an unrelated task whose ID had drifted — requiring a denotate + UUID-lookup recovery. The skill had no ergonomic to capture the stable UUID at create time. | The skill's Step 5 (create) and Step 6 (report) emitted only the numeric ID. The fix resolves the immutable UUID via the `+LATEST` virtual tag (`task +LATEST _get uuid`) as a separate Bash call and surfaces it in the report so downstream annotate/modify/done address the task by UUID. | `plugin-compliance-check.sh` `check_skill_body()` — for `task-add` specifically, asserts the SKILL.md body contains the literal string `task +LATEST _get uuid` (proves the UUID-emit survives future bulk edits) | issue #1417 |
| `git-pr-feedback --all` surfaced automation-authored PRs (release-please, dependabot, renovate, image-updaters) purely because their CI was failing, forcing a per-PR `AskUserQuestion` before dispatch — dispatching a subagent on them is almost always wrong (no review threads, protected changelog/version files). | `list-actionable-prs.sh` selected any non-draft PR with failing CI / unresolved threads / `CHANGES_REQUESTED` regardless of author. Now excludes well-known automation logins plus `*[bot]` / `*-bot` by default (extensible via `PR_FEEDBACK_AUTOMATION_AUTHORS`); `--include-automation` opts back in. | `git-plugin/skills/git-pr-feedback/scripts/tests/test-list-actionable-prs.sh` feeds a captured GraphQL response via the new `PR_FEEDBACK_FIXTURE` seam and asserts default-exclude, `--include-automation`, and env-var-extension behaviours | issue #1420 |
| `fix-registry.sh --fix` removed a stale `enabledPlugins` key from `~/.claude/settings.json` and reported `STATUS=FIXED`, but the file was chezmoi-managed — the chezmoi *source* still carried the key, so `chezmoi apply` re-added it and the fix silently reverted. The script gave no durability signal. | The script edited only the target file with no awareness of dotfile management. Now `warn_if_chezmoi_managed` runs whenever Stage 2 touches `settings.json` (dry-run and real), emitting `SETTINGS_CHEZMOI_MANAGED=true`, `SETTINGS_CHEZMOI_SOURCE=<path>`, and a `WARNING=` line via `chezmoi source-path` (overridable for tests with `FIX_CHEZMOI_BIN`). | `health-plugin/skills/health-plugins/scripts/test-fix-registry.sh` Guard 4 mocks `chezmoi` and asserts the warning + source path appear for a managed file and stay absent for an unmanaged one | issue #1481 |
| `langchain-plugin/skills/deep-agents` documented `npm install @langchain/deep-agents` plus eight `import { DeepAgent } from "@langchain/deep-agents"` lines. That scoped name returns 404 on the npm registry — it is unclaimed, so anyone could publish malicious code under it and every user copy-pasting the install command would run it (dependency-confusion). The real package is `deepagents` and the real API is `createDeepAgent()`, not a `DeepAgent` class — the whole skill was built on a hallucinated package + API. Flagged by an external security researcher. | No lint encoded the invariant "a package an install/import command names must exist on its registry". The risk generalizes across ecosystems (PyPI, crates.io, RubyGems, Go modules), so the check is package-manager-agnostic. | `scripts/lint-package-references.sh` — denylist-based scan of `SKILL.md`/`skill.md`/`REFERENCE.md` for known-nonexistent `(ecosystem, package-name, fix)` triples; skips blockquote lines so gotcha callouts can cite the broken form. Wired into `.pre-commit-config.yaml` and the `Plugin: Lint skills` workflow (`lint-context-commands.yml`). Seeded with `@langchain/deep-agents` → `deepagents`; extend the `denylist=()` array when more phantom packages surface. | npm-package-reference-check |
| `project-plugin/skills/project-continue` read deprecated blueprint v1/v2 state paths `.claude/blueprints/prds/` (SKILL.md:42) and `.claude/blueprints/work-orders/` (:47), so the resume skill never found PRDs or work-orders in a current-format (v3.x) blueprint repo — where state lives under `docs/`. The skill was silently broken against every blueprint repo on the canonical layout. | The path strings were never updated when blueprint state moved from `.claude/blueprints/` to `docs/` (ADR-0011). Static lint passed because the paths are valid strings; only a semantic check knows they point at a deprecated location. | `plugin-compliance-check.sh` `check_skill_body()` — for `project-continue` specifically, errors if the SKILL.md body contains `.claude/blueprints/` (scoped to that skill because `blueprint-migration`/`blueprint-upgrade` legitimately cite the old paths as historical move sources) | issue #1503 |
| `scripts/check-version-pin-coverage.sh` took ~4–5 min per run — slowing every local `git commit` (pre-commit) and the `plugin-pr-checks` CI step — because its `find … -path '*/skills/*'` walk descended into `.claude/worktrees/<name>/` agent-clone copies of the whole repo. With several concurrent worktrees present it re-scanned the same skill files many times over (**499 real files became 12,768**); a secondary cost was a subshell + two `echo` forks per code-fence marker. | The walk had no prune for `.claude/worktrees/`, so it audited sibling agents' checkouts instead of only the real tree; the fence-state toggle also forked a subshell on every ` ``` `/`~~~` line. | `scripts/tests/test-check-version-pin-coverage.sh` "TEST F: .claude/worktrees/ copies are pruned, not scanned (#1492)" adds a `SKILL.md` under `.claude/worktrees/agent-deadbeef/…/skills/` and asserts `FILES_SCANNED` is unchanged and no `.claude/worktrees/` path leaks into the report. The script's `find` gained a `-path '*/.claude/worktrees/*' -prune` clause and the fence toggle became a pure-bash `if`. | issue #1492 |
| A schema-bound, worktree-isolated agent cut off (e.g. by a rate limit) *after* implementing its change but *before* emitting the final StructuredOutput call is reported as failed — yet the work sits fully intact as uncommitted WIP in its worktree. The orchestrator's result array shows nothing, so the completed work is invisible and easily discarded; a naive re-dispatch redoes work that was already done. | `agent-patterns-plugin`'s `parallel-agent-dispatch` "Handling a Missing Return" had no empty-vs-dirty worktree discrimination step and no checkpoint-WIP-commit brief instruction, and REFERENCE.md had no dedicated salvage subsection for the StructuredOutput-cut-off case (the existing salvage routine covered only the pre-commit-hook stall). | `scripts/check-agent-failure-contract.sh` asserts `parallel-agent-dispatch/SKILL.md` carries the literal `empty vs dirty` discrimination, the `WIP at checkpoints` brief instruction, and the `#1491` reference, and that `REFERENCE.md` carries the `WIP salvage before re-dispatch` subsection heading plus `#1491`. Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml` (same entry as #1422). | issue #1491 |
| `health-check` reported `PROJECT_SETTINGS=MISSING` and N/A for every project-level check when the workspace root had no `.claude/` but the real config lived one level down (parent-workspace / monorepo layout, e.g. `tooling/.claude/settings.json`). `check-settings.sh` resolved the project `.claude/` strictly at `--project-dir`, so a fully-populated nested config was invisible. | The script never looked below the workspace root. The fix adds one-level-down discovery: when `${project_dir}/.claude/settings.json` is absent, it globs `${project_dir}/*/.claude/settings.json` — a single match resolves `project_dir` to it and emits `PROJECT_DIR_RESOLVED=`, multiple matches emit `PROJECT_DIR_HINT=` and keep MISSING behavior, none stays MISSING with no hint. | `health-plugin/skills/health-check/scripts/tests/test-check-settings.sh` covers all four cases (single→resolve, multiple→hint, root-present→no-resolution, none→MISSING-no-hint) | issue #1483 |
| A `git-write` agent dispatched under `isolation: "worktree"` ran its `git fetch` / `git checkout -B` / `git rebase --autostash` against the **main checkout** instead of its assigned worktree, silently corrupting the user's main working tree (autostash pop-conflict + stray branch). Distinct from the #1319 *transient* leak: here the agent thread's bash cwd reset between calls landed on the session's primary cwd (the main repo root), so every bare `git` mutated `main`. The orchestrator believed the work was sandboxed. | `parallel-agent-dispatch/SKILL.md` and `git-plugin/agents/git-ops.md` did not tell git-write agents to be resilient to the documented cwd reset — no guidance to pin the worktree root and prefix git calls with `git -C "$WORKTREE"`, no post-run main-repo integrity check. | `scripts/check-agent-failure-contract.sh` asserts `parallel-agent-dispatch/SKILL.md` carries the `#1480` reference AND the literal absolute-path idiom `git -C "$WORKTREE"` (the defence against the cwd reset). SKILL.md Pillar 1 gains a "cwd-reset leaking git writes" subsection (distinct from the #1319 transient-leak section) and `git-ops.md` gains an "Important: Worktree Isolation and cwd Reset" section. | issue #1480 |
| `docs/diagrams/plugin-relationships.d2` drifted out of sync with the plugin set: a removed `command-analytics` plugin (and a stale `sync` node) were still drawn, ~10 per-plugin skill/agent counts were wrong, and the three plugins added in #1522 (`comfyui`, `macos`, `taskwarrior`) were absent. The rendered `.svg` is generated so the drift could only be fixed at the `.d2` source — and nothing guarded against it recurring. | The diagram's node labels (`label: "<name>\n<N> skills"`) were hand-maintained with no check tying them to disk. `scripts/check-docs-index.sh` validated README / PLUGIN-MAP counts (Check 3) but not the diagram. | `scripts/check-docs-index.sh` Check 4 parses each `.d2` node label, flags a node naming a non-existent plugin (`diagram_node_dangling`, ERROR) or a stated count diverging from disk (`diagram_count_drift`, WARN); the `.svg` is generated and intentionally not parsed. `scripts/tests/test-check-docs-index.sh` TEST E covers dangling-node, wrong-count, and correct-node (no-false-positive) cases. | issue #1523 |
| `project-plugin/hooks/project-distill-nudge.sh` (Stop) fired mid-`/session-wrap`, racing the skill's pending "Apply? (y/n)" confirmation and splitting it into a double-question. Root cause was twofold: its wind-down regex scanned every `"role":"user"` transcript line — and a slash-command expansion carries the skill's own markdown ("Wrap up a working session…"), which matched — and it had no guard for a wrap flow already being in progress. Tool-result lines (also `role=user`) inflated the turn floor too. | Transcript `"role":"user"` lines are three distinct things (genuine prompt, tool result with `tool_use_id`, command expansion with `<command-name>`); the hook treated all three as user text. The sibling user-level `session-wrap-nudge.sh` had gained an in-progress guard but the near-duplicate plugin hook never did — classic duplicated-script drift, resolved by collapsing both into one nudge (design D4). | `session-plugin/hooks/test-session-end-nudge.sh` asserts: skill markdown in a command expansion does NOT trigger the wind-down gate; tool_result lines do NOT count toward the turn floor; a `session-wrap`/`session-end`/`session-distill` invocation in the transcript suppresses the nudge; the fired reason carries the literal `session-plugin:session-end` + offer-only contract. Skill side, wrap/end confirm via `AskUserQuestion` so the turn never ends mid-confirmation (see `hooks-reference.md` § Stop-Hook Nudges). | PR #1561 |
| `hooks-plugin/hooks/bash-antipatterns.sh` blocked read-only `echo`/`printf` commands carrying an fd-redirect-to-/dev/null (`echo "hi" 2>/dev/null`, `printf … 2>/dev/null`) as if they wrote a file — even when the `2>/dev/null` belonged to a sibling command inside `$(...)`. The #1701 `[^0-9]>\s*/dev/` exemption did not fire when the fd redirect was the ONLY redirect (the leading `2` is a digit), so a common survey/diagnostic shape (`echo label $(gh … 2>/dev/null)`) aborted with the Write-tool reminder. | The detector used a single negative-exemption regex that a digit-prefixed redirect defeated. Fix: strip every `[0-9]*>\s*/dev/<target>` (both `>/dev/` and `2>/dev/`) from the scanned view first, so only a surviving `>` to a real (non-/dev) file remains as the signal — preserving the #1701 mixed-write case (`echo x > out.txt 2>/dev/null` keeps `> out.txt` and still blocks). | `hooks-plugin/hooks/test-bash-antipatterns.sh` "fd-redirect to /dev/null on echo/printf is allowed; real file writes still block" block: the 5-row #1722 repro + the #1721 `$(... 2>/dev/null)` command-substitution shape must be ALLOWED (exit 0); guard-integrity counter-tests confirm `echo "text" > file`, `printf … > out.md`, and the #1701 `echo x > realfile.txt 2>/dev/null` mixed write still block (exit 2). | issue #1722, #1721 |
| 45 skills were stored with a lowercase `skill.md` filename instead of the canonical `SKILL.md`. Claude Code matches `SKILL.md` case-sensitively, so on any case-sensitive filesystem (Linux, CI, contributors on ext4/btrfs) those 45 skills silently fail to load — and rulesync (OpenCode export) also matched case-sensitively, treating them as supporting files instead of converting them. Invisible on macOS's case-insensitive APFS, where `SKILL.md` resolves to `skill.md`. | The existing `plugin-compliance-check.sh` skill discovery globs with `-iname` (case-insensitive), so it never flagged the casing; no check asserted the canonical filename. | `scripts/check-skill-filename-case.sh` uses `git ls-files` (independent of the running FS's case sensitivity) to flag any tracked skill file matching `skill.md` case-insensitively but not exactly `SKILL.md`. Wired into `.pre-commit-config.yaml`. | issue #1606 |
| `git-plugin/skills/deadbranch` classified branches as merged via the `deadbranch` CLI's commit-ancestry check (`git branch --merged`-style), which under-reports badly on **squash-merge** repos (the release-please / conventional-commit default): a squash collapses a branch into one fresh-SHA commit on the base, so the branch's own commits are never ancestors and it reads as **unmerged**. In one real cleanup only 4 of 28 actually-merged local branches were flagged "safe to delete"; the rest forced a `--force` (which also sweeps genuinely-unmerged work) or a hand audit. | The skill had no squash-aware reclassification. The fix adds a read-only Step 1.5 that reclassifies each `deadbranch`-unmerged branch via two deterministic signals that survive squash-merge AND later base file-drift: a `git merge-tree` no-op (merged tree equals base's tree) OR a MERGED PR for the branch head (`gh pr list --state all --head … --json state`, reading `state`/`mergedAt` per gh-json-fields.md). Reclassified branches still flow through the existing dry-run + confirmation gates. | `plugin-compliance-check.sh` `check_skill_body()` — for `deadbranch`, asserts the SKILL.md body retains both `merge-tree` and `gh pr list --state all --head` (the two reclassification signals). Dropping either would silently revert to ancestry-only detection. | issue #1869 |
| `git-plugin/skills/git-triage/scripts/git-triage.sh` packed the full PR `body` as the 8th field of the per-PR categorization `@tsv` row. Bot (Dependabot/Renovate) PR bodies are large multi-line markdown with embedded tabs, so on read every enum column slid right: `PR_<n>_REVIEW` held the worst-check conclusion, `PR_<n>_WORST_CHECK` held the entire ~9 KB body, and `categorize_pr` was fed garbage → fell through to `uncategorized` for **every** PR. On a 10-PR repo the script also blew past the 25 KB Read cap (92.8 KB output), making Step 4 of the skill non-functional for any repo whose PRs have non-trivial bodies. Secondary bug: `(.reviewDecision // "null")` did not normalize empty-string `""` (returned by bot PRs with no review requested), so the `awaiting-review` branch could misfire even with aligned columns. | Closing-keyword extraction was the only consumer of `body`, but it was packed into the same TSV row as the enum fields. The fix drops `body` from the enum row entirely and extracts closing refs (single `#NNN` tokens, TSV-safe) in a **separate** jq pass keyed by PR number, and normalizes empty-string `reviewDecision` to `null`. | `git-plugin/skills/git-triage/scripts/tests/test-git-triage.sh` "#1627" block adds a bot-PR fixture with a multi-line body (embedded tabs/newlines) and `reviewDecision:""`, then asserts `WORST_CHECK=SUCCESS` (body never shifts the enum column), `REVIEW=null` (empty-string normalized), `CATEGORY=awaiting-review` (not uncategorized), `CLOSES=#4242,#4243` (extraction survives the separate pass), and that the body never appears verbatim in the KEY=VALUE output. | issue #1627 |
| `project-plugin/skills/project-discovery/scripts/analyze-skills.sh` walked the whole repo with `find … -path '*-plugin/skills/*/SKILL.md'` and never pruned `.claude/worktrees/`. Each linked worktree is a full repo clone, so in a checkout with N active agent worktrees the analyzer enumerated the skill tree N+1 times (a session with ~31 worktrees ran >5 min without finishing and had to be killed; `COVERAGE` / `CANDIDATE` / `TOTAL_SKILLS` were also inflated by the duplicated copies). Same class as the #1492 prune fix for `check-version-pin-coverage.sh`, never applied here. | The `find` had no `-path '*/.claude/worktrees/*' -prune` clause, so it descended into sibling agents' checkouts instead of only the real tree. | `project-plugin/skills/project-discovery/scripts/tests/test-analyze-skills.sh` plants a `SKILL.md` under `.claude/worktrees/agent-deadbeef/…/skills/` and asserts `TOTAL_SKILLS=1` (the worktree clone is pruned, not double-counted) and that no `.claude/worktrees/` path leaks into the output. Auto-discovered by `scripts/run-skill-script-tests.sh`. The script's `find` gained the `-path '*/.claude/worktrees/*' -prune` clause (grouping the SKILL.md/skill.md alternation under the print branch). | issue #1548 |
| The `Claude: Changelog review` workflow never surfaced the upstream `TaskOutput` tool deprecation (2.1.83), so `hooks-plugin/hooks/bash-antipatterns.sh` kept recommending a dead tool until #1591/#1633. Three converging causes: the triage keyword set (`Hook/Skill/Agent/Permission/Plugin/MCP/Sandbox/Breaking`) had no deprecation dimension, so a "Deprecated `X` in favor of `Y`" line scored zero; the candidate-file map only ever pointed at `.claude/rules/*.md`, never at the skill/hook/agent code that actually names the tool; and the analysis logic was inline workflow bash with no unit test. | The "changelog → rule docs" mental model had no "changelog → plugin code" path, and nothing was testable outside the YAML. | `project-plugin/skills/changelog-review/scripts/analyze-changelog.sh` (extracted, structured `KEY=VALUE` output) adds a DEPRECATION dimension and a deprecation→plugin-code bridge (greps `*-plugin/`, `.claude/rules/`, `CLAUDE.md` for each deprecated identifier and surfaces referencing files as candidates). `scripts/tests/test-analyze-changelog.sh` fixtures a `TaskOutput` deprecation referencing a fake hook and asserts it is surfaced, that an unreferenced token raises no flag, and that an oversized excerpt flags `STATUS=WARN`. | issue #1638 |
| `hooks-plugin/hooks/bash-antipatterns.sh` was reported to false-positive-block `echo`/`printf` used purely for stdout display (section headers like `echo "=== … ==="`) and to miscount pipes for `grep`-headed transform pipelines. The current hook already gates the echo/printf→file rule on an actual `>` redirection, so all three repros pass (exit 0) — no code change was needed; the risk is a future bulk edit silently dropping the `>` gate and reviving the false positive. | The echo/printf→file detector correctly requires a real file-redirection target; without a locked-in regression test, a refactor that broadened the match back to `.*` (or dropped the `>` requirement) would re-block stdout-only headers undetected. | `hooks-plugin/hooks/test-bash-antipatterns.sh` #1701 block asserts stdout echo/printf headers (with `;` separators, no `>`) and a `grep`-headed transform pipeline are allowed (exit 0), while guard-integrity counter-tests confirm a genuine `echo "text" > file` and `printf 'x' > out.md` still block (exit 2). | this PR |
| `git-issue` / `git-issue-manage` advertised bare issue numbers only (`args: "[issue-numbers...]"`), so a user pasting a GitHub issue **URL** (`https://github.com/owner/repo/issues/123`) or `#123` had it silently ignored, and a URL pointing at a **different** repo than the current remote had no `-R owner/repo` plumbing. An agent sweep (haiku + opus-low cold-readers, see `.claude/rules/skill-argument-handling.md`) flagged the input-form gap on both skills with both models agreeing. | The skills' Mode Detection / Step 1 assumed numeric tokens; no normalization step mapped `#N` / URL forms to `(number, repo)`. The semantic invariant is that the advertised URL form is actually normalized in the body (`/issues/<N>` shape) **and** cross-repo refs carry `-R`. | `plugin-compliance-check.sh` `check_skill_body()` — for `git-issue` and `git-issue-manage`, asserts the SKILL.md body contains both `/issues/` (URL normalization) and `-R ` (cross-repo plumbing). Guards a bulk edit re-tightening args back to "issue numbers" and dropping Step 0. | this PR |
| `project-refocus` accepted no focus directive (`args: ""`; its `argument-hint` described *when* to invoke, not *what to type*), so `/project:refocus focus on X, ignore Y` was dropped — the user could not steer the Done/Remaining/Stale bucketing. | No `## Parameters` section parsed `$ARGUMENTS`. The fix parses it as an **optional steering directive** that biases bucketing. The load-bearing invariant is *steering, not override*: the directive must not cancel a live user boundary (the `auto-mode.md` Conversation-Stated-Boundaries hazard). | `plugin-compliance-check.sh` `check_skill_body()` — for `project-refocus` (scoped on the **directory name**, not `name: refocus`), asserts all four tokens survive: `$ARGUMENTS`, `focus directive`, `Stale-eligible`, `boundary`. Dropping the boundary token would re-introduce the override hazard. | this PR |
| `.claude/rules/skill-fork-context.md` declared `context: fork` "BROKEN for plugins" long after the upstream blocker was fixed. anthropics/claude-code#16803 (silently-ignored-for-plugins) closed COMPLETED 2026-04-18; #33154 (the `[1m]` rate-limit half of the gate) closed as not-planned/stale — it was a Cowork *product* bug, never a CLI tracker. The repo's "revisit when both resolved" gate expired degenerately (#33154 will never resolve cleanly), leaving the rule stale and `context: fork` needlessly banned for single-subagent verbose skills. | The gate watched an issue that could only ever go stale, not fixed. Resolved by moving to an empirical canary: `code-quality-plugin/skills/code-review` (single-subagent, verbose) gets `context: fork` restored and is verified on an Opus `[1m]` session before rolling out to the other 10 skills in #980. The remaining `[1m]` cascade hazard is reframed as a constraint on **parallel** fan-out, not on a single fork. | `plugin-compliance-check.sh` `check_skill_body()` — for `code-review` (scoped on directory name + `code-quality-plugin`), asserts the SKILL.md retains `^context: fork`. Dropping it erases the canary and the only on-disk signal the restoration is in flight; a deliberate rollback updates this guard and the rule together. | issue #980 |
| `github-actions-auth-security` covered Claude *prompt* injection but not the GitHub Actions **script-injection** class — untrusted run-context values (`github.event.*.title`/`.body`, comment bodies) interpolated `${{ … }}` directly into a `run:` script, handing shell execution to anyone who can open a PR. GitHub's secure-use reference flags this as the single most-missed workflow item, and the Claude Code workflow templates this repo scaffolds consume exactly that untrusted context. | No skill or rule documented the env-var indirection mitigation (bind the untrusted value to an `env:` variable, reference the quoted shell variable). Added `.claude/rules/github-actions-security.md` (the secure-use baseline) and the script-injection section + strengthened `pull_request_target` guidance in the skill. The semantic invariant is that the env-var indirection example survives bulk edits. | `plugin-compliance-check.sh` `check_skill_body()` — for `github-actions-auth-security`, asserts the SKILL.md body retains both the `Script Injection` section heading and the `PR_TITLE` corrected-pattern token. Dropping either would silently restore the injection footgun. | this PR |
| In a multi-request session Claude kept building on a PR branch after its reality changed — committing onto a branch whose PR had already **merged** (work belongs on a fresh branch off the updated default), or whose tip was **behind origin** because a teammate / another agent / a CI auto-fix pushed under it. Nothing fired *mid-session before building further*: the `subscribe_pr_activity` MCP tool was unused, the `drift-aggregator` nudge only fires at SessionStart, and `git-coworker-check` covers only local-checkout collisions. | No remote PR/branch-sync guard existed. Added the `git-plugin` trio (`.claude/rules/pr-branch-sync.md`): the read-only `/git:pr-sync-check` skill (emits a shared `VERDICT` incl. `pr_merged`, reads `state`/`mergedAt` per gh-json-fields.md), the `check-branch-sync-on-push.sh` PreToolUse hook (`ask`-not-deny, cached per session+branch), the `git-drift-probe.sh` SessionStart probe (reuses `drift-protocol.sh`), and the `/git:pr-watch` skill (wraps `subscribe_pr_activity`). The semantic invariant is the verdict contract + the native watch tool name survive bulk edits. | `git-plugin/hooks/test-check-branch-sync-on-push.sh` (behind/merged/closed→ask, in-sync→allow, cache-hit→silent, `git -C` worktree routing, opt-out); `git-plugin/skills/git-pr-sync-check/scripts/tests/test-check-pr-sync.sh` (every verdict + CI roll-up); `plugin-compliance-check.sh` `check_skill_body()` asserts `git-pr-sync-check` retains `VERDICT=`/`pr_merged`/`mergedAt` and `git-pr-watch` retains `subscribe_pr_activity`. | this PR |
| `/configure:all` aborted at invocation with `Shell command failed ... ugrep: warning: .project-standards.yaml: No such file or directory`. Its Context section ran `grep -m1 "^standards_version:" .project-standards.yaml` against a single hardcoded file; when the file is absent (any project not yet onboarded — the common case) grep writes to stderr and exits non-zero, and stderr from a Context backtick aborts the skill before its body runs. ~30 sibling Context greps across 17 skills (configure-status/-select/-pre-commit/-skaffold/-linting/-formatting/-tests/-sentry/-mcp/-memory-profiling/-dockerfile/-ux-testing/-makefile/-integration-tests, blueprint-adr-validate/-prp-execute, rust cargo-machete, testing test-run, api-tests) shared the bug. The existing `grep-hardcoded-multi-file` rule only caught the two-explicit-files shape. | Rewrote each to `find DIR -maxdepth 1 -name FILE -exec grep PATTERN {} +` (empty output + exit 0 when missing; the find-exec form ends in `{} +`, so it does not match the new rule). | `lint-context-commands.sh` rules `grep-hardcoded-single-file` (file token by extension) and `grep-hardcoded-bare-file` (Makefile/Dockerfile/Gemfile/Rakefile); file discovery also excludes the gitignored `dist/` rulesync build output | this PR |
| `code-quality-preflight-cue.sh` (PostToolUse) emitted a spurious `/code-quality:code-lint` cue when creating a diagram source/artifact file — a ≥50-line `.d2`/`.svg` write tripped the large-payload structural signal even though `/code-quality:code-lint` has nothing to lint for diagram assets (the real validation is `d2 <file> <out>` rendering). | The early silent-extension `case` covered only `*.md`/`*.txt`; diagram/binary extensions fell through to the size-based signal. Extended the exclusion to `*.d2`/`*.svg`/`*.png`/`*.jpg`/`*.jpeg`/`*.pdf`. | `code-quality-plugin/hooks/test-code-quality-preflight-cue.sh` test (i) asserts a 60-line `.d2` Write and a `.svg` Edit are silent (no `decision:block` JSON) | issue #1730 |
| The regex `lint-context-commands.sh` is a denylist of known-bad Context-command *shapes* — every prior fix above (`test -f` #899, redirection #900, `command -v` #1205, `git config --get` #1206, `git rev-parse`/`git remote` #1351, `find .claude` #1482, single-file grep this PR) added one rule for one variant after someone got burned. The shapes not yet enumerated stayed live: a full execution sweep found **121 genuine aborts across 71 published skills** (93 `find <missing-dir>`, 15 `git remote get-url` in a remote-less repo, jq-on-missing-file, …) that no regex rule caught. | Added `scripts/check-context-command-execution.sh` — a **semantic** backstop that runs every published skill's `## Context` backtick command in a bare one-commit git sandbox and fails on any non-zero exit / non-empty stderr, catching the whole abort class regardless of shape (ENV_MISSING tool/auth gaps and UNPARSEABLE extractions are classified out; `.claude/skills/` repo-internal meta-skills excluded). Swept all 121 to robust forms (`find . -path '*/<dir>/*'`, `git remote -v`, `find -exec jq`). | `scripts/check-context-command-execution.sh --strict` wired into `.github/workflows/lint-context-commands.yml` (full-repo) and `.pre-commit-config.yaml` (staged files); regression test `scripts/tests/test-check-context-command-execution.sh` (bad-grep→FAIL, find-exec→PASS, missing-tool→ENV_MISSING, missing-dir→FAIL, git-history→PASS) | this PR |
| 13 plugin agents ran on `sonnet`/`haiku` (4 sonnet: `attribute-router`, `performance`, `refactor`, `research`; 9 haiku: `dependency-audit`, `docs`, `search-replace`, `test`, `container-build`, `git-ops`, `k8s-diagnostics`, `terraform-ops`, `test-runner`), contradicting the user-global always-Opus standard (`~/.claude/rules/agent-and-tool-selection.md`) with nothing to stop them drifting back. A weak subagent's output re-enters the main loop as a tool result and degrades everything downstream; Opus-low beats Sonnet-high on both quality and tokens, so `effort` (a session setting), not `model`, is the cost lever. | No lint asserted `model: opus` on agent files, and `agent-development.md` / ADR-0009 still taught a three-tier haiku/sonnet/opus palette. | `scripts/check-agent-model.sh` (+ `scripts/tests/test-check-agent-model.sh`) requires `model: opus` on every `*/agents/*.md`; carries an empty `AGENT_MODEL_ALLOWLIST` seam for a future measurement-instrument agent file (the cold-read-gate haiku reader is a skill-inline dispatch, not an agent file, so nothing is exempt today). Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml`. | this PR |
| `code-quality-preflight-cue.sh` (PostToolUse) over-fired on small standalone shell scripts: a ~25-line shell wrapper with an `export FOO=bar` line tripped Signal 2 (the `export` public-symbol regex) even though shell `export` is a builtin assignment, not a public API symbol — and the cue unconditionally suggested `/evaluate:evaluate-skill` even when no skill file changed (a no-op on ordinary edits). | Signal 2 used one regex for all file types and the cue text was static. Shell extensions (`.sh`/`.bash`/`.zsh`) now use a regex without the `export` alternative (they still fire on Signal 3 ≥50 lines); the `/evaluate:evaluate-skill` clause is appended only for paths under a `skills/` tree. | `code-quality-plugin/hooks/test-code-quality-preflight-cue.sh` tests (j) small shell wrapper with `export` is silent + large shell script still fires, and (k) non-skill edit omits `evaluate-skill` + `skills/` path includes it | issue #1766 |
| `just export-opencode` aborted with `Failed to parse frontmatter … YAMLException: duplicated mapping key … modified` because `testing-plugin/skills/test-setup/SKILL.md` carried two `modified:` keys (a stale `2026-05-09` plus a freshly stamped `2026-06-18` — a date-stamping pass appended a new key instead of replacing the existing one). rulesync's (js-yaml) parser rejects duplicate mapping keys, so the OpenCode export staged 365 skills then died on the one bad file. The existing PyYAML frontmatter check missed it: `yaml.safe_load` silently keeps the *last* duplicate value rather than erroring. | The parse check used `yaml.safe_load`, which tolerates duplicate keys. The fix loads the frontmatter through a `SafeLoader` subclass whose `construct_mapping` raises `ConstructorError` on a repeated key, matching rulesync's strictness, and surfaces it as the existing `PARSE_ERROR` issue. | `plugin-compliance-check.sh` `check_skill_frontmatter()` (DupKeyLoader) + `scripts/tests/test-plugin-compliance-frontmatter-dupkey.sh` (dup-key fixture → flagged, clean fixture → not flagged); wired into `.pre-commit-config.yaml` | this PR |
| A concurrent agent fleet sharing one checkout flipped the repo to `core.bare = true` and injected a junk `[user]` identity mid-session. Root cause: 21 test/hook shell scripts ran real `git -C "$VAR" init` / `git init --bare "$VAR"` against an `$(mktemp -d)` sandbox dir with no empty-value guard; when the dir resolved empty, `git -C ""` acted on the CWD (the shared main checkout). The leak is invisible — the test passes, the corruption surfaces later as "fatal: this operation must be run in a work tree". | The scripts had no `[ -n "$VAR" ]` / `\|\| exit` guard between the `mktemp -d` and the git op. Guarded all 54 sites across 21 scripts (Python tests exempt — `mkdtemp`/`tmp_path` raise, never return empty) and added a file-level linter so any future repo-targeting-git script must guard every `mktemp -d`. | `scripts/check-git-sandbox-guards.sh --strict` (+ `scripts/tests/test-check-git-sandbox-guards.sh`, 7 cases); wired into `.pre-commit-config.yaml` | issue #1692 |
| When `actionlint` was wired as a blocking gate (#1776), its embedded run-block shellcheck reported 58 findings (47 SC2086 unquoted expansions, 10 SC2129 individual redirects to `$GITHUB_OUTPUT`, 1 SC2034 dead var) across 8 workflows; to ship green, embedded shellcheck was disabled via `-shellcheck=` in both `plugin-pr-checks.yml` and the pre-commit `actionlint` hook — so run-block shell linting was silently unenforced. | The findings were left unfixed and the gate ran with embedded shellcheck off. | Cleaned all 58 (quoted shell expansions, grouped `>> "$GITHUB_OUTPUT"` writes with `{ …; } >>`, removed the dead `SUMMARY=` var) and removed `-shellcheck=` from both surfaces. The gate is its own regression check: `actionlint -ignore 'property "result" is not defined'` now runs embedded shellcheck and exits 0; any new run-block SC finding fails it. | issue #1775 |
| `scripts/check-driver-freshness.sh` once reported two dependencies whose `modified:` date was EQUAL to the driver's `reviewed:` date as "newer", failing a pre-commit run; it could not be reproduced afterward. The `-gt` comparison at the dependency check is correct (equal is NOT stale), so this was never a logic bug — it was undiagnosable because the FAIL output printed re-derived human dates only, never the raw integers actually compared. | No epoch diagnostics: the stale-dep FAIL message showed `modified:`/`reviewed:` human dates but not the `dep_epoch`/`driver_epoch` integers, so a parse/timezone/whitespace glitch that made two equal dates compare unequal would leave no trace. The fix records the compared epochs in each `stale_deps` entry (e.g. `[epoch 1769904000]`) without touching the `-gt` comparison or exit codes. | `scripts/tests/test-check-driver-freshness.sh` asserts equal dep date == driver reviewed → exit 0 + prints OK (the equal-date invariant), and newer dep date → exit 1 with both compared epoch integers present in the FAIL output (proves the diagnostics); wired into `.pre-commit-config.yaml` | this PR |
| `SendMessage`-resume of a **completed** worktree-isolated agent (`Agent` with `isolation: "worktree"`) does not re-enter its worktree — the resumed run executes in the orchestrator's **main checkout**, silently losing worktree isolation. Resuming several parallel file-mutating agents this way runs them concurrently in the main checkout and tangles branch state. `parallel-agent-dispatch` (and `agent-teams`) documented re-dispatch only for WIP-salvage / rate-limit contexts and never warned about this resume hazard. | The dispatch skills had no caveat that `SendMessage`-resume drops worktree isolation. The fix documents it in `parallel-agent-dispatch` (subsection "Resuming agents: SendMessage loses worktree isolation"), cross-references it from `agent-teams`, and directs re-dispatch of a fresh `isolation: "worktree"` agent for parallel file-mutating continuations (reserving resume for read-only / single-checkout work). | `scripts/check-agent-failure-contract.sh` asserts `parallel-agent-dispatch/SKILL.md` contains BOTH the literal `loses worktree isolation` AND `#1546`. Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml` (same entry as #1422). | issue #1546 |
| `Workflow({resumeFromRunId})` re-executes an already-**succeeded** `isolation: "worktree"` agent instead of returning its cached result — the **opposite** failure to #1546 (which loses isolation): here the whole agent re-runs, re-firing its outward side effects and opening a **duplicate PR** (#1858 dup of #1857). Non-worktree `agent()` stages cache correctly on the same resume, so resuming a workflow to retry a few rate-limited worktree agents silently re-runs the successful ones too. The documented resume contract ("completed `agent()` calls return cached results") holds for ordinary agents but not worktree-isolation agents. | The dispatch skill had no caveat that `resumeFromRunId` re-runs succeeded worktree agents. The fix documents it in `parallel-agent-dispatch` (subsection "Resuming a workflow: `resumeFromRunId` re-runs succeeded worktree agents") and `agent-development.md` (Dynamic Workflows resume caveat), directs fresh **sequential** re-dispatch of only the failed agents (checking `gh pr list` first so no duplicate PR is opened), and cross-references the existing `agent-coworker-detection.md` note. | `scripts/check-agent-failure-contract.sh` asserts `parallel-agent-dispatch/SKILL.md` contains BOTH `re-runs succeeded worktree agents` AND `#1868` (guards G/H in `scripts/tests/test-check-agent-failure-contract.sh`). Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml` (same entry as #1422). | issue #1868 |
| `git-pr-feedback`'s Step 3 decision table was framed around suggestions being mostly correct (`fix is correct → accept`; `fix needs adjustment → variant`) and never foregrounded **verifying the reviewer's claim against source / live state before accepting** — which matters most for automated reviewers (Gemini Code Assist, Copilot), whose suggestions are frequently confidently-wrong. In one session 3 of ~6 Gemini findings were wrong and only verification (driven by a user-global rule, not the skill) caught them; each was correctly refuted with evidence. | The skill had no "verify before accepting" step and no `refuted` action in the decision table, so the discipline lived outside the skill. The fix adds verification prose (independently verify bot/automated-reviewer claims against the actual source/live system/docs before acting) and a `Refuted` decision row (reply with the refutation + evidence, do not change code). The semantic invariant is that the verification prose and the refuted action survive bulk edits. | `plugin-compliance-check.sh` `check_skill_body()` — for `git-pr-feedback`, asserts the SKILL.md body retains both `automated reviewer` (verification prose) and `Refuted` (the decision-table action). Dropping either would silently restore "apply bot suggestions on trust". | issue #1545 |
| 11 GitHub workflows invoked Claude on `haiku` (5) or `sonnet`/default (6) with no `--effort` set anywhere, paying opus-equivalent quality risk without the cost lever. The agent migration (PR #1691) standardised subagents on `model: opus` but workflows — top-level invocations — were never covered, and nothing stopped them drifting. Opus-low beats sonnet-high on quality+tokens; haiku supports no effort at all; opus defaults to `--effort high`, so effort must be explicit to save. | No lint asserted the workflow model/effort standard, and three audit-workflow prompts still taught the superseded `haiku/sonnet/opus` skill-model palette. Migrated all 10 in-scope workflows to `--model opus --effort {low\|medium}`, fixed the stale prompt palettes, and added the guard. | `scripts/check-workflow-model.sh` (+ `scripts/tests/test-check-workflow-model.sh`) requires `--model opus` + an explicit valid `--effort` on every invoking workflow; classifies reusable-only (external model) and no-invocation workflows out. Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml`. See `.claude/rules/workflow-model-effort.md`. | this PR |
| `blueprint-init` Step 5 wrote the document-management rule (and Step 8 the initial development/testing rules) to a **hardcoded** `.claude/rules/`, ignoring the `structure.generated_rules_path` the user chooses in Step 4a. In a repo whose `.claude/rules/` is rulesync-managed (generated from `.rulesync/rules/`) or hand-authored, blueprint's writes collided with that pipeline. Separately, Step 3 marked "migrate documents (Recommended)" **unconditionally**, the wrong default for a heavily cross-referenced `docs/` set (~40 refs from README/scripts/CI all break on migration). | Step 5 / Step 8 / the `.claude/` tree diagram hardcoded `.claude/rules/`; Step 3 never measured reference density. The fix resolves `RULES_DIR=$(jq -r '.structure.generated_rules_path // ".claude/rules/"' …)` at all three write spots, and gates the Step 3 "(Recommended)" marker on whether any candidate doc is **referenced outside** `docs/` (dropping it and surfacing the count when so). | `plugin-compliance-check.sh` `check_skill_body()` — extends the `generated_rules_path` assertion to include `blueprint-init` (#1675), and adds a `blueprint-init`-scoped block asserting the body retains `referenced outside` (Step 3 cross-reference gating, #1674) | issue #1675, issue #1674 |
| A concurrent agent fleet sharing one checkout flipped the shared repo to `core.bare=true` (and leaked a junk `GIT_DIR`), so every linked worktree's `git status`/`commit` failed with "fatal: this operation must be run in a work tree" and two uncommitted untracked scripts were wiped. The prevention side (`check-git-sandbox-guards.sh`, issue #1692) already landed; the **detection + recovery** complement was missing, so a session hit by the flip misread the cascade of git failures as its own fault with no recovery guidance. | `detect-coworkers.sh` had no signal for a bare flip / leaked `GIT_DIR`. Added a sixth signal (`git rev-parse --is-bare-repository` + `GIT_DIR`/`GIT_WORK_TREE` env checks) raising the `bare_flip_suspected` verdict, plus recovery (`core.bare false` / unset leaked env / reflog+worktree-branch salvage) and commit-early guidance in `git-coworker-check/SKILL.md` and `agent-coworker-detection.md`. | `git-plugin/skills/git-coworker-check/scripts/tests/test_bare_flip.sh` asserts `detect-coworkers.sh` raises `VERDICT=bare_flip_suspected` (and `BARE_FLIP_DETECTED=true` / `BARE_FLIP_COUNT=1`) against a `core.bare=true` sandbox and stays `clear` on a normal checkout; every `mktemp -d` is guarded per `check-git-sandbox-guards.sh` | issue #1692 |
| `configure-web-session` had no drift signal for already-onboarded repos after the canonical spec changed. PR #1668 refreshed the spec (Renovate-managed pins, `scripts/path-bootstrap.sh` wired first, allowlist-safe download URLs); a portfolio audit afterward found 7/7 onboarded repos silently out of spec — `install_pkgs.sh` "exists" so each reads as compliant, and the drift was invisible until a manual cross-repo audit (one repo even used runtime `api.github.com/.../releases/latest` lookups that break under the web "Limited" allowlist). | The skill only checked install-script *completeness*, never drift against the *current* spec, and had no portfolio re-audit path. The fix adds Step 2b (PRESENT/DRIFT/ABSENT against the current spec for an already-onboarded repo, re-applying drifted items) and Step 7 (a portfolio sweep over repos with `scripts/install_pkgs.sh` that runs `--check-only` and reports deltas as a positive signal). | `plugin-compliance-check.sh` `check_skill_body()` — for `configure-web-session`, asserts the SKILL.md body retains the case-insensitive tokens `spec drift` AND `re-audit`. Dropping either erases the drift/re-audit step. | issue #1670 |
| Three parallel "add a regression row" PRs (#1742/#1743/#1746) each appended a row to **this** table at the same anchor, so all three showed GitHub `CONFLICTING` despite green CI and needed a hand rebase + union resolution. The conflict mode is purely *additive* — both sides only insert lines — which is mechanically union-mergeable with no LLM, yet `auto-resolve-conflicts.yml` sent every conflict (including these) to an Opus run. | `.gitattributes` had no `merge=union` driver for append-only files, and the conflict workflow had no deterministic pre-pass — so trivially-additive conflicts blocked the merge button and burned an LLM run. | `.gitattributes` marks `.claude/rules/regression-testing.md merge=union` (git auto-resolves at merge time); `scripts/resolve-additive-conflicts.py` is a deterministic pre-pass in `auto-resolve-conflicts.yml` that union-merges additive conflicts (both sides insert-only) and routes only genuine semantic conflicts to Claude. `scripts/tests/test-resolve-additive-conflicts.sh` asserts additive→auto-resolved-no-LLM, semantic→left-for-LLM (markers preserved), mixed→partial; wired into `.pre-commit-config.yaml`. | this PR |
| A worktree-isolated agent met a corrupted (`core.bare=true`) worktree and "worked around" it by exporting `GIT_DIR`/`GIT_WORK_TREE`; because `GIT_DIR` overrides `git -C`, every later git op — and a regression test that shelled `git init`/`git config` — wrote to the **shared** common config, flipping the whole checkout to bare and breaking ~35 sibling worktrees mid-run. DISTINCT from the empty-`mktemp -d` vector (#1692/#1719): the sandbox path was correct; the exported env was the hijack. | `parallel-agent-dispatch` documented only the empty-mktemp and cwd-reset (#1480) worktree-git leaks, not the exported-`GIT_DIR` vector, and nothing asserted the agent-behavior rule (a bare worktree is corruption → STOP; never export git env to paper over it; neutralize inherited env with `env -u GIT_DIR …` in sandboxes). | `scripts/check-agent-failure-contract.sh` asserts `parallel-agent-dispatch/SKILL.md` carries `#1692` + the `env -u GIT_DIR` idiom and `REFERENCE.md` carries the "Worktree GIT_DIR-export leak" section; SKILL.md Pillar 1 gains the hazard paragraph + REFERENCE.md section. | issue #1692 |
| `scripts/check-context-command-execution.sh` (the `lint` job's "Execute context commands in a bare sandbox" step) intermittently FAILED PRs on files the PR never touched. Two causes: (1) a broken-pipe/pipefail race — the per-command classification used `printf '%s' "$err" \| grep -Eqi "$RE"`; with `set -o pipefail`, a `grep -q` that matched and closed the pipe early while printf was still writing a large stderr string made printf take SIGPIPE (141), so pipefail reported the pipeline non-zero and the `if` flipped, misclassifying an UNPARSEABLE/ENV_MISSING as FAIL nondeterministically; (2) false-positive extraction of `!`backtick`` strings that are not real Context commands — `gh-cli-agentic:299` (a `!`gh run view $ID …`` line inside a ```markdown documentation fence) and `binary-analysis:204` (a markdown table row where `` `Rar!` `` abuts the next cell's opening backtick, matching the `!`` pattern and executing `52 61 72 21 …\|…` → `syntax error near unexpected token '\|'`). | The three classification/deny greps were piped; and the extraction loop matched `!`` on every line with no fenced-code-block or table-row awareness. The fix replaces all three `printf \| grep -Eq` with here-strings (`grep -Eq "$RE" <<<"$x"` — no pipe, no pipefail/SIGPIPE race), and gates the extraction loop on a fenced-code-block toggle (``` / ~~~) plus a markdown-table-row skip (line starts with `\|`) — a real Context command is a `- <label>: !`<cmd>`` list item, never inside a fence and never a table row, so neither guard can drop a legitimate command. | `scripts/tests/test-check-context-command-execution.sh` F asserts a fixture whose only `!`backtick`` occurrences are inside a fence and a table row extracts NOTHING (`PASS=0`/`FAIL=0`/`UNPARSEABLE=0`/`ENV_MISSING=0` — a buggy harness would run the fenced gh command as ENV_MISSING and the table row as UNPARSEABLE); G asserts no `write error: Broken pipe` on harness stderr | issue #1744 |
| `scripts/check-driver-freshness.sh` intermittently failed pre-commit with "dependency newer than driver's reviewed: date" when a dependency's `modified:` date EQUALLED the driver's `reviewed:` date — the non-reproducible #1704 symptom whose epoch diagnostics finally revealed a **1-second** gap between the two compared epochs for identical date strings. ROOT CAUSE: BSD `date -j -f "%Y-%m-%d" "<date>" +%s` fills the unspecified time-of-day fields with the *current* wall-clock time, so `driver_epoch` (parsed once up front) and each `dep_epoch` (parsed later in the loop) differ by 1s whenever the two `date` calls straddle a second boundary, making an equal date compare `-gt` (newer). The earlier #1704 work added the epoch diagnostics but never fixed the parse. | `date_to_epoch()` parsed a bare `%Y-%m-%d`, which is time-of-day dependent on BSD. The fix pins parsing to `00:00:00` (`date -j -f "%Y-%m-%d %H:%M:%S" "<date> 00:00:00"` on BSD; `date -d "<date> 00:00:00"` on GNU) so the epoch is a pure function of the date. | `scripts/tests/test-check-driver-freshness.sh` TEST C parses the same date twice across a forced `sleep 1` second boundary and asserts the epochs are identical (the determinism invariant); the `date_to_epoch_portable` test helper is pinned to the same midnight form so TEST B's `epoch <N>` assertions fail if the script regresses to current-time parsing. | issue #1704 |
| `scripts/lint-shell-scripts.sh` enforced `.claude/rules/shell-scripting.md` (`#!/usr/bin/env bash` shebang, `set` flags, standard `block()`, `TOOL_NAME` not `TOOL`) but was wired into **nothing** — not pre-commit, not CI, not the justfile — even though `shell-scripting.md` cited it as the canonical linter. A script with `#!/bin/bash`, a hook missing `set -euo pipefail`, or a `block_with_reminder()` could land unchecked (the shellcheck-py hook runs but does not enforce these conventions). | The comprehensive linter existed but nothing invoked it; the convention was documented-but-unenforced. | `scripts/lint-shell-scripts.sh` wired into `.pre-commit-config.yaml` (`files: '\.sh$'`, `pass_filenames: false`) and `plugin-pr-checks.yml` (self-test + run); gained an optional `ROOT_DIR` arg (for fixtures) and a `dist/` + `.claude/worktrees/` prune (the #1492/#1548 lesson). Regression test `scripts/tests/test-lint-shell-scripts.sh` pins the severity split (hook-missing-`set` → ERROR, non-hook → WARN) and the always-ERROR checks (bad shebang, non-standard `block()`, `TOOL` var). `just lint-shell` added for local↔CI parity. | this PR |
| No `actionlint`/`zizmor` ever ran on `.github/workflows/*.yml` — only `check-workflow-model.sh` (Claude model/effort). YAML schema, expression, and **script-injection** validation was absent; on first run actionlint found two `${{ github.head_ref }}` values interpolated into inline `run:` scripts (`skill-splitter.yml`, `validate-plugin-configs.yml`) — the class `.claude/rules/github-actions-security.md` mandates fixing — plus four `steps.<id>.outputs.result` references to a claude-code-action step that exposes no `result` output (the audit-issue creation step's `if` was always false → audits never filed). | No workflow YAML linter was wired; the convention relied on review. | `rhysd/actionlint` pinned `v1.7.12` in `.pre-commit-config.yaml` + a `plugin-pr-checks.yml` step (`go install` then run). The two injection findings fixed via env-var indirection. Embedded run-block shellcheck disabled (`-shellcheck=`; 58 pre-existing SC2086/SC2129 info/style findings) and the `outputs.result` latent-bug findings ignored (`-ignore 'property "result" is not defined'`), both as documented, tracked follow-ups — so the gate is green now and blocks NEW issues. | this PR |
| `hooks-plugin/hooks/test-bash-antipatterns.sh` builds throwaway sandbox repos with `git -C "$(mktemp -d)" init` / `git config`. When run under an **injected absolute `GIT_DIR` / `GIT_WORK_TREE`** — exported by an agent papering over a `core.bare=true` worktree, or inherited from the pre-commit hook's environment — those vars OVERRIDE `git -C "$sandbox"`, so the ops target the **real shared `.git`** instead of the throwaway dir, writing `core.bare=true` and a junk `[user]` / `[commit]` identity into the shared config. DISTINCT vector from the empty-`mktemp -d` case (#1692, which `check-git-sandbox-guards.sh` guards): here the sandbox path is correct but the inherited env hijacks it. Observed 2026-06-21 flipping the shared checkout to bare mid-run, breaking ~35 sibling worktrees; PR #1736 had to commit with `--no-verify` to avoid re-triggering the leak through the pre-commit hook. The pre-commit hook's *own* environment is itself an instance: it exports `GIT_DIR` + an absolute `GIT_INDEX_FILE`, so a partial `unset GIT_DIR GIT_WORK_TREE` still leaves the sandbox `git commit` pointed at the real index ("invalid object" / "index file smaller than expected"). | The suite never neutralized inherited git context. The fix unsets the FULL family — `unset GIT_DIR GIT_WORK_TREE GIT_INDEX_FILE GIT_OBJECT_DIRECTORY GIT_COMMON_DIR GIT_NAMESPACE GIT_PREFIX` — at the top of `test-bash-antipatterns.sh`, which protects every git op in the suite AND the hook subprocesses it spawns (they inherit the cleaned env), for current and future ops alike. | `hooks-plugin/hooks/test-bash-antipatterns-git-env-isolation.sh` stands up a throwaway "real" repo, snapshots its `.git/config`, runs the suite with `GIT_DIR`/`GIT_WORK_TREE` exported at it, and asserts the config is byte-identical afterward (plus `core.bare` not flipped, seeded `user.email` intact). Wired into `.pre-commit-config.yaml`. | issue #1745 |
| `agent-patterns-plugin:agent-teams` (SKILL.md + REFERENCE.md) was built entirely around the `TeamCreate`/`TeamDelete` tools, **removed in Claude Code 2.1.178** — so the auto-discovered skill instructed Claude to call tools that no longer exist (and taught the `--enable-teams` flag + per-team `~/.claude/teams/<name>/config.json` model, both superseded by the implicit team gated on `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` with `team_name` accepted-but-ignored). The changelog review (#1733) flagged the rule-doc edit but the skill code itself stayed broken. | The skill modelled the pre-2.1.178 explicit-team lifecycle. Reworked SKILL.md + REFERENCE.md (and the README + `parallel-agent-dispatch`/`custom-agent-definitions`/`workflow-wave-dispatch` cross-refs) to the implicit-team model: spawn via `Agent` with a `name`, coordinate via `SendMessage`/`Task*`, shut down via `shutdown_request` (no create/delete). | `plugin-compliance-check.sh` `check_skill_body()` — for `agent-patterns-plugin/agent-teams`, asserts the SKILL.md body retains `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS` AND `implicit team` (the markers of the post-2.1.178 model); the literal `TeamCreate`/`TeamDelete` legitimately survive only in the BREAKING note + Common Mistakes row, so the guard asserts the new model's *presence* rather than the words' absence. | issue #1733 |
| The `Claude: Changelog review` workflow's deprecation→plugin-code bridge (`analyze-changelog.sh`, #1638) missed the 2.1.178 BREAKING removal of the `TeamCreate`/`TeamDelete` tools, so the `agent-patterns-plugin:agent-teams` skill kept instructing Claude to call tools that no longer exist. The bridge extracted tokens only by deprecation *grammar* (`Removed \`X\``, or `` `X` is/are/now/been removed``); the changelog's verb-after `/`-separated pair shape ("BREAKING: `TeamCreate`/`TeamDelete` tools removed") matched neither, so the removed identifiers were never grepped against plugin code and surfaced only as a rule-doc follow-up. | The token extractor had no shape for a verb-after BREAKING removal naming a `/`-separated tool pair. Broadening the general deprecation forms would reintroduce the documented `SendMessage`/`enabledPlugins`/`mcpServers` false positives, so the fix adds a **BREAKING-gated** awk extractor: on a line carrying both `BREAKING` and a strong removal verb (`removed\|deleted\|dropped\|unshipped\|gone`), every backticked token *before* the verb is surfaced (replacements named after "in favor of" stay excluded). | `project-plugin/skills/changelog-review/scripts/tests/test-analyze-changelog.sh` "#1733" block asserts a `BREAKING: \`TeamCreate\`/\`TeamDelete\` tools removed` line raises `ACTIONABLE_DEPRECATION=1`, surfaces BOTH tokens and the referencing skill file, and that a replacement named after the verb (`in favor of \`SendMessage\``) is NOT surfaced; the existing #1638 negative assertions (enabledPlugins/SendMessage/mcpServers non-actionable) stay green. | issue #1733 |
| New skill `configure-gitignore` appends a managed Claude Code runtime-state block to a repo's `.gitignore` (`.claude/worktrees/`, `/worktrees/`, `.claude/scheduled_tasks.lock`, `.claude/settings.local.json`) and is wired into `configure-repo` so every onboarded repo ignores session-generated files. The load-bearing entry is `.claude/worktrees/` (an agent worktree is a full repo clone; committing one is a large accidental diff). A future bulk edit "tightening" the skill could silently drop that entry or the managed-block delimiter, reverting the cross-repo guarantee. | The skill is new; no guard asserted its managed-block contents survived edits. | `plugin-compliance-check.sh` `check_skill_body()` — for `configure-gitignore` (scoped on directory name), asserts the SKILL.md body retains both `.claude/worktrees/` and the `managed by /configure:gitignore` block marker. | this PR |
| `evaluate-improve`'s `--best-of N` ranked candidate skill edits by **golden-set pass rate only**, so a candidate that lifted the aggregate while leaving the *motivating* failures broken could win and be applied — there was no gate confirming the edit fixed the specific cases that triggered it. PR #1593 had already added the RHO reproduce-and-verify + best-of-N machinery (the W25 research-radar #1662 candidates TRACE/AEGIS re-suggest), but the AEGIS source-cases delta-verify framing — re-run the *source-failure set*, not the whole golden set, and confirm the failure count shrinks before applying — was the one residual gap. | The skill captured no source-failure set and gated apply on nothing. The fix captures the failing-with-skill case IDs in Step 1, adds a **Delta-verify gate** that re-runs only those cases against the drafted candidate before any write and requires `delta > 0` (with a `--force-apply` override; no-op on an empty set), and re-ranks `--best-of` by source-failure delta first, golden-set pass rate second. | `plugin-compliance-check.sh` `check_skill_body()` — for `evaluate-improve` (scoped on `evaluate-plugin`), asserts the SKILL.md body retains `Delta-verify gate`, `source-failure set`, and `shrink`. Dropping any would silently revert ranking to pass-rate-only. | issue #1662 |
| The `PR: Auto-resolve conflicts` workflow (`auto-resolve-conflicts.yml`) ran on every push to main yet never resolved PR #1786. Its `find-conflicts` job read `gh pr list --json mergeable` ~3s after the triggering push, but GitHub recomputes a PR's mergeability ASYNCHRONOUSLY after its base moves, so a PR made conflicting by *that* push still read `UNKNOWN` (not `CONFLICTING`). The `select(.mergeable == "CONFLICTING")` filter returned an empty matrix, the `resolve` job was SKIPPED, and the run reported green while doing nothing — the conflict (a same-line `actionlint`-flag collision with #1785) sat unresolved, with no `pull_request`/`schedule` re-check to catch it. | The job read mergeability ONCE instead of waiting for GitHub to settle it. The fix polls the open-PR list until no PR is left `UNKNOWN`, bounded by a wall-clock deadline (loop-integrity), then filters for `CONFLICTING`; `inputs.pr_number` routed through env (script-injection baseline). | `scripts/check-auto-resolve-mergeability-poll.sh` asserts `auto-resolve-conflicts.yml` inspects `mergeable == UNKNOWN`, `sleep`s between poll rounds, and bounds the poll with a `DEADLINE`/`date +%s` — a revert to a single read trips it. Regression test `scripts/tests/test-check-auto-resolve-mergeability-poll.sh`; wired into `.pre-commit-config.yaml` + `plugin-pr-checks.yml` (self-test + run). | this PR |
| `taskwarrior-plugin`'s linked-task drift (a pending task whose GitHub issue/PR has closed/merged) was only retired when someone *remembered* to run `/taskwarrior:task-reconcile` — forge state is not a local event, so nothing surfaced it autonomously. The reconcile *logic* was already a deterministic script (`reconcile.sh`); the gap was purely **triggering**. | The SessionStart `taskwarrior-drift-probe.sh` only checked for missing UDAs. Extended it to also run `reconcile.sh` in dry-run and emit a `stale_linked_tasks` drift finding, debounced behind a per-project TTL cache (default 4h) so the `gh` poll runs at most once per interval; read-only, never warns on `GH_AVAILABLE=false`. Opt out with `CLAUDE_TASKWARRIOR_DRIFT_NO_RECONCILE=1`. | `taskwarrior-plugin/hooks/test-taskwarrior-drift-probe.sh` (auto-discovered by `run-skill-script-tests.sh` via `*/hooks/test-*.sh`) asserts: stale>0 → finding emitted + reconcile ran; stale=0 → no finding; fresh cache → reconcile NOT re-invoked (cached count used); stale cache → reconcile runs + cache rewritten; opt-out env → no finding but UDA check still runs; `GH_AVAILABLE=false` → no finding; UDA + stale findings coexist | this PR |
| The `scheduled-audits` (`agentic-audit`) and `workflow-model-audit` workflows guarded their "Create audit issue" step on `steps.audit.outputs.result` — but `anthropics/claude-code-action@v1` exposes no `result` output (only `branch_name`, `execution_file`, `github_token`, `session_id`, `structured_output`), so the `if` was always false and these monthly audits silently never filed an issue. actionlint flagged it (`property "result" is not defined`) but the finding was suppressed with `-ignore` in two gate surfaces pending the fix. | Replicated the proven `research-radar.yml` pattern: pass `github_token`, grant `Bash(gh issue create *)`, and instruct Claude to run `gh issue create` itself in the prompt; deleted the downstream `outputs.result` step. With no action step reading `result`, the `-ignore 'property "result" is not defined'` suppression was removed from both `plugin-pr-checks.yml` and `.pre-commit-config.yaml`. | `actionlint` (the workflow gate in `plugin-pr-checks.yml` + the `.pre-commit-config.yaml` hook) now runs without the `-ignore` suppression — a re-introduced `outputs.result` read on a `claude-code-action` step makes actionlint exit non-zero and the gate fails. | issue #1774 |
| `code-quality-preflight-cue.sh` fired on the **first** Edit of a planned multi-edit sequence and told the agent to lint "before continuing", so the demanded pre-flight ran against a deliberately half-applied refactor. Real case: edit 1 of 4 introduced `const inline = variant === "inline"`; the lint reported `inline` unused and suggested renaming to `_inline` — actively wrong, since three edits later the symbol is used four times. Unlike this hook's four prior over-trigger reports (#1730, #1766, #1825, #1905) this is **not** a file-type false positive: `src/index.ts` is a legitimate lint target. The mismatch is *when*, not *what*. Worse, the once-per-session dedup meant the mis-timed cue consumed the session's only shot, so genuinely-worth-linting later edits could never cue at all. | A PostToolUse hook cannot see the future, so per-edit detection is inherently early for edit N of M. Fix has two composing halves: (a) reword the cue to "once this edit sequence is complete" so a mid-sequence agent is never pushed to lint an incomplete tree; (b) a per-file sequence debounce (recency marker under `<cache>/.edits/<session_id>/`, TTL `CODE_QUALITY_PREFLIGHT_CUE_DEBOUNCE_TTL`, default 120s) that stays silent while edits to the same file are in flight **without** consuming the session budget, so the one cue lands on a settled edit. Recency is armed by every non-excluded Edit/Write, structural or not. Detection signals and all file-type exclusions were left untouched — true-positive detection is unchanged. | `code-quality-plugin/hooks/test-code-quality-preflight-cue.sh` block (n): n1 asserts a mid-sequence structural edit is silent **and** that a later settled edit in the same session still fires (two independent failures pre-fix, so a partial fix cannot pass); n2 pins the debounce to the file, not a blanket second-call mute; n3 closes the no-`session_id` repeat hole; n4 proves re-arm after TTL expiry; n5 pins the `TTL=0` escape hatch; n6 asserts the emitted `.reason` defers to sequence completion and never says "before continuing". Verified 43/0 post-fix vs 38/5 against `git show HEAD` of the hook. | issue #2272 |
| `configure-release-please` (SKILL.md + `release-please-standards`) presented the legacy `MY_RELEASE_PLEASE_TOKEN` PAT as the single canonical release token, with no mention of the **GitHub App-token pattern** (`actions/create-github-app-token` → `RELEASE_PLEASE_APP_ID` var / `RELEASE_PLEASE_PRIVATE_KEY` secret) that is the laurigates org standard. gitops provisions the App credentials on `release_please = true` repos, so following the skill verbatim produced a PAT workflow that diverged from every other repo and never consumed those credentials; the compliance check would also flag a correct App-token workflow as needing the PAT. | The skill documented only the PAT form. The fix documents the App-token pattern as the **preferred** option in both skills (workflow templates, token-config table, installation, troubleshooting) and instructs the compliance check to treat an existing App-token workflow as compliant rather than flag it. | `plugin-compliance-check.sh` `check_skill_body()` — for `configure-release-please`, asserts the SKILL.md body retains both `create-github-app-token` and `RELEASE_PLEASE_APP_ID`. Dropping either would silently revert to PAT-only guidance. | issue #1789 |

| The `task-add` skill's #1417 "capture the stable UUID after add" fix shipped `task +LATEST _get uuid`, but `_get` is a DOM accessor that takes an `<id>.<attribute>` reference — given a tag filter it returns **empty** (exit 0). So the skill captured no UUID and silently reverted to the very numeric-ID-drift bug #1417 was meant to fix (a later `task <id> annotate` lands on a renumbered task). The compliance guard pinned the literal string but never ran it — a syntactic-only gate that cemented the broken command. Verified on taskwarrior 3.4.2: `task +LATEST _get uuid` → empty; `task +LATEST uuids` → the UUID. | The skill recommended a non-working `_get` filter form. Fix: SKILL.md uses `task +LATEST uuids` (with `task +LATEST export \| jq -r '.[0].uuid'` as the alt); the broken form survives only in a `>` gotcha callout. The compliance guard now pins the working form AND errors if the broken form reappears outside a blockquote. | `taskwarrior-plugin/skills/task-add/scripts/tests/test-uuid-capture.sh` (auto-discovered via `*/skills/*/scripts/tests/test-*.sh`) **executes** the commands in a scratch store: asserts `_get uuid` returns empty (documents the failure), `+LATEST uuids` returns a valid UUID, and the SKILL.md uses the working form outside callouts — closing the syntactic-vs-semantic gap. `plugin-compliance-check.sh` `check_skill_body()` pins `task +LATEST uuids` and bans the broken form outside blockquotes. | issue #1417 |
| The broken `task +LATEST _get uuid` form (a `_get` DOM accessor given a tag filter → returns empty; fixed for `task-add` in #1819) had **also** propagated into `session-plugin`: `session-end/SKILL.md` (prose + Agentic-Optimizations table), the `session-end-nudge.sh` cue text, and — the syntactic-pin trap again — `test-session-end-nudge.sh` *asserted* the broken cue. The #1819 compliance guard banned the form only for `task-add`, so the propagation was invisible. | Scoping the negative ban to one skill let a known-broken command spread. Fix: replaced all five sites with `task +LATEST uuids`, and **generalized** the compliance ban from `task-add`-only to **every** skill file (a non-working command is wrong everywhere). | `plugin-compliance-check.sh` `check_skill_body()` — the `task +LATEST _get uuid` ban now runs for ALL skills (outside blockquotes), not just `task-add`. The `session-end-nudge.sh` cue is covered by `test-session-end-nudge.sh` asserting the working `task +LATEST uuids` form. | issue #1417 |
| The `on-modify-taskwarrior-plugin` native hook only warned on hyphenated tags — it did not enforce the claim/identity invariant that `/git:coworker-check`'s fourth detection signal depends on. A bare `task start <id>` (bypassing `/taskwarrior:task-claim`) set `+ACTIVE` with **no** `agent`/`host`/`branch`/`worktree` UDAs, a silent hole in coworker detection; and a crashed agent's `+ACTIVE` claim lingered forever because `task-status` *detected* stale claims but nothing *acted* on them. Native hooks fire on every local `task` command regardless of source, so on-modify is the one place to repair both for skill-bypassing usage. | The hook had no claim-invariant stamp and no stale-claim expiry. Extended `on-modify-taskwarrior-plugin` to (1) stamp identity from the env (`agent`/`host`/`branch`/`worktree`; pid left to task-claim) when a task goes `+ACTIVE` (`start` newly set) without an `agent`, (2) warn on a claim takeover (`agent` change, never reject), and (3) drain identity + drop `+ACTIVE` (delete `start`) when a touched claim's `start` exceeds the TTL (`CLAUDE_TASKWARRIOR_CLAIM_TTL_HOURS`, default 4; opt out with `CLAUDE_TASKWARRIOR_NO_CLAIM_EXPIRY=1`). All paths fail open. | `taskwarrior-plugin/skills/install-native-hooks/scripts/tests/test-on-modify-hook.sh` (auto-discovered via `*/skills/*/scripts/tests/test-*.sh`) pins: identity stamped on newly-`+ACTIVE`-without-agent; takeover warns without draining; stale claim drops `start` + drains identity; fresh claim is not expired; malformed JSON / missing `jq` fail open; hyphenated-tag warning preserved; opt-out env suppresses expiry. | issue #1808 |

| New skill `configure-worktreeinclude` generates a `.worktreeinclude` whose gitignore-syntax patterns tell Claude Code which gitignored files to copy into new worktrees (the `--worktree` flag, subagent worktree isolation, parallel desktop sessions) — `.env`, local secrets, local config — built from the repo's *actual* gitignored files rather than a generic template, and wired into `configure-repo` (Step 3d). Two load-bearing invariants: (a) the include list is derived from the repo (the `git status --ignored` detection step), and (b) only gitignored files are ever copied so each pattern is verified against `git check-ignore` (a pattern over a tracked file is an inert no-op). A bulk edit "tightening" the skill could drop the repo-derived detection or the gitignored-only verification, reverting it to a copy-paste template. | The skill is new; no guard asserted its detection/verification steps survived edits. | `plugin-compliance-check.sh` `check_skill_body()` — for `configure-worktreeinclude` (scoped on directory name), asserts the SKILL.md body retains both `git status --ignored` and `git check-ignore`. | this PR |
| In a nested-repo / portfolio workspace, `isolation: "worktree"` worktrees the **outer** session repo, not the **nested** independent repo the agent was told to edit — so the nested repo is absent from the worktree, the Edit-tool isolation guard blocks the only remaining path (the shared checkout), and the agent must hand-roll its own worktree. The isolation guarantee silently didn't apply to the repo that mattered (issue #1838; harness worktree-resolution behavior, dispatch-side mitigation documented). | Documented the pitfall + remediation in `parallel-agent-dispatch` (Pillar 1 subsection + REFERENCE.md "Nested-repo worktree isolation"): detect session-repo ≠ target-repo, then isolate the nested repo explicitly (create its worktree off its own `origin/main`, or brief the agent to do so first). | `scripts/check-agent-failure-contract.sh` asserts `parallel-agent-dispatch/SKILL.md` carries `#1838` and `REFERENCE.md` carries the "Nested-repo worktree isolation" section. | issue #1838 |
| `feedback-plugin:feedback-session` documented only flag-style inputs; freeform feedback prose was handled only incidentally (args interpolated into the body), so its behavior was undefined and could regress. Users who know exactly what they want filed had to rely on transcript inference. | Documented freeform prose as a first-class input: non-flag, non-plugin-name text is captured as `$SEED_FINDINGS` explicit seed findings that flow through dedup / confirmation / issue-creation alongside the transcript scan (Parameters + Step 2a + Quick Reference). | `plugin-compliance-check.sh` `check_skill_body()` — for `feedback-session`, errors if the body drops the `SEED_FINDINGS` seed-findings contract token. | issue #1844 |
| `feedback-plugin:feedback-session` carried `disable-model-invocation: true`, so the `Skill` tool refused to invoke it — but `session-plugin:session-end` (Step 4) orchestrates it through the Skill tool. The two were in direct conflict: the orchestrated feedback pass failed (`cannot be used with Skill tool due to disable-model-invocation`) and had to be filed by hand, bypassing the skill's dedup / label / target-repo logic. The skill already gates issue creation behind an `AskUserQuestion` confirmation, so model invocation does not bypass user review. | Removed `disable-model-invocation: true` from the skill frontmatter so it is agent-invocable as the orchestrator documents. | `plugin-compliance-check.sh` `check_skill_body()` — for `feedback-session`, errors if the frontmatter sets `disable-model-invocation: true` (it must stay Skill-tool-invocable for session-end). | issue #1843 |
| The `on-add`/`on-modify` native hooks act on one task at a time, so two SessionStart-era costs lingered: the drift probe polled `gh` per-linked-task on a TTL (re-checking tasks whose linkage never moved), and a raw `task start`/`stop` that bypassed `/taskwarrior:task-claim`/`task-release` left the `/git:coworker-check` session marker out of sync (a raw start with no marker → invisible to coworker detection; a raw stop → stale marker lingers). `on-exit` is the only native hook with whole-changeset visibility — the one place to batch outward sync and reconcile the marker for skill-bypassing usage. | Added the opt-in `on-exit-taskwarrior-plugin` hook: (1) queues the UUID of every touched task carrying a `ghid`/`ghpr` linkage UDA to `<data>/claude-plugin-ghsync.queue`; `scripts/drain-ghsync-queue.sh` (run by `taskwarrior-drift-probe.sh` before its stale-check) resolves them in one batched `task export`, busts the drift TTL cache for the affected projects, and clears the queue (fails open on a stale/corrupt queue). (2) maintains `<git-dir>/.claude-session-<pid>` for a touched task carrying `pid`+`worktree` — writes it skip-if-exists when the task goes `+ACTIVE` (never clobbering a skill marker), removes it after a raw stop/done (guarded against a sibling active claim sharing the pid+worktree). All paths fail open; `CLAUDE_TASKWARRIOR_NO_GHSYNC_QUEUE`/`CLAUDE_TASKWARRIOR_NO_MARKER_UPKEEP` opt out. | `taskwarrior-plugin/skills/install-native-hooks/scripts/tests/test-on-exit-hook.sh` pins: linkage UDA → UUID queued (and no-linkage → not queued, whole-changeset, opt-out, missing-jq fail-open); marker written on raw `+ACTIVE`; skill marker not clobbered; stale marker removed on raw stop; removal guarded by a sibling active claim. `taskwarrior-plugin/scripts/tests/test-drain-ghsync-queue.sh` pins: dedup/garbage-filter, one batched `task export` → project cache bust, empty-project → `_all` bust, queue always cleared, no-task fail-open. `taskwarrior-plugin/hooks/test-taskwarrior-drift-probe.sh` pins the drain runs before the stale-check (busting a fresh cache forces a re-poll) and the opt-out suppresses it. | issue #1810 |
| Linked-task drift was surfaced at SessionStart (#1792) but never swept on a **cadence** — forge state changes whether or not you're in a session, and queues spanning repos you don't open daily silently accumulated stale trackers. The reconcile *logic* (`reconcile.sh`) already existed; the gap was a scheduled trigger plus a bounded auto-apply lever so an unattended sweep could close only the *unambiguous* verdicts. | Added `reconcile.sh --only-verdicts=<csv>` (restricts the apply set; non-listed stale verdicts — notably `pr-closed` — are reported `method=keep` but never closed; UNKNOWN is never stale so the flag only narrows the set; emits `ONLY_VERDICTS=` + a per-task `project=` token) and `taskwarrior-plugin/scripts/scheduled-reconcile.sh` — a cadence wrapper (macOS LaunchAgent): notify-only by default (`reconcile.sh --all` dry-run → `telegram-notify`/desktop notification on `STALE_COUNT>0`, mutating nothing), opt-in bounded auto-apply (`--apply` → `--only-verdicts=pr-merged,issue-closed`). Honors `GH_AVAILABLE=false` → emit nothing (never a false "0 drift"); exits 0 (parallel-safe); `CLAUDE_TASKWARRIOR_NO_SCHEDULED_RECONCILE=1` opts out. | `taskwarrior-plugin/scripts/tests/test-scheduled-reconcile.sh` (auto-discovered via `*-plugin/scripts/tests/test-*.sh`) pins: `--only-verdicts` closes only pr-merged/issue-closed and never pr-closed/UNKNOWN even with `--apply` (and `--apply` alone still closes every stale verdict, back-compat); the wrapper's notify-only default invokes reconcile WITHOUT `--apply`; `GH_AVAILABLE=false` and zero-stale both suppress the notification; the opt-out env makes it a no-op (reconcile never invoked); `--apply` delegates with the bounded `--only-verdicts=pr-merged,issue-closed`. | issue #1793 |
| `task-status`/`task-coordinate` *detected* `+ACTIVE` claims older than N hours but nothing *acted* on them, so a crashed/exited agent's claim lingered and polluted `/git:coworker-check` (its 4th signal reads `+ACTIVE` claims) and `task-coordinate`'s "in flight" view. Unlike linked-issue drift (a `gh` poll), a dead claim is **deterministically observable locally** — `kill -0 "$pid"` failing on the claim's own `host` is a fact, not a judgment — so it is a clean script/hook candidate, no LLM in the loop. | No deterministic releaser existed. Added `taskwarrior-plugin/scripts/release-stale-claims.sh`: snapshots `+ACTIVE` via `task +ACTIVE export \| jq` (exit-0 on empty), loops by **UUID** (not numeric id), gates on the `host` UDA == current hostname (cross-host claims are not ours to judge), then `kill -0 "$pid"`; a dead PID → `task <uuid> stop` (drains `+ACTIVE`) + clear `pid` UDA + annotate `auto-released: claiming PID gone`. Dry-run by default; `--apply` to mutate; all mutations pass `rc.confirmation=no` + `</dev/null`. Structured `KEY=VALUE`/`STATUS=`/`ISSUE_COUNT=` output. The SessionStart `taskwarrior-drift-probe.sh` runs it in dry-run and emits a `stale_claims` finding (local event ⇒ no poll/TTL, per `.claude/rules/drift-detection-triggering.md`; opt out with `CLAUDE_TASKWARRIOR_DRIFT_NO_STALE_CLAIMS=1`). | `taskwarrior-plugin/scripts/tests/test-release-stale-claims.sh` (auto-discovered via `*-plugin/scripts/tests/test-*.sh`) pins: dead PID on this host → counted stale + `--apply` releases it (stop/modify pid:/annotate, UUID-keyed, `rc.confirmation=no`); live PID (`$$`) → untouched; foreign-host claim → untouched; dry-run mutates nothing; empty `+ACTIVE` set → no-op. `taskwarrior-plugin/hooks/test-taskwarrior-drift-probe.sh` pins the `stale_claims` finding surfaces when >0, stays silent at 0, and the opt-out suppresses it. | issue #1792 |
| A `refactor` subagent assigned a large **closed list** of mechanical deletions (symbols/files) stops early but returns a plausible — often truncated — self-report, so the under-delivery is invisible unless the orchestrator re-runs the authoritative checker (`knip`/build/test). Observed in silverbucket-helper #346: a "misc" cluster agent assigned ~23 symbols across ~11 files completed only ~5; two sibling agents also returned summaries cut off mid-sentence. The truncated-summary symptom is a harness-level context/rate-limit cutoff (not fixable in plugin code), but a silent under-delivery is detectable via a machine-checkable manifest + mandatory independent verification. | `agents-plugin/agents/refactor.md` had no completion-manifest requirement and no batch-size cap, and `parallel-agent-dispatch` (SKILL.md + REFERENCE.md) documented self-verification (Pillar 4) and reviewer-verification (Pillar 5) but no refactor-specific "emit a manifest, never trust it alone" pointer. The fix adds a Completion Manifest section + a ≤~10-symbol/≤~6-file batch cap to `refactor.md`, and a manifest line + "mechanical-batch self-reports are never trusted alone" language (re-run the authoritative checker, diff the manifest) to the dispatch skill and its REFERENCE.md, all referencing #1601. | `scripts/check-agent-failure-contract.sh` asserts `refactor.md` carries the `Completion Manifest` section + `#1601`, and `parallel-agent-dispatch/SKILL.md` + `REFERENCE.md` carry `#1601` + the manifest language. Regression test `scripts/tests/test-check-agent-failure-contract.sh` (real-repo + faithful-fixture pass, each marker stripped → fail). Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml`. | issue #1601 |
| The `obsidian-plugin` read-only vault audit skills (`vault-orphans`, `vault-wikilinks`, `vault-mocs`, `vault-tags`, `vault-stubs`) leaned on the `obsidian` CLI / `vault-agent` analyzers, which require the **running Obsidian app** — with it closed the CLI errors (`The CLI is unable to find Obsidian…`) and the whole audit family is unusable for headless / batch / scheduled vault-health work. The detection *methodology* was sound; only the **data source** needed a file-parsing path (the one `vault-frontmatter` already models). | Added an `## Offline Fallback (App Closed)` section to each of the five audit skills documenting the direct `.md`-parsing path — frontmatter (`tags`/`aliases`/`context`) plus `[[wikilink]]` resolution (basename + relpath + alias, case-insensitive; `![[embed]]` against attachments) — framed as: the CLI / analyzers are the live-index path, direct parsing is the deterministic headless default. The CLI-bound `search-discovery` skill gained an `## Offline Limitation` section pointing at that path. Documentation-only; a self-contained Python parser was deliberately deferred (it would duplicate the external `vault-agent` analyzers). | `scripts/check-vault-offline-fallback.sh` (wired into `.pre-commit-config.yaml`) asserts each of the five audit skills retains the `## Offline Fallback (App Closed)` heading with the live-index/headless framing (catches a stub heading), and that `search-discovery` retains `## Offline Limitation` + the offline-path pointer. | issue #1727 |
| `comfyui-node-scaffold`'s `scaffold.py` produced a CI-green pack that was invisibly missing the registry-ready / fleet-consistent **finishing pass**: `[tool.comfy] Icon = ""` (empty), no banner, no `registry-health.yml` / `clear-autorelease-labels.yml`, and a `## What it does\n\nTODO` README stub. Nothing failed — the pack published looking unfinished on registry.comfy.org and drifting from every sibling (`comfyui-touch-manager` was the lone pack missing all four for weeks with zero CI signal). Fixing each deployed pack by hand leaves every *future* pack broken the same way; the generator is the right place. | The scaffold now EMITS the deterministic pieces — `icon.svg` + `banner.svg` (family-style placeholders) with `Icon`/`Banner` wired to the `…/main/{icon,banner}.png` raw-GitHub URLs, a `just assets` rsvg-convert recipe, and the `registry-health.yml` + `clear-autorelease-labels.yml` workflows — replaces the bare README TODO with a real `## What it does`, and prints a **finishing-pass audit** at the end of every run that flags the two pieces it can't do from stdlib (PNG rasterization; the heavy pack-specific screenshot pipeline) and diffs the new pack's top-level entries against a mature sibling. | `comfyui-plugin/skills/comfyui-node-scaffold/scripts/tests/test-finishing-pass.sh` (auto-discovered via `*/skills/*/scripts/tests/test-*.sh`) EXECUTES the generator and asserts icon.svg/banner.svg emit as valid XML, `Icon`/`Banner` point at the raw-GitHub PNG URLs (never `Icon = ""`), the two workflows + `renovate.json` emit with no `dependabot.yml`, the `just assets` recipe is present, the README `What it does` is not a bare TODO, and the audit prints. `plugin-compliance-check.sh` `check_skill_body()` pins the four finishing-pass file-map entries + non-empty `Icon`/`Banner` + `print_finishing_pass_audit`. | issue #1877 |
| `task-reconcile`'s `reconcile.sh` only saw tasks carrying a `ghid`/`ghpr` UDA (`select(.ghid != null or .ghpr != null)`) and checked every ref against the **CWD** repo, so it silently missed the majority of real stale tasks — those referencing an issue/PR only in the **description/annotation text** (`Merge PR #16`, `+gh-issue #292`) — and mis-resolved **cross-repo** refs (a `project:immeral` task monitoring `foundryvtt-mcp#142/143/144` was checked against immeral's repo). In a 14-task portfolio reconcile, ~10 closes came from description-text/cross-repo refs the skill could not have found. | Added a `refs` jq function that extracts refs from `ghpr`/`ghid` UDAs **and** three unambiguous text forms (`github.com/owner/repo/(issues\|pull)/N`, `owner/repo#N`, bare `#N` via negative-lookbehind so shorthand like `prompt-editor#42` is NOT matched → safe false-negative). Selection broadened to `select(refs \| length > 0)`; `ref_state repo kind num` resolves each ref (with `-R owner/repo` for cross-repo; pr-then-issue fallback for unknown kind, cached per `repo\|kind\|num`). Multi-ref tasks are stale only when EVERY ref is done (any open → live; any unreadable → keep; any `pr-closed` → aggregate `pr-closed`, kept out of the bounded `--only-verdicts` auto-apply). | `taskwarrior-plugin/scripts/tests/test-reconcile-refs.sh` (auto-discovered via `*-plugin/scripts/tests/test-*.sh`; stubs `task`+`gh`) pins: bare-`#N` description task selected + classified (gap 1); ghid/ghpr UDA back-compat; cross-repo `owner/repo#N` resolved with `-R owner/repo` (gap 2, asserted against a `gh` invocation log) + kind fallback; multi-ref with an open ref → `live` (never closed); multi-ref all-done → stale; multi-ref merged+closed → `pr-closed` kept out of the bounded apply; shorthand `prompt-editor#60` NOT selected. `test-scheduled-reconcile.sh` (28 assertions) still green (UDA path + `--only-verdicts` back-compat). | reconcile-desc-crossrepo-refs |
| The `context: fork` canary (`code-quality-plugin/code-review`, #980) had proven out — it ran live and un-reverted for ~3 weeks after canary PR #1666 merged — but the restoration had not rolled out to the remaining **single-subagent** verbose-output skills, so their verbose forked-context output kept reaching the main window. A future bulk edit could also silently drop `context: fork` from the canary or any rolled-out skill with no gate to catch it. | Rolled `context: fork` (with `agent: general-purpose`) out to the seven single-subagent skills named in #980/#1667 (`agents-analyze`, `test-analyze`, `test-full`, `claude-blog-sources`, `docs-generate`, `evaluate-skill`, `dry-consolidation`) and generalized the single-skill canary guard into a list-driven guard over all 8 skills that carry it. The parallel-fan-out skills (`git-pr-feedback`, `evaluate-plugin-batch`, and `code-antipatterns` — whose execution strategy is a *mandatory* parallel agent delegation, not the optional Agent-Teams escape hatch) deliberately keep `context: fork` OFF (the `[1m]` concurrent-subagent cascade hazard) and are intentionally absent from the guard list. | `plugin-compliance-check.sh` `check_skill_body()` — the former single-`code-review` `^context: fork` assertion is now a `for`-loop over the 8 rolled-out skills (keyed by `plugin/skill-directory`), asserting each retains `^context: fork`. Dropping it from any erases the rollout; a deliberate rollback updates this guard and `.claude/rules/skill-fork-context.md` together. | issue #1667 |
| The "stale reviewed date" currency check over `.claude/rules/*.md` was unmeasurable: 25 of 41 rule files carried no `reviewed:` frontmatter field and 1 (per the issue) held the literal `reviewed: YYYY-MM-DD` placeholder, so any staleness audit over the field silently skipped most of the corpus and read as clean. Nothing enforced that a rule file even *has* a real reviewed date. | Backfilled `created:`/`modified:`/`reviewed:` on every `.claude/rules/*.md` (created = first-commit date via `git log --diff-filter=A --follow --format=%cs`; modified = last-commit date via `git log -1 --format=%cs`; reviewed = the enforcement-pass date `2026-07-04`) — inserting only the missing field inside the existing frontmatter (preserving `paths:` etc.) and prepending a minimal block to the 7 files that had none. Added Check 5 to `check-docs-index.sh`: for every rule file, WARN `rule_reviewed_missing` when the `reviewed:` field is absent from its OWN frontmatter, or `rule_reviewed_placeholder` when the value contains `YYYY` (a body-example `YYYY-MM-DD` is not flagged). WARN, matching the sibling `doc_count_drift` checks so it surfaces in the weekly `scheduled-audits` roll-up (no hard ERROR). | `scripts/tests/test-check-docs-index.sh` TEST F asserts a rule fixture missing `reviewed:` → `rule_reviewed_missing`, a fixture with `reviewed: YYYY-MM-DD` → `rule_reviewed_placeholder`, and a fixture with a real `reviewed:` date plus a `YYYY-MM-DD` body example → NOT flagged. TEST A (real repo clean) stays green now that all 41 files are backfilled. | issue #1851 |
| `/configure:all` hardcoded a 19-entry component list (of 37 component skills on disk), so "all" silently skipped ~18 components (api-tests, integration-tests, load-tests, ux-testing, mcp, makefile, justfile, feature-flags, package-management, mise, gitattributes, gitignore, worktreeinclude, argocd-automerge, claude-plugins, reusable-workflows, surface, web-session); README's 7 categories, docs/flow.md's 9 domains, and that list were three hand-maintained taxonomies that had all drifted apart (configure-readme existed on disk but was absent from the README), and configure-status re-implemented its own scan tables instead of reusing component detection. | No single source of truth existed for the component roster. Added `configure-plugin/skills/configure-all/components.yaml` (the authoritative manifest: domains, components, orchestrators, advisory, reference_skills) plus `configure-all/scripts/list-components.sh` (parses the manifest, cross-checks skills on disk, emits `COMPONENT=`/`DOMAIN=` rows per structured-script-output.md); configure-all/-select/-status now consume the lister instead of hand-maintained lists, and docs/flow.md + README.md were re-sectioned to mirror the manifest. | `scripts/check-configure-components.sh --strict` (+ `scripts/tests/test-check-configure-components.sh`, 6 cases; lister regression `configure-plugin/skills/configure-all/scripts/tests/test-list-components.sh`) asserts: manifest ↔ disk agree (every entry exists, every skill dir bucketed exactly once), every `/configure:<x>` literal in configure-all/SKILL.md resolves to a manifest entry (blocks reintroducing a hardcoded list), flow.md's mapping table names every component and nothing dangling, and README lists every manifest skill. Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml`. | this PR |
| ADR numbers are chosen at branch time but claimed only at merge time, so two in-flight ADR PRs can pick the same number and both land — the FVH infrastructure #2015 collision where two ADRs both claimed `0038`, a third was assigned `0040` over the apparent gap, and the README index silently missed 0039/0040 for a week. `blueprint-adr-validate` validated ADR *relationships* (supersedes/extends/cycles) but had no numbering-collision guard, so the race was invisible until implementation time. | Added `blueprint-plugin/skills/blueprint-adr-validate/scripts/check-adr-numbers.sh` (issue #1585) — the lightweight validation-only variant: it detects `duplicate_adr_number` (two working-tree files share a number, ERROR), `adr_number_collision` (a working-tree ADR's number is held by a **different** filename on `origin/main` — the pre-merge parallel-PR case, ERROR), and `adr_missing_index_row` (ADR absent from the README index, WARN). Resolves `docs/adrs/` or `docs/adr/`, emits the `STATUS=`/`ISSUE_COUNT=` convention, degrades to OK when no ADR dir / base ref exists. Wired into `blueprint-adr-validate/SKILL.md` Step 2b + Agentic Optimizations. | `blueprint-plugin/skills/blueprint-adr-validate/scripts/tests/test-check-adr-numbers.sh` (auto-discovered via `*/skills/*/scripts/tests/test-*.sh`) builds a git fixture and pins: clean tree → OK; base-ref collision → ERROR + exit 1; within-tree duplicate → ERROR; missing index row → WARN + exit 0; no ADR dir → OK; absent base ref → graceful degrade. `plugin-compliance-check.sh` `check_skill_body()` asserts the SKILL.md retains the `check-adr-numbers.sh` invocation. | issue #1585 |
| A reviewer briefed with `execution-grounded-review`'s "trace claims to execution evidence" philosophy returned **zero findings** on a safetensors adapter save/load round-trip, because it treated "a passing test exists for this claim" as sufficient. The real bug: the test's operation SEQUENCE (`construct → forward immediately`) diverged from the production call path (`construct → generate batches [consuming lazy RNG] → first forward`). For stateful/RNG-dependent code (lazy init like burn's `Param<T>`, global mutable RNG, caching), a narrower hand-built repro draws its deferred state at the same relative point in both the "train" and "reload" paths, so the round-trip "works" (no error) while a real trained adapter's reload silently diverges — the passing test just exercised the one ordering that couldn't see the bug. | Added **Step 3a** ("Match the test's operation sequence to production") to `execution-grounded-review/SKILL.md`: for a round-trip / determinism / reproducibility / idempotence claim, don't stop at "a test exists and passes" — verify the test reproduces the real production call sequence, not a convenient shorter one; grade `UNVERIFIED` until it does. Calls out stateful/RNG-dependent code (lazy init, global mutable RNG, caching) as the high-risk class and carries the concrete verifier prompt from the issue. Also added a stance row, a Step 4 triage row, and an anti-pattern row. | `plugin-compliance-check.sh` `check_skill_body()` — for `execution-grounded-review`, asserts the SKILL.md retains both `Match the test's operation sequence to production` (the Step 3a heading) and `not just a convenient` (the verifier prompt). Dropping either would silently revert to "a passing test is sufficient evidence". | issue #1968 |
| `session-end` had no blueprint pass: a session closing WO-linked (`bpid`) taskwarrior tasks left the blueprint feature tracker undrained — nothing triggered `/blueprint:blueprint-feature-tracker-sync --drain-wave` at the session bookend, so `tasks.pending` silently drifted from reality until someone ran the sync by hand | session-plugin and blueprint-plugin had no functional tie; the survey collector surfaced no blueprint state, so session-end had no qualify signal for a drain pass | `session-survey.sh --with-blueprint` `BLUEPRINT` section (TESTS J–M in `test-session-survey.sh`, incl. the explicit-path union jq that keeps `phases[].status` out of feature counts); `plugin-compliance-check.sh` `check_skill_body()` pins `--drain-wave` + `UNDRAINED_COUNT` in session-end | PR #TBD |
| `session-spinup` never retrieved GitHub issues in Claude Code on the web (and any unauthenticated-`gh` environment): the collector gates all GitHub queries on `gh auth status`, and when the gate failed the `PRS`/`GITHUB_DRIFT` sections emitted `ASSIGNED_ISSUES=0 / DRIFT_COUNT=0 / STATUS=OK` — a silent false zero indistinguishable from "no open issues". REFERENCE.md even instructed "skip it silently", so the briefing presented an unqueried GitHub as a clean state; web sessions have no `gh` at all (sandbox-guidance.md), so issues were *never* surfaced there. | The digest carried no availability signal for GitHub (unlike `TASK_AVAILABLE=` for taskwarrior), and the skill had no MCP fallback. Fix: `session-survey.sh` emits `GH_READY=` in the summary, `PRS`, and `GITHUB_DRIFT` sections; SKILL.md Step 1b falls back to the GitHub MCP tools (`mcp__github__list_issues` / `list_pull_requests`) with in-skill taskwarrior dedup when `GH_READY=false`, or states `github: not queried (gh unavailable)` in the briefing. | `session-plugin/scripts/tests/test-session-survey.sh` TEST N asserts `GH_READY=false` inside both the `PRS` and `GITHUB_DRIFT` section bodies (and the summary) when gh is absent, that the drift section stays parse-stable, and that a present-but-unauthenticated gh reads the same as absent; TEST C asserts `GH_READY=true` with an authed stub. | this PR |
| validate-pr-issue-links.sh scans HEAD's range not the --head branch → false-positive block on gh pr create from another checkout | Hook hardcoded HEAD instead of resolving --head/-H; scanned an unrelated commit range | test-validate-pr-issue-links.sh (head-ref range cases) | issue #1993 |
| docs/PLUGIN-MAP.md header total ("N plugins and M+ skills") drifts silently — only the README headline was guarded | check-docs-index.sh Check 3 parsed the README intro but never the PLUGIN-MAP.md header line | check-docs-index.sh PLUGIN-MAP header guard (+ test-check-docs-index.sh cases) | issue #1948 |
| Five markdown-structure lint scripts hand-rolled markdown structure detection (fenced-code-block toggles, table-row skips, blockquote awareness); two shipped bugs from it — `check-context-command-execution.sh` executed a fenced `!`cmd`` example / misparsed a table cell abutting a backtick (#1744), `check-version-pin-coverage.sh`'s fence-state toggle (#1492). The recurring query ("inline-code spans inside list items, excluding fences/tables/blockquotes") is a one-liner against a real markdown parse and an unwinnable regex arms race otherwise. | No shared parser existed; each script re-derived markdown structure with its own line-based state machine. `.claude/rules/offload-to-deterministic-substrate.md` applied one level down — the substrate is already deterministic (pre-commit/CI), it was using the wrong parser. | `scripts/lib/extract-md-elements.py` (tree-sitter + tree-sitter-markdown, self-contained `uv run` PEP723 so CI needs no install step) emits structured `inline_code` (with container/blockquote/is_bang) + `fence` + `fence_line` rows per structured-script-output.md. `check-context-command-execution.sh` (extraction) and `check-version-pin-coverage.sh` (fence-line scan) migrated to consume it — behavior-preserving on the real repo (context harness FAIL=0/PASS=633 unchanged; version-pin ISSUES byte-identical). Semantic guard `scripts/tests/test-extract-md-elements.sh` pins the fence / `~~~` / table-cell / blockquote / nested-list invariants; both consumers' existing regressions (incl. #1744 fixtures F/G, #1492 prune/fence) stay green. Wired into `.pre-commit-config.yaml` + `plugin-pr-checks.yml`. The three denylist lints (`lint-context-commands.sh`, `lint-mcp-tool-references.sh`, `lint-package-references.sh`) are deferred: they carry no fence/table state machine (a per-line regex shape-denylist and a one-line `>` blockquote skip), so there is nothing structural to offload. | issue #2009 |
| `agent-patterns-plugin:mcp-management`'s Configuration Patterns section guided writing a project-scoped `.mcp.json` but never mentioned that Claude Code gates a *newly-added* project MCP server behind a one-time trust/approval step (via `/mcp` or a session restart, both user-only) before its tools are callable in the current session. After writing the config the server reads `⏸ Pending approval` and stays uncallable, so the agent discovered the gate via a `claude mcp list` status check mid-session and had to ask the user how to proceed — a round-trip the skill could have preempted. | The skill documented the write but not the post-add approval gate. Added a `### Post-add approval gate (project-scoped)` subsection instructing the agent to tell the user proactively (right after writing the entry) that a one-time `/mcp`/restart approval is needed, rather than discovering it via a status check. | `plugin-compliance-check.sh` `check_skill_body()` — for `agent-patterns-plugin/mcp-management`, asserts the SKILL.md retains both `Pending approval` and `Post-add approval gate`. Dropping either would silently revert to write-only guidance. | issue #2030 |
| PR #2049 was auto-closed by GitHub — never merged — and its work sat stranded on a branch, unnoticed. It was stacked on `feat/pi-tier-manifest` (base != `main`); when that base PR (#2048) was squash-merged and its branch deleted, GitHub closed the child instead of retargeting it, and **a closed PR whose base ref is gone cannot be reopened** (`gh pr reopen` → *"Could not open the pull request"*). A follow-up sweep found 26 dead branches, 2 of which carried real work that had **never had a PR opened at all** — so no `pull_request` event ever fired for them. | Nothing swept for "commits on a branch that never reached the default branch". An event-driven `pull_request: closed` workflow cannot fix this: by the time the event fires the base ref is already deleted (the reopen window is gone), it needs a caller file in **every** repo, and it is structurally blind to never-PR'd branches. Added a **scheduled sweep** instead — `scripts/check-stranded-work.sh` (takes `--repo`, so one run can sweep siblings) classifying each branch by PR fate: `stranded_autoclose` (closed-unmerged AND base ref 404s) and `stranded_no_pr` (commits, no PR, past a grace period), vs the three that must stay **silent**: `landed` (merged PR), `closed_deliberate` (closed-unmerged but base ref ALIVE = a human decision — 11 of the 26 were this), and `in_flight` (pushed within `--min-age-days`, default 7). Two traps encoded: `git cherry` (**not** `rev-list`/ancestry/tree-containment) detects patch-equivalence so a **squash-merged** branch isn't misreported as stranded; and the **base-ref-404 check is the sole discriminator** separating an accident from a deliberate close. | `scripts/tests/test-check-stranded-work.sh` drives the pure `classify` stage via `--fixture` (hermetic — no network, no `gh`, no remote) over all six branch shapes from the 2026-07-12 sweep, pinning the three NEGATIVE cases hardest: deliberate close (base alive) NOT reported, squash-merged branch (still "ahead" by SHA) NOT reported, and branch pushed today with no PR NOT reported (the live run flagged exactly this false positive). Wired into `.pre-commit-config.yaml`; the sweep runs weekly via `.github/workflows/stranded-work-audit.yml`, which opens one `stranded-work` issue and emits nothing when clean. | issue #2049 |
| `session-plugin` had no home for a bug/gap/fix noticed in a *third-party* repo: `session-wrap`'s GitHub destination is scoped to the cwd repo's post-merge follow-ups, so everything noticed-in-passing about a dependency either evaporated at session end or became an untagged personal task that never surfaced as "file this upstream." | Added an upstream issue/PR candidate detection+routing pass to `session-wrap` (destinations row, LOG-IT signal, Categorise row, Step 4 per-candidate AskUserQuestion) and surfaced it in `session-end`'s Wrap pass under the confirmation gate. The load-bearing guarantee is that the pass **never files blind**: *Track for later* → `+upstream` taskwarrior task; *File now* always routes `workflow-verify-before-filing` (upstream-HEAD verify + dedup) → `cold-read-gate` → file. | `plugin-compliance-check.sh` `check_skill_body()` — for `session-wrap`, asserts the body retains `+upstream` (track path) AND `workflow-verify-before-filing` (never-file-blind gate); for `session-end`, asserts the body retains `workflow-verify-before-filing` (the gate survives at the orchestrated bookend). | session-upstream-candidate-pass |
| `session-distill` had no deterministic substrate (unlike the capture side's `session-survey.sh`): it re-read the whole conversation for commands/edits and re-ran `just --dump` from memory every time, so justfile-recipe capture went stale (the LLM had to *remember* to check recipes) and a session-invented multi-step process had no home. Both are instances of `.claude/rules/offload-to-deterministic-substrate.md`. | Added `session-plugin/scripts/distill-survey.sh` — the distill-side analogue of `session-survey.sh` — mining the session transcript (`~/.claude/projects/<slug>/<sid>.jsonl`) for `RECIPE_CANDIDATES` (cross-session recurrence + commit-bracketing + novelty vs `just --dump`, not within-session frequency), `HOT_FILES` (exact `toolUseResult.filePath`), `COMMIT_INTERVALS`/`COMMAND_DIGEST` (grouping only — sequence naming stays LLM judgment), and `RULE_HINTS_FROM_TOOLING` (repeated `toolDenialKind`). Rewired `session-distill` Step 1/2 to consume the digest, added the `--process` → project-local `.claude/skills/` route, and made `session-end`'s Distill qualify gate consume `--summary`. | `session-plugin/scripts/tests/test-distill-survey.sh` (churn excluded; cross-session novel command surfaces; just-covered excluded; exact HOT_FILES; `.claude/worktrees/` pruned; SKIP on empty/missing; `--summary` shape; denial signal). `plugin-compliance-check.sh` `check_skill_body()` asserts `session-distill` retains `distill-survey.sh` + `--process` + `.claude/skills/`. | distill-survey-substrate |
| An implementer agent **completed its work fully** (one clean commit, clean tree) then went idle emitting only `{"type":"idle_notification","idleReason":"available"}` — its final report message was never delivered to the orchestrator. The work wasn't lost; the *communication* was, and it is intermittent (sibling agents in the same wave delivered normally — loractl post-M8 sweep, 2026-07-11). An orchestrator reading the idle as a silent exit would respawn, discarding a done task. | `parallel-agent-dispatch`'s "Handling a Missing Return" covered silent stalls (pre-commit-hook block, StructuredOutput cut-off) but not the idle-without-report communication-loss variant. The fix adds an "Idle without report (#2039)" subsection: an `idle_notification` with no preceding report is not a failure signal (run the empty-vs-dirty branch check first); recover with `SendMessage` to the named agent to resend the Return Contract (resume-from-transcript makes it cheap; a report re-request is read-only so the #1546 caveat does not apply); never respawn; brief implementers to SendMessage the report to the lead as their final act. | `scripts/check-agent-failure-contract.sh` asserts `parallel-agent-dispatch/SKILL.md` carries `Idle without report`, `idle_notification`, and `#2039` (guards I/J in `scripts/tests/test-check-agent-failure-contract.sh`). Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml` (same entry as #1422). | issue #2039 |
| A live session closed the wrong taskwarrior task ("282 now points to a gitops task") — `task-claim`/`task-release`/`task-done` each resolved a task once early, then reused that same possibly-numeric id across several LATER, separately-issued mutating `task` commands, with a real human-timescale gap between resolve and mutate (an `AskUserQuestion` pause in `task-release`, a `SlashCommand` coworker-check in `task-claim`). A concurrent agent's `task done` renumbering ids in that window made a later step silently mutate the wrong task — the same bug class as #1417's bulk-loop case, but at single-task granularity, previously undocumented. `task-done`'s post-close `depends:` sibling query had the same gap (a completed task's numeric `id` becomes `0` and may be reassigned). Also found: 4 broken citations (not the 2 originally spotted) to a `.claude/rules/taskwarrior-bulk-operations.md` rule file that never existed on disk. | Created `.claude/rules/task-id-stability.md` as the central citation, repointed all 4 broken citations to it, and extended `task-claim`/`task-release`/`task-done` to resolve `$TASK_UUID` once (extending the existing Step 1/2 `export \| jq` projection) and mutate every later call by `$TASK_UUID` instead of the caller-supplied `$TASKID` — including the post-close `depends:` query. Added UUID columns to `task-status`/`task-coordinate` reports as convenience only (not the safety mechanism — see the rule's "why not a visible UUID column" section). An adversarial-review pass caught that a plain grep for the substring `TASK_UUID` would repeat the #1417→#1819 syntactic-only-gate mistake (it can't detect a *partial* conversion where only some mutating calls were fixed). | `taskwarrior-plugin/scripts/tests/test-task-id-stability.sh` — EXECUTES the renumbering scenario against a scratch taskwarrior store (proves numeric-id addressing breaks under renumbering while UUID addressing does not) AND greps each SKILL.md for every individual mutating command line, asserting each uses `$TASK_UUID` and none still uses the stale `$TASKID` (catches a partial conversion the #1417 lesson warns about); also asserts the broken citation string is fully gone repo-wide. `plugin-compliance-check.sh` `check_skill_body()` adds a cheap syntactic `$TASK_UUID`-presence pin for the three skills, on top of (not instead of) the executable test. | this PR |
| `task-reconcile`'s `reconcile.sh` argument parser ended in a silent `*) ;;` catch-all, so an unrecognised flag was **swallowed, not rejected**. Because `--only-verdicts` is the only thing bounding a destructive `--apply`, any caller/script version skew (a stale plugin cache predating #1793, a typo'd flag) silently converted the bounded apply into an **unbounded** one — observed live closing a `pr-closed` task (`method=keep` promised) with no warning, no error, exit 0. The unattended LaunchAgent wrapper `scripts/scheduled-reconcile.sh` passes the flag through, so a skewed install would over-close with nobody watching. The existing `--only-verdicts` regression could not catch it: it runs against a version that HAS the flag; the failure only appears when the flag is absent/unrecognised. | Fail-fast per `code-quality.md`: the catch-all now rejects unknown arguments — `reconcile.sh: unknown argument: <arg>` + usage on stderr, exit 2 — before any classification or mutation. Exit-code contract documented in the script header and `task-reconcile/REFERENCE.md` (exit 0 clean run, 1 task-binary missing, 2 unknown argument). | `taskwarrior-plugin/scripts/tests/test-scheduled-reconcile.sh` test G passes a misspelled `--only-verdictz=…` alongside `--apply` and asserts exit 2, the unknown flag named on stderr, usage printed, and **nothing imported** (the stub's import capture stays empty — pre-fix it captured an unbounded close of all three stale tasks). | issue #2057 |
| `agent-patterns-plugin:cold-read-gate` did not specify **how** to run the fresh reader (synchronous vs background) or how to retrieve its critique. A session gating an upstream issue + PR body spawned the haiku reader with `run_in_background: true`; the completed background agent only emitted `idle_notification`s, a `SendMessage` asking for the critique produced another idle notification (a completed background agent's output returns as the task-completion result, not on the message channel), and the gate was abandoned — its deterministic leakage scan done by hand. | The skill's dispatch step gave the Agent template but no execution mode. Fix: Step 2 mandates **synchronous** dispatch (`run_in_background: false` — the critique IS the returned tool result), documents that a background reader's critique must be read from the **task-completion result** (never polled via `SendMessage`), and Step 4 gains an inline-deterministic-scan fallback so an unretrievable critique never blocks publication. | `plugin-compliance-check.sh` `check_skill_body()` — for `cold-read-gate` (scoped on `agent-patterns-plugin`), asserts the SKILL.md retains both `run_in_background: false` and `task-completion result`. Dropping either would silently revert to execution-mode-unspecified dispatch. | issue #2063 |

| `configure-plugin:claude-security-settings` guided per-subcommand allow rules plus a force-push deny backstop but never surfaced the colon-vs-space wildcard distinction, so an implementer agent wrote the deny rules in colon form (`"Bash(git push --force:*)"`). The `:*` suffix was observed prefix-matching the raw command string, so the rule also matched `git push --force-with-lease …` — silently hard-blocking the safe recovery form stacked-PR workflows depend on (deny rules cannot be overridden except via `bypassPermissions`). Caught pre-push by the lead reviewer in laurigates/loractl#39; the skill had validated the result without flagging the widening. Current official permission docs state an end-of-pattern `:*` is equivalent to the trailing space form, but the equivalence is not version-pinned in the changelog and the widening was observed live. | The skill documented only the generic wildcard syntax; nothing warned that flag-scoped deny patterns must use the space form (`"Bash(git push --force *)"`, whose trailing-space word boundary stops the match at the exact flag). The fix adds a "Flag-Scoped Deny Rules: Use the Space Form" subsection (space-form example + colon-form gotcha callout + audit guidance to flag `<flag>:*` deny entries) and a pointer from the Deny Patterns quick-reference section. | `plugin-compliance-check.sh` `check_skill_body()` — for `configure-plugin/claude-security-settings`, asserts the SKILL.md body retains both `--force-with-lease` (the widening evidence) and `Bash(git push --force *)` (the correct space form). Dropping either would silently remove the caveat. | issue #2038 |

| New skill `agent-patterns-plugin:multi-model-delegation` encodes the protocol for consulting foreign models (PAL `chat`/`consensus`) on design work: identically-briefed independent draws, diff for the split, adjudicate the split against the codebase (never model confidence), graft-never-adopt-wholesale — plus the kimi `temperature` 400 gotcha (pal-mcp-server#67) whose opaque provider error otherwise costs failed calls and wrong theories. A bulk edit "tightening" the prose could silently drop the inversions that make the skill worth invoking (majority-voting model answers launders a coin-flip into a researched-looking decision). | The skill is new; no guard asserted its load-bearing tokens survived edits. | `plugin-compliance-check.sh` `check_skill_body()` — for `multi-model-delegation` (scoped on directory name), asserts the SKILL.md retains `disagreement, not the union`, `Adjudicate against the code`, and `` temperature` for kimi ``. | this PR |
| Nothing validated `docs/blueprint/manifest.json` — the control surface every blueprint skill reads. `blueprint-plugin/schemas/` held `adr.schema.json` and `feature-tracker.schema.json` but no manifest schema, and every consumer degrades gracefully on bad input (`get-automation-config.sh` coerces a non-numeric `autonomy_level` to 0, `get-validation-config.sh` falls back per key, `blueprint-sync-ids.sh` treats a missing `id_registry` as absent). Correct at runtime, but it makes a **typo indistinguishable from an intentional omission**: `autonomy_levle: 3` reads as level 0 and `adr_dris: [...]` reads as unconfigured, silently, forever. | No schema existed. Added `blueprint-plugin/schemas/manifest.schema.json` (draft-07, matching the two sibling schemas) with `additionalProperties: false` on the fixed-key blocks (root, `project`, `structure`, `feature_tracker`, `automation`/`work_orders`, `validation`/`status_vocabulary`, `id_registry`, `workspaces`) — the only thing that catches the typo class — and open blocks where keys are user/registry data (`task_registry`, `custom_overrides`, `created_by`, the `generated`/`documents`/`github_issues` maps). Entry point is `/blueprint:status` Step 2a (it already parses the whole manifest and is read-only) via `blueprint-plugin/scripts/check-manifest-schema.py`, a self-contained `uv run` PEP723 script (the `extract-md-elements.py` pattern) emitting the `STATUS=`/`ISSUE_COUNT=` convention. Fails open on no manifest / older `format_version` / missing `jsonschema`. | `blueprint-plugin/scripts/tests/test-check-manifest-schema.sh` (auto-discovered via `*-plugin/scripts/tests/test-*.sh`) is the SEMANTIC gate a syntactic "is it valid JSON" check would miss: it asserts `autonomy_levle`, `adr_dris`, and a misspelled top-level block are **rejected by name and JSON pointer** (all three are well-formed JSON), that the correctly-spelled `validation` block still passes (guard integrity), that open blocks accept unknown keys, that the repo's own live manifest validates, and that all three degradation paths stay exit-0. `test-skill-scripts.yml` gained `*-plugin/scripts/**` + `*-plugin/schemas/**` path triggers and a `setup-uv` step so it actually runs in CI. | issue #2136 |
| Blueprint autonomy level 3 (ADR-0020, issue #2005) ships two workflow TEMPLATES in `blueprint-plugin/templates/` — outside `.github/workflows/`, so `check-workflow-model.sh`/`actionlint` don't lint them — plus the untrusted-issue-body WO-packet parser and the level-3 gate. A bulk edit could silently drop `--model opus`/`--effort`, inline the untrusted `github.event.issue.body` into a `run:` (script injection), or remove the independent `verify:` job / budget / stuck ceiling. | No guard covered templates outside `.github/workflows/`, and the parser handles untrusted input. | `scripts/check-blueprint-level3-templates.sh` (+ test) pins model/effort + the "issue body referenced exactly once" injection guard + the `verify:`/state-packet loop-integrity markers + actionlint; `blueprint-plugin/scripts/tests/test-blueprint-wo-packet.sh` proves the parser never executes body content and rejects underspecified packets; `test-blueprint-wo-guard.sh` proves the level-3/auto_execute/budget/stuck gate; `plugin-compliance-check.sh` pins the scaffold SKILL's safety tokens. | issue #2005 |
| The `blueprint-adr-validate` collision guard (shipped for #1585) reported `STATUS=OK` on a **real ADR-0016 double-assignment**. Two compounding causes made it structurally blind: `adr_number()` derived the number from the `NNNN-title.md` **basename**, so `docs/adrs/objs-format.md` — which claimed `id: ADR-0016` in **frontmatter** only — contributed no number at all; and `ADR_DIR` resolved to a **single** directory, so the two claimants (`docs/blueprint/adrs/0016-…md`, registered in the manifest, and the unregistered `objs-format.md`) were never compared. The cause is recorded inside one of the colliding files: its author computed "next free slot" from the manifest, not knowing `objs-format.md` had already self-assigned 0016 — #1585's branch-time-vs-claim-time race in a form the shipped guard could not see. Verified against the old script: `ADR_COUNT=0`, `STATUS=OK`, exit 0. | The guard collected numbers from basenames in one directory, and `adr_missing_index_row` could only WARN — never repair — even though a stale index is how #1585 itself said the original 0038 collision went unnoticed for a week. Fix: (1) UNION the basename-derived number with a frontmatter `id: ADR-NNNN` parse, reporting the claim source (`basename`/`frontmatter`/`basename+frontmatter`) so a number with no filename is not baffling; (2) accept a repeatable `--adr-dir` and read the directory set from the manifest `validation.adr_dirs` via `get-validation-config.sh` (**not** a second config mechanism — per the owner's cross-comment on #2128); (3) add the manifest `id_registry` as the unambiguous arbiter (`.id_registry.documents`, matching `blueprint-sync-ids.sh`) reporting `adr_registry_mismatch`; (4) add `generate-adr-index.sh` with a `--check` mode, turning the unfixable WARN into a repairable state. All degrade silently to OK/exit 0 with no manifest, no `id_registry`, or no ADR directory. | `blueprint-plugin/skills/blueprint-adr-validate/scripts/tests/test-check-adr-numbers.sh` (extended; the six #1585 tests A–F stay green) adds the exact reported repro — an unregistered frontmatter-only claimant colliding across a **different** directory → ERROR, which the old guard reported OK for — plus repeatable `--adr-dir`, `ADR_DIRS` honoured from the validation config, registry-mismatch, and silent-OK degradation. `test-generate-adr-index.sh` pins round-trip across two directories, `--check` clean/drifted/markers-absent, and rejects the issue's broken `awk` recipe shape (its `END` block prints one row regardless of input). `plugin-compliance-check.sh` `check_skill_body()` pins `generate-adr-index.sh`, `--adr-dir`, and `id_registry` in the SKILL body. | issue #2129 |
| The blueprint feature tracker had **five mechanical defects accumulated over ~3 months** with nothing to detect them: `statistics` diverged from `features` (`total_features: 25` vs 27 actual, `completion_percentage: 68.0` vs 66.7 — and since `statistics` is a **cache** of the features collection, every downstream "68% complete" figure quoted from that file was wrong); status enum drift (one `"completed"` against seventeen `"complete"`, silently undercounting); a dead `pending` statistics bucket; two timestamp fields for one fact (`updated_at` vs `last_updated`, already disagreeing); and `FR-025` cited by five documents — including inside another feature's own `evidence` string — but never minted, so invisible to every status query. #1354 fixed sync **writing** wrong statuses; nothing detected divergence arriving by any other route. | No verification counterpart existed. Added `blueprint-plugin/scripts/blueprint-tracker-check.sh` (six checks; recomputes `completion_percentage` with arithmetic lifted byte-identically from the sync writer) plus the additive manifest `validation` block read via `blueprint-plugin/scripts/get-validation-config.sh`. Two ground-truth corrections against the issue's prose, both verified in `schemas/feature-tracker.schema.json`: `features` is an **object** keyed `^FR\d+$` in the canonical schema but an **array** in the reporting repo (both handled, shape reported as `FEATURES_SHAPE=`), and `partial` is a **legitimate** schema bucket — the issue wrongly calls it dead; `pending` is the dead one. The plugin-owns-invariants boundary the issue asked to be argued: the **schema** owns the feature-status enum; `status_vocabulary` governs **doc frontmatter** conventions, with a near-miss in the configured vocabulary downgraded to a WARN naming the canonical spelling. | `blueprint-plugin/scripts/tests/test-blueprint-tracker-check.sh` (29 assertions) pins every defect **and the issue author's own false positive**: `.tasks.pending[]`/`.completed[]` legitimately repeat an FR id already in `features` (the documented drain design), so status is read from the features collection ONLY and there is deliberately no duplicate-record check — a negative case asserts an FR in both places is never reported as a duplicate. Also pinned: `partial` NOT flagged while `pending` is, both `features` shapes, and a missing tracker → OK/exit 0 (most repos, including this one, have none). Config surface covered by `test-get-validation-config.sh` (63 assertions). | issue #2128 |
| `agent-patterns-plugin:multi-model-delegation` told the orchestrator to "pass code via `absolute_file_paths`" with no budget caveat, so a real 3-provider consult dispatched a ~84K-token, 7-file attachment set that was **rejected by two of the three models**. `absolute_file_paths` is capped at ~60% of context headroom and the cap varies **wildly** per model — on a 262K context, `gpt-5.3-codex` allowed ≈ 76,800 tokens but `kimi-k2.7-code` only ≈ 28,311. Because the identical-briefs invariant is the whole basis of the protocol, the naive fix (trim per model) silently un-identicals the briefs and destroys the signal. Two sibling dispatch mechanics bit in the same consult: a `working_directory_absolute_path` pointing at a scratchpad outside `PAL_WORKSPACE_ROOT` is rejected (`must reside within the PAL workspace root`), and three **concurrent** `chat` calls returned `model_used` values rotated across each other while `provider_used` stayed request-consistent — so the skill's existing "verify independence via `provider_used`" note understated the hazard (pal-mcp-server#68). | The skill documented the attachment parameter but none of its limits. Fix: a `PAL Mechanics That Bite` table carrying the per-model budget (with the observed numbers, so the ~60%/varies-wildly claim is falsifiable), the workspace-root restriction, the concurrency caveat, and the registry-retirement fallback; plus a `The Curated Excerpt Bundle` section encoding the pattern that resolved all three — ONE `<repo>/tmp/<consult>/context-excerpts.md` of §-numbered **verbatim** excerpts titled with real file path + line range, sized under the smallest model's budget and attached unchanged to every model. | `plugin-compliance-check.sh` `check_skill_body()` — the `multi-model-delegation` block gains a second token loop asserting `smallest model's budget bounds the bundle` (the sizing rule that keeps briefs identical), `must reside within the PAL workspace root`, and `untrustworthy under concurrency`. Dropping any re-introduces a silent dispatch failure whose symptom (a rejected call, or a same-model pair masquerading as independent draws) is easily misread as flakiness. | issue #2120 |
| `project-plugin/skills/project-init` carried `- GitHub CLI: !`which gh`` in its `## Context` block. `which` signals "not found" purely through its **exit code** — it exits 1 and writes **nothing** to stderr — and a non-zero exit from a Context backtick aborts the skill before its body runs. So `/project:init` was unusable for anyone without `gh` on PATH: precisely the fresh-machine case the skill exists for. Same abort class as #899 / #1205 / #1206 / #1351. Two things kept it invisible: `lint-context-commands.sh` had **no `which` rule** (it was the only `which` in a Context block repo-wide, so no prior burn added one), and the executing backstop `check-context-command-execution.sh` only fires when **the harness box itself** lacks the tool — on any developer or CI machine that has `gh`, the probe passes and the abort stays latent (verified: `--strict` reports `PASS=3 FAIL=0` here, and `FAIL=1 EXIT=1` with `gh` removed from PATH). | Moved the probe out of `## Context` into the skill body, run via the **Bash tool** where a non-zero exit is survivable, with an explicit availability report and an `--github` fallback — the capability check is kept, not dropped. A `find` over a fixed set of bin directories was rejected: it errors to stderr when a start directory is absent (the #1482 class, so it only moves the abort) and cannot see `gh` installed via mise/asdf/nix/cargo, yielding a false "not installed". | `lint-context-commands.sh` rule `which-in-context` (anchored to `^- .*!\`…\`` so `which` in fenced code, prose, or a table is not flagged); regression test `scripts/tests/test-lint-context-commands.sh` — the first test for this linter — pins the rule firing, the no-false-positive case, a clean block, the sibling `command-v-in-context` rule, and that no `which` remains in any Context block repo-wide. Wired into `.pre-commit-config.yaml` and the `Plugin: Lint skills` workflow. | issue #2145 |
| `comfyui-plugin/skills/comfy-node` and `foundryvtt-plugin/skills/foundryvtt-module` shared **824 eight-word shingles (containment 0.482** — roughly half the smaller file), the largest cross-file overlap in the marketplace by a wide margin (next: 0.282). The shared material was a common **gitops repo-adoption procedure**, and the two copies **had already drifted** — so this was a correctness problem, not just double context cost. Concrete drift found: comfy-node's Phase 2 invoked the scaffold via a hardcoded `.claude/skills/…` path (wrong for a plugin-installed skill) where the foundry copy used the documented `${CLAUDE_SKILL_DIR}` substitution; the foundry copy had compressed away comfy-node's Phase 3 rationale and two branch-protection-hook guardrails; and each carried half of the release-please failure-mode row. | Deduped per `.claude/rules/skill-consolidation.md` §2: cross-plugin ⇒ **one canonical owner** referenced by `plugin:skill` **name**, never a shared `REFERENCE.md` (a relative path is dead for anyone installing one plugin without the other). `comfy-node` owns Phases 3–5 + the branch-protection note + the generic failure rows, and gained an *Adapting Phases 3–5 to another repo class* substitution table so the reference is followable end to end; `foundryvtt-module` keeps only its spec/preflight/scaffold and a FoundryVTT deltas table. Drift reconciled before deduping (`${CLAUDE_SKILL_DIR}` ported **into** the owner; the fuller rationale/hook note kept; the failure row merged). | `plugin-compliance-check.sh` `check_skill_body()` — for `comfyui-plugin/comfy-node`, asserts the counter-findings survive (`rename + default-branch change + base-branch fixups`, `confirmed on the first real run`, `Adapting Phases 3–5 to another repo class`); for `foundryvtt-plugin/foundryvtt-module`, asserts the `comfyui-plugin:comfy-node` name-reference is present **and** that the delegated procedure is not re-inlined. Measurement guard: `check-context-engineering.py` no longer reports the pair (0.482 → 0.028, below the 0.15 reporting threshold). | issue #2142 |
| The SKILL.md size gate measured **bytes** (`wc -c`) while `.claude/rules/skill-quality.md` documented it as measuring **characters** and calibrated the 26,000 ceiling as a `chars / 4` token proxy. For an all-ASCII body the two are identical, so the conflation was invisible; for the em-dash/arrow-heavy bodies this repo actually writes, bytes exceed characters and a file can cross the ERROR ceiling on UTF-8 encoding overhead alone. `agent-patterns-plugin/parallel-agent-dispatch` measured 26,024 bytes vs 25,850 characters (87 non-ASCII characters × 2 extra bytes = the full 174-byte gap) and **failed every unscoped whole-repo run** — latent because `plugin-pr-checks.yml` scopes the scan to changed plugins, so it only fired for someone touching an unrelated plugin. The tightening was unintended and unevenly distributed across skills. | The gate used `wc -c`. Bare `wc -m` is not a drop-in fix: it counts characters only under a UTF-8 locale and silently returns bytes under `C`/`POSIX`. Fixed by counting decoded UTF-8 characters with `python3` (already a hard dependency of the script — `check_skill_frontmatter` parses YAML with it), which is locale-independent by construction; `errors="surrogateescape"` keeps a malformed byte as one character rather than raising. `skill-quality.md` updated in the same commit so the documented unit and the implementation agree. | `scripts/tests/test-plugin-compliance-skill-size.sh` gains the **straddle** fixture — ASCII filler plus 200 × U+2014 EM DASH so bytes (26,258) sit above the ceiling while characters (25,858) sit below — and asserts it does NOT trip ERROR, that the reported number equals the character count, and that it never equals the byte count. A fixture-validity guard asserts the straddle property itself, so the test cannot silently degrade into a no-op. Verified against the pre-fix `wc -c` implementation: all three new assertions fail. Every pre-existing fixture is ASCII (bytes == characters) and could not exercise this. | issue #2135 |
| `session-distill`'s **Cross-Repo Promotion (`[PROMOTE]`)** hand-off instructed `git -C "$PLUGINS_REPO" switch -c <branch> origin/main` and then Write/Edit **directly in the shared, long-lived local `claude-plugins` checkout**. In this portfolio that checkout is frequently contended by a concurrent Claude session, so the sequence raced: the coworker's operation **autostashed the in-flight edit** and moved `HEAD` to its own branch, and the subsequent `git add`/`commit` reported *"nothing to commit, working tree clean"* — the edit had been stashed away underneath, with no error at any step. A recovery `git worktree add` then also failed (`already used by worktree`) because `HEAD` had moved again between calls. The promote landed only via a hand-rolled isolated `git clone` (PR #2110). | The hand-off assumed exclusive ownership of a shared checkout and carried no coworker-hazard cross-references. Fix: the recipe now clones the plugins repo into a throwaway `mktemp -d` (`git clone --depth 1 --single-branch --branch main`), branches/edits/commits/pushes **there**, verifies `git log --oneline origin/main..HEAD` before pushing, and `rm -rf`s the throwaway after the PR URL is in hand. A **clone**, not a `git worktree`: a worktree registers in the shared checkout's `.git` (so it is still reachable by the contending session, and was the form observed failing), whereas a clone shares no `.git` at all. Cross-references added by name: `repos/.claude/rules/shared-checkout-branch-isolation.md`, `repos/.claude/rules/concurrent-session-pr-check.md`, and `git-plugin:git-coworker-check`. | `plugin-compliance-check.sh` `check_skill_body()` — the `session-distill` block gains a positive pin on `git clone --depth 1 --single-branch`, `shared-checkout-branch-isolation`, and `git-plugin:git-coworker-check`, **plus a negative guard** erroring if `PLUGINS_REPO" switch` reappears (the exact broken form). Both branches verified by mutating the SKILL body and observing the ❌. | issue #2113 |
| `agent-patterns-plugin/parallel-agent-dispatch/SKILL.md` had grown to **25,850 characters — 150 chars (0.6%) below the 26,000 ERROR ceiling**, while carrying **25 literal tokens pinned by two guards** (23 in `check-agent-failure-contract.sh`, 2 in `plugin-compliance-check.sh` for #1549). At that headroom any routine edit adding one hazard subsection breaches the gate, and the pin density means the breach cannot be relieved by trimming freely — most prose is load-bearing for a grep. NOTE: this was surfaced by the #2174 epic analysis as a claimed *live* build break at "26,024 characters"; that figure is the **byte** count. #2135 had already fixed the gate to count decoded UTF-8 characters, so the file measured 25,850 chars and the gate reported **WARN, not ERROR** — the premise was refuted, but the 0.6% headroom was a real latent fragility worth clearing. | Extracted whole coherent blocks to `REFERENCE.md` (which has no ceiling), leaving a one-line pointer for each: the #1838 nested-repo narrative, the #1692 `GIT_DIR`-export narrative, the Skill-less-agentType evidence (new `REFERENCE.md` § "Skill-less agentType — evidence" carrying the ~88k/~12k injection sizes and the #1549/#1550 batch-failure data), plus prose condensation in the #1546/#1868 resume sections, the rate-limit and `TaskStop` sections, and Pillars 2–4. **Extraction only — no guidance changed.** 25,850 → 23,404 chars (2,596 headroom, 17× more than before). | No NEW guard needed: `plugin-compliance-check.sh` `check_skill_size()` already gates the ceiling (it reported the WARN that measured this), and `check-agent-failure-contract.sh` already pins every load-bearing token. Both were run as the extraction's acceptance gate — and `check-agent-failure-contract.sh` **earned its keep mid-task**, catching a re-wrap that split the pinned literal `WIP at checkpoints` across a newline (the grep is `-qF` on one line), which was fixed before commit. All 34 pinned tokens (25 SKILL.md + 9 REFERENCE.md) re-verified individually, plus the 3 in the untouched sibling files. | Refs #2174 |
| PR #2114 ported `bash-antipatterns.sh`'s read detectors from regex to an `ast-grep` structural classifier and, as an unintended side effect, **widened** the `cat`/`head`/`tail` block from "a bare read as the whole command" (the pre-#2114 `^\s*cat\s+…` + "no pipe anywhere" scope) to "a bare read **anywhere** in a compound command". The AST rules matched any `command` node not inside a `pipeline`, so `cd repo && git log -3 && cat package.json`, `ls dir/; head -20 file`, and `echo "===" && cat -n file` all began blocking. W31 friction: same-session repeat-block rate **15.2% → 46.3%** (3.0× above a stable six-reading 6.3–20.0% band), 70 events / 41 sessions, ~30% of all friction events that week; per-session prevalence 21.6% → 68.3%. Of 72 recovered blocked commands ~47 (65%) did not begin with `cat`/`head`/`tail`. The repeat rate is the diagnostic: a bare `cat f.md` has a one-to-one `Read(file_path=…)` substitution, a compound diagnostic has **none** — the agent must split one Bash call into a Bash call plus N `Read` calls and re-plan, so it re-attempts a variant and is re-blocked in the same session. | The AST rules carried no positional constraint; `not: { inside: { kind: pipeline } }` was the only scope gate, and a compound command's arms are `list` children, not pipeline stages. Fixed by remedy (a) of #2148 (narrow, not demote — the block sat happily at 6–20% at its pre-#2114 scope, so the minimal correct action is to revert the widening rather than retire a working steer): `cat-read`, `head-tail-read`, and `task-output-read` now require `inside: { kind: program }` **plus** `nthChild` position-1 forward *and* reverse with `ofRule: { not: { kind: comment } }` — i.e. the read is the **sole statement** of the parsed program, with a trailing `# comment` excluded from the sibling count so `cat f.md  # notes` still fires. The now-redundant `not: { inside: { kind: pipeline } }` clause was dropped (`inside: { kind: program }` subsumes it). The three **write** detectors (`cat-write`, `echo-printf-write`, `sed-inplace`) keep their original scope — they guard file mutation, not context budget. | `hooks-plugin/hooks/test-bash-antipatterns.sh` "read block fires only when the read is the WHOLE command (#2148)" block replays the issue's differential-reproduction table: six compound shapes recovered from W31's blocked events must exit 0 (incl. the `git fetch; git status \| head -3; …; head -20 f; ls dir` preflight command that motivated the fix, and `sleep 5 && cat …/run.output`), while GUARD rows pin that every bare whole-command read still exits 2 (`cat`, `head -50`, `tail -50`, a task-output read, and a bare read carrying a trailing `# comment`), and that the write detectors still fire inside a compound command. Two pre-existing rows were flipped to the new scope with the reason inline (the `sleep 5 && cat …output` message assertions moved to the bare form; the #1900 `cat x && ssh host …` row now expects 0, with its real invariant — that a bare local `cat` is still classified as a local command node — pinned by a new adjacent row). Verified green on both local `ast-grep` 0.44.1 and the CI-pinned 0.43.0. | issue #2148 |
| `configure-plugin:configure-surface`'s Step 5 GitHub Action example SHA-pinned `uses: Connorrmcd6/surface@004c916… # v0.6.2` but set only `args: check` — and surface's `action.yml` declares `version:` with **`default: latest`**, so `install.sh` resolves `releases/latest` over the API at run time. Following the example verbatim produced a workflow that **looks** fully pinned while installing a **floating** binary on every run, contradicting the skill's own hardening checklist ("Installer \| Checksum-verified or vendored at the pinned ref") and its Step 3 rationale that the SHA "is the real immutable anchor". Checksum verification is not a mitigation: it proves the download matches *its own* published hash, not that it is the version the consumer pinned. Since `surf check` is a merge gate, a new upstream release changes gate behaviour with no local change — a red check nobody can attribute to an edit. | The example pinned the wrapper, not the artifact the wrapper installs. Fix: pass `version: v0.8.0` beside the SHA-pinned ref (both examples refreshed to v0.8.0, whose `action.yml` also ships the upstream installer-pin fix, so the stale "not yet in a tagged release" caveat could be cut), plus a `Binary version` hardening-checklist row. The guard side extends the EXISTING coverage script rather than adding a new one: `uses:`-line state now persists into the step's `with:` block, so an **absent** `version:` (`version_input_missing`) or a **floating** one — `latest`/`main`/`master`/`stable`/`nightly`/`edge`/`HEAD`/empty — (`version_input_floating`) is an ERROR. Both are gated on a curated `INSTALLER_ACTIONS` list (seeded with `Connorrmcd6/surface`): blanket-flagging every pinned action would opine on toolchain `setup-*` steps that track `latest` deliberately — `astral-sh/setup-uv` in `python-plugin/skills/python-development/REFERENCE.md` is such a case, observed and deliberately left off the list. | `scripts/check-version-pin-coverage.sh` (`INSTALLER_ACTIONS` + `flush_pending_uses`, new `VERSION_INPUT_COVERED=` counter); `scripts/tests/test-check-version-pin-coverage.sh` TEST G pins both defect shapes as ERROR with `--strict` exit 1, and — as the guard-integrity half — asserts an explicit `version: v0.8.0`, a bare `actions/checkout` with no `version:`, **and** an off-list action left at `version: "latest"` are all unflagged. Verified against the real pre-fix file (`git show HEAD:…SKILL.md` → `version_input_missing`, exit 1) and the fixed file (`STATUS=OK`). Already wired into `.pre-commit-config.yaml` + `plugin-pr-checks.yml`. | issue #2175 |
| `testing-plugin:test-analyze` shipped a category→agent routing table naming **6 `subagent_type` values that resolve to no agent anywhere** — `code-review`, `system-debugging`, `code-refactoring`, `test-architecture`, `cicd-pipelines`, `documentation` (the agents were renamed to `review`/`debug`/`refactor`/`test`/`ci`/`docs`). Every `Task` dispatch the skill instructed failed at "Agent type not found". Nothing caught it: the names are valid strings, the frontmatter parses, the body structure is fine — only a cross-file check against the actual agent inventory can see it. The same table was duplicated at **three** differently-sized sites in one file (7 rows in Command Flow step 3, 6 rows under `## Subagent Selection Logic`, 7 rows in the Prompt section), which is how the names went stale in three places at once and how the row counts diverged. Three sibling skills carried the identical defect: `code-quality-plugin:code-refactor` (`code-refactoring`), `code-quality-plugin:code-review` (`code-review`), `documentation-plugin:docs-generate` (`documentation`). | Collapsed the three copies into ONE authoritative `## Subagent Routing` table; Command Flow and the Prompt now reference it instead of restating it. All values moved to the plugin-qualified `plugin:agent` form (`agents-plugin:review`, …) — the form that resolves for a plugin-provided agent (a bare name only resolves for user/project-level agents, per `~/.claude/rules/agent-and-tool-selection.md`). Performance was split off `system-debugging` to `agents-plugin:performance` (which exists and is exactly for it) rather than folding it into `agents-plugin:debug`. The three sibling skills were fixed in the same pass — a repo-wide guard is only meaningful if the class is actually closed. | `scripts/check-subagent-types.sh` (+ `scripts/tests/test-check-subagent-types.sh`, 31 assertions) builds the agent inventory from every `*-plugin/agents/*.md` and ERRORs on any `subagent_type` in a skill body that resolves to nothing; a bare-but-resolvable name is a WARN (escalated by `--strict`), built-ins (`general-purpose`, `Explore`) pass, and two illustrative non-dispatches are path-scoped exceptions. **Guard integrity:** TEST B feeds the verbatim pre-fix table and asserts exit 1 with all six stale names reported by `VALUE=`; TEST F asserts prose mentions, a jq field read, and the uppercase `SUBAGENT_TYPE=` shell var extract nothing. Wired into `.pre-commit-config.yaml`. | Refs #2170, Refs #2174 |
| `scripts/audit-skill-descriptions.py` reported **2x every count it produced** — "Audited 814 skills across 44 plugins" against 407 real files, WARN band 52 against 26. `find_skills()` collected `rglob("SKILL.md")` and `rglob("skill.md")` into one list and deduplicated only by **Path equality**. Both globs are genuinely needed (a case-SENSITIVE filesystem matches a real lowercase `skill.md` only with the second — `check-skill-filename-case.sh` exists because 45 skills once carried that name), but on macOS's case-INSENSITIVE APFS the two globs return the **same inodes under two spellings**, which are unequal as `Path` objects. Every skill was audited twice. The doubling is uniform, so no ratio or percentage looked wrong — only the absolute counts, and only if you knew the real skill count. | Deduplicate by filesystem identity `(st_dev, st_ino)`, keeping the `SKILL.md` spelling (its glob runs first). `os.path.realpath` is **not** a fix and was rejected on measurement: it does not canonicalise case on APFS — it returns whichever spelling was asked for, so all 814 paths stayed distinct (verified: 814 unique realpaths vs 407 unique `(st_dev, st_ino)`). Distinct inodes also mean a case-sensitive filesystem carrying a real `SKILL.md` **and** a real `skill.md` in one directory still yields two entries, preserving the second glob's purpose. | `scripts/tests/test-audit-skill-descriptions-dedup.sh` (SEMANTIC — it EXECUTES a copy of the real script against a planted fixture and asserts the audited count equals the number of distinct FILES; a grep for `st_ino` would pass on a partial fix). The trap it dodges: on a case-sensitive filesystem the double-count cannot arise naturally, so a naive fixture passes there **even against the broken script**. It therefore creates the duplicate-path condition directly — `ln SKILL.md skill.md` (succeeds on a case-sensitive FS, giving two paths on one inode; fails EEXIST on a case-insensitive one, where the duplication already exists) — plus a fixture-validity guard asserting the naive path-dedup count really does over-count, so the test cannot degrade into a no-op. A fourth fixture skill is stored lowercase-only with **no** uppercase counterpart, pinning that a genuinely-lowercase `skill.md` is still discovered on Linux. Verified against the pre-fix script on BOTH filesystem kinds (a real case-sensitive APFS volume via `hdiutil`): 4/4 assertions fail, exit 1. Wired into `.pre-commit-config.yaml` — repo-root `scripts/tests/` is **not** covered by `run-skill-script-tests.sh`'s discovery globs. | issue #2119 |

| `foundryvtt-plugin/templates/` (#2205) shipped real workflow YAML and a `package.json` whose pins nothing in this repo had ever *checked* were reachable by Renovate — the guard scanned `*/skills/**/*.md` only, so a scaffold template could add a pin at any path and no gate would notice. The reported premise ("Renovate's built-in `github-actions` `fileMatch` is anchored at the repo root, so the template's workflows are invisible") was **refuted by measurement**: a `renovate@44.14.3 --platform=local --dry-run=extract` run showed all four foundryvtt template files already matched and their deps extracted — the built-in patterns have been `(^\|/)`-prefixed since renovatebot/renovate#25813 (Nov 2023), and `ignorePaths` does not exclude `templates/`. The real gap was narrower and lived elsewhere: `blueprint-plugin/templates/*.workflow.yml` stores workflows **flat**, with no `.github/workflows/` path segment, so 9 pins (`actions/checkout@v6` ×6, `anthropics/claude-code-action@v1` ×3) matched no manager at all. That distinction matters because the naive fix — a `customManagers` regex over every template `uses:` — would have double-managed the ~13 already-covered foundryvtt pins under two managers while the actually-unmanaged ones were the ones nobody had looked at. | `renovate.json` widens the **built-in** manager (`"github-actions": { "managerFilePatterns": ["/(^\|/)[^/]+-plugin/templates/.+\\.ya?ml$/"] }`) — `managerFilePatterns` is ADDITIVE for built-in managers, so the defaults are deliberately not restated, and the pattern is plugin-generic rather than keyed to `foundryvtt-plugin` so the next scaffold tree is covered on arrival. Verified by a before/after local extract: exactly `+2` files newly matched, both blueprint templates, deps extracted. No `pinDigests` rule for templates — a scaffold emits a starting point a human reads, so templates keep tag-form refs like the repo's own CI workflows. | `scripts/check-version-pin-coverage.sh` gains a template scan asking the **coverage** question the markdown scan cannot (there the defect is the pin's *shape*; here a perfectly-shaped `@v6` is invisible because of its *path*). Manager patterns are read out of `renovate.json` plus Renovate's own built-in defaults, so the guard tracks the config instead of restating it. New counters `TEMPLATE_FILES_SCANNED=` / `TEMPLATE_PINS_COVERED=`. `scripts/tests/test-check-version-pin-coverage.sh` TEST I weights the NEGATIVE cases hardest, since a guard that flagged every pin under `templates/` would be wrong in the common case: a flat `*.workflow.yml` → ERROR + `--strict` exit 1, while the template's own `.github/workflows/ci.yml`, its `package.json`, and a floating-`@main`-only file are **not** flagged; then widening the fixture's `renovate.json` clears the finding, proving the guard reads the config rather than carrying a hardcoded verdict. Non-vacuity anchors (`TEMPLATE_FILES_SCANNED` == 3, `TEMPLATE_PINS_COVERED` > 0 and strictly increasing) stop the "not flagged" assertions passing against a scan that saw nothing. Mutation-verified against the pre-fix checker: 5 assertions fail, and the three negative controls correctly still pass. | issue #2222 |
| `plugin-compliance-check.sh` `check_skill_when_to_use()` emitted a hard ❌ ERROR (exit 1) for any SKILL.md lacking `## When to Use This Skill`, but that mandate had already been **demoted** on 2026-07-26: `.claude/rules/context-engineering.md` § Supersedes makes the section *recommended where it disambiguates sibling skills*, and `.claude/rules/skill-quality.md` § Required Sections carries the matching banner. The rule and the guard contradicted each other on `main`, so a skill correctly following the new rule failed the lint. Severity was **local-only**: `plugin-pr-checks.yml` wraps the compliance run in `\|\| true`, so CI never saw the exit code and the block bit `pre-commit` / hand runs — a developer-friction bug, not a CI wedge, and therefore invisible to every green PR. | The check was never updated when the rule was demoted. Fix: the missing-section and heading-without-a-table branches now `recommendations+=("⚠️ …")` / `has_warnings=true` and the function returns 1 (WARN) instead of 2 (ERROR) — matching `check_skill_size()`'s existing recommendation path, so the plugin's When-to-Use column degrades to ⚠️ and the run exits 0. | `scripts/tests/test-plugin-compliance-check.sh` — the script's first general self-test (only two narrow siblings existed: `test-plugin-compliance-skill-size.sh`, `test-plugin-compliance-frontmatter-dupkey.sh`). SEMANTIC, not syntactic: the pre-fix script *also* printed a line naming the heading, so a string check proves nothing — the load-bearing assertions are **exit 0** and the **absence of any ❌**. It builds a throwaway repo root (the script `cd`s to `$(dirname "$0")/..` and resolves `marketplace.json` / `release-please-config.json` / `.release-please-manifest.json` from there, so a bare `/tmp` fixture can never reach exit 0) and runs a copy of the real script. Guard integrity: a baseline fixture WITH the section must already exit 0 (else the exit-0 assertion proves nothing), and a heading-without-a-table case must still be *detected* — so the change cannot degrade into the check silently not running. Verified against the pre-fix script: 8 of 12 assertions fail, exit 1. Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml` (the `\|\| true` wrapper means the self-test is CI's only signal here). | issue #2141 |

| `scripts/check-loop-integrity.sh` guarded **nothing on the file that matters most**. It pins five files; the fifth (script line 74) is `agent-patterns-plugin/skills/execution-grounded-review/SKILL.md` — the Pillar-1 oracle that `.claude/rules/loop-integrity.md` makes other looping skills delegate their stop condition to. But the `.pre-commit-config.yaml` hook's `files:` regex listed only **four** of those five paths, omitting `execution-grounded-review/SKILL\.md`, so editing that skill alone never triggered the guard; and the guard appeared **nowhere** in `.github/workflows/plugin-pr-checks.yml`, so no CI surface ran it at all. Net effect: a bulk edit "tightening" the oracle skill could drop its `loop-integrity.md` cross-reference and both gates would stay silent. The hook's mere existence made this invisible — and `.claude/rules/regression-testing.md` compounded it by describing the guard as "wired into `.pre-commit-config.yaml`", which reads as fully wired. Independently flagged as check 4 of #2164. | The `files:` regex was written from the four originally-pinned paths and never extended when `EXEC_REVIEW` was added to the script — the same class as any allowlist that drifts from the code it mirrors — and no CI step was ever added for either the guard or its self-test. | Added `execution-grounded-review/SKILL\.md` to the hook's `files:` regex so it now covers every path the script asserts on, and wired the self-test + `--strict` run as **blocking** steps in `plugin-pr-checks.yml` (this repo wraps the `plugin-compliance-check.sh` run in `\|\| true`, so a guard's own step is CI's only real signal). **No new test was written**: `scripts/tests/test-check-loop-integrity.sh` case 4b already empties the `execution-grounded-review` fixture and asserts `ISSUE_COUNT=1`, so the guard's *firing* on that path was already proven — only the *trigger* was missing, which is a wiring defect, not a detection one. | Refs #2164, Refs #2174 |

| `agent-patterns-plugin/skills/parallel-agent-dispatch` carried its whole sidecar in one `REFERENCE.md` — the corpus had **129** single-sidecar skills and **zero** `references/` directories, so a run that needed one salvage routine loaded every table in the file. `.claude/rules/context-engineering.md` (C3, judged 2.9) asks for the multi-file shape instead. The hazard in splitting it: this skill is the most heavily pinned in the repo (**23** literals in `check-agent-failure-contract.sh` on SKILL.md, **9** on REFERENCE.md, plus 2 in `plugin-compliance-check.sh` for #1549), and a split that moves a pinned literal out from under its guard breaks the build — while a split that leaves the guard pointing at a now-thin index would keep passing while asserting nothing. | The sidecar was split into `references/{dispatch-contract,brief-templates,failure-recovery,worktree-hazards}.md` **by the consumer path**, not by size, and `REFERENCE.md` became a thin index. Content was moved verbatim (line-range extraction, no prose rewritten) so every pinned literal survives on its original single line — the `-qF` greps are line-scoped, and a re-wrap that split `WIP at checkpoints` across a newline is exactly how #2174's extraction nearly broke. The 9 REFERENCE.md pins were **repointed** at the file that now owns each one in the same commit. | `scripts/check-agent-failure-contract.sh` — the #1424 (`9:1`/`is_error`/`30%`), #1491 (`WIP salvage before re-dispatch`), #1692, #1838, and #1601 markers now assert against `references/*.md`; a NEW index-integrity loop requires `REFERENCE.md` to still link all four (a reference file nothing links to is unreachable from the skill even though it exists). `scripts/tests/test-check-agent-failure-contract.sh` gains guards **K** (strip `9:1` from `failure-recovery.md` → exit 1, proving the checker reads the split file rather than having been silently disarmed), **L** (strip the #1838 section from `worktree-hazards.md` → exit 1), and **M** (strip a `references/` row from the index → exit 1); its fixture builder now copies `references/`. `.pre-commit-config.yaml`'s `files:` regex extended to `references/.*\.md` so editing a reference file triggers the guard. Convention recorded in `.claude/rules/skill-quality.md` § `references/`. | issue #2143 |
| `workflow-orchestration-plugin/skills/workflow-verify-before-filing/` had **no `scripts/` directory at all** — Phase 3's rate-limit pacing loop lived as a copy-paste bash block in `REFERENCE.md`, so every wave retyped it and re-derived the ~70s pacing, the 130s backoff, the 4-attempt cap, the continue-past-failure behaviour, and the `filed-urls.txt` manifest slightly differently. The block was also **GitLab-shaped** (`glab issue create -R`, `export GITLAB_HOST`, `/-/work_items/` URLs) while every sibling skill in the plugin is `gh`-shaped, so a GitHub wave had to hand-translate it. The migration plan's rationale for shipping the harness first claimed "Phase 3 is already a deterministic substrate" — that premise is false, and acting on it would have promoted the JUDGEMENT half (verify / search / draft / cold-read) to an executable while leaving the MECHANICAL half as retyped prose: `.claude/rules/offload-to-deterministic-substrate.md` applied backwards, in the same change that cites it. | Extracted the loop to `scripts/file-wave.sh`, generalized to a `gh` \| `glab` dispatch that is **deterministic and reported** (`--forge` flag > `$FILE_WAVE_FORGE` > a set `GITLAB_HOST` > `gh`; the chosen source is emitted as `FORGE_SOURCE=`). No project-path sniffing: `group/project` and `owner/repo` are indistinguishable, so a heuristic would be silently wrong. `REFERENCE.md` keeps only the *rationale* (why 70/130/4) and points at the script; SKILL.md Phase 3 invokes it. | `workflow-orchestration-plugin/skills/workflow-verify-before-filing/scripts/tests/test-file-wave.sh` (auto-discovered via `*/skills/*/scripts/tests/test-*.sh`) runs offline against planted result JSON and STUB `gh`/`glab` binaries on a prepended PATH, always `--pace-seconds 0 --backoff-seconds 0`. It pins the invariants a retyped loop loses: empty input → `STATUS=OK` + exit 0 (the parallel-safe contract); only `disposition == "file"` is filed while gated-out candidates are counted; each forge's OWN argv shape (`gh issue create --repo/--title/--body` vs `glab issue create -R/-t/-d -y`) plus `GITLAB_HOST` reaching glab; the full dispatch precedence chain; the `# ` heading backfilling an empty title while the `<!-- target: … -->` comment never reaches the body; an exhausted create retried exactly `--max-attempts` times, recorded `FAILED`, and the batch CONTINUING (exit 1); the manifest capturing both URLs and failures; `--dry-run` invoking nothing; and an unknown argument exiting 2 rather than being swallowed (the #2057 lesson). | issue #2166 |
| The dynamic-workflow migration introduces this repo's first tracked plugin-side `.js` files, bundled at `*/skills/*/workflows/*.workflow.js` — and **no gate can see them**: `check-agent-model.sh` globs `*/agents/*.md`, `check-workflow-model.sh` globs `.github/workflows/*.yml`, `lint-shell-scripts.sh` globs `*.sh`, and `.pre-commit-config.yaml`'s 62 hooks contain zero JS linter. So a harness calling `agent(prompt, {model:'sonnet'})`, or omitting `effort` entirely, passes every check in the repo — silently violating the always-Opus subagent standard and the cost-economics in `.claude/rules/workflow-model-effort.md`. Same shape as #2005 (blueprint level-3 templates live outside `.github/workflows/`, so a dedicated guard was the only way to reach them). | Added `scripts/check-workflow-js-model.sh` — per `agent()` call: ERROR on a non-opus `opts.model`, a missing `opts.effort` (opus defaults to `high`, forfeiting the only cost lever), or an effort outside `low medium high xhigh max`; WARN on an omitted `model` (issue #2210 scopes session-model inheritance as acceptable, but `agent-development.md` § "Model Selection for Agents" wants an explicit opus, so the drift is surfaced without failing) or a non-literal expression value. Plus the checkable half of `.claude/rules/workflow-vs-skill.md` § Layout: the `<purpose>.workflow.js` name, and reachability from a sibling `## Workflow harness (template)` section that names the file ("an orphan `.js` is dead weight") — with the two extra `#1868` / finalise-stage-only clauses required when the harness dispatches `isolation:'worktree'` agents. Parsing is a comment- and string-masking scan (python3), not a raw regex, so `agent(…, {model:'sonnet'})` inside a prompt template or a `//` comment is correctly not a call site. Discovery prunes `.claude/worktrees/` (#1492/#1548) and an unknown argument exits 2 rather than being swallowed (#2057). **Zero `.js` files is the repo's current state and reports STATUS=OK / exit 0** — a guard that errors on an empty corpus is broken. | `scripts/tests/test-check-workflow-js-model.sh` (40 assertions) EXECUTES the guard against planted fixture trees — the corpus is empty, so every positive case is planted. Pins: empty corpus OK/exit 0 (plain AND `--strict`); missing effort, non-opus `sonnet`, invalid tier, orphan `.js`, and a worktree template missing its clauses all ERROR with `--strict` exit 1 while a plain run still exits 0; an omitted model is WARN-only and exits 0 even under `--strict`; an unknown flag exits 2 with usage and scans nothing; a `.claude/worktrees/` clone is pruned, not double-counted, and its path never leaks. **Guard integrity** (so the suite cannot degrade to a no-op): the compliant fixture must report `AGENT_CALLS=1` — otherwise every "no issues" assertion is vacuous — and a fixture whose only other `agent(…, {model:'sonnet'})` occurrences sit in a comment and a template literal must still report exactly one call and STATUS=OK. Verified by mutation: dropping the `missing_effort` branch fails 4 assertions, removing the worktrees prune fails 1, and scanning raw source instead of the masked copy fails 3. Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml` (self-test + `--strict` run). | issue #2210 |
| `scripts/check-workflow-js-model.sh` (#2210) raised `non_opus_model` as a hard ERROR on **every** non-opus `agent()` call with **no exception seam** — while `~/.claude/rules/agent-and-tool-selection.md` § "Sanctioned exception: cold-read gates run on Haiku" carves out exactly one: the cold-read reader is the **measurement instrument**, not a delegate ("a stronger model would answer a different, easier question — do not 'fix' haiku cold readers to Opus"). Forcing it to opus destroys what it measures. Its sibling `check-agent-model.sh` had reserved an `AGENT_MODEL_ALLOWLIST` seam for precisely this "future measurement-instrument" case; the seam was never carried across, so the guard was a live blocker on #2168 — `docs/plans/dynamic-workflow-migration.md` lines 81/86 are two such cold-read `agent()` calls (labels `coldread:` / `recoldread:`, `model:'haiku'`) that would fail the moment the template became a real file. | Keyed the exemption on the CALL's `label`, not on the file: a harness holds exactly one cold-read stage, so a file-granular allowlist (the right shape for `check-agent-model.sh`, whose unit is a whole agent `.md`) would silently also permit a `sonnet` planner sitting beside the reader in the same harness. A call is exempt only when BOTH its `opts.label` starts with `coldread`/`recoldread` (anchored, case-insensitive — the label must DECLARE the role, so `"not-a-coldread"` does not qualify) AND `opts.model` is the literal `haiku` — so a mislabelled `sonnet` still ERRORs, and a mislabel can buy at most the one documented model. Exempted calls also skip `missing_effort` (haiku supports no `effort` at all per `workflow-model-effort.md`, so requiring one makes a compliant cold reader unwritable) but a *present* effort is still tiered. The parser gained a `label_text()` that cuts a template literal at its first `${` interpolation, so the sketch's interpolated label is read as the static prefix `coldread:`. Never silent: `EXEMPTED_CALLS=` is emitted **even at 0** and each exemption is itemised under an `EXEMPTIONS:` block. `check-agent-model.sh`'s header ("nothing is exempt today") updated — still true of agent FILES, now cross-referencing the call-granular sibling seam. | `scripts/tests/test-check-workflow-js-model.sh` block **L** (28 new assertions, 68 total) weights the NARROWNESS cases hardest, since the risk of a carve-out is that it becomes a bypass: L1 replays the **verbatim** #2168 sketch (template-literal label, no `effort`) → `STATUS=OK`, both calls parsed AND both exempted; **L2** puts a `sonnet` planner in the SAME file as the cold reader and asserts it still ERRORs with `EXEMPTED_CALLS=1` — exactly what a file-granular allowlist would have silently permitted; **L3** a `sonnet` call labelling itself `coldread:` still ERRORs; **L4** a `haiku` call with no cold-read label still ERRORs; **L5** `"not-a-coldread"` does not qualify; **L6** an exempted call with `effort:'turbo'` still raises `invalid_effort`. Verified by mutation: the pre-fix guard fails **17** of them; dropping the `haiku` requirement fails L3; dropping the label requirement fails L4+L5; making the counter conditional-on-non-zero fails the always-emitted assertion. | issue #2216 |

| `scripts/check-version-pin-coverage.sh` walked the **gitignored** `dist/` rulesync build output, so a stale local export hard-ERRORed every local `git commit` touching a skill file — `STATUS=ERROR ISSUE_COUNT=29` with 16 of the findings `dist/` duplicates of pins already counted against their real sources (e.g. `dist/opencode/skills/configure-surface/SKILL.md` still carrying the pre-#2175 `Connorrmcd6/surface` step with no `version:` input, while the real source was already fixed). Invisible to CI by construction: `dist/` is never committed, so no PR can catch it and no green build indicates health — local-only friction, the same shape as #2141. A finding there is also unactionable: the fix site is always the source skill and the next `just export-opencode` overwrites it. It inflated every counter too — `FILES_SCANNED=1111` against ~589 real skill files. | The `find` at `scripts/check-version-pin-coverage.sh` pruned `.claude/worktrees/` (#1492) but not `dist/` — the same class of defect (a walk descending into a copy of the repo), just a *generated* copy rather than a *cloned* one. The sibling `scripts/lint-context-commands.sh` already excluded it via `--exclude-dir='dist'`. Fix: add `-path '*/dist/*' -prune -o` alongside the existing worktrees prune. Measured on the real repo: `FILES_SCANNED` 1111 → 589, `STATUS` ERROR → WARN, `ISSUE_COUNT` 29 → 14, and zero `dist/` paths in the report. | `scripts/tests/test-check-version-pin-coverage.sh` "TEST H: dist/ build output is pruned, not scanned (#2214)" — modelled on TEST F (#1492): copies the fixture's defective SKILL.md under `dist/opencode/skills/…` and asserts `FILES_SCANNED` is unchanged, no extra ERROR appears, and no `dist/` path leaks into the report. **Guard integrity** (without it the three assertions above would also pass with the whole check disabled): one distinct defective pin is planted at BOTH a real path and a `dist/` path, and the real one must still be flagged **exactly once**, with `FILES_SCANNED` growing by exactly 1. Verified by mutation — 6 of the 7 new assertions fail against the pre-fix script, while the guard-integrity anchor passes on both. | issue #2214 |
| A generator's deferred follow-ups were tracked as a **printed note in the generator's stdout**, which is not a gate. `comfyui-node-scaffold`'s finishing-pass audit (#1877) enumerated the rasterize step correctly — and the step was still skipped on four packs, because nothing downstream had a stake in it: the audit returned `None`, `main()` returned 0 unconditionally, it could not be re-run against an existing pack, and it died with the *generator's* session while the follow-up is performed later, in the *generated* repo, usually in a different session. Evidence that this is deferral, not amnesia: `comfyui-output-swap`'s agent read the audit, wrote "**Finishing pass** (tracked, not blocking)" in its closing report, and the PNGs landed **31 hours later** only after the user asked. `comfyui-touch-manager` published with `Icon = ""` and no `Banner` key for **5 days** (8 more packs, git-only evidence). Decisive gap: the emitted `tests/test_publish_hygiene.py` listed `icon.png`/`banner.png` in `EXPECTED_RUNTIME` — an **allowlist of what may ship** — while nothing asserted they **exist**, so a pack whose `[tool.comfy] Icon` resolves to a 404 passed lint, typecheck, build, both test suites, and the registry security scan. A checklist artifact (JSON, feature-tracker, or TodoWrite) was **rejected**: the list already existed three times (printed audit, the pack's committed `RELEASE-CHECKLIST.md`, the SKILL table), and one written *and graded* by the same agent is the self-judged stop condition `.claude/rules/loop-integrity.md` names. | The fix is a gate **in the generated repo**, not a list in the generator. (1) `test_registry_display_assets_present` added to the emitted `tests/test_publish_hygiene.py`: parses `[tool.comfy]` `Icon`/`Banner`, asserts each named PNG is a **git-tracked** file and its source SVG no longer carries `PLACEHOLDER-GLYPH`; the failure message names `just assets`. Mirrors the `git diff --exit-code -- web/dist` CI step already present for the other derived artifact, and needs no new job — `tests/` already runs via `uv run --group dev pytest -v`. `pathspec` moved to a lazy import inside `_ignore_spec()` so the module is importable (and the new test executable) without the dev group. (2) `print_finishing_pass_audit` split into `finishing_pass_findings()` + two thin callers — the post-scaffold print and a new `scaffold.py --verify <pack-dir>` emitting `structured-script-output.md` (`ICON_PNG=`, `PLACEHOLDER_GLYPH=`, `SCREENSHOTS=`, `SIBLING_GAP_COUNT=`, `STATUS=`, `ISSUE_COUNT=`; exit 1 on ERROR). One function means emit and check cannot disagree. (3) **Severity graded** — a flat `[ ]` list made a publish-blocking gap look identical to a deferrable one, which is precisely how "tracked, not blocking" became defensible: missing PNG / surviving placeholder = ERROR, screenshots + README prose = WARN. (4) `/comfy-node` Phase 6 runs `--verify` and reads `STATUS=` before hand-back (ERROR is never a follow-up item; WARNs go to `project:comfyui-nodes`), and the pack's `RELEASE-CHECKLIST.md` gains the finishing-pass items rather than a competing JSON sibling. | `comfyui-plugin/skills/comfyui-node-scaffold/scripts/tests/test-finishing-pass.sh` (29 assertions) — **EXECUTES** the emitted pack test rather than grepping for it, the #1417 lesson (a syntactic pin on a semantic property let a broken fix ship). It scaffolds a pack, `git init` + `git add`, then runs `test_registry_display_assets_present` standalone and asserts it **FAILS** while `icon.png` is missing (naming `just assets`), **PASSES** once both PNGs are committed, **FAILS again** when `PLACEHOLDER-GLYPH` is re-armed even though the PNGs exist, and **FAILS** on a PNG present on disk but absent from the index. Plus `--verify` exit 1 + `STATUS=ERROR` on an unfinished pack and — the guard-integrity half, without which a gate hardwired to ERROR would pass — exit 0 + `ICON_PNG=present` once finished. Test 7 was tightened from "the audit prints" to also assert it **grades** (ERROR/WARN present) and points at `--verify`. Verified by mutation: removing the emitted pack test fails 3 assertions, neutering `--verify` fails 5. Git env (`GIT_DIR`/`GIT_WORK_TREE`/…) is unset at the top per #1745 so the sandbox git ops cannot reach the shared checkout. | scaffold-finishing-pass-gate |
| The two gates shipped by the finishing-pass fix (#1877 → PR #2226) **disagreed with each other**, so one pack got opposite verdicts from CI and the audit. `finishing_pass_findings()` graded an undeclared `[tool.comfy]` `Icon`/`Banner` as **ERROR**, but the emitted `tests/test_publish_hygiene.py` asserted only that the assets dict was **non-empty** ("at least one declared") and then validated only the keys that WERE declared. A pack declaring `Icon` and no `Banner` therefore passed pack-CI green while `scaffold.py --verify` reported `STATUS=ERROR` — and `comfyui-image-browser` sat in exactly that gap (no `Banner` key, no `banner.svg`, no `banner.png`, no `just assets` recipe). A one-sided gate is worse than either verdict alone: the green CI is the signal a maintainer acts on, so the audit's ERROR reads as the audit being wrong. | The strict reading is correct — the registry renders an undeclared banner as blank — so the pytest was tightened to match the audit rather than the audit relaxed to match the pytest: `test_registry_display_assets_present` now seeds `problems` with one entry per **missing** key (`Icon`, `Banner`), each naming the raw-GitHub URL to point it at and `just assets`, before the existing tracked-file and `PLACEHOLDER-GLYPH` checks run over whatever WAS declared. Both of those retain the `if source.exists()` guard, so the 8 fleet packs whose `banner.png` has no SVG source (older `registry_banner_compose.py` provenance) still pass. Known cost, accepted deliberately: `comfyui-image-browser` goes CI-red until banner artwork is drawn for it, making that artwork a prerequisite of its test sync rather than a follow-up. | `comfyui-plugin/skills/comfyui-node-scaffold/scripts/tests/test-finishing-pass.sh` block 12 — the **agreement** proof, and it EXECUTES both gates rather than grepping either (the #1417 lesson). It scaffolds a pack, finishes it (both PNGs rasterized + `git add`-ed), then drops **only** the `Banner = ` line from `pyproject.toml`, leaving every PNG present and tracked so the undeclared key is the sole variable — and asserts the pack test exits 1 naming `Banner is unset` **and** `--verify` exits 1 with `STATUS=ERROR` + `BANNER_PNG=undeclared`. Three guard-integrity assertions stop it degrading into a no-op: 12a requires the finished pack to be green on **both** gates first (without it, 12b would also pass against a gate hardwired to fail), a fixture-validity check asserts the `Banner` key really was dropped, and `ICON_PNG=present` must survive so "both gates fail" is attributable to the Banner key rather than to general breakage. Verified by mutation — reverting the pytest to the at-least-one-declared form fails exactly the two pack-test assertions while the `--verify` half stays ERROR, which is the divergence itself. | reconcile-finishing-pass-gate |
| `comfy-registry-lifecycle/SKILL.md` documents that **glyph colour encodes the sub-family** — `#ffb02e` (orange) for touch/interaction packs, `#6ba6ff` (blue) for info/gallery packs — and `registry_banner_compose.py` honoured it (`--accent` defaults to `#6ba6ff`). But `scaffold.py`'s `ICON_SVG` and `BANNER_SVG` both **hardcoded orange**, so the generator could not emit the rule its own comment stated: every scaffolded pack started orange regardless of sub-family, and an info/gallery pack had to be recoloured by hand or silently shipped off-spec. The cost landed 2026-07-30: a vector-banner restyle built from the hardcoded-orange template flipped **four** published info/gallery packs (`gallery-loader`, `model-gallery`, `prompt-editor`, `sampler-info`) from blue to orange — orange glow, sweep and tagline against a still-blue icon — erasing the sub-family signal their previous banners carried. Measured: mean pixel went blue-dominant (B>G>R) → orange-dominant (R>G>B) on all four. Nothing caught it, because the split reads as arbitrary drift unless you have found the one sentence in a sibling skill that declares it intentional. | Added `ACCENT_PALETTES` + `apply_accent()` and a `--subfamily {touch,info}` flag threaded through `build_file_map` to BOTH SVGs, so the generator emits the documented rule. Only the six accent tokens are mapped (glow inner/mid, sweep, constellation web, dots, glyph); the tile gradient `#1f1f2a`→`#12121a`, its `#2a2a36` stroke, the `#000000` backdrop and the `#f5f5f7` wordmark are **neutral in both sub-families** and must survive untouched, or the family tile drifts and `just assets`' `346x346+27+27` consistency gate fails. The four affected packs were recoloured back by an accent-only transform that leaves every coordinate, gradient stop, opacity, transform, font size and text position identical — so each blue banner is its orange twin's exact geometric mirror — and excludes the icon motif (the pack's own artwork) entirely. | `comfyui-plugin/skills/comfyui-node-scaffold/scripts/tests/test-finishing-pass.sh` block 13 EXECUTES the generator for BOTH sub-families and asserts `--subfamily info` emits `#6ba6ff` in `icon.svg` **and** `banner.svg` with **no** orange token surviving in either. Two guard-integrity halves stop it degrading: the default `touch` scaffold must still emit `#ffb02e` (otherwise the assertions would pass against a template recoloured blue wholesale), and each neutral token's count must be **identical across the two sub-families** (otherwise an over-broad recolour that ate the tile or wordmark would pass). Verified by mutation — reverting `apply_accent` to the pre-fix no-op fails exactly 4 assertions. | scaffold-subfamily-accent |
| The fleet-drift checker compared only files the scaffold emits **context-invariantly**, so `icon.svg` / `banner.svg` — pack-specific artwork — were outside every policy bucket and **nothing checked them at all**. That is exactly where a real regression lived: a vector-banner restyle flipped four info/gallery packs' banners to orange while their icons stayed blue, and every managed-file check stayed green because no file the checker compares had changed. The deeper problem is that a checker which only diffs against the template **structurally cannot** judge artwork: a deliberate per-pack divergence (a blue pack) and a drifted one are the same diff, so the honest default becomes "ignore artwork" — and the documented sub-family rule (`comfy-registry-lifecycle`: `#ffb02e` touch/interaction, `#6ba6ff` info/gallery) goes unenforced forever. | Added a `[subfamily]` table to `fleet-policy.toml` declaring each pack's family, and a check asserting that `icon.svg` **and** `banner.svg` both carry the declared family's primary accent. Declaring the intent is what makes artwork checkable — the checker now compares each pack against **its own stated design**, not against the template. Only the PRIMARY glyph/tagline accent decides the family: `comfy-registry-lifecycle` permits secondary accents (`#ffd866`, `#6bff8e`) to "appear sparingly" in either family and two packs really use them, so matching any blue/orange token would flag legitimate art. The palette is read from the scaffold's `ACCENT_PALETTES` so there is one definition. A pack absent from `[subfamily]` is an ERROR (`UNCLASSIFIED_PACK`), mirroring `UNCLASSIFIED_TEMPLATE`. | `comfyui-plugin/skills/comfyui-node-scaffold/scripts/tests/test-fleet-drift.sh` cases 11-13 (58 assertions total). Case 11 replays the exact regression — a pack declared `info` with both SVGs blue raises nothing; recolouring **only** its banner back to orange is caught as `banner.svg\|touch(declared=info)` with exit 1, while the still-correct `icon.svg` and the untouched touch pack are **not** reported. Case 12 is the false-positive guard: a pack carrying `#6bff8e`/`#ffd866` secondaries must raise no accent row (asserted per-pack, NOT via exit code — earlier cases deliberately leave managed drift, so the exit code is not attributable). Case 13 pins manifest completeness. The suite also had to start declaring its own scaffolded fixtures, since an undeclared pack is now correctly an ERROR. Mutation-verified: never-report fails 2, matching on a secondary token instead of the primary fails 14, dropping the undeclared-pack guard fails 2. | fleet-subfamily-accent-check |
| The #1877 finishing-pass gate was fixed for `comfyui-node-scaffold` only, never generalized — `foundryvtt-module-scaffold` emitted a repo whose manifest invariants **nothing checks**. A FoundryVTT module is installed from a GitHub release and loaded out of `dist/`, so five hand-maintained strings must agree: `module.json` `id`, the `const MODULE_ID` in `vite.config.ts` (which pins the Vite library output filename), the `MODULE_ID` exported by `src/constants.ts` (the settings/i18n namespace), the `esmodules` entry, and the zip basename the release job uploads. The generated repo's CI is blind to every one of them: `biome check`, `tsc --noEmit`, `vite build`, `gitleaks`, and the smoke suite (2 assertions: a MODULE_ID literal and `registerSettings()` not throwing) all stay **green** while `esmodules` names a file the build never emits — the only symptom is a module Foundry loads to nothing, with no error anywhere. Same for an install URL pinned to a tag instead of `releases/latest/download/` (installed copies never see an update) or a `download` URL whose basename drifts from the release job's `zip -r ../<id>.zip` (every install 404s). Correct by construction at scaffold time; broken later, in the generated repo, in another session — which is exactly why a note in the generator is not a gate. Deliberately **not** gated: `module.json` `$.version` vs `package.json` — `release-please-config.json` already carries an `extra-files` entry (`type: json`, `path: module.json`, `jsonpath: $.version`), so one release PR bumps both; a second gate would double-gate a working mechanism. | Two callers over one findings function, mirroring the comfyui fix. (1) The **primary gate is in the generated repo**: an emitted `tests/manifest.test.ts` that the repo's own `test` CI job already runs on every PR (no new job), asserting the id agrees across the manifest / Vite config / `constants.ts`, that `esmodules` equals `[\`${id}.mjs\`]`, that declared `styles`/`languages` paths exist, that the manifest URL ends with `/releases/latest/download/module.json`, and that the `download` basename equals the zip the release workflow uploads. It carries **no template placeholders** — every value is read out of the repo at run time — so it survives a rename and is byte-identical in the cargo-generate port. (2) `finishing_pass_findings()` + `scaffold.py --verify <module-dir>` emitting the `structured-script-output.md` contract (`MODULE_ID_MATCH=`, `ESMODULES=`, `MANIFEST_URL=`, `DOWNLOAD_URL=`, `RELEASE_ZIP=`, `DECLARED_ASSETS=`, `LOCKFILE=`, `ISSUE_COUNT=`, `STATUS=`; exit 1 on ERROR), plus a post-scaffold print through the same function so the note and the verdict cannot disagree. **Severity graded**: ERROR = Foundry cannot install/load/update the published module; WARN = it loads but something is unfinished (a declared asset that 404s, no `bun.lock` yet — so a fresh scaffold is WARN, not ERROR). `/foundryvtt-module` Phase 6 reads `STATUS=` before hand-back, matching `/comfy-node`. | `foundryvtt-plugin/skills/foundryvtt-module-scaffold/scripts/tests/test-manifest-invariants.sh` (35 assertions) — **EXECUTES** the generator and `--verify` against real trees rather than grepping scaffold.py for a check's text (the #1417 lesson). Each defect is applied to a fresh copy: esmodules drift, a rename in `vite.config.ts`, a rename in `src/constants.ts`, a tag-pinned manifest URL, a `download` basename drift, and a renamed release zip must each report ERROR with exit 1; a missing declared style must stay WARN/exit 0 (installable). The **guard-integrity half** — without which a gate hardwired to ERROR would pass every case above — asserts a fresh scaffold is ERROR-free (`STATUS=WARN`, only `LOCKFILE=absent`) and that all three variants reach `STATUS=OK` / `ISSUE_COUNT=0` / exit 0 once `bun.lock` exists. It also diffs the emitted test against the cargo-generate template's copy, which `test-template-parity.sh` cannot see while `cargo-generate` is unavailable (it SKIPs — and it is absent from CI too). Verified by mutation: neutering the esmodules check fails 3 assertions, the id-agreement check 4, the release-zip and manifest-URL checks 2 each, dropping the emitted test 2, forcing exit 0 fails 7, and hardwiring `STATUS=ERROR` fails 10. The emitted test's own teeth were verified by running `bun run test` in an installed module against all six defects (red each time, green on the restored tree). Git env (`GIT_DIR`/`GIT_WORK_TREE`/…) is unset at the top per #1745. | scaffold-fix-backport-foundryvtt |
| `scaffold.py` generates 13 ComfyUI pack repos, and **nothing detected drift between them and the template, or between the packs themselves**. `tests/test_publish_hygiene.py` was intended to stay byte-identical fleet-wide with **no gate at all**, and a stale `just assets` recipe silently distorted banner artwork in one pack for months. Measured 2026-07-30: **21** templates render context-invariantly (so they are mechanically byte-comparable), and **all 13 packs** drift on some of them — `release-please.yml` 13/13, `RELEASE-CHECKLIST.md` 13/13, `clear-autorelease-labels.yml` 10, `publish.yml` 9, `.gitattributes` 9, `.pre-commit-config.yaml` 8, `.gitignore` 8, `biome.json` 7, `vitest.config.js` 4, plus the `justfile` `Assets` block absent or stale in 9. The load-bearing finding is that **drift is bidirectional**, so the obvious fix (sync template → pack) is a silent-revert bug: every pack is AHEAD on `release-please.yml` (`runs-on: ubuntu-slim`, a deliberate 3×-cheaper runner, + `release-please-action@v5` vs the template's `ubuntu-latest`/`@v4`); the TEMPLATE is ahead on `RELEASE-CHECKLIST.md`; Renovate independently pushes packs ahead even on config files (`comfyui-touch-manager` on `actions/checkout@v7`, `comfyui-touch-tooltips` on biome 2.4.16); and `tests/js/__mocks__/app.js` is **pack-owned** (each pack extends the mock — not drift at all). | A **reporting** checker plus a declared sync policy — never an auto-apply. `comfyui-plugin/skills/comfyui-node-scaffold/fleet-policy.toml` declares per-file authority: `managed` (byte-identity, ERROR), `seed` (never compared), `shared` (packs compared to EACH OTHER — the fleet leads, a minority pack is a WARN, and a fleet consensus differing from the template is a back-port signal), and `block` (a named `##########` section of a placeholder-carrying template, for the justfile's `Assets` recipe — the original bug — since whole-file comparison there is too noisy). `scripts/check-fleet-drift.py` **derives** the comparable set by importing `scaffold.py` and rendering `build_file_map` across all four variants × two pack names, keeping paths whose body is identical everywhere — never a hand-copied list, which would drift exactly like the thing it audits; a derived template with no policy entry is itself an ERROR, so the manifest cannot silently fall behind the scaffold. Emits the `structured-script-output.md` contract (`FILES_COMPARED=`, `MANAGED_DRIFT=`, `BACKPORT=`, `SHARED_MINORITY=`, `UNCLASSIFIED_TEMPLATE=`, `ISSUE_COUNT=`, `STATUS=`; exit 0 on OK/WARN, 1 on ERROR, **2 on an unknown argument** per #2057), takes `--fleet-root`/`--pack`, prunes `.claude/worktrees/`+`dist/` (#1492/#1548/#2214), and reports `STATUS=OK`/exit 0 on a missing or empty fleet root (a checker that errors on an empty corpus gets disabled). The weekly `.github/workflows/fleet-drift-audit.yml` clones the fleet and opens ONE issue via `--issue-body`, emitting nothing when clean. | `comfyui-plugin/skills/comfyui-node-scaffold/scripts/tests/test-fleet-drift.sh` (45 assertions) — SEMANTIC: it **executes** the checker against fixture packs scaffolded by the real generator, then mutated (the #1417 lesson: a syntactic pin on a semantic property let a broken fix ship). Pins: a `managed` mutation → ERROR naming that pack AND file with the sibling untouched; a `seed` mutation (`app.js`, `ci.yml`) → **not reported at all** (the guard that stops the checker becoming noise); a `shared` file changed identically fleet-wide → a `BACKPORT` signal at WARN/exit 0, never managed drift and never a pack's fault; changed in ONE pack → exactly one `SHARED_MINORITY`; a stale justfile `Assets` block → `BLOCK_DRIFT`; an unknown flag → exit 2 with usage and empty stdout; a missing/empty fleet root → OK/exit 0; and a policy manifest with an entry removed → `UNCLASSIFIED_TEMPLATE` ERROR. **Guard integrity** (without it every "no issues" assertion is vacuous): the clean run must report `PACK_COUNT=2`, `TEMPLATE_PATHS>0`, and `FILES_COMPARED` **exactly equal to** `(managed+shared+block) × packs` — a checker that skipped the managed/shared loop but still ran the block loop would clear a bare `> 0` bar. Verified by mutation, 9 neutered rules, **all killed**: dropping the managed compare fails 4 assertions, treating `seed` as managed 8, treating `shared` as managed 4, dropping the back-port signal 3, dropping the manifest-completeness check 4, dropping the block compare 3, comparing nothing 9, always-exit-0 3, and erroring on an empty corpus 4. | comfyui-fleet-drift-checker |
| `session-wrap` told the agent to **create a taskwarrior task for an open PR** — its LOG-IT filter led with "PR open and waiting (capture URL + gate)", its destinations row said taskwarrior gets "add tasks for surfaced threads" (unbounded), and `REFERENCE.md` justified the PR row with *"Won't resurface by itself"*. That justification was **false**: `session-survey.sh` emits a `PRS` section carrying every open PR with `PR_n_URL` + `PR_n_STALE_DAYS`, and `session-spinup`'s SURFACE filter is *required* to show "open PR from a recent branch (especially review/CI-stale)". So the task was created at wrap, replayed at spinup **beside the PR it duplicated**, and then closed by `taskwarrior-plugin:task-reconcile` when the PR merged — a full create/surface/close cycle carrying no information the PR didn't. The same shape applied to assigned issues (`GITHUB_DRIFT` already surfaces assigned-but-untracked ones) and to journal todos (the `- [ ]` example was literally "Nudge … PR #1607 reviewers"). It also contradicted the user-global `taskwarrior-tracking.md` precondition "**not** already tracked somewhere durable (GitHub issue/PR, calendar) — annotate/link the existing tracker instead of duplicating". | Added a `### Don't duplicate an existing tracker` subsection to `session-wrap`: an open PR/issue **is its own tracker**, with a 4-row situation table routing each case (PR open + nothing to say → nothing; PR open + an existing task covers the work → **annotate the existing task** with the URL, which is also what lets `task-reconcile` auto-close it, never a second task; assigned issue with no task → nothing; a manual step / deferred decision / blocker the PR body can't hold → task, or a post-merge GitHub issue linked from the PR). The LOG-IT list drops the PR entry and DO-NOT-LOG gains "an open PR or assigned issue as its own task"; the litmus became two-clause ("…and is there no open PR or issue already carrying it?"); the Categorise table gained an "Already tracked by an open PR / assigned issue" row; `session-end`'s inline taskwarrior-sync pass and the plugin README carry the same constraint. The coupling is documented **both** ways — `session-spinup/REFERENCE.md` now records that its PR/issue surfacing is load-bearing for wrap's rule, so the two cannot drift apart silently. | `plugin-compliance-check.sh` `check_skill_body()` — for `session-wrap`, asserts three tokens: `is its own tracker` (the principle), `annotate the existing task` (the positive alternative — without it the rule degrades to "drop it on the floor" and the reconcile linkage is lost), and `GITHUB_DRIFT` (one of the two digest sections that make the principle *true*, so the claim stays falsifiable rather than a bare assertion). Verified by mutation: replacing any one token individually produces the ❌ and the restored file is clean. | session-wrap-redundant-pr-tasks |
| `git-plugin/agents/git-ops.md` shipped `git branch --merged main | grep -v 'main\|master' | xargs git branch -d` in an **always-on agent prompt** — a delete loop keyed on a signal known to be wrong. `git branch --merged` is an *ancestry* check, so on a squash-merge repo (the release-please default) a squash collapses a branch into one fresh-SHA commit and the branch's own commits are never ancestors: a fully-landed branch reads as **unmerged**. This is the identical defect already fixed for `git-plugin:deadbranch` (#1869), which measured only 4 of 28 actually-merged branches flagged safe — but the fix was applied to the *skill* only and never swept to the *agent*, where it sits in the system prompt of every `git-ops` dispatch. `-d` refuses the unsafe deletes today, so nothing errors; the classification is still wrong and the usual "fix" for the refusals is `-D`, which deletes real work. | Replaced the Branch Cleanup block with `just -g branch-audit` (the encoded recipe) plus the two squash-surviving signals in authority order — `gh pr list --state all --head <branch> --json state,mergedAt` (a MERGED PR is authoritative) and `git cherry main <branch>` (`-` = patch already upstream) — and a "never delete on `--merged`" caveat naming why. `git remote prune origin` is unaffected and kept. | `scripts/check-agent-tool-selection.sh` extended with the two shape rules above (already pre-commit-wired and blocking, and it already walks `*/agents/*.md`, so no new wiring). Verified by **mutation**, not just on the fixed tree: the guard is run against the *verbatim pre-fix* `git-ops.md` (`git show HEAD:…`) and must exit 1 naming line 161, while the fixed file — whose prose still cites `git branch --merged` to teach the hazard — must exit 0, proving the rule discriminates instruction from explanation. Allowlist prefix-matching is covered by a directory-prefix case of the same shape as the `experiments/claude-probe/` seed. | issue #1869 (agent-side sweep) |
| `scripts/infra-compliance-check.sh` flagged every skill granting a bare unscoped `Bash` in `allowed-tools` as a 🟡 "Broad Bash permission" — the **house standard**, not a finding — so the audit scored the repo down for following its own convention: **191 of 408** SKILL.md files flagged, Security Posture **1/15** (26/217 clean), overall 78/100. The ratio moved the wrong way as the corpus grew more compliant, because the loop incremented `security_total` once per offender but **never** incremented `security_pass`. The same block's `find . -path '*/skills/*'` had no `.claude/worktrees/` or `dist/` prune (the #1492 / #1548 / #2214 class): each agent worktree is a full repo clone, so in a 63-worktree checkout it walked ~26,000 files instead of ~408 and the whole script **timed out past 120s**. Separately, neither audit script ended in an explicit `exit`, so the exit code `scheduled-audits.yml` depends on was whatever the last statement happened to return — `blueprint-health-check.sh` ended on a bare `if`. | Deleted the detector block (which was the only unbounded recursive walk in the file — every remaining `find` is `-maxdepth 1` or scoped to one plugin's `skills/` dir) and left a comment naming why it must not be reinstated; measured 0 findings / 15/15 / 92-100 and 12.3s → 5.0s on a clean tree. Added an explicit terminal `exit 0` to both audit scripts, stating the "always exit 0 having emitted a complete report" contract rather than inheriting it. | `scripts/tests/test-audit-scripts-exit.sh` gains a **structural** find-boundedness assertion plus a wall-clock backstop. The structural half is load-bearing and the timing half is not: CI checks out a clean tree with **no worktrees**, so a reintroduced unpruned walk would be fast there and a pure timing bound would pass. Every `find` must carry `-maxdepth`, prune `.claude/worktrees`, or start from a specific subdirectory. Whole-line comments are stripped before extraction so the prose explaining the removed walk (which necessarily quotes it) is not itself a finding. **Guard integrity**: a "has at least one find" assertion stops the check degrading into a vacuous pass. Verified by mutation — splicing the verbatim pre-fix script back in reports `unbounded: find . -path '*/skills/*' …` and fails. | bare-bash-detector-retirement |
| `code-quality-plugin:code-lint` — a skill whose entire job is "run the right linter" — shipped four `{{ if PROJECT_TYPE == "python" }}` / `{{ endif }}` blocks. **Claude Code renders nothing in a skill body**, so all four language branches arrived at the agent verbatim, `PROJECT_TYPE` was never bound anywhere in the skill, and both the language selection and the `${1:-.}` / `${2:+--fix}` shell-parameter substitution were left as an exercise. Silent by construction: the skill loads, every structural lint passes, and the agent quietly picks a branch (or pastes `{{ endif }}` into a shell). A repo-wide sweep found the same defect in **four** more skills — `tools-plugin:deps-install` (7 `PACKAGE_MANAGER` branches), `project-plugin:project-init` (`{{ if $3 == "--github" }}`), `typescript-plugin:bun-add` (inline `{{ if DEV }}` inside the one fenced command), and `testing-plugin:test-analyze` (Handlebars `{{#if ARG2}}` / `{{/if}}`). `.claude/rules/agentic-optimization.md` § Environment Detection was **teaching** the pattern (`{{ if CI }}--frozen-lockfile{{ endif }}`), which is where it propagated from. | All five rewritten to the same shape: an explicit **detection step** the agent performs (marker files / lock files / bound `$ARGUMENTS`) followed by a **lookup table** it reads, with every command written against a named placeholder the Parameters section says the *caller* supplies. The rule's example was replaced with a lookup table plus a real shell conditional (scaffold-fix-backport: fixing the instances without the generator leaves the next author to repeat it). | `scripts/check-unrendered-templates.sh` (+ `scripts/tests/test-check-unrendered-templates.sh`, 58 assertions) — SEMANTIC and **mutation-verified**: eleven neutered rules, all killed. It EXECUTES the guard against the **verbatim** pre-fix bodies (all five reproduced as fixtures, 34 markers reported by name), and weights the false-positive half equally, since a guard that flags legitimate Go/Helm chart templates gets disabled: trim markers / `{{ end }}` / `$.Values` must **not** be flagged, and a `.claude/worktrees/` + `dist/` copy must be pruned rather than double-counted. Two defects were found by the mutation pass and would otherwise have shipped: (1) the Go `$`-rooted skip was written as a bare `$`, which silently swallowed project-init's **shell positional** `{{ if $3 == … }}` — the discriminator is `$.`, not `$`; (2) the Go dot rule was written as `.*`, which also matched the `...` ellipsis and made the ellipsis rule dead code — narrowed to `.[A-Za-z]*`, so TEST G now kills its own mutation; (3) discovery reverted to an absolute scan base makes the guard scan **nothing** from inside a worktree — the exact #2219/#2290 defect, now pinned. The keyword set gained `for`/`endfor` and Handlebars `#each`/`/each` from the concurrent PR #2284, whose narrower guard covered loop forms this one initially missed; Go's `range` is deliberately EXCLUDED (the one loop keyword with four legitimate rendered uses in the corpus), and that exclusion is falsifiable — TEST N carries a **bare** `{{ range $k, $v := … }}` with no trim marker, since the trim-marker forms are skipped structurally whatever the keyword set is and would let `range` back in unnoticed. Guard integrity throughout: the fixed-shape fixture must report `FILES_SCANNED=1` / `FILES_EXEMPT=0` (otherwise every "clean" assertion is vacuous), and TEST F asserts the render-directive exemption is what exempts by feeding the **identical** markers without the declaration and requiring exit 1. | issue #2265 |
| 191 of 408 SKILL.md files grant a bare unscoped `Bash`, and **nothing decided whether that was the standard or drift**: `skill-development.md` prescribed it, `agentic-permissions.md` implied against it, and no lint enforced either — which is what let an external scanner's "CRITICAL" finding land on the repo's own house style, and what let `infra-compliance-check.sh` score the repo down for following it (#2250). | Ratified the standard (a bare `Bash` is correct when the skill's payload is shell — frontmatter can only SUBTRACT from session grants, so narrow patterns there buy no safety `settings.json` isn't already providing) and linted the **opposite** defect. 5 genuinely-unused grants removed (`shadow-mode`, `comfy-flow-control`, `comfy-conditionals`, `comfy-math-strings` — all four had only ASCII **diagrams** in unlabeled fences — and `test-tier-selection`, whose `bash` fence holds only comments and `/slash:commands`), taking grantees 191 → 186 and `ISSUE_COUNT` to 0 with an EMPTY allowlist. | `scripts/tests/test-check-unused-bash-grant.sh` is SEMANTIC (executes the checker against planted fixture trees, never greps the script — the #1417 → #1819 lesson) and was validated by **mutation**: five neutered rules, all killed. Two defects were found by that process and would otherwise have shipped: (1) the markdown parser was resolved against `--project-dir` rather than the script's own location, so in any tree that is not this repo it silently vanished and **every** grantee read as inert — a 191-finding false-positive storm; it now resolves via `BASH_SOURCE` and **fails closed** with `STATUS=ERROR` when the parser is missing. (2) The worktree/`dist` prune test was **vacuous** — the clone was planted at the fixture root, which discovery's `-maxdepth 1` outer find never reaches, so it passed with or without the prune; the fixture now plants inside a plugin directory, where the prune is what actually stops the double count. Guard integrity: positive-control assertions (`BASH_GRANTEES` ≥ 8, `SKILLS_SCANNED` == 10) stop every "not flagged" case passing against a checker that parsed nothing. | unused-bash-grant-lint |
| `feedback-plugin/scripts/friction_parse.py` classified friction events with two regexes loose enough that the weekly friction report's own numbers were misleading. (1) The `tool_error` signature matched a bare `\bnot found\b`, so any payload reporting an absent *thing* — kubectl `Error from server (NotFound): pods "x" not found`, Blender `enum "X" not found` — clustered as a shell/PATH lookup failure: in W32 `error:bash:not-found` was the **3rd-largest signature by raw count** (28 events / 20 sessions / 35.0% same-session repeat) with exactly **1 genuine (3.6%)**, a **4.7× growth** over W31's 6 events — and W31 had already flagged it as a watch-list over-match caveat. (2) `HOOK_BLOCK_RE` matched `exit(?:ed)? (?:with )?(?:code )?2` **anywhere** in tool output, so `ugrep`'s exit-2-on-missing-file landed in `hook_block` → `hook:unclassified`: **6 events = 27% of all `hook_block` events were not hook blocks**. Both defects are silent by construction — a mis-clustered event is well-formed and the report reads as researched. | Anchored `not found` to the shell/PATH forms only (`command not found`, `: not found`) with the matched token normalised back to the historical `not-found` spelling so the signature stays comparable week-over-week; replaced the `exit code 2` alternative with `PreToolUse[^\n]*hook error`, the harness's own wrapper prose. Measured on the same 7d window (262 transcripts, 371 events, unchanged): `not-found` signatures **36 → 2**; `hook:unclassified` **6 → 0**. The hook fix also corrected the *opposite* error — `PreToolUse.*blocked` required the literal word "blocked", so **42** REMINDER-format `PreToolUse:Bash hook error:` events had been misfiled as `tool_error`; `hook_block` is 28 → 64 and **all 64 now contain `PreToolUse`**. | `feedback-plugin/scripts/tests/test_friction_parse.py` — two SEMANTIC tests (`test_not_found_signature_matches_only_shell_forms`, `test_exit_code_2_is_not_a_hook_block`) driving the real parser end-to-end over a new `fixtures/regex_overmatch/` transcript, per the #1417 → #1819 lesson that a syntactic grep for a regex string cements a broken one. Each holds both halves: the two false positives must NOT classify, and `pnpm: command not found` / a real `PreToolUse:Bash hook error:` line MUST still classify — without the positive half a parser that classified nothing would pass. Validated by **mutation** against the verbatim pre-fix parser (`git show HEAD:…`): both fail, reproducing `error:bash:not-found` on the kubectl payload and `hook:unclassified` on the ugrep payload, while the 4 pre-existing tests stay green. Wiring gap closed in the same commit: the `test_friction_*.py` suites matched no discovery glob (`run-skill-script-tests.sh` and `Test: Skill scripts` find only `test-*.sh`), so they ran by hand only — `feedback-plugin/scripts/tests/test-friction-parse.sh` is a shim that runs all three. | friction-parser-overmatch |
| Two PostToolUse cue hooks — `hooks-plugin/hooks/bash-antipatterns-teach.sh` and `blueprint-plugin/hooks/blueprint-structural-cue.sh` — emitted `hookSpecificOutput.updatedToolOutput` as a JSON **string** (`jq -n --arg out …`). The harness validates that field against the **tool's own output schema**, which is an object, so it rejected every emission (`Invalid input: expected object, received string`, `"path": []`) and silently fell back to the original output. Both hooks were **silent no-ops** — 29 discarded outputs over 50 transcripts / 7.5 days. Neither script errored and both ran in ~100ms, so nothing surfaced. The belief was written into a version comment ("Claude Code 2.1.121+") rather than pinned by a test, and **both existing suites pinned the broken shape**: `test-bash-antipatterns-teach.sh` fed a `tool_response` **string** the harness never sends and asserted mere *presence* of `updatedToolOutput`, while `hooks/spec/blueprint_structural_cue_spec.sh` asserted "output should include updatedToolOutput" — and that ShellSpec suite **never ran anywhere** (shellspec is not installed, not in CI, not in any recipe, and `spec/` matches no glob in `run-skill-script-tests.sh`). | Bash has a natural free-text field, so the teach hook now reads `.tool_response` as an **object** and emits `$orig + {stdout: $augmented}` — overwriting exactly one field of the harness's own object and inventing no keys (a union of invented keys would be a fresh rejection under a strict schema, with the same silent-fallback failure mode). Write/Edit has **no** free-text field — `content` is the file's actual content, so a cue banner there would make the model believe the banner was written to disk — so `updatedToolOutput` is the wrong mechanism for that event entirely: the blueprint cue switched to the `{"decision":"block","reason":…}` channel that `code-quality-preflight-cue.sh` already uses successfully for the same event, with `continueOnBlock: true` added to **both** `hooks.json` registrations. That config half is not optional: `decision:block` without it **ends the turn**, converting a silent no-op into an active regression on the first structural edit of every session. | `hooks-plugin/hooks/test-bash-antipatterns-teach.sh` (extended) and `blueprint-plugin/hooks/test-blueprint-structural-cue.sh` (new, replacing the never-run ShellSpec suite; both auto-discovered by `run-skill-script-tests.sh`'s `*/hooks/test-*.sh` glob). SEMANTIC, not a grep for `--argjson` — the #1417 lesson. The teach suite's fixture changed to the real Bash result object, so its ~13 existing `assert_emits` cases now read `.updatedToolOutput.stdout` and **all fail** against a string; on top of that: type is `object`, all five Bash fields present, non-`stdout` fields (`stderr`, `interrupted`, `noOutputExpected`) merged through **unmodified** (kills a fix that rebuilds from defaults), `stdout` ordered original → divider → hint, a negative assertion that `"noOutputExpected":` does **not** appear inside `stdout` (the old `tostring` blob leak), and both defensive branches (string / absent `tool_response`). The blueprint suite pins the channel (`.decision == "block"`), channel **exclusivity** (`hookSpecificOutput` absent — stops a "fix" emitting both), a **corruption guard** feeding a sentinel `content` and asserting it appears nowhere in the hook's output (pre-fix `tostring` serialized the file's own content into the tool result), all 17 detection signals and 6 silent cases ported from the retired spec, dedup/bypass/no-session paths, and a `hooks.json` gate asserting `length == 2 and all(.continueOnBlock == true)` — the count clause matters because `all` over an empty array is vacuously true. **Guard integrity**: the teach suite asserts baseline `stdout` is non-empty, so "type == object" cannot pass against a hook emitting `{}`. Verified by **mutation** against `git show HEAD:<path>`: teach suite 19 pass / **20 fail**, blueprint suite 9 pass / **26 fail** (including the `hooks.json` gate). | issue #2275 |
| The `session-spinup-nudge.sh` SessionStart hook blocked session start for **2.8s p50** and hit its 15s hard timeout on 3/42 runs producing **ZERO output** — every `echo` in `session-survey.sh` sat after the network work, so a SIGKILL at the hook budget erased the whole digest rather than truncating it. Cause: four **serial** `gh` calls (~2315ms of the 2809ms p50), the first of which was a `gh auth status` probe — a full network round-trip spent deciding whether to make network round-trips — and none of the four had any per-call bound, so one hang consumed the entire budget. `--summary` (the hook's mode) also made both PR calls whose output it never prints (`THREADS` omits `pr_count`). | Dropped the auth probe and derived `GH_READY` from whether a real query returned 0, preserving the "not queried" vs "genuine zero" distinction (`drift-detection-triggering.md`) — an absent, unauthenticated, or timed-out `gh` still reads `GH_READY=false`, with `GH_TIMEOUT=true` as a separate diagnostic (`GH_READY` stays a boolean; consumers branch on the literal `false`). The remaining calls run **in parallel**, each under its own watchdog bounded by `SESSION_SURVEY_GH_TIMEOUT` (default 4s) — implemented with a background `sleep`+`kill` pair, not `timeout(1)`, which is GNU coreutils and absent from stock macOS. Emission was reordered so every **no-network** section is written before the GitHub-backed ones, and `--summary` now launches only the call it prints. | `session-plugin/scripts/tests/test-session-survey.sh` TESTS **O/O2/P/Q** and `session-plugin/hooks/test-session-spinup-nudge.sh` **W2**. O runs a 10s-hung `gh` stub at `SESSION_SURVEY_GH_TIMEOUT=1` and asserts ≤4s wall clock, that `PROJECT`/`GIT`/`TASKWARRIOR` are still emitted, and that the timed-out query reports `GH_READY=false` **inside the `PRS` section body** — not a phantom clean zero. **O2 is the load-bearing structural half** (no timing test can express it): it asserts the line number of `=== END TASKWARRIOR ===` is *less than* that of `=== PRS ===`, which is what makes a truncated digest useful. P asserts the argv log contains no `auth status` while still containing the real `issue list`/`pr list`. Q pins parallelism (3 × 2s stubbed calls ≤3s). Verified against `git show HEAD:…`: O measured **40s**, O2 found PRS at line 15 vs TASKWARRIOR at 38 (network section FIRST), P saw `auth status`, Q measured 8s, W2 measured 40s. | issue #2276 |
| `session-survey.sh` derived the taskwarrior project slug from the repo **directory basename** and queried `task project:"$project" … export`, so a slug that matches no task produced a **confident** `OPEN_TASKS=0` — indistinguishable from an empty queue. Two shapes: (a) #2271, the basename differs from the real slug (chezmoi source dir `~/.local/share/chezmoi` → `PROJECT=chezmoi`, while 23 tasks live under `project:dotfiles`; also worktrees, monorepo subdirs, repos cloned under another name); (b) #2232, a **portfolio** checkout (`~/repos/laurigates` → `PROJECT=repos`) where the project-scoped query structurally cannot match — a session spanning 25 repos filed 5 tasks under other slugs and the survey said 0. `session-end`'s Step 2 gate (`TASK_AVAILABLE=true AND OPEN_TASKS ≥ 1`) then silently skipped the Taskwarrior-sync pass. | ONE coherent scoping solution, not two bolted-on fallbacks: a **single** all-projects `(status:pending or +ACTIVE)` export scoped in `jq`, which is what lets the collector SEE that the detected slug matched nothing *while tasks exist elsewhere*. That one snapshot feeds all four consumers (scoped count, the `+ACTIVE`-elsewhere footnote, the alternate-slug candidate, the recent-task fallback), replacing the former second `task +ACTIVE export`. Ladder: slug matches → `TASK_SCOPE=project`/`PROJECT_CONFIDENCE=high`; else the **git remote's** repo name (local, no network, only evaluated in the failing case) → `remote-name` + `PROJECT_RESOLVED=`; else `all-projects-fallback` with `RECENT_TASK_*` rows inside `--recent-days` (default 2). `OPEN_TASKS` stays an **integer** in every branch — `OPEN_TASKS=unknown` was rejected because three call sites do `-gt` arithmetic on it (`session-survey.sh` ×2, `session-spinup-nudge.sh`) and would all silently evaluate false; the uncertainty lives in the separate `TASK_SCOPE`/`PROJECT_CONFIDENCE` keys. An explicit `--project` is user-asserted and keeps its confident zero. `session-end`'s gate became `OPEN_TASKS ≥ 1 OR RECENT_TASK_COUNT ≥ 1`. | `session-plugin/scripts/tests/test-session-survey.sh` TESTS **R/R2/S/S2/T/U** and `session-plugin/hooks/test-session-spinup-nudge.sh` **W1**. R builds a repo whose basename is `chezmoi-src` with `origin → …/dotfiles.git` and tasks only under `dotfiles`. **Guard integrity is weighted hardest**, since "always report low" would pass R and S while destroying the signal: R2 moves the same tasks to `chezmoi-src` and requires `project`/`high` with `PROJECT_RESOLVED` **absent**; T requires an empty store *and* an explicit `--project` against a full store to stay `high`; S2 pins that visible uncertainty survives when `RECENT_TASK_COUNT` drops to 0 outside the window. **U is the design pin**: it asserts exactly **one** `export` invocation in the task argv log and **no** `project:` filter — structurally forbidding a second fallback query being bolted on later. Verified against `git show HEAD:…`: 30 of these assertions fail (U saw 2 exports and a `project:` filter; W1's nudge named `project:chezmoi-src` instead of `project:dotfiles`). | issue #2271, issue #2232 |
| `session-survey.sh` emitted `UNPUSHED` as a **two-line value**. `unpushed=$(git log '@{u}..HEAD' --oneline 2>/dev/null \| grep -c '' \|\| echo 0)`: on a branch with **no upstream** — any freshly created local branch, the modal session-start state — `git log` fails, so `grep -c ''` printed `0` **and exited 1**, meaning the `\|\| echo 0` ALSO ran and the value became the literal `"0\n0"`. The digest then carried a bare `0` line inside the `GIT` section, violating the `KEY=VALUE` contract (`structured-script-output.md`) and feeding a stray token into the LLM's digest. It also made `[ "${unpushed:-0}" -gt 0 ]` an unconditional false (an `integer expression expected` error swallowed by the trailing `2>/dev/null`), so **unpushed commits never contributed to `THREADS`** on a no-upstream branch. | `\| awk 'END{print NR+0}'` — always exits 0, always prints exactly one line, and needs no `\|\|` fallback at all. | `session-plugin/scripts/tests/test-session-survey.sh` TEST **V** asserts `grep -c '^UNPUSHED='` is exactly 1 **and** `grep -c '^0$'` over the whole digest is 0 (the bare-line assertion is the one that fails pre-fix; the sandbox repo is already on a no-upstream branch, so the condition arises naturally). Verified against `git show HEAD:…`: the bare-integer assertion reports `got '1', wanted '0'`. | session-survey-unpushed-two-line |
| Fixing the EMPTY-column shift in `session-survey.sh` (TAB is an IFS *whitespace* character, so `IFS=$'\t' read` collapses a run of tabs and an empty field shifts every later column left) introduced a **second** shift of the same class. The delimiter moved from TAB to **US (0x1F)** and the projections moved from `@tsv` to `join($us)` — but **`@tsv` escapes `\t`, `\n`, `\r` and `\\` by construction, and `join()` escapes nothing**, while the accompanying `gsub` stripped only tabs. So a task whose description or annotation contained a **newline** split one row into two and shifted every later column: a task with a 2-line description emitted `TASK_1_DESC=line one` plus a phantom `TASK_2_UUID=line two` whose `TASK_2_ACTIVE` held the modified stamp. In the all-projects fallback rows the damage is worse — the row count is read from the emitted rows, so the task **vanished from the digest entirely** (`RECENT_TASK_COUNT` short by one), which is the same silent-zero failure #2271/#2232 were filed about, reintroduced by their own fix. A literal US byte in the content is the identical hazard. | Strip every byte that can break a row, in all four projections (recent-task feed, task-detail description, task-detail annotations, stale-elsewhere feed): `gsub("[\\t\\r\\n\\u001f]";" ")`. The `\u001f` escape is the delimiter itself — a US byte arriving in task text is indistinguishable from a column boundary, so content-sanitising it is what makes `join($us)` safe at all. Verified that jq honours `\u001f` inside a character class rather than assuming it. | `session-plugin/scripts/tests/test-session-survey.sh` TEST **AA2** — the sibling of AA (which covers an EMPTY column; this covers a POPULATED one carrying a row-breaking byte). Asserts a 2-line description yields `OPEN_TASKS=1` with the newline flattened, the `ghid` still in `TASK_1_GHID`, and **no phantom `TASK_2_UUID=`**; that the same task survives into the fallback rows (`RECENT_TASK_COUNT=1`) rather than vanishing; and that an embedded US byte is flattened without shifting `GHID`. **Guard integrity**: an ordinary description must still pass through byte-identical, so the fix cannot degrade into blanking the description column. Verified by **mutation** on both halves independently — dropping `\r\n` from the class fails 5 assertions (reproducing the phantom row AND the vanished fallback task); dropping only `\u001f` fails the 2 US assertions while the newline ones stay green. | session-survey-field-sanitizer |
| `macos-performance-benchmark`'s `bench/cpu.sh` gated PASS/WARN on a **70% multi-core scaling-efficiency floor** (`MACOS_PERF_SCALING_WARN_PCT`). Scaling efficiency (multi-core throughput ÷ *n* × single-core) only means something when every core is equivalent — false on Apple Silicon's P+E topology, where 4 of a 10P+4E machine's 14 cores run at a fraction of P-core throughput. A **healthy** 10P+4E Mac measures **24–27%**, so the benchmark WARNed on every healthy asymmetric Mac, and the WARN propagated into the run's Overall Status (`report/generate.sh` derives 🟡 from any WARN row) — a correct machine reported as degraded. Lowering the floor was rejected: no single number is simultaneously right for symmetric and asymmetric topologies, so the threshold is unfixable rather than mis-tuned. | Demoted the ratio from a gate to an **info line**: `report_scaling()` in `lib/common.sh` computes it and emits `info` only (INFO rows render in the report as ℹ️ and are excluded from the PASS/WARN/FAIL counts), annotated with the P:E split from `hw.perflevel0/1.physicalcpu` when available so the number is actionable rather than mysterious. `MACOS_PERF_SCALING_WARN_PCT` was deleted from `config.sh`, with a comment naming the issue so an exported value reads as a documented no-op rather than a bug. The **absolute** multi-core throughput is now scored via the existing self-calibrating `score_hib` baseline — a per-machine comparison carrying no cross-topology assumption — so the multi-thread section keeps a real health gate. | `macos-plugin/skills/macos-performance-benchmark/scripts/tests/test-scaling-efficiency-info.sh` (20 assertions) is SEMANTIC: it **sources `lib/common.sh` in a sandbox and executes `report_scaling()`**, asserting the emitted `summary.tsv` rows rather than grepping for the fix's wording. Pins: the issue's real 24% value is still reported and lands exactly one INFO row with zero WARN/FAIL; 0% (worst conceivable) still cannot WARN or FAIL; 92% (symmetric-like) still cannot PASS — the metric is not a verdict in either direction; degenerate inputs (empty, zero divisor, non-numeric) emit nothing and raise no `set -u` error. **Guard integrity**: a control run calls `warn`/`pass` directly and requires those rows to appear, so the "emits no WARN" assertions cannot pass against a sandbox that never wrote the TSV — a bug the first draft actually had. Mutation-verified twice: against the verbatim pre-fix files 10 assertions fail, and against a *differently spelled* reintroduced floor (a `warn` inside `report_scaling`, every static grep still green) the behavioural assertions still fail 3 — the syntactic-vs-semantic gap from #1417/#1819. | issue #2183 |
| Two `comfyui-node-scaffold` generator defects had only ever been fixed on **deployed packs**, so every future pack was born with them (`~/.claude/rules/scaffold-fix-backport.md`). (a) `MODAL_KIT_VERSION` pinned `^0.10.0`'s ancestor `^0.2.0` — **four minors** behind the published `@laurigates/comfy-modal-kit` — and the pin is invisible to Renovate (it lives in a `.py` generator; this repo's `customManagers` and `check-version-pin-coverage.sh` both scan only `*/skills/**/*.md` + `scripts/install_pkgs.sh`), so nothing flagged the rot; #2222 tracks extending Renovate to generator/template paths. (b) The `banner.svg` overflow (#2186's second half) had **already been fixed** on main by 9c8f5f47 — verified empirically rather than re-fixed: on a 1344×576 rsvg render the derived 44-char tagline ends at x=1210 (133 px margin), while the same banner carrying the full 166-char `--desc` reaches x=1343 — but it was pinned by **nothing**. Separately (#2187) release-please bumps `pyproject.toml` and has no native `uv.lock` support ([release-please#2561](https://github.com/googleapis/release-please/issues/2561)), so a pack's lock self-version trailed the manifest on **every** release (comfyui-filename-prefix at v0.1.1 and v0.1.2). | Bumped the kit pin to `^0.10.0` (verified end-to-end: `bun install` → `tsc --noEmit` → `bun run build` → `vitest` all green on a scaffolded pack, and `openModalShell`/`fuzzyRank`/`highlightMatches` all still exported by 0.10.0), and emitted the `uv.lock` `toml` `extra-files` updater in the pack's `release-please-config.json` using the jsonpath **already documented and fleet-deployed** by `comfy-registry-lifecycle` §1 (a second form here would make every new pack drift from the packs that skill describes). Added `--verify`'s `RELEASE_PLEASE_UVLOCK=` grade so an *existing* pack's wiring is auditable ("sweeps miss packs"). Two plausible-but-wrong jsonpaths were ruled out by **executing** release-please 17.11.1's `GenericToml` against the real lock: `$.version` THROWS (`uv.lock`'s top-level `version = 1` is the lockfile FORMAT revision), and `$.package[?(@.name=="<pack>")].version` is a **silent no-op** — `TaggedTOMLParser` wraps every scalar as `{start,end,value}`, so `@.name` is an object, hence `.name.value`. **No claude-plugins package was touched**: all 44 release-please packages here are `release-type: simple` with no `uv.lock` (the only lock in the repo, `vault-agent/uv.lock`, is not a released package), so the issue's "add it to this repo's config" step has zero applicable targets. | `comfyui-plugin/skills/comfyui-node-scaffold/scripts/tests/test-finishing-pass.sh` blocks 14–16 (64 assertions total, up from 45), all **executing** the generator. Validated by mutation, four neutered rules, all killed: reverting the pin to `^0.2.0` fails 1; deleting the `extra-files` block fails 6; restoring `TAGLINE = DESC` (pre-fix banner) fails 2; making `derive_tagline` always truncate fails the short-`--desc`-verbatim guard. Guard integrity throughout — the gesture variant must declare **no** modal-kit dep (else the pin assertion matches everything), a short `--desc` must survive verbatim (else an empty tagline satisfies both overflow assertions), and stripping `extra-files` must flip `--verify` to `RELEASE_PLEASE_UVLOCK=unwired` (else a finding hardwired to `wired` passes). Block 16's semantic half runs a real `uv lock` and asserts via `tomllib` that the configured selector matches **exactly one** `[[package]]` entry at the pyproject version — catching a uv format change or a bad name substitution that the string checks cannot see; it SKIPs cleanly without `uv`. The kit-freshness probe is an advisory `NOTE` on stderr, never a `check()` — a hard failure the day the kit publishes a minor is the "gate red on arrival" hazard in `.claude/rules/generated-fleet-drift.md`; bounded with `--fetch-timeout`/`--fetch-retries=0` and suppressed by `SCAFFOLD_OFFLINE=1`. | issue #2186, issue #2187 |

| Four guards could report `STATUS=OK` / exit 0 **having scanned zero files**, because each pruned agent-worktree clones with a bare `*/.claude/worktrees/*` glob against an **absolute** scan base. When the scan root is ITSELF an agent worktree (`…/claude-plugins/.claude/worktrees/agent-<id>`), every descendant path contains `/.claude/worktrees/`, so the prune matched the entire root and the walk found nothing — while still exiting 0. Measured from inside a worktree at `218a3798`: `check-agent-model.sh` → `No agent files found` (vs **21** agent files), `check-unused-bash-grant.sh` → `SKILLS_SCANNED=0` (vs **408**), `check-version-pin-coverage.sh` → `FILES_SCANNED=0` (vs **589**), `check-workflow-js-model.sh` → 0 (corpus legitimately empty, which is why it hid the bug). Worktree-isolated subagents are this repo's normal way of doing plugin work, so **an agent verifying its own change locally was structurally incapable of failing**, and the orchestrator could not tell that from a real pass. Worse, the false green *vacuously satisfied* the #2255 gate for flipping `check-unused-bash-grant.sh` to `--strict` ("expect `STATUS=OK ISSUE_COUNT=0`") — the ratchet would have locked against a corpus that was never opened. | Discovery now runs from **inside** the scan root against **relative** paths (`cd "$proj_dir"` + `find .`), so the root is `.` and its absolute prefix cannot match while copies nested anywhere below it still prune correctly — the fix `scripts/check-subagent-types.sh` already used and documented. Anchoring the prune to `"$proj_dir/.claude/worktrees/*"` was tried first and **rejected**: it silently stops pruning a worktree nested inside a plugin dir, which the existing `test-check-agent-model.sh` TEST F caught immediately. Zero-scan is now distinguishable from clean-scan per the issue's unifying remedy: where an empty corpus means a misfire (plugin dirs present but no files discovered) the guards emit `STATUS=ERROR` + `TYPE=nothing_scanned` naming the likely cause; where it is legitimate (`check-workflow-js-model.sh` has zero bundled `.js` — correct, and must stay exit 0) they keep `STATUS=OK` but mark it explicitly as `SCANNED_EMPTY=true`. A tree with no plugin dirs at all stays green, so the loud case remains credible rather than being disabled. | `scripts/tests/test-guard-worktree-root-discovery.sh` (new, 21 assertions) pins the CLASS across all four guards: a fixture whose ROOT PATH is worktree-shaped must yield a **non-zero** scan count from each. Mutation-verified against the pre-fix scripts (`git show HEAD:…`), which report `SKILLS_SCANNED=0 / STATUS=OK / ISSUE_COUNT=0`. **Guard integrity**, without which every assertion would pass against a checker that discovers nothing: a real defect planted under the worktree-shaped root must still be CAUGHT (`check-workflow-js-model.sh` flags a planted `model:'sonnet'` and `--strict` exits 1), and the misfire/legitimately-empty branches are asserted separately so `SCANNED_EMPTY=true` cannot become a blanket excuse. `scripts/tests/test-check-agent-model.sh` gains TEST G with the same shape plus a *nested*-worktree assertion proving the fix did not simply disable sibling pruning. | issue #2219 |
| `.github/workflows/plugin-pr-checks.yml` ran six `scripts/check-*.sh` guards behind a `paths:` filter listing `*-plugin/**`, `**/skills/**`, `.github/workflows/**`, `**/*.py`, `ruff.toml` — and **no `scripts/**`**. `**/*.py` covered Python under `scripts/`, which is what made the list look complete, but every guard invoked was a `.sh`. So a PR changing only `scripts/check-*.sh` + `scripts/tests/test-*.sh` never ran them in CI: observed on #2215, #2217, #2218, each registering only `conventional-commits` and a skipped `doc-audit`. Pre-commit DID run the hooks locally (their `files:` regexes match), so the changes looked verified while CI was blind — **a guard with no CI signal for its own regressions**. | Already resolved for the wrong-but-sufficient reason by #2258, which removed the `paths:` filter from this workflow **entirely** — a path-filtered workflow never *reports* its context, and a required check that never reports blocks the merge permanently. So no change to the trigger was needed here; what was missing is anything stopping a future re-add. That matters because re-adding a narrower filter is a natural cost-saving impulse and the issue's own acceptance criteria float `scripts/**.sh` as an open "judgement call". | `scripts/check-workflow-script-triggers.sh` (new) parses each workflow, extracts every repo-relative `scripts/…{.sh,.py}` invoked from a `run:` block, and ERRORs when it matches none of that workflow's `pull_request` `paths:` patterns (implementing GitHub's `**`-crosses-separators glob semantics). A workflow with no `paths:` filter is never flagged — nothing can be orphaned. `scripts/tests/test-check-workflow-script-triggers.sh` (28 assertions) makes TEST B the load-bearing case: it replays the **verbatim** pre-#2258 filter against `check-agent-model.sh` + `check-loop-integrity.sh` and requires both to be named. TEST C pins the subtlety that hid the original bug — a `.py` under `**/*.py` is correctly NOT flagged — so the guard discriminates rather than blanket-failing. Guard integrity: TEST A asserts the real repo run scanned a non-zero number of workflows and is not `SCANNED_EMPTY`. The guard **caught a defect in its own CI wiring on first run** (an unquoted `paths:` in a step name made the workflow unparseable). Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml`. | issue #2219 |
| `scripts/run-skill-script-tests.sh` carried the **identical** absolute-base prune defect #2290 fixed in four sibling guards, and #2290's own class test did not cover it. Its discovery ran `find "$root_dir"` against an absolute base while pruning the bare glob `*/.claude/worktrees/*`, so when the root is ITSELF an agent worktree (`…/claude-plugins/.claude/worktrees/agent-XXXX`) the root's own path contains `/.claude/worktrees/`, every descendant matches the prune, the whole walk is skipped, and discovery returns **zero** tests. Reproduced: `--root <worktree-shaped dir>` → `TOTAL=0 SCANNED_EMPTY=true STATUS=WARN`, exit 0. It fails **worse** than the four `check-*.sh` siblings — a collapse here means every skill-local and hook regression suite in the tree silently does not run, i.e. the substrate that was supposed to catch #2290's class is itself blind. **Latent, not active**: the `justfile` recipe and `test-skill-scripts.yml` both invoke the runner with the default relative root `.`, and a relative root cannot match the prune, so only an absolute-root invocation from inside a worktree — an agent verifying its own change, which is this repo's normal way of doing plugin work — hits it. #2282's non-empty `scripts/required-to-run-tests.txt` already converts a collapse into an ERROR (every declared entry raises `required_test_missing`), so it is no longer *silent*; the discovery bug was still there. | Discovery now runs from INSIDE the root against RELATIVE paths (`cd "$root_abs" && find .`), per `scripts/check-agent-model.sh` / `check-subagent-types.sh`: the root becomes `.` so its absolute prefix cannot match, while worktree copies nested anywhere below it still prune correctly. Anchoring the prune to `"$root/.claude/worktrees/*"` was rejected for the same reason as in #2290 — it stops pruning a worktree nested inside a plugin dir. Ripple: `norm_path()` simplifies to a `./` strip (every path now arrives as `./<rel>` regardless of how `--root` was spelled) and the loop invokes `"$root_abs/$rel"` so the caller's cwd is unchanged. The **default** `--root .` invocation is byte-identical before and after (`find .` already emitted `./…`), so CI logs do not move; only absolute-root callers see the path form change. A `--root` that does not resolve now exits 2 instead of reporting `SCANNED_EMPTY=true` / WARN / exit 0 — a typo'd root reading as "found nothing" is the very failure this class is about. | `scripts/tests/test-run-skill-script-tests.sh` CASE 9 (now 56 assertions, up from 40) plants the fixture tree UNDER a `.claude/worktrees/agent-…` path and asserts discovery still finds its tests — **guard integrity**: `TOTAL=2` alone would hold for a runner that listed files and ran none, so `PASSED=1`, the per-file `SKIP=` classification, and required-manifest resolution are asserted too; and a worktree clone NESTED below the worktree-shaped root must still be pruned, proving the fix did not simply disable pruning. CASE 10 pins the unresolvable-root exit 2. Mutation-verified twice: against the pre-fix runner, and against a copy with **only** the `find` line reverted — all 7 CASE 9 assertions fail, attributing them to the discovery change alone. The runner is also added as the **fifth guard** in `scripts/tests/test-guard-worktree-root-discovery.sh` (the repo's home for this bug class), whose pre-commit `files:` regex is extended to `run-skill-script-tests\.sh` so editing the runner triggers it — the allowlist-drift lesson from #2164. | issue #2219, issue #2221 |
| `git-plugin/agents/git-ops.md:170` told agents **"Prefer the encoded recipe"** and routed them to `just -g branch-audit` — a recipe living in a **private dotfiles repo** this marketplace can neither version nor regression-test. Ten lines below, the *same file* gave the correct manual ladder (a MERGED PR is authoritative, then `git cherry`) and cited `~/.claude/rules/pr-merge-hazards.md` #1 on tree-containment being one-way, so **the agent's documented fallback was more correct than the tool it recommended**. Measured 2026-08-04, the recipe's REVIEW bucket was ~90% FALSE on two repos: `laurigates/claude-plugins` 191 REVIEW rows of which 174 had landed (91%), `ForumViriumHelsinki/infrastructure` 245 of which 216 had landed (88%), with **zero** regressions the other way. Two silent defects: (1) `merge-tree` used as the **primary** signal — once the base drifts over the same files the trees differ for work that fully landed, so a non-match proves nothing (132 branches); (2) `gh pr list --limit 500` against repos holding 1881 and 1684 PRs, so any branch whose merged PR fell outside the page read as `PR=—` → "no merged PR" → REVIEW (32 branches, e.g. #955, #885, #1203, #1301). Neither surfaces as an error; the failure mode is a REVIEW bucket the user cannot trust, pushing toward keeping hundreds of dead branches or deleting them unexamined. `git-plugin/skills/deadbranch` carried the same inversion — its Step 1.5 listed the `merge-tree` no-op as signal **1** and the MERGED-PR check as signal **2**. | Inverted the routing so the **authority ladder is the primary instruction** in both files: `gh pr list --state all --head <branch> --json state,mergedAt` (authoritative) → `git cherry` (patch-equivalence) → `merge-tree` demoted to an explicitly **positive-containment-only** shortcut. The recipe is still named, but only as a convenience carrying the measured caveat, so a reader learns the ~90% figure at the mention rather than trusting its REVIEW bucket. The pagination trap is named at the command that avoids it: `--head <branch>` is an **exact** per-branch query, unaffected by `gh pr list`'s 30-item default cap. The dotfiles-side recipe itself is **out of scope for this repo** — tracked separately. | `scripts/check-branch-containment-guidance.sh` (+ `scripts/tests/test-check-branch-containment-guidance.sh`, 26 assertions) — six rules: `branch_audit_uncaveated` (a mention must carry both `REVIEW bucket` and `#2268`), `recipe_before_ladder` / `merge_tree_before_pr` (ordering — neither may precede the authoritative `gh pr list … --head` line), `merge_tree_uncaveated` (`positive-containment` token required when merge-tree is used for **branch** containment), `ladder_missing` (containment guidance with no ladder at all — this is what stops the two ordering rules going vacuous if the line is deleted), and `paginated_containment` (a `gh pr list` with a bare `--limit N` **and** `state`/`mergedAt` in `--json` **and** no `--head`). Deliberately narrow: rules 1–4 scan `git-plugin/**/*.md` only, so `.claude/rules/gh-json-fields.md` and `pr-merge-hazards.md` stay free to quote the broken forms while TEACHING them (the instruction-vs-explanation discrimination `check-agent-tool-selection.sh` already makes); frontmatter, blockquotes, and `# Wrong`-marked anti-examples are skipped everywhere. Verified by **mutation** on both sides: run against the *verbatim pre-fix* files it exits 1 naming `git-ops.md:173` and `deadbranch/SKILL.md:77`, and nine neutered rules each turn the suite red — including reverting the discovery walk to absolute paths, which (because this repo's agent worktrees live under `.claude/worktrees/`) pruned the **entire** corpus and reported a vacuous OK over zero files. That defect was found BY the mutation sweep before commit, and is the same one #2290 fixed across four sibling guards — which is why TEST A asserts a non-empty corpus rather than only `STATUS=OK`, and why TEST I re-runs discovery from a worktree-shaped scan root. Complies with #2290's zero-scan standard: plugin dirs present but zero markdown discovered is `TYPE=nothing_scanned` / ERROR, while a tree with no plugin dirs at all stays `SCANNED_EMPTY=true` / OK / exit 0 (a guard that errors on a legitimately empty corpus gets disabled). Wired into `.pre-commit-config.yaml`; CI wiring deferred to #2219. | issue #2268 |
| Two residuals of the #2219 guard work, both left behind by #2297. **(a)** `check-version-pin-coverage.sh` resolved `script_dir` from `$BASH_SOURCE` *after* the `cd "$proj_dir"` that #2290 added, so a **relative** invocation — `bash scripts/check-version-pin-coverage.sh …`, the form pre-commit and CI use — looked for `scripts` **inside the scan root** and printed `line 244: cd: scripts: No such file or directory` to stderr on every run. Harmless to the verdict (the failed `cd` is confined to a subshell, and `$script_dir` is only read on the `files_scanned > 0` path where the helper resolves anyway), but it is noise on every invocation and a real break for any caller gating on empty stderr. An **absolute** invocation cannot reproduce it, which is why it survived every test in the suite. **(b)** #2297 anchored the two *negative* scan-count assertions in `test-guard-worktree-root-discovery.sh` but left the ten *positive* ones on unanchored `contains`. The trap is symmetric: an unanchored `SCANNED=1` is satisfied by any `*_SCANNED=1` **sibling key**, so a guard that discovered nothing in its own corpus could pass on a neighbour's counter. These guards keep gaining keys (`TEMPLATE_FILES_SCANNED`, `EXEMPTED_CALLS`, …), so the collision surface only grows. | (a) `script_dir` hoisted above the `cd`, with the reason recorded at both the definition and the old site. (b) Added `lacks_line()` beside #2297's `contains_line()` and converted **every** whole-line `KEY=VALUE` assertion in the file — both polarities, all four guards. `contains()` is deliberately retained where the match is genuinely a substring: prose (`No agent files found`) and tokens inside indented `ISSUES:` rows (`nothing_scanned`, `non_opus_model`). | `scripts/tests/test-guard-worktree-root-discovery.sh` gains a `relative invocation prints nothing on stderr` assertion which runs from `$repo_root` via a **relative** path *on purpose* — written the obvious way (absolute, like every sibling assertion) it is a no-op that passes against the defect. Mutation-verified in both directions: moving `script_dir` back below the `cd` fails exactly that assertion, and reinstating the pre-#2219 absolute-base prune (`find "$PWD"`) still fails the **anchored positive** assertion — so anchoring did not make the class test vacuous, which is the risk when tightening a matcher. 21 → 22 assertions. | issue #2219 |
| `scripts/run-skill-script-tests.sh` printed `PASS=<file>` for a test that **SKIPped**, so a gate whose dependency was absent on the runner was indistinguishable — in the CI log and in the `TOTAL=`/`FAILED=` roll-up — from a gate that ran and passed. `foundryvtt-plugin/skills/foundryvtt-module-scaffold/scripts/tests/test-template-parity.sh` (the acceptance gate for carrying `scaffold.py` **and** its cargo-generate port, #2205) had therefore **never once executed in CI**: `.github/workflows/test-skill-scripts.yml` installs `ast-grep` and `uv` precisely so their gates run but installed nothing for `cargo-generate`, so the SKIP branch was the only branch CI ever took. Two independent causes, either alone sufficient — the workflow's path triggers also matched no template file (`*-plugin/templates/**`), so a PR editing **only** the template, exactly the divergence the parity test exists to catch, would not run the workflow at all. Verified: 9/9 parity assertions pass when cargo-generate is actually present, so the gate was correct all along — it simply never ran. | Three fixes. (1) The workflow installs cargo-generate from the **prebuilt release tarball** (a `cargo install` compiles several hundred crates, minutes per run, for one test), cached and version-pinned via a new `renovate.json` customManager for annotated `<TOOL>_VERSION:` env pins in workflow files. (2) `*-plugin/templates/**` added to both the `pull_request` and `push` path lists — kept generic rather than foundry-specific, since other plugins will grow template directories (#2224). (3) The runner classifies a SKIP as its own outcome: `SKIP=<path>`, `PASSED=`/`SKIPPED=`/`FAILED=` counters, a `SKIPS:` block carrying each reason, and `STATUS=WARN` so a run with skips is never silently `OK`. Detection is "exited 0 and every **non-indented** non-empty line is a `SKIP:`/`SKIP -` notice", which covers both punctuation conventions in the corpus and the indented `Install: …` continuation line `test-bash-antipatterns.sh` emits, while leaving a **partial** skip inside an otherwise-executing suite a PASS. Tests that must genuinely run are **declared** in `scripts/required-to-run-tests.txt` (a skip there is ERROR/exit 1, as is a listed path matching no discovered test) — a blanket fail-on-any-skip would be red on arrival for taskwarrior/lizard and get disabled. | `scripts/tests/test-run-skill-script-tests.sh` (40 assertions) is SEMANTIC: it **executes** the real runner against planted fixture trees and asserts on its structured output — a grep for `SKIP=` in the runner would pass against a half-fix that never classifies anything as a skip. It pins each skip punctuation form, the indented-continuation shape, a stderr-only skip, required-skip → ERROR, manifest drift → `required_test_missing`, failure outranking the skip WARN, empty corpus → OK/exit 0, unknown argument → exit 2 (#2057), and `.claude/worktrees/` pruning. **Guard integrity**, without which every "not counted as a pass" assertion is vacuous: `PASSED=2` must hold (a runner classifying *everything* as a skip would otherwise sail through), a **partial** skip must stay a `PASS=` and must NOT appear as `SKIP=`, a required test that passes must raise nothing, and the repo's own manifest must be non-empty with every entry present on disk. Verified by mutation against the pre-fix runner (`git show HEAD:scripts/run-skill-script-tests.sh`): **24 of 40 assertions fail**, including the headline "colon-form skip NOT reported as PASS=". Wired into `.pre-commit-config.yaml` and run as its own step in `test-skill-scripts.yml` — repo-root `scripts/tests/` is not covered by the runner's own discovery globs. | issue #2221 |
| The SKIP-accounting fix (#2221) surfaced two more gates that had never executed on the runner, and they are the worst possible pair to leave unrun: `taskwarrior-plugin/skills/task-add/scripts/tests/test-uuid-capture.sh` **is** the executable gate added for #1417/#1819 after a **syntactic** guard — a grep for the literal `task +LATEST _get uuid` — let a non-working command ship (`_get` is a DOM accessor; handed a tag filter it returns empty, so the skill captured no UUID and silently reverted to the numeric-ID drift #1417 was meant to fix). An executable test that never executes is a syntactic gate again, one level up. Its sibling `taskwarrior-plugin/scripts/tests/test-task-id-stability.sh` skipped the same way — and its skip is broader than it looks, gating Parts 2 and 3 (the partial-conversion grep and the citation-drift guard) which need no `task` binary at all. Both were correctly WARN rather than ERROR because neither was declared in `scripts/required-to-run-tests.txt`. | The workflow builds Taskwarrior **3.x** from source, pinned in the top-level `env:` block with a `# renovate:` annotation and cached on the version tag (`actions/cache`, ~4 min cold on 4 vCPU, a single ~2 MB binary warm). From source, unlike the cargo-generate step it sits beside, because there is nothing prebuilt to fetch — v3.4.2 ships exactly one uploaded asset, the `task-3.4.2.tar.gz` **source** tarball, plus GitHub's two auto-generated archives (checked against the release). `apt-get install taskwarrior` was rejected on a stronger ground than speed: ubuntu-24.04 universe pins **2.6.2**, and these skills document 3.x, so a 2.x gate could read green on guidance broken for every real user — the same "gate that passes without testing the real thing" failure this row is about. Both paths added to `scripts/required-to-run-tests.txt`, so a future skip is exit-1, not a warning. (Supersedes that manifest's original "taskwarrior … absent by design" note.) | No new check — this is a **wiring** defect, not a detection one, and the detection already exists twice over: `run-skill-script-tests.sh` reports `required_test_skipped` + `STATUS=ERROR` + exit 1 for a declared test that skips (pinned by `scripts/tests/test-run-skill-script-tests.sh`, 40/40 green, whose guard-integrity assertion already requires every manifest entry to exist on disk), and the two tests are themselves the semantic gates. Verified in **both** directions on the real corpus, not a fixture: with Taskwarrior 3.4.2 built and on PATH, `TOTAL=85` with both reported `PASS=`; with the binary moved aside, both flip to `SKIP=` and raise `SEVERITY=ERROR TYPE=required_test_skipped … MSG=task CLI not available` with exit 1 — where before this change they read `PASS=`/`STATUS=OK`. Both suites also pass on 3.4.2 unmodified (4/4 and 25/25), so the gate is green on arrival rather than red-on-arrival-and-disabled. | issue #2221 |
| `scripts/check-branch-containment-guidance.sh` (#2268) flagged **its own PR's changelog entry** and hard-blocked every commit in the repo. PR #2281 landed the guard; release-please then generated `git-plugin/CHANGELOG.md` v2.50.5 — *"route branch containment to the authority ladder, not branch-audit"* — which names `branch-audit` with no `gh pr list … --head` line beside it, so the guard raised `ladder_missing` + `branch_audit_uncaveated` against a **clean checkout of `origin/main`**. Both are false positives by construction: a changelog entry NAMES a fix, it does not GIVE branch-containment guidance. It is also **unfixable at the file** — `CLAUDE.md` and `.claude/rules/release-please-protection.md` forbid hand-editing release-please output, and the next release reverts any edit. The real cost was normalising `--no-verify`: three separate agents hit it in pre-commit in one session and each bypassed, and a guard everyone routinely bypasses protects nothing. Structurally this is the sibling of #2214 (`dist/` build output) — a walk descending into **generated** content and reporting findings whose fix site does not exist. | Excluded release-please-generated changelogs from both scan loops, detected **structurally, not by a path list** (a filename allowlist is the maintenance trap `documentation-authoring.md` warns about — it goes stale the moment a plugin is added, and 44 plugins already carry one). `is_generated_changelog()` requires a semver heading, optionally linked to a `…/compare/…` diff, followed by a **parenthesised ISO date** — release-please's exact output shape, which a hand-written Keep-a-Changelog file (`## [1.0.0] - 2026-01-15`: dash-separated date, no parens) does not match. The `CHANGELOG.md` basename is a second deliberately conservative condition — a general shape, not a list — so a hand-written doc that merely QUOTES a release heading stays in scope. Verified all 44 plugin changelogs match and no hand-authored one exists. The `nothing_scanned` misfire check was re-keyed from **judged** to **DISCOVERED** files, so an exclusion can never mask a misfired walk (#2219). | `scripts/tests/test-check-branch-containment-guidance.sh` TESTS **J** and **K** (26 → 50 assertions). J replays the **verbatim** generated entry → `STATUS=OK`/exit 0 with the changelog named in no finding. **K is the non-vacuity half, and carries most of the weight**: excluding `*.md`, or every file named `CHANGELOG.md`, would pass J while asserting nothing — so K1–K3 plant the *same sentence* in a hand-written `SKILL.md` and require both findings, and K4–K6 plant a **hand-authored Keep-a-Changelog `CHANGELOG.md`** and require it still flagged with `…EXCLUDED=0`. Guard integrity: J5 puts a compliant guidance file beside the changelog and requires `GIT_PLUGIN_FILES_SCANNED=1` (a fix that narrowed the whole walk would otherwise pass J1), and J6 pins that no misfire is masked. Counters `GIT_PLUGIN_GENERATED_CHANGELOGS_EXCLUDED=` / `PLUGIN_MD_GENERATED_CHANGELOGS_EXCLUDED=` are emitted **even at 0**, keeping "scanned nothing" distinguishable from "found nothing" (#2219/#2255). Mutation-verified against the pre-fix guard (`git show origin/main:…`): **8 of 50 assertions fail** — A1/A2 reproducing the live block on `main`, J1–J5 the new behaviour, K5 the counter — while K1/K2/K3/K4/K6 pass on **both** versions, which is exactly what an unchanged-behaviour guard-integrity assertion should do. | PR #2281 fallout (deferred item 4 of PR #2308) |
| `external-pr-merge-guard.sh`'s structural awk scanner had four defect classes, all of which let a **real merge of an external PR** through. (1) A **fail-OPEN selector-shape gate**: once the command word was structurally established as `gh pr merge`, an argument that did not match a narrow selector whitelist turned the statement back into an ALLOW — reachable purely by a contributor-chosen branch name (`_wip`, `feat/a,b`, `feat/100%-done`), by a redirection bash strips before `gh` sees argv (`gh pr merge 2>/dev/null 2231`), or by a stdin redirect (`xargs -n1 gh pr merge < prs.txt`). (2) **Wrapper blindness**: the stripper skipped only flags and bare durations, so a wrapper flag taking a SEPARATE value (`sudo -u ci`, `env -C dir`) left a non-command word at the head, and `find -exec` / `parallel` / `watch` / `script` / `uv run` were not treated as wrappers at all. (3) **Missing command positions**: backticks were not statement separators, operators inside nested script text did not separate statements there, and a heredoc fed to a shell (`bash <<EOF`) was never scanned as script text. (4) The issue's own headline false positive survived for the api half — the `pulls/N/merge` route matched under **any** command word, so `rg …/pulls/2231/merge docs/` was denied with a message that misdiagnosed it. | The shape gate is **deleted**: the command word is already known structurally at that point, so the statement IS a merge whatever its argument looks like, and an unresolvable selector falls through to the author check, which denies. Redirections are skipped when picking the selector so the denial names the right PR. Wrappers now also scan the rest of the statement (on the MASK, so quoted prose behind a wrapper stays prose), and `nest` (is this quoted string script text?) is computed from the EFFECTIVE command word, making `sudo -u ci bash -c "…"` a shell invoker. The api route now requires a command word that can actually call it (`gh`/`curl`/`wget`/`http`/`xh`/`hurl`). Net: 20 real merge forms flip ALLOW→DENY, 4 mention forms flip DENY→ALLOW. One narrow false positive is accepted deliberately — unquoted prose directly behind a wrapper (`sudo echo gh pr merge 5`) denies, which is fail-closed; the quoted form is unaffected. | `hooks-plugin/hooks/test-external-pr-merge-guard.sh` — 47 new assertions (59 → 106), all semantic. The load-bearing addition is `ck_lookup`, which asserts DENY **and**, via the `gh` stub's `STUB_LOG`, that the hook resolved and looked up the selector the command actually merges — the verdict alone is weak because the stub answers for any selector. New sections pair every DENY with an ALLOW control so they cannot pass by blanket-denying: the wrapper section pairs `sudo -u ci gh pr merge` against `sudo -u ci echo "…"` and `bash <<EOF` against `cat <<EOF … EOF`; the api section pairs `gh api`/`curl` calls against `rg`/`echo`/`grep` of the same route. Invariants asserted: a merge established at command-word position is denied regardless of what its argument looks like; the PR actually looked up is the one the command merges; and a wrapper/shell-invoker/heredoc that EXECUTES the phrase is a merge while one that PRINTS it is not. | issue #2307 |
| `git-stash-reminder.sh` **permanently silenced genuine session stashes** in any stash namespace the session had not already Stopped in. One defect wearing three hats: the capture-on-first-observation branch absorbed EVERY stash present into the baseline and `exit 0`'d, so a stash the session itself created was swallowed by the very capture that first observed it — reached via a moved cwd, via a repo first entered while stash-free (made worse because the no-stashes fast path sat ABOVE the capture, so no baseline was written at all), or via a linked worktree (because `refs/stash` lives in the git **common** dir and is shared, while the baseline key was derived from `--show-toplevel`). A separate defect: SessionStart fires again on resume/compact/clear, and re-stamping `.session-start` retroactively moved the age bound, un-reporting a stash the Stop hook had already flagged. | Key the baseline on the realpath of `git rev-parse --git-common-dir` in **both** writers (one stash namespace → one baseline); move the no-stashes fast path BELOW the capture so entering a repo establishes its baseline and its session-start bound before any stash exists there; and on first observation capture only the stashes **older than the session start**, then fall through to the age filter instead of exiting — so a pre-#2306 stash is still absorbed and silenced while a genuine session stash is reported, on that Stop and every Stop after. Both writes in `git-stash-session-init.sh` are now **write-once per session**. A missing/unwritable capture still exits silently, so degrading never produces a false alarm. | `hooks-plugin/hooks/test-git-stash-reminder.sh` — 33 assertions, hermetic (`CLAUDE_STASH_BASELINE_DIR` sandbox, one guarded `mktemp -d`, full `GIT_DIR` family unset per #1745). Every invariant is **paired**, so the suite fails both an over-reporting hook and a permanently-silent one. False positives killed: a moved cwd onto a 16-month-old stash → SILENT twice; an absent baseline with no session marker → SILENT, and silent again after the capture; a 0-byte baseline + a FUTURE-dated stash → SILENT, preceded by a control asserting a POPULATED baseline suppresses the same stash; the pre-#2306 legacy flat baseline → SILENT. True positives preserved **and extended**: a repo stash-free at SessionStart, a repo entered AFTER SessionStart (same fixture shape as the false-positive case, differing only in the stash's age), a linked worktree in both directions (with an asserted premise that `refs/stash` really is shared), and a SessionStart re-fire in another repo AND the same repo (`sleep 1` between fires so a re-stamped marker would provably move the bound). Keying is isolated by "capturing repo B leaves repo A's baseline intact", where A's stash is FUTURE-dated so the age filter provably cannot be the explanation. Stash dates use git's raw `@<epoch> <tz>` form so GNU/BSD `date` cannot diverge. | issue #2306 |
| The `TaskCompleted` agent prompt's base-ref ladder led with `git rev-parse --abbrev-ref --symbolic-full-name @{u}` — which resolves to the branch's **own** remote counterpart, so on the issue's exact repro (a committed *and pushed* branch) `BASE..HEAD` is empty and the pre-fix "no work was done" verdict reproduces unchanged. Both rungs also failed in this very checkout (`@{u}` → `fatal: no upstream configured`; `git symbolic-ref --short refs/remotes/origin/HEAD` → `fatal: ref … is not a symbolic ref`) even though `refs/remotes/origin/main` exists, so the fix was a no-op here. Separately, Step 2 asserted flatly that the whole branch range "are evidence for this task", so a zero-work task on a branch already carrying an earlier task's commit had a non-empty CHANGE SET and the withhold clause **structurally could not fire** — a real weakening versus the original prompt, which blocked it on an empty `git diff --stat`. | Drop `@{u}` from base resolution entirely (it is named only as the anti-pattern, with the reason); resolve the DEFAULT branch via `git symbolic-ref --short refs/remotes/origin/HEAD` then a `git rev-parse --verify --quiet` probe of `origin/main`/`origin/master`/`main`/`master`; set `BASE=$(git merge-base HEAD <default-branch>)`. Branch commits become **CANDIDATE** evidence, step 8 requires identifying which parts of the change set correspond to the task in Context, and the withhold clause gains a third disjunct ("nothing in the change set corresponds to the task described in Context") so a no-work task is blocked exactly as it was pre-fix. The no-base-ref path now also covers `BASE == HEAD`. | `scripts/check-taskcompleted-verification.sh` + `scripts/tests/test-check-taskcompleted-verification.sh` (36 assertions). The guard as first written **pinned the wrong invariant and blocked this repair** (`STATUS=ERROR RULE=base_ref_hardcoded`): `base_ref_hardcoded` matched `origin/(main\|master)` **anywhere**, so it flagged the documented `rev-parse --verify` probe, while `base_ref_resolution` still **accepted `@{u}`** — i.e. it rejected the correct prompt and passed the defective one. Repaired to pin both polarities: `base_ref_hardcoded` now forbids only an unconditional base (a bare `<branch>..HEAD` range or `BASE=<branch>`), `base_ref_resolution` requires a real default-branch resolution with `@{u}` removed from the accepted spellings, a new `base_ref_merge_base` requires the divergence point, and a new `base_ref_from_upstream` fires on `@{u}` used **as** the base while still permitting the single mention of it as a named anti-pattern (E6 pins exactly that discrimination). Mutation-verified in both directions: the corrected prompt exits 0, while the intermediate `@{u}`-first ladder exits 1 on `base_ref_merge_base`. | issue #2301 |
| `session-survey.sh`'s `declared_project_of` returned `.claude/session.json`'s `.project` **unsanitized**. That file is repo content — it arrives with a clone, unlike the caller-supplied `--project` flag — so it is untrusted input to a `KEY=VALUE` digest whose consumers read line-wise (`session-spinup-nudge.sh:53` does `grep -m1 "^KEY="`). `{"project":"comfyui-nodes\nOPEN_TASKS=999\nTHREADS=0"}` injected two fabricated rows the nudge read as authoritative; `{"project":["comfyui-nodes","x"]}` cleared the `type=="object"` guard and emitted a 4-line `PROJECT` value. Four coverage gaps were proven deletable by mutation with the suite still 240/240 green: the untrusted-shape cases, the repo-root arm of the declaration lookup, the inner re-application of the `$HOME` boundary to git's own upward discovery, and the summary-mode `PROJECT_AMBIGUOUS_TASKS` emission. | Require `.project` to be a plain single-line **string** (jq type check + a `[[:cntrl:]]` reject) and **REJECT** anything else rather than flattening it — flattening would emit a garbage slug that matches no task and earns a CONFIDENT zero, the exact failure #2304 exists to stop. Rejection routes through the malformed-JSON degradation path the suite already asserts, so the failure mode stays singular and no new vocabulary key is introduced. The pre-existing `--project` flag is caller-supplied (a different trust class) and deliberately unchanged. | `session-plugin/scripts/tests/test-session-survey.sh` — 240 → 275 assertions, semantic throughout (every case EXECUTES the collector against a real nested-repo sandbox with a stubbed `task`). New `check_count_line` exact-line-count matcher: a duplicated `KEY=` row is invisible to both a presence and an absence assertion — only counting sees it. AG3 asserts the injected value never reaches the digest, that the digest AND the hook summary each carry EXACTLY ONE `^OPEN_TASKS=` and one `^THREADS=` row, that the injected `THREADS=0` never suppresses the nudge, and — **guard integrity, weighted equally** — that an ordinary single-line string declaration STILL wins outright with its own count, so the fix cannot degenerate into rejecting every declaration. AG4 pins the repo-root arm with an attributable fixture (2 tasks under the declared slug, 1 under the basename, so a lookup that missed the repo root reports 1, not 2). AJ2 pins the inner `$HOME` boundary using the dotfiles shape the code comment names — `$HOME` is itself a git repo with a PLAIN non-repo directory between it and the nested repo, which AJ structurally could not reach. | issue #2304 |
| `scripts/check-version-pin-coverage.sh` is a **whole-repo** guard (`pass_filenames: false`), so the only thing that can regress is whether its TRIGGERS select a change to any surface it scans. Both triggers were narrower than the scan: TEST J probed the templates trigger with `.yml` candidates only, while the guard scans EVERY non-`.md` file under `*-plugin/templates/` — so narrowing the pre-commit regex to `-plugin/templates/.*\.ya?ml` and the CI pathspec to `'*-plugin/templates/**.yml'` both left the suite at `PASSED=50 FAILED=0` exit 0. Separately the regression test's own `files:` regex did not name the two config paths it asserts on, the allowlist-drift shape already called out for #2164. | Widened TEST J's coverage rather than any production guard — `scripts/check-version-pin-coverage.sh` was **not touched at all**, and neither was `.github/workflows/plugin-pr-checks.yml`; problem 2 was a test-coverage gap, not a workflow defect. The regression test's `.pre-commit-config.yaml` `files:` regex now names `.pre-commit-config.yaml` and `.github/workflows/plugin-pr-checks.yml`. | `scripts/tests/test-check-version-pin-coverage.sh` TEST J — 11 new assertions (50 → 61). J1 evaluates candidate paths through pre-commit's own `filter_by_include_exclude` and now requires the templates trigger to select a **non-YAML** (`foundryvtt-plugin/templates/foundryvtt-module/package.json`, a real repo file whose pins the guard reads) and an **extensionless** (`demo-plugin/templates/Dockerfile`) template, so an extension-anchored narrowing of any spelling fails. J2 executes the workflow's real `changed` step against a throwaway repo whose only diff is `demo-plugin/templates/stack.toml`, requires the extracted `if:` to evaluate TRUE, then runs the checker against that same tree requiring exit 1 + `template_pin_unmanaged` — pairing a reaching trigger with a firing guard at a non-`*.yml` extension. J3 asserts this test's OWN trigger reaches both config paths, with four guard-integrity rows (the checker script, the markdown-extraction helper and the test file must still be selected; an unrelated workflow and an unrelated script must NOT be) so the widened regex cannot degrade into a catch-all. Mutation evidence: the three narrowing mutations fail 2, 1 and 2 assertions respectively against the repaired suite, and zero against the pre-repair one. | issue #2285 |
| `finops-plugin` instructed a **retired** REST endpoint in three skills and both executing scripts. `/orgs/{org}/settings/billing/actions` returns HTTP 410 (`This endpoint has been moved`), as do its `packages` and `shared-storage` siblings. Worse than a plain break: `billing-summary.sh:17` swallowed the 410 with `2>/dev/null` and printed `(requires org admin access)`, so the reader was sent to obtain an `admin:org` scope that could not have helped, while `github-actions-finops.sh:133` continued with empty billing. Both read as ordinary permission gaps, so nothing looked broken. The portfolio's own always-loaded rule `ci-cost-read-the-billing-api.md` documents this exact 410 **and** warns that it "is easy to misread as 'no billing data available on this plan'" — the plugin performed precisely that misreading, which is the point: an always-loaded rule did not stop a plugin in the same repo from contradicting it. | Swapped all five call sites to `/settings/billing/usage`, verified live against both a user and an org account before writing (the org form was previously unverified — the rule's 410 evidence covered only `/users/`). This is **not** a URL swap: the retired endpoint returned a flat `{included_minutes, total_minutes_used}` summary, the replacement returns one line item per repo × month × SKU, so the projection now sums them and the emitted keys change (`BILLING_MINUTES`, `BILLING_NET_USD`, `BILLING_GROSS_USD`). Net **and** gross are both emitted because `netAmount` is the spend — public-repo minutes are fully discounted, so a repo can show a large gross and cost nothing. The misattributed failure text now names both possible causes. The windowed-by-default caveat is recorded at each call site. | `scripts/check-dead-api-endpoints.sh --strict` (+ `scripts/tests/test-check-dead-api-endpoints.sh`, 17 assertions) — a denylist guard in the shape of `lint-mcp-tool-references.sh` / `lint-package-references.sh`, but scanning `.sh` as well as `.md` because the defect lived in a script. Shell comments and markdown blockquotes are skipped so a file may still TEACH the hazard (the instruction-vs-explanation split `check-agent-tool-selection.sh` makes), and TEST 5 pins that the skip is **narrow** — a live call sitting beside an explanatory comment in the same file is still caught, so TEST 3 cannot pass by muting whole files. Mutation-verified: run against the pre-fix tree it reports all **7** sites and exits 1; against the fixed tree `ISSUE_COUNT=0`. Guard integrity: a non-empty tree must not report `SCANNED_EMPTY`, and `FILES_SCANNED>=2`, else every "clean" assertion would pass against a walk that read nothing (#2290's class). The existing `test-github-actions-finops.sh` fixture was migrated to the line-item shape and **caught a real bug during the fix** — the fixture path fed `.billing` through unfiltered while only the live fetch filtered by product, so a `packages` row summed into an Actions-minutes total; the filter moved to the point of use so both paths agree. | issue #2321 |
| `blueprint-init` Step 8 **wrote** three rules (`development.md`, `testing.md`, `document-management.md`) into the generated-rules directory and **registered none of them** in `generated.rules`, while `blueprint-generate-rules` Step 5 did register its four. The two producers of that directory disagreed about whether their output was tracked, so `/blueprint:sync` — which detects staleness by comparing a **registered** record's `content_hash` against the file — was structurally blind to everything `init` created: a local edit undetectable, a revised template never propagating, sync reporting clean over a set it could not see. Measured across the portfolio: **22 repos** over ~7 months, three template revisions unpropagated, only **1 of 23** copies of `document-management.md` matching the shipped template. None of it surfaced as an error. Three further defects made the same contract incoherent from the reader end: `blueprint-sync` Step 2 **double-suffixed** (`test -f .claude/rules/{name}.md` against keys that already carry `.md`, so every registered rule read as missing) and **hardcoded** `.claude/rules/` (wrong in every repo that chose an isolated `generated_rules_path` in Step 4a — #1043/#1675); `hooks/blueprint-drift-probe.sh` iterated `(.generated.rules // [])[]`, an **array** form against the object map `schemas/manifest.schema.json` defines, matching nothing on every real manifest; and `blueprint-promote` carried the same double-suffix plus hardcoded path. A fifth mismatch surfaced while making the contract coherent: `generate-rules` documented `"content_hash": "sha256:..."` while **both** readers compare bare `sha256sum`/`shasum` output, so a prefixed value could never match. | Treated as ONE contract change, not five edits — landed piecemeal they conflict. `blueprint-plugin/scripts/register-generated-rules.sh` is the single writer both producers now invoke (init Step 8a, generate-rules Step 5), so they cannot drift apart on record shape or hash computation; `blueprint-sync` Step 2, `blueprint-promote` Step 2 and the drift probe resolve `$RULES_DIR` from `structure.generated_rules_path` and iterate `to_entries[]`. **Key form chosen: the bare filename relative to `generated_rules_path`, INCLUDING `.md`** (`development.md`) — the form `generate-rules` already shipped, so no existing manifest needs migrating; relative, so the registry survives a `generated_rules_path` change. **Hash form: bare lowercase hex, no `sha256:` prefix** — the form both readers already compute. Consumers resolve `"$RULES_DIR/$key"` and never append `.md`. Per `~/.claude/rules/scaffold-fix-backport.md` the generator is the fix site; the 22 existing consumer repos are deliberately NOT migrated by this change. | `blueprint-plugin/scripts/check-generated-rules-registration.sh` (+ `blueprint-plugin/scripts/tests/test-check-generated-rules-registration.sh`, 28 assertions) — the defect is a **missing write**, so a syntactic pin on whatever spelling shipped would cement it (#1417 → #1819). Each of the five rules is a CONCEPT with several accepted spellings, and the suite pins **both** halves: deleting Step 8a → `init_missing_registration` + `registration_omits_hash`, exit 1 (CASE C), while a behaviour-preserving reword that keeps only the script invocation and never says "content_hash" → exit 0 (CASE D). Without the reword half an always-firing guard passes; without the removal half a never-firing one does. CASE D also swaps the delegated writer for a hash-less one and requires the finding to follow the **behaviour**, not the prose. CASE E accepts a hand-rolled `generated.rules` spelling; CASE I proves a blockquote / shell comment citing the broken forms is teaching, not using. Guard integrity: CASE A requires the repo run to be non-vacuous (`FILES_SCANNED=35`, not `SCANNED_EMPTY`) — else every "clean" assertion passes against a walk that read nothing (#2290's class) — and CASE B requires the compliant fixture to pass before any break is applied. `blueprint-plugin/scripts/tests/test-register-generated-rules.sh` (32 assertions) is the semantic half: it EXECUTES the writer against sandbox manifests and asserts on the JSON that lands — object map, `.md`-suffixed keys, bare-hex `content_hash` equal to the file's real digest with no `sha256:` prefix, a non-default `generated_rules_path` honoured, and the consumer round trip (`"$RULES_DIR/$key"` resolves to a real file while the double-suffixed path does not). Mutation-verified against the verbatim pre-fix files: **8 findings, exit 1**. Wired into `.pre-commit-config.yaml` and `plugin-pr-checks.yml`. | issue #2331 |
| `blueprint-plugin/hooks/blueprint-structural-cue.sh` fired its "public API changed" cue on **prose**. Signals 2–6 are source-code greps, but they ran against the payload of *any* file the Edit/Write touched: an English sentence containing `export ` tripped the public-API signal in a Markdown file (observed on a table cell reading "…the same consumer-contract test before the export can be called correct"), and `^[+[:space:]]*pub ` matches a list item beginning with the word "pub". The dropped guard was visible in the source but not in behaviour: `file_ext="${file_path##*.}"` was computed at line 50 and referenced **nowhere else** (`grep -c file_ext` → 1), while its sibling `base_name` was used by the manifest and `openapi.*` checks. It was most likely lost when detection widened in #1616 from manifests + export lines to signals 2–6. The failure is quiet by construction — a cue is not a gate and dedups to once per session — which is exactly why it survived: the cost is one spurious nudge that trains the reader to skim past the cue, eroding the signal the dedup exists to protect. | Signals 2–6 now require `is_code_file` (`js jsx mjs cjs ts tsx mts cts rs py pyi go`); signals 1 and 7 (manifests, `*.proto`/`*.graphql`/`schema.prisma`, `openapi.*`) stay extension-free because they identify themselves by path or basename. The issue's suggested `case … *) is_structural=0` shape was **not** taken: placed after signal 1 it would clobber the `plugin.json` manifest verdict, since `.json` is not a source extension — the guard has to gate the individual code signals, not zero the accumulated result. Second, an early exit for the harness's per-session scratch tree (`{/tmp,/private/tmp,/var/folders/*}/claude-*/…`), where `/blueprint:derive-plans` can never be actionable; deliberately narrower than `bash-antipatterns`' all-of-`/tmp` carve-out so a plain `/tmp/foo.ts` still gets the cue. | `blueprint-plugin/hooks/test-blueprint-structural-cue.sh` (extended, +26 assertions; auto-discovered by `run-skill-script-tests.sh`). SEMANTIC, not a grep for `is_code_file`: each new case feeds a payload that is byte-for-byte code-shaped and asserts the verdict turns on the **extension alone** — `export interface Foo { a: string }` in `.md`/`.txt`/`.json`/`.yaml`/no-extension is silent, the *same string* in `.ts` still fires. Guard integrity: 7 "still fires" controls (`.ts .js .go .rs .py .mjs .tsx`) plus 5 asserting the path/basename signals were **not** swept up by the gate, so the suite cannot be satisfied by a hook that went silent everywhere; and a `/tmp/x.ts` control pins the scratchpad exit's narrowness. Mutation-verified against `git show HEAD:<path>`: pre-fix **48 pass / 12 fail**, post-fix **60 pass / 0 fail**, with all 48 pre-existing assertions green in both. | issue #2336 |
| `scripts/check-taskcompleted-verification.sh` — the regression guard for #2301 — reported `STATUS=OK ISSUE_COUNT=0` on a **fully reverted** verifier, via two independent holes. (1) `worktree_still_inspected` accepted the bare tokens `git status` and `working tree`, both of which occur throughout the prompt in prose that is NOT the inspection step — one of them inside a **negation** (`not raw git status`) and another in the line asserting the opposite of the invariant (`may live in COMMITS, not only in the working tree`). Deleting the prompt's only inspection step (`3. Inspect the working tree: git status --porcelain and git diff --stat.`) still passed: the rule matched vocabulary, never an instruction. (2) The extractor joined every `type: "agent"` TaskCompleted prompt with `"\n".join(prompts)` **before any rule ran**, so a token carried by ANY hook satisfied a rule for ALL of them — a verifier replaced verbatim by the #2301 bug (*"inspecting the working tree only… If the tree is clean, no work was done"*) passed as long as a sibling agent hook mentioned the words. Latent while the event carried one agent hook, live the moment a second was added, with nothing warning at that point. | (1) Anchor the rule on the **instruction**: an accepted spelling is now either a working-tree-scoped `git status` invocation carrying a machine-readable flag (`--porcelain`/`--short`/`-s`/`-uall`/`--untracked` — forms prose about the tree does not use) or an imperative inspection verb bound to a `git status`/`git diff` call with no sentence boundary (`[^.;]*`) between them. (2) Extract each agent prompt to its **own file** plus a `<i><TAB><statusMessage>` index — the prompts are never concatenated — and evaluate the rule set against the VERIFIER alone, naming it in every finding (`PROMPT=agent[N]`). The verifier is **identified, not inferred**: with one agent hook it is that hook; with several it is the one whose `statusMessage` matches `/verif/i` (a real harness-honoured agent-hook field the verifier already carried, not a synthetic key), and zero or several matches is itself a finding (`verifier_unidentified` / `verifier_ambiguous`) rather than a guess. The 13-rule audit also found `base_ref_unresolved_path` accepting `no upstream`, which names a **different** condition (the branch having no `@{u}` — which this prompt forbids as a base), so a reword of the `@{u}` anti-pattern sentence would have satisfied it while the unresolved-base handling was gone; that spelling was dropped. | `scripts/tests/test-check-taskcompleted-verification.sh` — 36 → 61 assertions. TEST K replays mutation 1 and its **fixture-validity** half is load-bearing: K4/K5 assert the mutant STILL contains `working tree` and `git status`, so the pass proves the rule stopped accepting vocabulary rather than the vocabulary having vanished. TEST L replays mutation 2 with a sibling carrying the shipped text (L9 asserts the sibling really does carry it, else the union case is vacuous) and **L10 is the attribution control** — the same two-hook shape with an INTACT verifier must still pass, so the ERROR is the reverted verifier's, not merely a consequence of having two hooks. TEST M pins identification, weighted on the cases a naive fix would get wrong: M4/M5 put the intact verifier SECOND behind a reverted one, so a guard that took index 0 (or unioned) reports ERROR; M6/M7 pin that a sole hook needs no marker. TEST N is the reword half — `git status --short` (N1) and a bare imperative `Read git status and git diff` (N2) must PASS, while **N3** puts pure prose about the working tree in the same position and must still FIRE, which is what stops N1/N2 passing against a rule quietly relaxed back to the old match. Mutation-verified twice: against the pre-fix guard **17 of the new assertions fail**, and against a copy with ONLY the two `worktree_still_inspected` patterns reverted (per-prompt evaluation intact) exactly K1–K3 and N3 fail — attributing each half to its own change. | issue #2335 |
| `scripts/run-skill-script-tests.sh` discovered tests with three globs (`*/skills/*/scripts/tests/`, `*-plugin/scripts/tests/`, `*/hooks/`), **none** of which matches repo-root `scripts/tests/test-*.sh` — where the self-tests of the whole-repo `check-*.sh` guards live, 44 of them. So a guard at `scripts/check-*.sh` had a self-test the runner never ran, and got CI signal only if someone hand-wired a step into `plugin-pr-checks.yml`. **17 of the 44 had no CI wiring at all**, and two of those were already RED on `main` and invisible: `test-check-docs-index.sh` (TEST A — `generated-fleet-drift.md` existed but was absent from the CLAUDE.md Rules table; `check-docs-index.sh --strict` exits 0 on a WARN, so CI was green while the self-test failed) and `test-check-feature-flags-catalog.sh` (`CLAUDE_HOOKS_DISABLE_CONFIG_DRIFT` read by `health-plugin/hooks/config-drift-probe.sh` but undocumented in `hooks-plugin/docs/feature-flags.md`; that guard is wired nowhere). Third instance of the same class after #2219 and #2221 — a guard that looks protected and is not. | Added a fourth glob, `-path './scripts/tests/test-*.sh'` — **anchored at the scan root**, not `*/scripts/tests/…` and not absolute. Discovery runs from inside the root against `.`-relative paths (#2219), so `./scripts` names this tree's scripts dir only; a `*/` spelling would swallow every nested `scripts/tests/` and an absolute spelling would reintroduce the prune collapse. `test-skill-scripts.yml`'s `paths:` widened to `scripts/**` (subsuming the former per-file list) — without it a PR changing only `scripts/check-foo.sh` + its self-test still fires no workflow, so the discovery fix would have been decorative for exactly the case it exists to close. Both pre-existing red tests fixed at source (one CLAUDE.md table row, one feature-flags table row). The **double-run** was measured, not assumed: 27 of the 44 also run as their own step in `plugin-pr-checks.yml`. Those steps are kept deliberately and the reason is recorded in the runner header and at the step — that workflow carries no `paths:` filter because `compliance` is a required check, so its steps are the only ALWAYS-ON signal, and each is locality-paired with the `--strict` guard run it validates. | `scripts/tests/test-run-skill-script-tests.sh` CASE 11 (56 → 67 assertions) plants a repo-root `scripts/tests/` fixture and pins discovery, SKIP classification, manifest eligibility on the repo-relative form, and composition with #2219 (a worktree-shaped root finds its own `scripts/tests/`). The **anchor** is asserted as hard as the discovery: a decoy at `tools/scripts/tests/` — deliberately not under a `*-plugin` dir, so no other glob can claim it — must stay undiscovered, and `TOTAL=3` excludes it. **Guard integrity**: `PASSED=2` requires the pre-existing globs to keep finding their own tests, so the count is attributable rather than coincidental. No exact TOTAL is pinned against the real corpus anywhere (a concurrent PR adding a test file would break that); every exact count is against a fixture this test plants. Mutation-verified: removing the glob fails 7 assertions, the `*/` spelling fails 3 (including the anchor), an absolute `"$root_abs/scripts/tests/…"` spelling fails 7. | issue #2333 |
| `scripts/check-feature-flags-catalog.sh` reported `STATUS=OK` / exit 0 **having opened no file**, for the entire life of the guard. It enumerated sources with `rg … 2>/dev/null`, and **ripgrep is not in GitHub's `ubuntu-latest` image** — so `command not found` was swallowed, both command substitutions yielded empty, `SOURCE_FLAG_COUNT=0`, `CATALOG_FLAG_COUNT=0`, `MISSING_COUNT=0`, clean. The guard is wired into no workflow, so this surfaced only when #2333's discovery glob first ran its self-test on the runner: assertions 1 and 2 ("real repo passes `--strict`", "documented flag → OK") **passed vacuously** — they are exactly what a checker that scanned nothing reports — while the two negative controls (3 and 4) failed, because an undocumented flag cannot be detected in a corpus that was never read. The #2290 class, in the fixture path as well as the real-repo path. Reproduced before diagnosing, not assumed: stripping every `rg`-bearing directory from `PATH` yields byte-identical output to the CI log. | Replaced `rg` with `find` + `grep`, which are POSIX and present wherever this runs, so there is ONE code path rather than a fast path that is silently a no-op in CI. Installing ripgrep on the runner was rejected — it fixes this workflow and leaves the guard a no-op for every other caller, and the failure mode would stay invisible. The `find` prunes replace what rg previously got for free from `.gitignore` (`.claude/worktrees/` clones #1492, `dist/` build output #2214) plus its explicit `!**/tests/**` glob, and discovery runs from INSIDE the root against RELATIVE paths (#2219/#2290) so a worktree-shaped scan root cannot match its own prune. Collected with `while read -r -d ''` rather than `mapfile -d ''` (bash 4.4+) and counted in the loop rather than via `${#arr[@]}` (expanding an empty array is itself an unbound-variable error under `set -u` on older bash) — both would have re-created a silent no-scan on macOS's bash 3.2. Behaviour preserved exactly on the real repo: `SOURCE_FLAG_COUNT=32`, `CATALOG_FLAG_COUNT=33`, `MISSING_COUNT=0` before and after. Reporting half: `FILES_SCANNED=` / `SCANNED_EMPTY=` are emitted always, and a resolved catalog with zero scannable sources is `STATUS=ERROR` + `TYPE=nothing_scanned` — a walk that misfired must never be able to report clean. | `scripts/tests/test-check-feature-flags-catalog.sh` 4 → 8 assertions. The load-bearing addition is **guard integrity on assertion 1**: `STATUS=OK` alone is vacuous, so the real-repo case now also requires `FILES_SCANNED > 0` and `SCANNED_EMPTY=false` — that pair is what would have caught this on day one. Plus `FILES_SCANNED=1` beside the undocumented-flag case (so its DENY is attributable to the flag, not to a walk that read one file by luck), and a new case 3b asserting a catalog-present/no-sources tree is ERROR + `nothing_scanned` + `--strict` exit 1. Mutation-verified across both axes: pre-fix guard with rg absent → **2 pass / 6 fail** (reproducing the CI failure exactly), pre-fix with rg present → 4 pass / 4 fail, fixed guard → **8 / 0 in both**. Verified under bash **3.2** as well as 5.x. | issue #2333 |
| `feedback-plugin/scripts/friction_parse.py` truncated every event's `evidence` at **400 characters**, which is a *display* budget applied to a *measurement* field. The marker distinguishing a hook message from its predecessor sits in the message TAIL, and the harness's own `PreToolUse:Bash hook error: [bash <abs-path>/<hook>.sh]: ` wrapper consumes ~150 chars before the hook's first word — so the post-#2148 head/tail message was cut mid-way through "This fires only when the read is the **WHOLE command**" and read identically to its pre-#2148 ancestor. Silent by construction: a truncated evidence string is well-formed, and the resulting counts read as researched. In the W33 window a naive read of parser output gave **15 post-fix / 12 legacy** hook blocks; recovering the same events from the raw transcripts corrected it to **27 / 0** — 12 phantom "legacy" events, enough to have argued for reopening the correctly-closed #2148. | Promoted the bare literal to a module-level `EVIDENCE_MAX_CHARS = 1200` (one edit next time, not a grep) and applied it at all four sites — including `body = text or json.dumps(tur)[:…]`, which feeds **classification**, not just the emitted field. Measured over a 1d window (26 transcripts, 82 events, unchanged): output 39,763 → 51,133 bytes (+28.6%), mean evidence 308 → 442 chars, and events sitting *at* the cap 30/82 → 7/82. Every `(kind, signature)` tuple is byte-identical before and after, so the raise widens the window without moving any verdict. `friction_cluster.py`'s `[:200]` truncations were checked and deliberately left: they are one-line renderings for the PR-body markdown table, while the clusterer's JSON `samples[].evidence` passes the parser's field through **untruncated** — so the raised budget does reach the analyst and the fix is not a no-op. | `feedback-plugin/scripts/tests/test_friction_parse.py::test_evidence_budget_reaches_hook_message_tail` over a new `fixtures/evidence_budget/` transcript. The fixture is **extracted from the deployed hook** (`sed` over the `head-tail-read` block in `hooks-plugin/hooks/bash-antipatterns.sh`), never retyped — a retyped copy is not the message, and its byte length is precisely what decides whether the tail survives. Two guard-integrity halves, without which the test could pass while asserting nothing: a fixture-validity assertion that the marker genuinely sits past char 400 in the untruncated source (it is at 455), and a parity assertion that the fixture still contains the hook's current block message byte-for-byte, so a reworded hook fails loudly instead of silently decoupling the test from reality. Mutation-verified against the pre-fix parser (`git show origin/main:…`): the new test fails with `len=400`, while all 6 pre-existing tests stay green; post-fix 7/7. | issue #2148 (measurement instrument) |
| `hooks-plugin/hooks/bash-antipatterns.sh`'s `sed -i` block advertised `(In-place edits of scratch files under /tmp are allowed.)` while blocking exactly that. The `sed-inplace` ast-grep rule's exemption — `not: { has: { kind: word, regex: '^((/private)?/tmp/\|/var/folders/)' } }` — is correct but **structural**: it requires the scratch path to be a literal `word` node *inside the sed command*, and in practice agents `cd` into the scratchpad first (or bind it to a variable), leaving sed a bare relative filename. The exemption therefore never fired on the shapes that actually occur. W34 friction measured **6 events / 6 sessions**, of which **4 (67%)** targeted `/private/tmp/claude-502/…` scratchpaths the message declares exempt — a hard block whose own message tells the agent the command should have passed, so the retry has nowhere to go. The existing suite could not catch it: every `sed -i` scratch row exercised only the literal-argument shape. | Fixed at the **consumption site**, not in the rule — teaching the parser about `cd`/variable shell state it cannot see would be the wrong layer. A `scratch_ctx` predicate additionally accepts a scratch prefix appearing as a `cd` TARGET or a variable VALUE, and the dispatch becomes `ast_matched "sed-inplace" && ! scratch_ctx`. Deliberately **not** a blanket "the command mentions `/tmp` anywhere" test: that would exempt `cd ~/repos/x && sed -i '' 's/a/b/' f.py 2>/tmp/log`, a repo edit whose only `/tmp` is a log redirect. The predicate scans `COMMAND_SHELL_ONLY` (heredoc bodies and trailing `#` comments already stripped) rather than raw `$COMMAND`, so a `cd /tmp/…` that is only *text* cannot grant the exemption (#2106's class). The block message string is byte-identical — the fix makes the message true rather than restating it. | `hooks-plugin/hooks/test-bash-antipatterns.sh` "sed -i scratch exemption is reachable via cd target / variable" + "cross-detector controls" blocks (13 assertions) replay W34's differential matrix: A (literal arg), B (`cd` target), C (variable value) and E (`/var/folders` via `cd`) must be ALLOWED; D (`cd` into a repo) and F (repo edit with a `/tmp` stderr redirect) must still BLOCK — F is what pins the predicate against the blanket form. Two text-not-state guards (a heredoc-quoted `cd /private/tmp`, a trailing `# cd /tmp/…` comment) must not exempt a real repo `sed -i`. Cross-detector controls assert the predicate is scoped to the `sed -i` branch: `chmod 777`, `git add -A`, a whole-command `cat`, `curl \| bash`, and **#2349's pair** (`git add && git commit` blocks, `git add && git status --short` allowed) are unchanged. Mutation-verified against `origin/main`: pre-fix **186 pass / 3 fail** — exactly B, C, E — post-fix **189 / 0**, with every pre-existing assertion green in both. | W34 friction §Signal C / PR-READY 1 |

| Each blueprint document type had TWO descriptions of itself — a schema in `blueprint-plugin/schemas/`, and a hand-rolled field list in a bash hook — with nothing keeping them in sync. They had drifted three ways, all silent. (1) `adr.schema.json` spelled the back-reference `superseded_by`; `validate-adr-frontmatter.sh` read `superseded-by` via `get_field`, so the "Superseded needs a replacement" check **never fired once** — `docs/adrs/0014` has sat `Superseded` with no back-link since March. The schema also required `date` where the hook required `created`/`modified`, and allowed 4 statuses to the hook's 7. Nothing referenced `adr.schema.json` at all: it was dead text, and a doc written from it described a document the hook would reject. (2) All three hooks read `.tool_input.content`, which **Edit does not send** (it carries `old_string`/`new_string`), so every Edit to a PRD/ADR/PRP skipped validation entirely while exiting 0 — indistinguishable from a clean document. (3) Not one ADR had frontmatter at line 1; every block sat below the H1, readable only by the hooks' own lax `awk '/^---$/{if(++n==2)exit}n'`. PRD and PRP had no schema at all. | The schema is now the only description. `scripts/check-schema.py` projects a markdown document into `{frontmatter, sections}` so **section** requirements are declared in the schema rather than in a grep loop, validates plain JSON via `--json-file`, and reconstructs the resulting document for an `Edit` payload. Severity is declared in the schema (`x-blueprint-severity: "warning"` + a `title` that becomes the message), so a missing section or a missing back-reference nudges rather than blocks per `.claude/rules/hook-block-vs-nudge.md` — a hard block would have refused edits to 20 of 22 ADRs that never had an "Options Considered". The three `validate-*-frontmatter.sh` hooks became thin wrappers over `validate-frontmatter.sh` and declare no field list. `get-validation-config.sh` reads the feature-status enum from `feature-tracker.schema.json` instead of hand-copying it, and that schema was widened to accept the **array** feature shape `blueprint-tracker-check.sh` already tolerated in a comment — a schema has to describe what the system accepts. | `blueprint-plugin/scripts/tests/test-check-schema.sh` (23 assertions) is SEMANTIC: it EXECUTES the validator, because a grep for a field name would pass against a schema nothing consumes — precisely the pre-fix state. Each defect is pinned by BOTH polarities: the hyphen spelling must SATISFY the back-reference requirement while the underscore is CALLED OUT and does not (a test asserting only "a bad ADR exits 2" would have passed throughout); an Edit introducing a violation exits 2 while a benign one exits 0 and an unreconstructable one fails open; a block below the H1 is rejected. **Guard integrity**: a conforming ADR must validate clean with `SCHEMA_APPLICABLE=true`, or every "rejected" assertion would hold against a validator that rejects everything. A "no field lists" loop greps all three hooks for `check_required_field` / `Valid values:` / an id regex, so a regrown list is the regression. Mutation-verified twice: against the pre-fix tree 19 of 23 fail, and against a copy with **only** the schema's spelling reverted to `superseded_by` (validator intact) exactly the 3 spelling assertions fail — attributing them to the spelling rather than to the file's absence. | issue #2136 |

## How to Add a Regression Check

### For `lint-context-commands.sh`

Add a `check_pattern` call with a descriptive rule name, a grep regex anchored to context command lines (`^- .*!\``), and a fix description. Include a regression comment referencing the PR:

```bash
# <Description of what this catches>
# Regression: <skill-name> had <description of bug> (PR #NNN)
check_pattern WARN \
  "rule-name" \
  '^- .*!`[^`]*pattern[^`]*`' \
  "fix description here"
```

Use `ERROR` for patterns that always break, `WARN` for patterns that break in some environments.

Update the comment block at the top of the file to include the new regression number.

### For `plugin-compliance-check.sh`

Add detection logic inside the appropriate `check_*` function (or create a new one). Include a regression comment:

```bash
# Detect <pattern description>
# Regression: <skill-name> had <description of bug> (PR #NNN)
```

If adding a new check function:
1. Define the function following the existing naming pattern (`check_skill_*`)
2. Add a `results_*=()` array in the status tracking section
3. Wire it into the main loop: `new_status=0; check_skill_new "$plugin" || new_status=$?`
4. Add `results_new+=("$(to_symbol $new_status)")` and `results_new+=("❌")` for missing dirs
5. Add the new column to the output table header and row
6. Add `$new_status` to the overall status loop

## Checklist for Bug Fixes

When fixing a skill-related bug:

- [ ] The original bug is fixed in the SKILL.md file
- [ ] A regression check is added to the appropriate script
- [ ] The check includes a comment referencing the PR number
- [ ] The "Known Regressions" table in this file is updated
- [ ] The fix commit follows conventional commit format: `fix(plugin): description`
| `session-survey.sh` reported `PROJECT_CONFIDENCE=high` beside an `OPEN_TASKS=` count that could belong to a **near-empty** slug while the real backlog sat under a **prefix sibling**. taskwarrior's CLI `project:<p>` filter is a **prefix** match, so `task project:comfyui list` returns all 60 `comfyui-nodes` tasks — a wrong slug verified that way looks confirmed. Observed live: `comfyui` (1 task) and `comfyui-nodes` (60) coexisting, the wrong slug enshrined in `CLAUDE.md` on exactly that evidence, and four follow-ups filed into the near-empty sibling. The collector's own scoping is a **hierarchy** match (`scope_of`: exact slug + `.`-separated children, #2271), so it never miscounted — which is precisely why the failure was invisible: `PROJECT_CONFIDENCE` answers *"is the detected slug trustworthy?"*, not *"do these tasks actually carry it?"*, and a prefix hit collapses the two questions into one. | Emit the split rather than pick a side. `PROJECT_EXACT_TASKS=` (always, an integer, like `OPEN_TASKS`) is the slug ALONE; `OPEN_TASKS` stays the hierarchy scope; `PROJECT_PREFIX_SIBLINGS=` / `PROJECT_PREFIX_SIBLING_TASKS=` (omitted when the store has none) name what a CLI prefix filter would ALSO have swept in, capped at 8 slugs and passed through the same row/column sanitiser every projected field gets. All three are computed against the slug `OPEN_TASKS` was counted under (`PROJECT_RESOLVED` when a rung resolved one). `PROJECT_CONFIDENCE` drops to `low` on **strict dominance** (`sibling_tasks > OPEN_TASKS`) — not on "a sibling exists", which would make every `dotfiles` beside a one-task `dotfiles-archive` read as unresolved forever. The downgrade deliberately applies to an **asserted** slug too (`--project` / a `.claude/session.json` declaration), unlike the adoption rungs: assertion fixes the slug's IDENTITY, it cannot assert what the store holds — and the reported case was a slug asserted on exactly this evidence. Nothing is adopted or rewritten; only the flag drops and the siblings are named. Reuses the single all-projects snapshot — no second `task` call, no `project:` filter. | `session-plugin/scripts/tests/test-session-survey.sh` TEST **AK** (47 assertions) EXECUTES the collector against the issue's own store (1 × `comfyui`, 60 × `comfyui-nodes`). AK1 is decisive: from the `comfyui` repo, `OPEN_TASKS=1` (never 60), `PROJECT_EXACT_TASKS=1`, `PROJECT_PREFIX_SIBLINGS=comfyui-nodes`, `PROJECT_PREFIX_SIBLING_TASKS=60`, `PROJECT_CONFIDENCE=low`. **Guard integrity is weighted equally**, since "always report low" would satisfy AK1 while destroying the signal: AK2 runs the SAME store from the owning slug and requires `high` + `OPEN_TASKS=60` + **no** sibling keys; AK3 requires a *minority* sibling to be named without costing confidence; AK4 pins that a `.` child counts inside `OPEN_TASKS`, is excluded from `PROJECT_EXACT_TASKS`, and is never reported as a prefix sibling; AK9 keeps an empty store a confident zero. AK5 pins the asserted-slug downgrade (and that `PROJECT=` is never rewritten), AK6 the summary/hook path, AK7 that still exactly one `export` runs with no `project:` filter, AK8/AK9 that a missing `jq` or `task` yields `TASK_SCOPE=unknown`/`none` + `low` + a parse-stable `PROJECT_EXACT_TASKS=0` and claims no siblings. Verified by **mutation**: against the pre-fix collector 20 assertions fail; an unconditional downgrade fails 13 (4 of them new); a blanket `startswith` sibling match fails AK4's 2. | issue #2323 |
| `cold-read-gate`'s reader prompt asked only for QUESTIONS / HESITATIONS / a verdict, so it measured **local** legibility and passed an artifact whose **ask** was incoherent. Observed 2026-08 on a registry-maintainer appeal that argued a scanner finding was unfixable by publishers and then asked those publishers to approve individual versions — four rounds of author revision had not surfaced it, because each half was individually true, and it took an **ad-hoc** prompt clause (not in the skill) for two independent readers to catch it. Two sibling gaps in the same class: a hesitation of the form "impact is environment-specific / I can't verify this" was triaged as prose to soften, when it is frequently a **testable hypothesis** (capping `RAYON_NUM_THREADS` at 1/2/4/8/24 answered exactly that objection and INVERTED it — the win saturates near 4 threads, so it is not a big-iron-only result: tracel-ai/burn#5332); and the persona table offered one persona per audience with no guidance for an artifact adapted to **two** channels, where an issue-triage reader and a chat-skimmer reader returned non-overlapping findings. | Promoted all three into the skill: the template's `Produce` list leads with `1. THE ASK — state in ONE sentence what this text wants you to DO` (with "if you cannot state it, that is the most important finding"); Step 3's two-bucket triage table gains a third column, **Answerable with evidence — measure it**, plus the rule that a bounded run beats a hedge; and the persona section gains "when one artifact serves two channels, give each reader its real audience". | `plugin-compliance-check.sh` `check_skill_body()` — the existing `cold-read-gate` block (which already pins `run_in_background: false` / `task-completion result` for #2063) gains a second loop asserting `THE ASK`, `Answerable with evidence`, and `give each reader its real audience`. Each is the marker of one addition, so a bulk edit "tightening" the prose cannot silently revert the gate to local-legibility-only critique, drop the measurement bucket, or lose the two-channel guidance. Verified by mutation in both directions: renaming any one token produces the ❌ naming it, and the restored file leaves the repo's only ❌ as the pre-existing `parallel-agent-dispatch` size error. | issue #2347 |
| `session-survey.sh` emitted a bare `GH_READY=false` with no cause attached, so a consumer could not tell a transient GitHub 5xx from an expired token, a repo with no GitHub remote, or a budget kill — and `session-end`'s own guidance leans on exactly that distinction. Only the budget kill was distinguishable, via the separate `GH_TIMEOUT=true`. The four causes want four **different** responses (re-run / `gh auth login` / nothing at all / raise the budget), so a single `false` forces the consumer to guess, and the natural guess — re-run — is futile for the two most common non-transient cases. Compounding it, `gh_job` discarded each call's stderr to `/dev/null`, so the one artefact that could have classified the failure was thrown away at the source. The default per-call budget made the failure likelier than it needed to be: `SESSION_SURVEY_GH_TIMEOUT` defaulted to **4s**, but `gh pr list --author @me` resolves the `@me` handle *before* it can run the query, so the budget covers TWO round-trips — tight on a slow link or a degraded API, and every overrun silently disabled the `GITHUB_DRIFT` dedup guard. | Capture each job's stderr to a per-job `.err` file and classify `(rc, stderr)` into one `GH_FAIL_REASON=` emitted alongside every `GH_READY=false` (all three sites: `PRS`, `GITHUB_DRIFT`, and the hook summary). Classification order is load-bearing: gh's remote-less message **also** says `` `gh auth login` ``, so the no-remote test must precede the auth test or every remote-less repo reads as an auth failure and the user is sent to a login that cannot help. `137`/`143` (the watchdog's SIGTERM/SIGKILL) → `timeout`; a job set that was never launched → `no-cli` (gh absent — distinct from `auth`, since neither a re-run nor a login helps). `GH_FAIL_DETAIL=` carries the first stderr line for `api-error` and `unknown` only — the two classifications the reason alone cannot act on — sanitised (control bytes stripped, capped at 200 chars) so foreign text cannot break the `KEY=VALUE` row. Matching uses a here-string, never a pipe, so a `grep -q` closing the pipe early cannot trip `pipefail` (the #1744 SIGPIPE race). Budget default raised 4s → 8s (and the invalid-value fallback with it); the calls are backgrounded and parallel, so the higher ceiling costs wall-clock time only when something is actually slow, and the SessionStart hook's own 15s timeout still bounds the run. | `session-plugin/scripts/tests/test-session-survey.sh` TEST **AM** (39 assertions) EXECUTES the collector against a stub reproducing the real `gh` stderr for each cause. **AM1 is the guard-integrity half and runs first**: a healthy `gh` must emit **no** `GH_FAIL_REASON` and no `GH_FAIL_DETAIL` at all — without it, a key emitted unconditionally would satisfy every other assertion while meaning nothing. **AM3 is the discriminator**: the fixture's stderr contains `gh auth login`, and the reason must still be `no-remote` and must never be `auth` — the natural (wrong) implementation orders those tests the other way. AM2/AM4–AM7 pin `auth` / `api-error` (+ the quoted first line, with the *second* stderr line asserted absent) / `unknown` (nothing fabricated from empty stderr) / `no-cli` / `timeout` (with `GH_TIMEOUT=true` still firing and never misread as `api-error`). AM8 pins that the reason reaches `GITHUB_DRIFT` and the hook summary, not just `PRS`. AM9 feeds 400 chars of stderr laced with `\001\002\037` and asserts the snippet is stripped, capped, lands as exactly one row, and that **every** emitted line is still a section marker or a `KEY=VALUE` row. AM10 pins the 8s default and that an explicit budget still overrides it. AM11 pins the consumer side — `session-end` must name `GH_FAIL_REASON` and map all six values, since a reason code nothing acts on is the gap the issue reported. Mutation-verified three ways: against the pre-fix collector **14 assertions fail** (AM1 correctly passing on both); reordering auth-before-no-remote fails exactly AM3's 2; and restoring `2>/dev/null` on the job fails 6. | issue #2425 |
