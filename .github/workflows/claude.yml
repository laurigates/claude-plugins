name: Claude Code

on:
  issue_comment:
    types: [created]
  pull_request_review_comment:
    types: [created]
  issues:
    types: [opened, assigned]

permissions:
  contents: write
  pull-requests: write
  issues: write
  id-token: write

jobs:
  claude:
    if: |
      (github.event_name == 'issue_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'pull_request_review_comment' && contains(github.event.comment.body, '@claude')) ||
      (github.event_name == 'issues' && contains(github.event.issue.body, '@claude'))
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Run Claude Code
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          plugin_marketplaces: |
            https://github.com/laurigates/claude-plugins.git
          plugins: |
            blueprint-plugin@lgates-claude-plugins
            configure-plugin@lgates-claude-plugins
            git-plugin@lgates-claude-plugins
            testing-plugin@lgates-claude-plugins
            code-quality-plugin@lgates-claude-plugins
            python-plugin@lgates-claude-plugins
            typescript-plugin@lgates-claude-plugins
            tools-plugin@lgates-claude-plugins
            rust-plugin@lgates-claude-plugins
            kubernetes-plugin@lgates-claude-plugins
            terraform-plugin@lgates-claude-plugins
            github-actions-plugin@lgates-claude-plugins
            documentation-plugin@lgates-claude-plugins
            project-plugin@lgates-claude-plugins
            sync-plugin@lgates-claude-plugins
            agent-patterns-plugin@lgates-claude-plugins
            accessibility-plugin@lgates-claude-plugins
            bevy-plugin@lgates-claude-plugins
            container-plugin@lgates-claude-plugins
            api-plugin@lgates-claude-plugins
            communication-plugin@lgates-claude-plugins
            agents-plugin@lgates-claude-plugins
            hooks-plugin@lgates-claude-plugins
